# ── Godot ────────────────────────────────────────────────────────────
# 导入缓存:每台机器自己生成(本仓库里 34M),换机重开编辑器会重建。
# ⚠ 但 `*.import` 文件**要提交** —— 它们存的是资源 UID 映射,
#   不提交会让每个人的资源 id 不同,场景引用随之错乱。
.godot/

# ── 探针产物 ──────────────────────────────────────────────────────────
# 截图探针写在根目录的 `_shot_*.png`(约 3.4M),随时可用
#   godot --path . --script res://tools/<x>_sheet.gd
# 重建。它们是**看的东西**,不是源。连带它们的 .import 一起排除。
_shot_*.png
_shot_*.png.import

# ── 工具链的本地状态 ──────────────────────────────────────────────────
# OMC / OMX 的会话状态、逐轮日志、metrics、subagent 跟踪。
# ⚠ 它们**不是项目源码**:每一轮都在变、是机器本地的,而且
# `.omx/logs/turns-*.jsonl` 里带**会话输出片段**(对话内容)。
# 2026-08-09 首次入库时漏掉了,47 个文件已进过历史 —— 见 CHANGELOG。
.omc/
.omx/
.superpowers/
.claude/worktrees/
.worktrees/

# ── 系统垃圾 ──────────────────────────────────────────────────────────
.DS_Store
*~

# ── 导出产物 ──────────────────────────────────────────────────────────
/build/
/export/
*.pck
*.zip
.probe.lock

# Claude Design 画布的**产物**(seed 出来的那个大 html, 单个 ~2MB, 内含整个编辑器)。
# ⚑ 设计**源**要进仓库:`docs/mockups/*/**.dc.html` + `canvas.json` + 稿子引用的底图。
# 先例:2026-08-31 收 cslot 画布时就是这么分的 —— 「只收 .dc.html 与 canvas.json」。
# ⚠ 它不加进来的话, 每次出稿都在 `git status` 里留四行未跟踪噪声, 而某次 `git add -A`
# 就会把 4 万行产物一起提交(2026-09-01 差点)。
docs/mockups/*/consumable-coin.html
docs/mockups/*/consumable-slot-empty-state.html
docs/mockups/*/shop-consumable-layout.html
docs/mockups/*/vinyl-queued-coin.html
