# Sync5 · 变更史

> **只记转折和它们的理由**,不记每个小改动。
> 现状看 [STATUS.md](STATUS.md) · 待办看 [TODO.md](TODO.md) · 经验看 [LESSONS.md](LESSONS.md)

---

## 2026-08-27/28 · 试玩反馈三批 + 经济 v2 收官 + 倍率 v3(d4deafa)

用户连续试玩报了 13 条,全部闭环(台账 docs/design/feedback_20260827.md)。三条大的:

- **⭐ 倍率奖励波动性(CLAUDE.md Target 原则 ④)**:用户「倍率不止奖励概率,还奖励
  波动性——否则期望一样谁不玩好凑的?」⇒ 新探针 tools/vol.gd 实测**带弃牌预算的
  追型达成率 + CV**,终表 1/2/3/7/8/9/12/15/18/18(公式含 λ 波动溢价 + 压缩幂,
  两根旋钮等真人手感)。小丑牌五因子定价框架同步入 numbers.md。
- **段分独立 + 达标即收工**:用户问「原作怎么处理」→ 查证 Balatro 亦独立清零;
  真痛点是「超额分白扔」⇒ 补 cash out(剩余拍 ×3◆),全局累加明确不做。
- **画风全切**:63 张老卡换 08-25 Quiet Glyph 全套,全池 77 张同风(此前是
  「彩色霓虹 69 张 + 抽象符号 14 张」两批打架)。

修的真 bug:换旗旧旗蒸发(→ 折半回收)· **sell_value 对 Target 恒为 0**(算价走了
「首张免费」规则,一分退不出且不报错)· 联票买完不补货(4 选 2 实为 3 选 1)·
**◆ 被读成 ♦**(「每持 2◆」→「方块 2」,全池改「金币每 N 枚」,判据入 LESSONS)·
续玩「点继续没反应」(本批引入的回归:首页层盖住恢复的牌桌)。

新功能:局内暂停(继续/退出本局)· 重开应用首页续玩询问 · 复合双盲注引擎
(试点「低音限流」z=−25.2,**成分单独挂弱上界豁免、复合 −25σ** = 乙封甲最优解的
第一份实证)· 万能解析 91× 提速 · bot 动作粒度(wrecker 复活)· 蒙色/蒙点入池 ·
A 级特效 FxBurst · 教学分镜化 v6 · sim 经济收支报表。

## 2026-08-27 · 大版本 662594e:四拍板 + journey + 体力闸门 + 周期课程(六线并行日)

worktree 并行模式的第一个完整周期:门跑着,五条开发线(拍板批/UI 批/体力线/B 线/journey)
全程不停,门完逐线合并、终验 2517/0、一次 push。

- **四拍板落地**(用户当场答的决策问卷):超级百搭**取代**百搭(wildcard 删卡,
  enable_wilds/大小王通道整体退役,万能唯一来源 = 4 张注入;特例价 4◆「超级卡就4金币」,
  说明入 levels.md)· 低频三卡**改条件放宽**(和弦/和声 → 新谓词 cache_mono_color 全同色
  ≈25%,静场 → 只禁弃牌[经济 v2 下多一层「省弃牌费」协同],数额按触发率反推;
  排练实测已活不动)· 预支自动签约维持。
- **journey 审计批**(docs/design/journey.md,18 动作 10✅ 6 缺口):弃牌键亮价
  (fee = 选中×1◆,不再暗扣钱)· fail 屏死因行(顶替「还差 N 分」——违约场景那行是
  系统说胡话,截图验收抓到)· 档位卡 = 图标回落 + 紧名前缀。
- **体力真闸门**(agent 线):_begin_run 唯一入口第 0 步扣 1;教学不扣、探针恒放行
  (仪器零污染)、旧档满值、不足零副作用;广告入口只留桩。
- **10-run 周期课程**(agent 线):director.json cycle 剧本 + roll_run 轴偏置(×3);
  hundred 实证主题命中 77 vs 无课程期望 39.1(2σ 外),考试局逐局有加码族;
  info 轴偏置弱(0.10 vs 0.07)留观察。
- **08-27 并行门收官**:两门并行 wall = max ✓(省 4.9h),但每门各慢一倍 ——
  尸检定位 **superwild 拖慢 bot 大盘**(1-2 张万能仍 52² 暴力,装卡局 ~3.6×);
  修法 = 万能全域候选构造,排下一机制批。132 量到;唯一红 = 旧世界的 hush/harmony
  (放宽批合并后自然消)。ranking 由 rankgen **正位**:16 档按实测杀伤入四段。
- 事故记账:agent worktree 目录两次被 git add -A 吞成 gitlink(.gitignore 模式被写歪
  `.worktrees/`),d294714 摘净修正;merge 冲突标记曾带进单测(那份结果作废重跑)。

## 2026-08-26e · 经济 v2 上半(用户四条规定 + A 案拍板;worktree 批)

用户压缩解空间:「我来做一些规定……你在剩余空间里找解(需调研)」。规定原话、连带账、
健康带与调法在 levels.md 经济 v2;外部调研收编为 research_cycle.md(King「retention
always wins」/ 近失 30% 倒 U / Balatro 利息是收入主干 —— 我们明确不做利息)。

- **弃牌收费 1◆/张**(推翻 08-06 弃牌免费 —— 那条又曾推翻更早的 1◆/张,转一整圈带闭环回来);
  开局 6→10◆;**卡价平化全 3◆**(稀有度退到出现权重轴,镜面稀缺税摘除;特例回来必须在
  levels.md 说明情况)。
- **牌型金币表 = 主收入(A 案,用户拍)**:1/2/2/3/4/5/6/8/9/10 —— 用户直觉三锚与
  −log₂P(≥k) 组合难度尺同构,唯一例外对子(8 选 5 下 92.9% 达成 = 保底工资)。
  表从 pattern.gd 硬编码搬进 economy.json `kind_coins`(db 十键齐全校验:缺键 = 静默 0◆)。
- **弃牌影子价 κ 回归 bot**(免费时代被删):plan 相对 keep_all 的增益 < n×κ 就少弃;
  κ 挂 `DISCARD_COST` 乘法(免费世界自动归零,旧读数逐位不变);初值 30 分/◆ 是方向锚,
  decay.gd 精扫排 ⑥ 后段。solver 不动:上界不考虑金币约束,偏松方向安全(声明)。
- 测试翻面:t_phrase(10◆ 起 · 收费 · 破产不能弃)· t_economy(平价/回收减半)。
- ⏳ 后续:sim 收支报表(局末余额/软破产率/弃牌分布对健康带)· 段工资去留 · 10-run 周期
  Director 剧本 · 全量门新基线(按新纪律与后续内容批合并跑)。

## 2026-08-26d · 升级系统整体删除(路线 ③,用户拍板)

原话:「升级太复杂等于要做好几套经济,现在能做是因为没考虑实质问题。删掉是为了减负,
不太会影响游戏乐趣。」—— 每个升级档都是一套要独立平衡的经济,那笔账从未真付过。

- **core**:`Joker.level/increment_scale/can_upgrade/upgrade_cost/has_scalable_effect/_SCALABLE`
  全删;`Fx.apply_effects/_do` 的 `scale` 参数与全部放大分支(含 2026-08-21 补的六个逃生口
  放大)删;`Run.snapshot` 不再写 `lv`(旧存档带 `lv` 读取时自然忽略,无迁移)。
- **view**:shop 的升级上架/渲染/offers/成交四段 + `upgrade_requested` 信号 +
  `set_upgrades_on` 删;编排器 `_on_shop_upgrade` 删。
- **tools**:bot 的「最便宜先升」段删。
- **data/校验**:economy.json `joker_upgrade` 节删;db 的 `_ECO_KEYS`/升级曲线校验/注释表
  同步;ui.json shop 节 10 个 `upgrade_*` 键删;lingo 升级条目删。
- **tests**:t_joker 的 `_t_upgrade` 整段删(含逃生口 A/B 与「可升级 ⇒ 有可放大通道」结构契约)。
- **文档**:CLAUDE.md 金币出口改「买牌 + 洗牌」、三原则条目改墓碑(**别再提议加回**);
  levels/tech/jokers 三篇同步。
- ⚠ **经济连带**:金币主出口易位(升级曾是主出口)—— 工资/价格的重锚并进路线 ⑥ 数值批;
  sim 侧 bot 不再升级,所有含升级的旧读数与新基线不可比(本批起又是新基线)。

## 2026-08-26c · 超级百搭上线(路线 ②):四张 JOKER 入堆 + 付费洗牌钓卡

用户创意原话:「向牌堆中插入 4 张 JOKER…凑牌的神卡,运气选手最爱」+「但也有成本,
你得有金币去洗牌才拿得到」。落地为**卡 + 动作**两半,金币第三出口成型:

- **引擎前提 · Pattern ≥3 万能解析捷径**:旧 `_score_five` 只有 ≤2 张的 52^k 暴力,
  k≥3 会**漏替换** → rank 15 直达 `_classify_fast` 计数数组越界(必崩,不只是慢)。
  新 `_score_many_wilds` = **候选构造**(同花顺窗[皇家/轮子]/四条/满堂/同花,~20 个
  候选全交 `_pack` 打分)—— 判型照旧走 `_classify(rules)`,近道/四指/红调/黑调
  **自动生效**,规则不开第二份。完备性 = t_pattern 随机 8 手对拍**缩减域暴力**
  (每万能试 3 花色等价类 × 13 点)。
- **Deck 注入表** `wild_extra`(来源 → 张数):同来源幂等(买新替旧再买回不翻倍),
  与大小王开关独立叠加;snapshot/fork 全带。注入 JOKER 用 suit 2/3(☆冷色,SUITS
  按下标取 suit ≤3 是硬约束)。acquire DSL 的 `wilds` 值语义化:≤2 = 大小王(百搭
  旧行为逐位不变),>2 = 注入;db 校验值域 2..6。
- **洗牌动作**:`Phrase.reshuffle()` = 整手回弃牌堆 → 弃牌堆洗回 → 重发 5 张
  (堆里 JOKER 全部回到可抽态 = 钓卡实体)。`reshuffle_cost: 3◆`(economy.json,
  方向锚待 ⑥)。**只在牌堆有万能时开放**(对无万能的局是免费全弃的付费重复 = 纯坑)。
  封条随旧手离场(金币解封 = versus「构筑相关成本」的合法解除);暗场/蒙面对新手**重掷**
  (否则 3◆ 把脸洗成空气)。盲注的弃牌张数限制不管洗牌(它有金币闸,对抗面后续设计)。
- **UI**:`DJKey` 半径参数化 + fee 徽章放宽到非 sort 键;洗牌键 = 72×72 紧凑款
  (金色,弃牌键正上方的行间空隙),缺省隐藏、牌堆有万能才亮、付不起压暗不藏
  (玩家要看得见价);拒绝浮原因 + 键抖。打点 `rsfl` 记事实(cost/at)。
- **卡**:`superwild`(rare/support/solver 证物 `*wild`),规则牌判据自动挡升级报价。
  素材 = expansion 库 `echo_wild` 走全管线(source→webslim 512→横幅→import),
  manifest S-75(82→83,构建/校验计数同步)。bot 洗牌臂**只绑注入**(wild_extra
  非空)—— 不动 wildcard 旧基线;动机 = 手无万能 + 手牌烂 + 留得下购买力,每拍至多一次。
- **待拍板**:与百搭(wildcard)共存还是取代 —— 当前缺省共存(可逆方向)。
- 读数:单测见下批;kit 单卡与增量门(路线 ④)接续。

## 2026-08-26b · 对抗批全量门收官:4.9h 跑完,五红三过期,全部尸检处置

门(17581s)整体判红,红全在**声明/仪器层**,内容零改动(「不为绿改卡改脸」守住):

- **wrecker 触发 0% —— 尸检出双层根因**:① `sim.json discard_bias.wrecker: 3` 是旧弃3档的
  **手抄残留**(对抗批把卡改成 `discards_gte: 6`,bot 凑数目标没跟上)→ 改 6 + `_comment` 写明
  必须与卡一致;② 改完仍不可达 —— bot 把弃牌张数当**动作次数**计(`beat_budget.discards: 4`,
  6 > 4 ⇒ 偏置整个不生效),而真人一次跨区多选 6 张只是一个动作 ⇒ **kit 新增 `UNREACHABLE`
  显式声明机制**(同 `WEAK_MAGNITUDE` 哲学:把债写明白,不把门糊绿;带反向锁,bot 变强会喊删)。
  单卡重跑:行内「⚠ 豁免(bot 不可达, 已声明)」,退出绿。粒度修复进 TODO(⑥ 前置)。
- **单调性「起始金币 −3→0 = −0.01 段」—— 抛硬币门第三次回来**(08-12 `+0.00✓`/08-13 `−0.00❌`
  同款):实测 ±SE≈0.03,−0.01 统计上就是零,旧判定把噪声符号当方向。按 08-15 注释里预告的
  真修法落地:**`_mono` 带宽从实测标准误推出**(|Δ|<2·SE_diff ⇒「⚠ 平坦,方向不可判」进 warn;
  方向反且超噪声带照旧红)。不是调宽 FLAT_BAND —— SE 跟着 n 与方差走。微测三情形 PASS。
- **crescendo 3.7% < 5%** → 挂 `weak_upper_bound`(得分侧重排近守恒,完美玩家可把大牌挪去
  高权重拍近乎全额补偿 —— 上界效应小是结构性的,真人待定)。
- **三条声明过期**(onetake/countdown/closing 已量到还挂着)→ 按门的要求摘掉。
- **hush(静场)8% / harmony(和声)5% 触发** —— 低频答案,已在 TODO 🔴 归用户
  (与和弦/排练同族:问题在条件不在数额,不为绿改数值)。
- **`tools/rankgen.py` 重刷 `data/ranking.json`**:33 脸四段齐,七张新脸首次真读数落位
  (countdown 落最难段,与 T4+弱上界预判相符),0826 bootstrap 排序被仪器读数替换。

## 2026-08-26 · 文档洁癖清理(用户:「不用了的,不符合现状的,都去掉,我洁癖」)

趁全量门后台跑(码冻结,只动 markdown)把文档层对齐现状。**史料区不动**
(CHANGELOG/LESSONS/`*_history`/`research_*`/评审记录 —— `blinds_history` 有「别再删第二次」的血案)。

- **删 5 篇**:`docs/design/meta.md`(META 随局外删除退役)· `docs/design/blind_candidate_atlas.md`
  (候选已消费,`blinds_review.md` 留了指认注)· `docs/design/art_prompts_gpt.md` ·
  `docs/review_20260821_raw.md`(正式稿在,原始稿冗余)· superpowers 旧计划里纯主角美术那篇。
- **墓碑 3 大节**:`jokers.md` 主角节(−111 行)· `ui_meta.md` META 节(−105 行)· `vision.md`
  状态账本节(12s 钟/23 卡时代的 Done/Remaining 快照)—— 各留一行指路。
- **重写 2 本账**:`STATUS.md` 头部数字全刷(2304 单测 · 76 卡 · 08-26 门在跑);`TODO.md`
  294 → 139 行,✅ 与作废项按它自己的宪章出清,「明确不做的」补三行新拍板
  (局外回归 · 内购 · 给锁配钥匙)。
- **散修 ~30 处**:tech.md(characters 节删 · DSL 表补 chance/target_streak/per/hold 白名单指向 ·
  76/68+8 计数)· blinds.md(08-10 旧横幅换现状 · 家族梯形图摘接线)· difficulty/jokers/vision
  的 12s 时代残句 → 8s · generating/research_pacing 的「META 未做 = D7 风险」补 08-24 拍板现状注 ·
  levels.md HUD 旧标签(小盲/大盲 · 进度点按 `hud.gd` 实况改为每段一枚 SegPill)· 三篇「由 X+X 合并」
  的自指坏注 · jokers_history 死链。全库 md 相对链接死链检查过零。
- ⏳ 码冻结解除后顺手:`view/hud.gd:35` 注释仍写「12 blinds in 4 gig groups」(行为无涉)。

## 2026-08-25b · 对抗批:versus.md 定稿 + 11 新脸 + 13 新卡 + 序列预算(设计→实装一天)

**起因**:用户三连问「构筑各玩各的 / 盲注拆小丑不够 / 玩法太少」→ 与用户共建出
**对抗代数**(盲注=条件+惩罚 · 小丑=条件+激励 · 解法四种:错开/抵消/利用/调度),
定稿 [`docs/design/versus.md`](docs/design/versus.md);期间用户连续裁决:玩家视角等效的
分类必须合并 · 解题不许给锁配钥匙(纯对策卡=商店彩票,替身/延长线/保险柜三张设计稿被砍)·
弃牌限制按**张**不按次 · 「虽然但是」句式高门槛 · 提到卡名必带功能描述。

**盲注侧**:一口气/限流/打烊张数重铸 · 封条/双封随机封 · 红灯(管手脚不管结果+补牌文本说谎)
与接线(解法单一)退役, 池 28→26 · **11 张新脸机制完工全部暂存**(高音/倒计时 886644/渐强
[得分侧,拍级目标不存在]/暗场/轮盘/变色灯/点名[可解除+货架+1]/蒙色/蒙点[用户创意,属性遮蔽,
边框颜色也是花色情报])· 概率/拍相/渐变/可解除四算子各建一次模全族覆盖。

**小丑侧**:13 新 + 3 改(镜面连续两拍才生效 · 拆迁弃 6 · 贝斯线 8 弃档[bot 手抄 12 被抓])。
新 DSL:chance(Beat 预掷)/target_streak/per: cache_rank_sum·hidden_scoring/
hold: cache_scoring·odds_mult·section_life·face_coins。素材全部取自用户预生成的
expansion_20260825 素材库按视觉隐喻配对(82 条资产校验绿);三素材库目录已进导出排除(否则 +80MB)。

**系统侧**:序列杀伤预算(attack_axes 参数自动推导 + 围殴修复循环收敛, 探针/Director 共用)·
开局亮整局(特写下方巡演路线行)· 胜负音效接入。流程探针 43/43 恒绿 · hundred 400 放置过。
**未做**:仪器统跑(ranking 重生成→暂存脸补 tier→kit 逐张→全量门, 等用户喊);
蒙色/蒙点入池前必修属性级信念;挂起六张深钩子卡(试音/延音踏板/指挥棒/定金/幸运币/定存)。

## 2026-08-25 · 全量门新基线 + 关卡读数重生成 + 包瘦身落地

**全量门(4.2h,08-16 以来第一次)**:脸覆盖 8659s、小丑牌覆盖 5147s、流程全绿;三红全部处置——
① 金币哨兵:「起始金币 0→+3」实测 +0.02 段, 不再是零效应, 按哨兵指示从 KNOWN_FLAT 摘掉(表清空, 结构保留);
② 打点回归:**08-18 埋的过期断言**(公示卡退役后特写接任, 而特写有探针闸 ⇒ 探针世界 intro 合法为零)——
tapeprobe 契约翻面为反向锁(探针世界冒出 intro = 特写探针闸破了), 复跑 0 bugs;
③ 尺子自检:bot_targets 对新世界(删主角被动 + 探针世界有 boon)过松, random 6.8% —— **预期中的重锚**。

**关卡读数重生成**(目标分与牌型不动, 08-24 拍板):`ranking.json` 重刷(rankgen 吃本次门的脸覆盖读数)·
`bot_targets` 重锚 [496,995,4270,5678] → **[390,987,4860,10615]**(curve adaptive 混合人群反解;
sim 复验 random 2.4% / 最强 35.6% 双判据过)· hundred 100 关新排序表下全绿。

**包瘦身实测(Web)**:pck **116MB → 30.7MB**, 总包 147MB → **67MB**(36MB 是引擎 wasm 固定成本)。
四刀:导出排除 redesign/marketing(−120MB 不该进包的)· wav→ogg(14MB→0.7MB, 样本级 8.0000s,
加载器 ogg 优先 wav 兜底, 正本已删)· 贴图有损导入 0.85(140 张, 目检无劣化)· 键控黑底转透明。

**键控(用户拍板②)**:黑底母版在派生线(512/横幅)统一「亮度转透明」(`key_black_to_alpha` 只此一份,
webslim 借调)—— 槽位里艺术从黑方块变成「悬在玻璃上的光」, 玻璃蓝调与网格透出。GPT 出图规范不变(纯黑底)。

---

## 2026-08-24(续③)· 重设计素材接入 + 光晕第四版(真 drop-shadow)

**小丑牌全套换新**(用户用 GPT 按提示词包产出 `assets/redesign_20260824/`,63 小丑 + 33 盲注概念,独立复核
数量/命名/尺寸全对后接入):新 1024² 原画进 `source/`(母版层)· `webslim` 重生成 art512 · `build_joker_runtime_art`
重生成 1024×400 横幅(⚑ **fit 改折中 cover**:新母版辉光满幅, 纯 fit 把主体缩成 360² 小块 —— OVERSCAN 1.35 放大主体、
居中裁掉溢出辉光)· 顺手补齐 jukebox/blacktone/redtone 三张历史缺图。盲注概念稿**未接运行时**(按包内 README,
指纹卡还差单色描边化)。⚠ 换 PNG 必须跟一次 `--import` —— 导入缓存会让旧图钉在屏上(截图当场抓到)。
仪器修正:`build` 的 EXPECTED_CARD_COUNT 67→69(双色调拆分后的过期常量)· `verify_joker_assets` 退役
cards/previews/prompts 三层检查(目录本就退役, prompts 溯源随旧美术线作废;新增 art512 检查)· 三个退役目录删除。
现校验 **69 records OK**。教学第 1/2 步文案按用户原话重写(lingo 同步)。

**首页光晕第四版 = 真 drop-shadow**(用户三轮:「特别强」→「不像光的自然展开, 到边界停了」→「特效方式不对」):
光晕 = **玻璃壳素材自身 alpha 剪影**的高斯模糊(壳的亮檐/顶弧/倒影尾都在光里, CSS drop-shadow 的定义),
不是任何几何形状。技法史四版全记在 `home.gd::_halo_tex` 头注释。顶栏头像环/LV 徽章改跟档位色(配套变色拍板)。

**盲注特写糊**:fp_*.svg 导入栅格 1×, 特写放大 2.5× 必糊 —— 29 个 .import 的 svg/scale 抬到 3.0(绘制端按目标
矩形画, 尺寸不受影响)。⏳ 特写清晰度归实机验收。

---

## 2026-08-24(续②)· 首墙两层放水 + 商店升级收口 + 首页按 v5 稿精修

**首墙两层放水**(用户:「原作前两轮的盲注仅仅是分数要求, 我们的花样是不是太难了」→ 批了「新手解锁层」
→ 追加「后面的关也偶尔可以有简单关, 这完全是难度控制的一类」):
① 新手层 —— 首墙的脸第 `s1_face_min_run`(=4)局起才掷, 前 3 局纯分数(第 1 局本就是教学);
② 常驻层 —— 解锁后每局首墙以 `s1_easy_chance`(=0.2)掷成纯分数关, **S2-S4 恒有脸**(压轴不放水, 原作 Boss 也从不缺席)。
两个数都在 `data/run.json`。判定一份(`SectionMod.wall_face_unlocked`), Director 与探针掷法两个入口共调;
概率放水只在 Director(游戏唯一漏斗)掷 —— **探针世界按全脸标定**(目标分偏严方向安全, curve UNLOCK_ALL_RUN 同款拍板)。
无脸首墙的盲注卡自动变下一场预告(BlindCard 既有 preview 路径)。hundred 四条硬规则改口径 + 简单关频率 3σ 走带自检;
t_director 新增门/频率/压轴恒有脸断言。

**商店升级收口**(用户:「升级也只能选一下」):升级 = 本次进店的那一下, 落定关店开拍(与买入同款;
此前 sold() 刷新后可连点把钱掼完);双击被 `state != DRAFT` 闸挡住。

**首页按 v5 稿精修**(设计稿入库为 docs/mockups/home.html 新权威):背景三件套逐值(渐变四停点/三团氛围光/115° 软斜光带)·
CRT 扫描线层(两族 168°/172° 滚动长线, 密度与滚速按 Claude Design 的放大观感校, 亮度两轮调淡)·
整卡光晕 = 模糊剪影贴图(StyleBox shadow 整面染色、描边环读成「第二个圈」两版技法都翻车后的第三版;两轮调淡至 0.11)·
侧条 = 邻卡玻璃壳切边(点击切关)· 顶栏放大一档且两端对齐卡玻璃亮边(实量 56/664)。
⚑ 新坑印:**GradientTexture2D 不许在 _draw 里现建现画** —— 像素生成延迟, 首帧白占位 + 静态层按键重画 ⇒ 白钉满屏(二分抓到)。

**教学关补丁**:压暗层常亮洞 = 顶栏 + 手牌框以下整个操作面(积分滚动/读秒边框全程可见), 洞形修方
(focus_radius 的圆判据加尺寸上限 —— 720×608 的版面曾被当成圆键挖成怪圆)· 提示条避让表加手牌框(不再压读秒光圈)·
第一步文案 =「每隔8秒手牌区的五张牌按照德州规则自动结算」。

---

## 2026-08-24(续)· 首页顶栏重做 + 教学关三处修复

**顶栏**(用户给参考图:「没有边框,融入背景;金币去掉,紫宝石去掉,右上角是分享按钮不是 +」):
无边框直画在黑底上 —— ♪ 圆盘 + 紫环 + LV 徽章 · NEON PLAYER + EXP 行 · ⚡ 体力胶囊 · 分享圆钮(点击复制分享文案进剪贴板)。
数字全真:EXP = 累计通关段数(`data/profile.json xp_per_level`)、**体力 = 上限 − 今日已开局数**(`energy_max`,每天回满)——
⚑ **体力只显示不拦人**,拦不拦/怎么回归用户拍。`sections_total` 键复活(v2 迁移不再清它)。

**断点续玩**(用户:「没有断点续玩」;移动 Web 刚需 —— iOS 内存回收重载后局面全丢):
存档点**恒在拍边界**(`_start_phrase` 发牌之前写 `Run.snapshot()` 进存档 `resume` 键):恢复时从同一副堆序重发,
拿回同一手牌;拍中被杀 = 这一拍重来;**商店里被杀 = 回滚到商店前那一拍**(结算后商店自然重开,购物不凭空消失,
也免去商店内钱包悬空)。恢复(`_resume_run`)**不走开局三步**——掷脸/喂 Director/记局数的产物全在快照里
(`core/run.gd::snapshot/restore` + `core/deck.gd::snapshot/from_snapshot`,含牌堆两堆顺序/规则旗/RNG 状态)。
启动时有快照直接回牌桌不过首页;局终(死/通关)清快照;教学关不存;探针不存不读。
新 Tape 流打 `resume: true` 标记(原流无 close 本就是「中断」信号)。t_run 加 snapshot→JSON→restore 圆环 ~30 断言。

**教学关**(用户三条):
① **读秒被压住** —— 压暗层盖在 orbit 之上,3-2-1 倒计时进了暗区;每步给节拍菱形区开常亮洞。
② **换牌后立刻跳小丑牌** —— 拍中推进(`tutorial_advance_if_done`)整个删除:拍中做完动作只把提示熄掉当回执
(08-18「应该消失」拍板仍成立),**步进只在拍末**;下一步的内容等结算后才上。
③ **第三步不再白给四张样品** —— 借展样品与转正收回全删;第 3 步开头弹**真商店**(首张 Target 免费三选一,
与正式局同流程),选完/继续都开拍,提示条讲「最多 4 个格子」;教学段每 3 拍的第二次店抑制(一关只教一次买)。

---

## 2026-08-24 · 局外 build 整体删除(用户拍板:「仅保留局内玩法,我们应该专注」)

用户两句话定案:「取消全部局外 build,直接删除代码和资源,包括主角、券,仅保留局内玩法」;
边界追问后拍板:**删** = 主角 + 券 + META 资产(宝石/唱片/彩带机/应援团/赛季)+ 成就·荣誉·档案栏;
**留** = 图鉴、Director 千人千面(跨局读战绩圈/faces_seen —— 它是局内内容分发,不是局外成长)、教学关、局内金币 ◆。
(另提的「画风可切换」当场搁置,未动。)

**删除面(172+ 文件)**:
- core:`character.gd` / `ticket.gd` / `asset.gd`;data:`characters.json` / `tickets.json` / `assets.json`;
  view:`heroes.gd` / `honors.gd` / `walker.gd` / `pick_walker.gd` / `tray.gd`;tests:`t_character` / `t_ticket` / `t_asset`;
  tools:`crew_sheet` / `dance_sheet` / `pick_sheet` / `traysheet` + `tools/art/` 主角管线三件;
  assets:`assets/characters/` 整目录(77MB)+ 主角/荣誉两张设计稿 mockup。
- **结算链收窄**:牌型 → target → support → 分数/金币(主角被动层与 `slot=-1` popup 删;`Settle.extra` 不再有 `character`)。
- **开局入口改名**:`choose_character(i)` → `start_run()`(教学分岔原样保留;9 个探针同步)。
- **存档 v2**:`_migrate` 清退役键(tickets/hero/gems/assets/wins_total/sections_total…);Director ctx 的键全留。
- **曲池放开**:`Music.track_pool` 回到十首全开(曲目是内容,不再是商品);首页静音(点唱机资产已删)。
- **首页**:档案栏整块删,顶部留黑(背景本来就是纯黑);页签 4 → 2(关卡/小丑牌,`Chrome.TAB_W` 292)。
- **手牌区**:走圈轨道/节拍菱形/3-2-1 倒计时保留(核心手感),主角小人删,轨迹色改固定金。
- lingo 表 314 → 227(四族 85 条),`t_lingo` 阈值 250 → 200;ui.json 删 tickets 节;gate.sh MECH_RE 同步。

**⚠⚠ 仪器再次全体新基线**:sim/runloop/curve/wallet/decay/decomp 删掉「先抽主角」那一次 RNG 消耗
(F-1:必须同一批一次删干净,已删干净)+ 结算链少了主角被动层 ⇒ 所有旧读数不可直接比。
Tape 口径断裂:`run` 事件不再有 `char/cn/meta`,`fired` 不再出现 `@character`,`ticket` 事件消失(probbook 不受影响)。

验证:`--import` 0 错误 · 全量单测(见 STATUS 回归表)· 首页/图鉴截图目视 ✓。

用户一句「能修的修完了吗」。诚实答案是**没有**:上一轮的「留待」里有一半是我能修、只是没排进外部审查顺序的。本批清掉:

**F 性能(评审 R「静态页 60fps 整屏重画 = 手机发热来源」)**:
- **首页拆两层**:`HomeScreen.FxLayer` 每帧只画雨 / 均衡器 / 脉冲按钮 / 扫描线,本体(程序化玻璃 + 3 neon + ~40 draw_string)
  **只在状态键变了才重画**(`Chrome.dirty(key)`,键 = 段 / 页签 / 拖拽位移 / 宝石 / 构建戳)。倒影层同样只随键重画。
- **三个图鉴页 + 荣誉页**:雨拆成共享的 `Chrome.RainLayer` 子节点;本体按各自状态键重画(album 连网格层一起)。
- **商店 / 开局的盲注板** `BlindBoard`:均衡器拆成 `EqLayer`,玻璃板只在 `setup()` 重画。
- **Shader 只编译一份**:`PaperCard._mask_shader` / `StageCard._mirror_shader` 静态缓存,材质仍按实例(此前每张卡 / 每次弃牌的幽灵卡 / 每个槽各编译一份)。
- 死代码:`home.gd` `_float/_glow/_toast`(无调用、每帧判断)删;`run_end._sub` 每字量两遍 → 一遍。
- 截图核过:首页 / 主角 / 小丑牌(顶与底)/ 荣誉 / 商店两态 —— 与拆层前同一张画。

**仪器四件**:
- **探针世界有 boon 了**:`RunLoop.Opts.boon` 缺省 `BOON_AUTO` = 从 deck_seed 派生的独立流掷一张(共享主流一个数不多消耗,配对臂不变);`""` = 明确无。
  30 局烟雾:四张 boon 10/5/10/5。⇒ **sim/curve/gate/price 下一次读数是新基线**。
- **replay 真注入**:`SYNC5_REPLAY_INJECT=1` 现在真的篡改每局第一条弃牌(弃一张不在手上的牌)并断言恰好多报一条;
  顺手抓到**更大的一件**:`tape/` 里 3000+ 份是 flow_probe 的机器局(没有任何弃牌/交换决策),「最近 5 份」全是它们
  ⇒ **重放连着几天在验空集且全绿**。现在只取真人局(跳过 `sess.id = -1` 与教学局)。A:56 决策点 0 违规;B:2 局抓到注入、3 局无弃牌可篡改(明说)。
- **bot 缺臂**:`popup` 在 `sim.json ev.cards` 里没有条目 ⇒ `_card_ev` 落底返回 0 ⇒ 一周里从未被买。补臂(每段首拍 1/6,同 opener);
  `_card_ev` 落底 `push_warning` 一次;`validate_sim` 反向校验 **score/solver 通路的 support 必须有 ev.cards 条目**。
- **context 三阈值进数据**:`director.json context_tuning`(lose_streak 2 / win_streak 3 / return_gap_days 3 / explore_mult 1.5),
  `shift_bias` 变成带阈值参数的纯函数,`SaveState.returning_run` 读 `Director.return_gap_s()`;校验锁白名单与下界。
- **商店 boost 改注入**:`Director.explore_boost(candidates, used)` 纯函数,编排器 `_open_draft` 注入 `set_explore_used`(探针恒空);
  shop 不再读 SaveState(它文件头承诺的注入制终于成立)。

**小件**:分数滚动(设计稿 t=2120ms count-up)**此前从未演过**(`resolve_hold 1.0s` < 碎片 1.5s,`_refresh` 一离开 RESOLVE 就拍成终值);
现在滚动期间不抢拍,探针实测 shown 在 2.0s 仍是旧分、2.6s 到终值 · 买入 / 付费刷新路径在编排器复查余额(`deny buy_stale/reroll_stale`)·
`layout.gd` 文件头「不许硬编码」改成真话 · CLAUDE/ui_meta 档位色三个旧 hex 对回 `theme.gd`。

**首页 mock 数归零**(用户:「首页 mock 数应该是 0,这块属于 meta 管,问题是 meta 是否包含了这块」—— 答:**没包含**,
meta.md 只定义了宝石 ↔ 资产):`SaveState.profile()` 一个函数返回 LV/XP/◆ 的真值(= 0),首页与四屏顶栏 `Chrome.page_bar` 共读;
成就页同口径 —— `cur` 改读终身计数 `SaveState.stats()`(runs / wins / sections,`settle_run_meta` 起累加 `wins_total`/`sections_total`),
没有对应计数的四条恒 0(让它们活要加事实计数,不是填数);排行页仍是壳(要联网)。洞与推荐写进 meta.md §8 + 待拍 #6。
**§8 当场拍板落地**(用户:「跨局金币可以删 chip,其他按你推荐的来」):◆ chip 删(首页 + 四屏 `page_bar`,◈ 单 chip 居中)·
**参与度等级**:`data/assets.json profile.levels` 十档(xp = 累计通关段数,与宝石收入同源;键白名单只许 xp/cn/name ⇒ **零数值由校验锁住**),
`Asset.level_for()` 纯函数 + 14 条测试;称号随档(雨夜新客 → 霓虹传奇,双语);顶级经验条满格显示 MAX ·
首页档案**跟当前主角**(头像按 `SaveState.hero()` 懒加载、名字走 `Lingo.t`),首页再没有一个 mock 数。截图核过 cn/en 两版 + 三图鉴页。
**仍归你的**:等级阈值手调 · 和弦/排练 · 副歌补偿 · 签名/包名/版本。
验证:**全量单测 2479 passed / 0 failed(`tools/unittest.sh` 四判据全过,§8 落地后重跑)**;flow 探针 43 局 / 175 商店 / 0 bugs;30 局 RunLoop 烟雾零错误。

---

## 2026-08-21 晚 · 外部全量审查(REQUEST CHANGES)逐条复核与修复

用户贴来一份外部审查(HIGH ×5 + MEDIUM 若干)。先逐条复核再修 —— **五条 HIGH 里四条属实,第五条(签名/包名)归用户**:

- **H1 RunLoop 段转换漏清段状态**(属实):探针只清了 `phrase_in_section/section_score`,`section_kinds`/`first_kind`/`section_discards_used`/
  `request_last` 全带进下一段 ⇒ 曲目税和「段内首拍」在探针世界与游戏不同。收成 `Run.reset_section_state()` 一份,`next_section()`/`reset()`/RunLoop 三处共用
  (又一次「规则在游戏里,不在模型里」,这次是**段内**的)。
- **H2 `RunLoop.fork()` 不完整**(属实):漏 `run_boon`/`cache_meta`/`section_kinds`/`section_discards_used`/`request_last`/`previous_raw_score`/`tutorial`,
  且 `_blind_rng` 没播种。补全 + `tests/t_run.gd::_t_fork_complete` 逐字段断言(含深拷贝)。
- **H3 单测/门禁红**(属实):`validate_tape` 把 `upload` 当必备键 ⇒ `t_tape` 红;改成可选。**四判据抽成 `tools/unittest.sh` 唯一一份**
  (gate.sh 转发 · CI 调它),`^ERROR` 白名单只按具体文案放行(退出时 RID 泄漏账 · t_tape 故意触发的 3 条保留字报错),不按前缀。
- **H4 权威文档自相矛盾**(属实):TODO:18 的「进行中」· levels.md 商店 8→7 · difficulty.md「只有 A 轴有实现」· tech.md 四个 08-05 全量示例块
  (**90 行旧小丑牌数值整块删**,改「键名 + 指向校验函数」)· `director.json` 顶部仍写「不读 context」。三篇 EOF 空行清掉。
- **H5 无 CI**:`.github/workflows/tests.yml`(Godot 4.6.2 headless · `--import` · `unittest.sh` · 日志留档)。签名/包名/版本仍归用户。
- **MEDIUM**:`tapeserver.py` 拒 `.`/`..` + realpath 容器检查 + 只收 `.jsonl` + Content-Length try/except · `core/save.gd` **原子写**(tmp→rename)
  + `.bak` 回退 + 版本键 `v` 与 `_migrate` · `probbook.py`/`replay.gd` 连 `sent/` 一起扫,probbook 过滤教学局与探针会话 ·
  `shop.gd` 升级槽只从非钉位里挑(曾可能顶掉 Target/规则牌保底)+ 两个按钮只建一次 · `gate.sh MECH_RE` 补 boons/characters/director/ranking/tickets,
  `changed_files` 含未跟踪文件。
- 审查里**过期**的一条:「gate.sh 只 grep `0 failed`」—— 当天上午批 C 已改成四判据,审查看的是旧版;本批只是把它再抽成单独脚本。

验证:**全量单测 2445 passed / 0 failed · exit 0 · SCRIPT ERROR 0 · 非白名单 ^ERROR 0**(08-21 深夜,本批代码改完后跑;白名单 6 条逐条有账)。
`unittest.sh` 通过数地板定 2400。**全量门仍未跑**(新基线)。

---

## 2026-08-21 · 全面评审 + 按批次修复(A 玩家可见 · B 发布线 · D 校验 · C 仪器 · E 文档)

8 个只读评审员按切片并行评(标准 = CLAUDE/LESSONS 的铁律判据),🔴 逐条复检后汇总成
`docs/review_20260821.md`(原始八份在 `review_20260821_raw.md`)。用户:「都修完了?」⇒ 当场按批次修:

**A 玩家可见**:
- **8 张卡升级是纯扣钱**(R1):`fx.gd` 六个逃生口操作码在「升级放大」段之前就 return ——
  vip/mirror/bassclef/warmtone/cooltone/undertone/bench/royalty 扣钱不涨。六处各自放大(乘子走 `1+(x−1)×scale`,
  加分 ×scale,金币通道不放大);`can_upgrade()` 改读 `has_scalable_effect()` —— **只有金币通道的卡不再挂升级报价**
  (独狼/小费罐/利息/皇室:此前的「升级」是卖空气)。t_joker 加逐操作码 A/B(满级 = Lv1 ×2)+ 全 roster 结构契约。
- **重开绕过 Director/min_run/局数/ctx**(R2):`_reset_run()` 后 `Run.reset()` 用缺省 `run_index=-1` 掷脸,主路径的
  「定局数 → 喂 Director → 掷脸 → 记局数」重开一样没走。收成 `_begin_run()` 一份,开局/重开共用;重开的 `Tape.begin` 补 `sess/tutorial`。
- **教学商店买的卡被没收**(R3):转正时只回收 `_tutor_samples` 里的四个实例,玩家买的留下;样品换下来**不退款**。
- **资产页矩形与 roster 错位**(R4):rect 与 id 成对存;`Asset.can_buy` 加 `on_shelf`。
- 小件:首页「弃牌 ◆0/张」→「弃牌免费」· 四档画三星 → 按段数 · 特写期间托盘收起 · 教学进度条 0/0 NaN ·
  `Joker.clone()` 补 `level` · 回归局熟脸不再被 novelty 开关吞。

**B 发布线**:`export_presets` 两预设补排 `assets/characters/source/*`(43MB)/ `assets/reference/*` / 联络表 / 评审图 / `build/*`,
删两条死过滤项;`build/` 与 `assets/reference/` 放 `.gdignore`。签名/包名/版本归用户。

**D 校验补洞**:jokers `kind`/`rarity` 枚举 · `_validate_effects` 抽出共用(**主角 effects 首次过白名单**;`counter_gte`/`per:counter:`
引用必须在 counters 里)· ui.json blindcard/jokercard 与脸/boon/卡**双向交叉校验**(删 5 条退役卡的孤儿 jokercard)·
`sim.json target_tf` **删掉**(Target 倍率过期的第二份,bot 镜面估值改从 jokers.json 推导 `_target_peak`)·
`joker_upgrade.costs` 长度与 `max_level` 对账 · `joker_prices`/`draft_rarity_weights` 键集相等 · `validate_ranking` 段数从 run.json 推 ·
券:`core/ticket.gd WIRED` 登记表(券的用法在代码里接线,没登记的 id 进 JSON = 红)+ `mult_min/max` 成对;
db.gd 那句「scope 写错会静默可用」的假门禁改口。

**C 仪器诚实**:`gate.sh tests()` 落实四判据(timeout · 退出码 · SCRIPT ERROR/^ERROR 计数 · 通过数地板 1976)·
`gate.gd` 事后判生死乘曲目税(RunLoop 新回传 `sec_kinds`),**trilogy 从 `weak_upper_bound` 摘出**(它的「恒零」是结构性的,下次全量门真测)·
`RunLoop.Opts.boon`(缺省空 = 旧世界;sim/curve 接 boon 是下一步)· `RunLoop` 给 `_blind_rng` 播种(sim A/A 抖动的根因候选,待 A/A 验证)·
`pair.gd` 退出码真的说话(第一关不一致或 |z|≥3 ⇒ 1)· 两处「逐字节等价」恒真断言改成**非空参数必须改变分布**(boost ×1000 / boon seen)。

**E 文档**:CLAUDE.md 第一屏指针 + 金币两出口 + 升级三原则 + 六条新原则 + 「开局三步同一份」;STATUS 三条现状/Android/脸计数/数据表/
工具表/文档地图;**TODO 776 → 254 行**(08-16/17 两批原文搬进 CHANGELOG 补账,加分族推导搬 jokers_history,BPM 搬 levels,
成本表搬 capability);LESSONS 补第八/九种假绿 + 三条判据,旧探针法就地标废;levels/tech/difficulty 三篇权威过期处就地 ⚠⚠;
context/meta 回写落地状态,meta 红线②改成真话;ui_meta/README 的 META 口径。

**未动(留待)**:F 性能(静态页每帧重画 / Shader 每实例编译 / shop 按钮泄漏)· sim/curve 真接 boon · replay 真注入 ·
三个 context 阈值进数据 · 主角碎片 · 全量门(修完 C 之后必须跑一次拿新基线)。
验证:批 A+D 宽探针 **1976/0**(13 域),批 C 探针见 STATUS。

---

## 2026-08-20 · price 全表出数(20.6h)+ 它抓出的一张静默丢脸

**Director 排序表从此是完美玩家尺子的精修版**(此前是全量门读数的第一版):
26 个「(脸,段)」实测分差重排,`rankgen.py` 重刷 `ranking.json`。
明显挪位:facedown 在 S1 比想象狠(z=-12.8)、blindspot 在 S2 掉到最狠端、
setlist 比门读数温和。10 个放置 |z|<3 量不到(时间族 + lostpage/lowend/wetink),
处置口径印在 price 输出尾部(真无代价 vs 样本不够的判别法)。

⚠⚠ **重刷当场抓出一张静默丢脸**:`trilogy` 和 raisedbar 同族(改目标分不改分数,
仪器盲区),但 rankgen 的设计性覆盖**只点了 raisedbar 的名** ⇒ trilogy 被丢出排序表,
而 `Director.ranked_pool` 是**取交** —— 这张脸会永不登场且不报错。两处修:
① rankgen 点名规则改**结构规则**(池里有、表里没 = 一律压最狠端并打印);
② `validate_ranking` 加**反向完备性**(池脸不在表里 = 红, 指路重跑 rankgen)。
⚑ 这是「点名清单挡不住同族新成员」的又一例 —— 守卫要写在形状上, 不写在名字上。

---

## 2026-08-20 · 设计稿全批照做:「一切先按你的设计来,我的工作是试玩然后给出调整」

context.md / meta.md 的待拍岔用户整体委托,推荐项一次落地(推荐「等」的仍在等):

- **C3 回归局**:隔 ≥3 天回来的第一局 = 强制温和档 + **熟脸**(novelty 反向:挑见得最多的,
  重建手感不给新东西);回归局压过 streak(上次的连败不是这次的状态)。
  `SaveState.returning_run()` 由 session gap + 本会话局数推,读点必须在 note_run_started 前。
- **C4 Boon novelty**:`BlindBoon.roll(rng, seen)` —— 没见过的 boon 优先,恒一次掷点。
- **C1 探索型货架**:没用过的 Target 货架权重 ×1.5。`Economy.weighted_pick` 加可选
  `boost` 参(**空表连乘法都不做** —— sim/bot/探针的随机流一位不能漂,41.9% 事故同款
  防御;t_asset 锁了逐字节等价 + RNG 消耗);采集 = 局末 Target 入 `targets_used`。
  三道闸:数据开关 · 探针恒空 · 零历史新玩家不动。
- **M3 点唱机**(90◈):首页奏响免费首曲 + 已购唱片(音量收小);没买保持静音 ——
  首页音景的口味题顺手解决。进局 `play_section` 显式拉回 -8dB。
- **M1 赛季脚手架**:`season_now` + 资产可带 `season`;**下架 ≠ 没收**(货架只摆
  常驻+本季,已持有永远显示生效)。出厂无赛季,零行为变化。
- **M4 META 快照进打点**:两个 `Tape.begin` 带 `meta: {gems, assets}`(留存分析的
  「持有资产数 × 回访」要用;记事实 —— 开局这一刻的钱包)。
- 四个 context 开关(novelty/streak_shift/returning/explore_shelf)同住 director.json
  `context` 节,**全开**;validator 白名单同步。
- 验证:探针 **1028/0**(含 boon/economy/shop 回归)· 11 行资产页截图 ✓(顺手把行距
  74→70 再收一轮 —— 加了点唱机后 11 行又压页签轨,同一个坑第三次)。
- 推荐「等」的仍在等:C5 死亡段细粒度(等 streak 过试玩)· C2 A/B(等回传上线)·
  主角碎片(下一批, 上锁名单归用户)· 疲劳轴(待留存数据)。

---

## 2026-08-19 深夜 · 两条拍板落地:streak_shift 开 + 资产循环推倒重做(v2)

用户两条拍板,一条开了开关,一条推翻了我 v1 的整个定价思路:

- **「开,就是要千人千面,但不要被察觉」** ⇒ `streak_shift: true`(连败 2 降档/连胜 3 升档)。
  「不被察觉」的保障 = 它只换**哪张脸上场**,目标分/规则/数值/UI 全不动。
  t_director 断言口径同步翻面(ships ON)。08-14「不读 context」至此完整推翻:
  当年是「不用千人千面」,现在是「就是要千人千面」。
- **「没有回本周期,参考蔚蓝星球的道具:买了你就想玩,玩了你就想买」** ⇒ 资产 v2:
  **分红阶梯整个删掉**(yield_gems 机制留在代码里,表里不用)—— v1 的错在把资产做成
  产钱的数字,那正是「回本周期」思维。v2 资产 = **玩的时候看得见听得着的内容**:
  **唱片 ×6**(assets/audio 十首只免费每场馆第一首,其余上架 —— `Music.track_pool`
  = 免费首曲 + 已购唱片,买了下一次进那个场馆就轮到新歌;v1 曾十首全开,现在收口)·
  **彩带机**(庆功彩带 26→52)· **应援团**(局内舞台荧光棒 4→8 手工插空 +
  结算荧光棒海 26→52)· 两张券合约照旧。宝石收入只剩演出本身(每段 2 + 通关 4)。
  db 校验加两条静默错拦截:track 必须指向真实 wav(买了张放不出声的唱片不报错)·
  flair 必须在消费端白名单(confetti/crowd)。
- 验证:探针 **1020/0** · 资产页 v2 截图 ✓(顺手抓到 10 行压页签轨,行高 84→74)。

---

## 2026-08-19 晚 · 两个新需求落地:Director 读 context + META 资产循环

用户:「我只有 2 个额外需求,『基于 context 生成关卡』的能力;meta 系统,
『买游戏资产-资产变现-买更多资产』的循环。」两件都有现成的规格底子,落在接口位上:

- **基于 context 生成关卡** = generating.md §3 的玩家状态 m 从「几乎是空的」变成实体。
  ⚠⚠ **推翻了 2026-08-14「Director 不读 context」的拍板**(difficulty.md §3 已就地标注)。
  实现:SaveState 存 m 的两个切片(**近期战绩** 20 局圈 + **faces_seen** 计数),
  `Director.roll_run(..., ctx)` 消费 —— **novelty**(同档内偏向见得最少的脸;
  这是 §2.6「新鲜感只能编码不能测」的编码处,缺省开)+ **streak_shift**
  (连败 2 降档 / 连胜 3 升档,阈值不对称是设计;DDA 味最重,**缺省关等用户拍**)。
  三条铁律不动:不碰目标分 · 改排布不改数值 · 数据侧禁玩家模型(validate_director
  禁用表保留,新 `context` 节只认两个布尔)。工程形状照 min_run 先例:
  **ctx 显式入参、缺省空 = 逐字节退回**(t_director 锁了等价 + RNG 消耗)。
- **META 资产循环** = ui_meta.md 三层生命周期的**永久层**第一块实装。
  `data/assets.json`(8 档阶梯,价格升序即阶梯,db 硬校验)+ `core/asset.gd`
  (接字典的纯函数,ticket.gd 同款)+ SaveState(gems 钱包 / settle_run_meta 一口结算)。
  **两条变现出口刻意分开**:财富 = 宝石(通关段数入账 + 资产每局分红, 只能再买资产)·
  力量 = 券(合约类资产每日加发, 走日清零的消耗品层)⇒ **循环碰不到局内金币**,
  「不许永久底倍率 / 不许白送局内金币」两条红线结构性成立;收入不挂绝对分数,
  绕开「赛季挂绝对分改求解器目标函数」那个未拍的岔。UI:荣誉页第三子页「资 产」
  (真数据, 点行即买)+ 结算屏收入行(失败也有 —— 参与收入是「输了也在攒」的可见证据)+
  首页/图鉴宝石接真钱包。数字全占位, 归用户手调。
- 顺带:券类资产的每日加发挂进 `settle_daily_ticket` 的跨天判定(「今天到了没」要在
  grant 之前读 —— settle 会把 grant_day 盖掉, 顺序反了就永远不发)。
- 验证:批次探针 **1011/0**(t_director+t_lingo+t_uplink+t_asset+t_ticket + 9 文件 parse)·
  资产页与结算收入行截图目视 ✓。⚠ 抓到两个自己的坑:t_director 里 `seen` 撞名(一个
  run() 是一个作用域);`_DIRECTOR_KEYS` 同时当必备键用, 可选的 `context` 不能进那张表。

---

## 2026-08-19 · 进入 1.1:券上线 + 音乐卡拍 + 英文化 + 回传通道(一批四件)

用户拍板进 1.1(备案自己去跑),四件按侵入性排序一批做完,遵守批次纪律
(只跑秒级验证,全量门等用户喊):

- **券上线**:`tickets.json enabled` 翻 true,`t_ticket` 第一条断言口径同步翻面
  (1.1 起谁关回去谁红)。四层实现 1.0 期间一直在树上被单测背书,上线就是一个词。
- **音乐卡拍(v2)**:先按纪律量波形 —— 10 首实测**恰好 8.0000s、首瞬态 ≤0.4ms、无渐入**,
  与素材 README 声明逐条对上(声明≠测量,这次真量了)。接线 = `Music.sync_beat()`:
  **钟起步的那一刻 seek(0),kick 永远落在拍首**;拍长偶数秒的约束保证任何拍都在小节
  边界收束,真正会漂的是拍与拍之间(结算/商店/特写),所以每拍归零一次。
  特写路径的 kick 由 `_closeup_done` 落(特写恰好 2.0s = 1 小节,但 tween 有帧抖动
  ⇒ 显式归零)。硬切靠首 kick 瞬态遮蔽,不需要淡入淡出。
- **英文化(主战场美国)**:机制 = **gettext 式单表** `data/lingo.json`(~300 条)+
  `core/lingo.gd`。三路共用一张表:DB 的 ui/tutorial/run 出口**在数据层整树替换**
  (23 个 `DB.ui()` 消费点零改动)· 代码字面量包 `Lingo.t()`(~90 处)· characters 的
  cn/title/fx;实体四族(joker/face/boon/ticket)不进表 —— 数据里本来就有英文 `name`,
  构造时 `Lingo.pick()` 挑。语言解析:`SYNC5_LANG` env → **探针钉死 cn**(读数不许跟
  宿主机语言走)→ 存档 `lang` 键(设置页开关的口,UI 还没有)→ 系统语言。
  `t_lingo` 四层守卫:数据完整性 / 源码扫描(裸中文字面量红)/ `Lingo.t` 实参必须在表 /
  双语行为断言。**Tape 不走语言层** —— 日志记事实,两台机器的日志必须长一样。
  英文截图目视 7 屏(home/draft/settle/tutor×2/crew/tray)修掉五处:BOSS 规则行截断 ·
  盲注板 boon 行与脸胶囊**叠压**(脸胶囊先算、boon 行让宽 + 缩字)· 卡名头带自适应缩字
  (Monochrome 被 0.62w 截半)· trigger 文案全族压进 ~28 字符 · 牌型名换扑克黑话
  (Trips/Quads/Str. Flush —— 结算分解框装不下 Four of a Kind)。中文对照截图逐屏无回归。
- **回传通道**:`core/uplink.gd`(簿记:待传=tape 目录减在写那局 · 传成搬 `sent/` 不删 ·
  「还没搬走」本身就是持久化的重试队列)+ `view/beacon.gd`(HTTPRequest:开机 5s 一扫 +
  回首页 poke,批量/重试上限)+ `tools/tapeserver.py`(参考接收端,按 install+文件名去重,
  `install_id == "probe"` 直接丢 —— 探针闸的服务端兜底)。**缺省关**,`tape.json upload`
  节填端点翻开;「enabled 开着但 url 空」是测试期红灯。curl 级 E2E 验过四码位与去重。

⚠ 两个当场踩的坑:① 新 `class_name` 忘跑 `--import`,探针空转(老教训新用);
② `var out := _walk(d)` 类型推断失败 ⇒ **Lingo 整个没编译过而套件还能跑出 275 绿 + 6 红**
—— 半编译树的红是级联假象(tutor_focus「白名单为空”),真根因在 SCRIPT ERROR 第一行。
③ 仪器坑:PCRE2 新版 `\p{Han}` 按 script-extensions 匹配,**「·」(U+00B7) 算 Hani**
⇒ 带间隔号的纯英文被判「混中文」;两处正则换显式 CJK 区间。
快速探针收口 **455/0**(t_ticket + t_lingo + t_uplink + 关键文件 parse),全量门本批未跑。

---

## 2026-08-18(手机试玩现场)· 四条反馈当场修 + 一颗探针抓住的启动雷

用户在手机上边玩边报, 四条全落:

- **教学步进拍中推进**(「照做了提示没消失」→「应该消失, 进入下一个提示」):
  推进原本只挂在拍末结算里, 做完动作要干等 10~12 秒。新增 `Run.tutorial_advance_if_done`
  (拍中版):只认真做到的、不吃兜底、**推进时清动作账** —— 第 3/4 步都是 swap,
  不清会一次交换连跳两步, 把练习位整个吞掉(t_tutorial 锁着);编排器动作上报处
  当场推进并重放提示。play 语义(把一拍打完)仍归拍末。
- **教学光感分级**(「光感太模糊了不舒服」):整片加法白盖在大区域上读作**雾**
  (均匀抬亮 = 对比被压平 = 模糊)。大区域的提亮撤到近零 —— 压暗层的对比本来就够;
  真正保留提亮的只有小件(DJ 键);under-glow 辉光 0.22/34 → 0.16/22 收紧边界感。
- **缓存牌选中抬升**(「盖住了点击不会抬高一点」):手牌选中会抬 18px, 缓存原本只有
  光效 —— 被压暗层/提示条半掩时选没选根本看不见。同一手势同一反馈, 差值与手牌同源。
- **替换态显式放弃**(「第五张想弃掉, 好像没有操作办法」):取消原本只挂在「点 Target 槽」
  上 —— 一个**没写进任何提示**的暗手势。预览卡下加「✕ 不换了」按钮, 与 Target 槽同一条
  取消路;⚠ 不做成预览卡自身的点击 —— tapped 在**按下**就触发, 一按就取消等于拖不了。
  未付款(成交才扣钱), 放弃无需退款。

⚠⚠ **探针抓住一颗启动雷**:第一版把 `replace.canceled.connect` 插进了 `_build_ui` 的
信号块 —— 那时 `replace` 还是 null, **整个 `_ready` 当场断掉, 真游戏开屏就崩**。
headless 套件不加载 view, 对这类错全盲;是 traysheet 渲染探针的 SCRIPT ERROR 抓到的。
⚑ 顺带又中一次「查得太窄」:探针日志先用 `^ERROR` 数了一遍 0, 而 `SCRIPT ERROR:` 不匹配
这个模式 —— **同一张判据表, 每个新日志入口都要重新对一遍。**

---

## 2026-08-18 · 券系统按拍板下线到 1.1:关的是开关,不是代码

用户:「我决定第一版去掉券,第二版商业化的时候加。」下线走「重看教学」的先例 ——
**机制与测试全留,关掉入口**:`data/tickets.json` 新增 `enabled: false` 总开关,
`SaveState` 五个券入口在探针闸旁边加同一句 `or not Ticket.enabled()` ⇒
不发放、不显示、不可用,存档里已有的券也隐身(不删,1.1 回来时跨天清零自然处理)。
四层实现 + 使用入口 + 42+ 条断言全部留在树上继续被单测背书;
`t_ticket` 第一条断言**锁住发行决策本身**(谁在 1.1 前翻回 true 它会响)。
⚑ 只闸唯一进水口(每日发放)其实就够让全系统死水 —— 连读口一起闸,
是为了把**这台机器今晚测试时已发进存档的券**也藏掉。

---

## 2026-08-18(凌晨)· 门收官 + 三件小补 + 目录大搬家:1.0 交验前的最后一批

**全量门 16192s(4.5h):唯一红点 `lowend`,其余全绿**(63 张小丑牌全量到,
黑调/红调 z=+7.1/+11.0;流程/打点/重放/尺子全过)。

- **`lowend` 进 `weak_upper_bound` 豁免**(按门自己的处置指引):z=−3.31 **统计显著**,
  但只占基准 4.0% < 5% 门槛。根因不是脸变了 —— **两对 ×3 把价值从「点数和」挪向
  「牌型×倍率」,而低音(只补 2~9)税的正是点数和**:2~9 组对子和高牌一样好使 ⇒
  对完美玩家咬合面真的变小,同时基准分被抬高,分子分母两头压比值。
  处置 = 改声明不改内容(§6 铁律);它对真人还叠着「读牌变难」的感知成本,价值在真人侧。
- **教学关真路径探针**:`SYNC5_PROBE_FRESH=1` 让 `seen_tutorial()` 在探针里返回 false ——
  探针从此走**真人同款入口**(choose_character 的教学分岔),tutorsheet 弃用
  「事后手按 `run.tutorial`」的旧办法(按晚了 = 两次「教学关冒出 BOSS 脸」假象的来源)。
- **每日发券提示**:第一拍飘字「今日券 · X」。此前静默发放 ——
  「冻结是看不见的」同一课:**没人报的赠品和没发过长得一样**。
- **目录大搬家(用户拍板「按 Godot 规范归位」)**:`resources/` 拆三路后删除
  (music → `assets/audio` · 设计稿 html + 引用素材 + godot-handoff → `docs/mockups/`
  **整体搬**,html 和它相对引用的 assets 不拆散)·`design/` → `docs/design/` ·
  全仓 100+ 文件的路径指针一次改写(.gd 注释 / JSON _comment / 根 md / docs 内链深度 / 记忆),
  验收 = 残留 0 · 双前缀 0。**`docs/.gdignore` 把设计材料挡出 Godot 扫描与出包** ——
  此前 mockup 素材一直被白白 import。
  ⚑ 搬迁判据:先证 `res://resources` **零 load**(全是注释指路)才动手;
  music 与其余去向不同 ⇒ 替换必须分两步,否则历史里的 `resources/music` 会被错写成 mockups。
  ⚠ 两个 sed 教训:zsh 的 `for f in $VAR` **不分词**(整串当一个文件名,砸在 sed 上才暴露);
  自指句会被自己的规则改写(「design/ → docs/design/」变成同义反复)—— **改写后要 grep 验证,
  还要人工看一眼曾描述这次改写本身的句子**。

---

## 2026-08-17 · 券系统收口:缺的只是「使用入口」,而交接把账记反了两个方向

接手时交接写着「发放出口与使用入口都没接」——查证后**两头都偏**:
每日发放和 seedmoney **早就接好了**(账记漏),而「18 + 24 条断言」**从未存在过**(账记多,
LESSONS 新增「第六种绿是假的:账本自己说谎」)。真正缺的是四张券的使用入口,本轮补齐:

- **boost 获得时掷值**(用户拍板「拿到手就知道」):`ticket_rolls` 与张数同长、FIFO,
  发放收拢到 `Ticket.grant_with_roll` 一个入口 —— 拆开走迟早有一条路径忘掷;
- **计分走 `Beat.settle` 的 `meta_mult`**:缺省 1.0 = 探针逐字节不变;raw_score 不吃加成
  (复读/回放读原始分);记分留在 core,view 只管「什么时候上、上多少」;
- **`Phrase.redeal_hand` 不是弃牌**:不计弃牌计数器、不占预算、不开 action_track ——
  否则周转/早弃被一张券白喂(零挂机成长);封印卡不换(券不许破解脸规则);
- **托盘 `view/tray.gd`**:chips 用 Button(不再自造命中测试)、只占自身矩形(PASS≠透明)、
  数据注入制(探针闸挡得了存档、挡不了目视);教学关不显示;boost 用后亮「已上」标牌
  (「冻结是看不见的」同一课:看不见的增益和没生效长得一样);
- **点唱券**:shop 只分流、券在编排器消耗;**不推付费阶梯**(券买的是「多看一次」);
  淘碟照常喂(刷新真发生了,代价是券);
- **顺手灭一个既有噪声源**:`joker_slot._draw` 在 pop 动画首帧把字号算成 0,
  每次开店刷 4+ 条 `p_size <= 0` ERROR 进门日志 —— scale < 0.08 整帧不画。

回归:tests **1868/0**(+109,四条判据全满足)· 三张截图目视 ✓(托盘/armed/商店券按钮)。
广告出口顺延(它是**加发**通道,等 TapTap SDK),1.0 代码侧到此只剩全量门。

---

## 2026-08-17 · 两对 ×2 → ×3:一次「推翻的推翻」,尺子换了拍板就该回位

用户:「C1 牌倍率我之前说过了,你看下有没有记录。」记录链查清:

1. **08-06 白天用户拍板 ×3**(「two pair 和 pair 怎么是一个倍率」);
2. **08-06 晚被覆盖回 ×2** —— 按「实测累计 `P(≥)` + 0.75 幂压缩」重定价算出 2.47;
3. **08-14 用户把定价口径换成组合**(技术溢价归玩家 · 实测会漂 · 组合可精确算),
   但表没跟着重算 —— 组合累计下同一套压缩 = `2×(92.88/53.22)^0.75 ≈ 3.04`,
   **与用户当初的 ×3 相符**。

⇒ **恢复 ×3**。终版表 `1/2/3/3/4/5/6/7/8/8`;两对与三条共 mult(原作本就 2,2/4,4,4/8,8),
chips 20<30 保扑克序。影响面全表最大:两对是策略后最常打出的牌型(33.5% 的拍)。

⚑ 判据一条:**拍板被仪器覆盖后,若那台仪器后来被换掉,要回头检查被它覆盖过的拍板** ——
这次是用户自己记得才翻的案,文档里三处(pattern.gd 注释 / cards.md 表 / TODO C1)
互相矛盾了一周没人发现。

**同批顺手收的尾**:twotone 拆分(08-16 实装)的三件外围 —— art manifest
(黑调占 S-15 旧位、红调**追加队尾** S-61:code 按下标推导,插中间会让 40+ 张全体改号)·
`joker_slot.gd` 字形/倍率牌 · `jokers.md` roster 行。`cards.md` 的倍率表同步修正
(Flush ×5 / Full ×6 早就落了代码,表里还写着 ×4)。

---

## 2026-08-17 · 补账:第一批 + 14 条真人 bug + 七种假绿(2026-08-21 从 TODO 搬来,原文未改)

> ⚠ 这一批当年只记在 TODO 的「交接」节里,CHANGELOG 一条都没有(2026-08-21 根文档评审抓到)。
> 下面是 TODO 原文整段搬迁;其中「七种假绿」的 #6/#7 与「三条探针盲区」已另补进 LESSONS。

## ⭐⭐ 2026-08-17 交接 · 下一个 session 从这里接

### 🚨 第一件事:**门已经过期了,必须重跑**

`gate.sh` 在 **14:51 全绿**(7 步一趟连续跑完)。但那之后**又改了 14 处**(全是真人试玩报的 UI/流程 bug),
所以**现在的树没有任何一趟连续的门背书**。已跑过的只有:单测 1759/0 · 流程回归 0 bugs · 若干针对性探针。

⇒ **下一步 = 重跑一次全量门(~4h)**, 之后才谈得上 1.0。

### ✅ 这个 session 做完的(两批)

**第一批(功能, 已过门)**:S1 目标分 921→560→**420** · 禁回 `min_run: 10`(**新开「脸按局数解锁」通路**)·
教学关重做(做中学 + 动作门)· 教学高亮改**白光 + 压暗** · **双色调拆黑调/红调** ·
**升级系统**(金币的主出口, 5 级 `[4,7,11,16]◆`, 按**增量**放大)· 加分族 **12 张改跟随尺度** ·
快闪回池(窗口改成每段第 1 拍)· Director 接线(**300/300 逐字节等价**已验)· 券的四层(数据/纯逻辑/存储/存档)·
`bot_targets` 重锚(尺子 random 7.5% → **4.0%**)

**第二批(真人试玩的 14 条 bug, 未过门)**:见下。

### 🐛 真人试玩抓到 14 条, 门一条都抓不到

| 症状 | 根因 | 谁的 |
|---|---|---|
| 替换小丑牌点不动 | `Hand` 全屏 PASS 盖住槽位 —— **PASS ≠ 透明**(赢命中测试, 事件传给**父节点**不漏给兄弟) | 既有 |
| 手牌弃不了(只能弃缓存) | 主角轨道框 `PanelContainer` 默认 STOP, 盖住手牌行 | **修上一条引出的** |
| 牌不刷出来 / 没教学 | `_reset_run` 把 `phrase` 清空后, 开场金 `phrase.coins +=` **空引用** | **我造的** |
| 教学关弹盲注公示卡 | `_enter_section` 缺教学关的闸 | 既有 |
| 教学中途退回首页 | 旧设计:走完就 `_open_home()` ⇒ **玩家一局都没打过就被弹出去** | 设计错 |
| 回首页再开局留着旧卡 | `run.reset()` 漏了「回首页再开局」这条路径 | 既有 |
| 不能同时选多张 / 手牌弃不了 | **能力压制**:`unlock` 把部件关着 ⇒ 玩家体感是「点了没反应」= 和 bug 一样 | 设计错 |
| 高亮反而变暗 / 好丑 | 光画在内容下面(不染花色的代价)⇒ 小圆钮本身没变亮, 只有周围变暗 | 我造的 |
| 文案里 `**` 星号原样显示 | `draw_string` 不解析 markdown | 我造的 |
| 开头要点一下才走 | 我加的「动手才起钟」—— **冻结是看不见的**, 「给你时间」和「卡住了」长得一样 | 我造的 |
| 同一句提示重复太多 | 软门**没有兜底**(给时钟做了 30s 兜底, 忘了给步进做) | 我造的 |

**⇒ 已全部修完, 但只过了单测 + 流程回归, 没过全量门。**

### ⚠⚠ 这个 session 最贵的教训:**七种「绿是假的」**

| # | 现象 | 真相 |
|---|---|---|
| 1 | 退出码 0 | 脚本 parse 失败, `SceneTree` 空转十几分钟 |
| 2 | `load()` 非 null | 类解析其实失败 —— **只有 `--check-only --script` 算数** |
| 3 | `1706 passed, 0 failed` | **一个域中途抛类型错被掐断, 47 条没跑**, 框架把「没跑」记成「没失败」 |
| 4 | `SCRIPT ERROR = 0` | 日志里 **120 万行 `ERROR:`** —— `push_error` 出的是 `ERROR:` 不是 `SCRIPT ERROR:` |
| 5 | 门在跑 | **我同时改源码** ⇒ 那一趟验的是一棵不存在的树, **日志里一个异常都没有** |
| 6 | 全量门绿 | **门覆盖不到的地方**(输入命中测试)绿了也不代表没坏 |
| 7 | 探针全过 | **我亲手把代码移出了测量范围** —— 券的探针闸让 `consume_ticket` 恒 false, 那行永远不执行 |

**⇒ 判据升到四条同时满足**:`退出码 0` **且** `failed 0` **且** `SCRIPT ERROR 0` **且** `^ERROR 0`
(剩余 `ERROR` 还要**逐条看过来源**), **外加通过数要涨**。全部写进 LESSONS。

### ⚠ 三条探针盲区(今天一半 bug 的共同成因)

1. **探针不点屏幕** ⇒ 输入命中测试、层序、`mouse_filter` 一律无覆盖
2. **探针进不了教学关**(`_is_probe()` ⇒ `seen_tutorial()` 恒 true)
3. **探针拿不到券**(同一道闸)⇒ 券相关代码零覆盖

⇒ **待办:给教学关补一个真走那条路径的探针**(照 `tutorsheet.gd` 的办法, 在 `choose_character`
**之前**把 `run.tutorial` 按上)。⚠ 今天两次「截图里教学关冒出 BOSS 脸」都是**探针按晚了**的假象, 不是 bug。

### 🎵 1.1 音乐(用户自己生成中)

**BPM 已算死 = 120(4/4)**, 见本文件「1.1 · 音乐」节。四条生成提示词已给用户。
⚑ 派生约束:**拍长一律取偶数秒**, 守住它 120 BPM 就永远对齐。

### ✅ 1.0 已出货(2026-08-19 凌晨;本节此后是历史)

**main = c0aecc9 已推 GitHub · APK = `build/sync5-taptap.apk`(119MB, com.sync5.dev, debug 签)·
隐私页已公开 https://kumaloha.github.io/sync5-privacy/ · Director 上岗(hundred.gd 100 关验证)。**
新 session 接手先读 memory 的 sync5-session-20260819(悬着的五件:Tap 资质截图 · 软著材料包 ·
换牌触控 · price 精修 · tiers 表)。⚠ webserve.py 是会话级进程, 给用户手机试玩要重启。

### ~~⏳ 1.0 还差(2026-08-17 晚更新;流程 = 我做完 → 用户验 → 验完出包)~~

**我的(✅ 08-18 凌晨全部做完, 等最终验证绿)**:
✅ 全量门跑完 —— **唯一红点 `lowend`** 已按门的指引进 `weak_upper_bound` 豁免
(z=−3.31 显著但只占基准 4.0% < 5%;根因 = 两对 ×3 把价值从点数和挪向牌型倍率,
低音税的正是点数和 ⇒ 咬合面真的变小 + 基准被抬高, 理由全文在 `docs/design/blinds.md`)·
✅ **教学关真路径探针**(`SYNC5_PROBE_FRESH=1` 让探针走真人同款入口;tutorsheet 已换新办法)·
✅ **每日发券提示**(第一拍飘字「今日券 · X」, 文案在 ui.json;方法留了探针注入口)·
✅ **目录整理**:`resources/` 拆三路后删除(music→`assets/audio` · 设计稿+素材+handoff→
`docs/mockups/` **整体搬**, html 与它引用的 assets 不拆散)· `design/` → `docs/design/` ·
全仓 100+ 文件路径指针一次改写(.gd 注释/JSON/根 md/docs 内链深度/记忆), 零残留零双前缀 ·
`docs/.gdignore` 把设计材料挡出 Godot 扫描与出包(此前 mockup 素材一直被白白 import)。
✅ **手机试玩四连修**(08-18 白天, 细账在 CHANGELOG):教学步进**拍中推进**(做完动作
提示当场换)· 光感**分级提亮**(大区域撤雾, 小件保留)· 缓存牌**选中抬升** ·
替换态**「✕ 不换了」按钮**(取消不再是暗手势)。⚠ 期间探针抓住一颗 `_ready` 启动雷
(信号连在构造之前), headless 套件对 view 全盲, **渲染探针是这类错唯一的网**。
✅ **教学高亮重做**(08-18 凌晨, 用户:「需要操作的区域提亮, 其他地方变暗;
有时候包了一层形状不对的光晕, 怪」)—— 病根 = **光和暗的形状是两套逻辑**:
光会画圆(弃牌键)、暗只会挖方 ⇒ 圆光外面套一圈没压暗的方角;多块高亮取并集 ⇒
手牌与缓存**之间的走道**也漏亮。修法:形状收成一份(`Widgets.focus_radius`,
光/暗/提亮三层共用)· 压暗层**逐块挖形状洞**(含反向圆角补片, 纵向重叠退回并集兜底)·
新增 **`TutorLight` 加法混合提亮层**画在内容之上 —— 历史上两版「最上层加光」翻车都是
**普通混合往灰里拉**, add 是加光不加灰, 这才是能把不透明小圆钮真正照亮的位置。
已跑真路径 tutorsheet 6 步逐张目视 ✓(圆钮圆光 · 双区域各自开洞 · 走道正确压暗)。
⏳ 收尾中:`lowend` 豁免后的单脸重验(在跑)→ 提交 → 交用户验 →
**出 Android 包**(TapTap 给朋友试玩)。单测已重跑 **1868/0**(目录搬迁后全绿)。
**用户的**:和弦/排练两张**条件坏了**待拍(改条件 or 删)· 教学关**新手数据**(找个没玩过的人)·
3 张插画(见 🔴)

✅ **券系统 2026-08-17 收完 → ⏸ 2026-08-18 按拍板下线到 1.1**(「第一版去掉券,第二版
商业化的时候加」)。下线方式 = 「重看教学」同款先例:**关开关不删代码** ——
`data/tickets.json` 的 `enabled: false`,SaveState 五个入口同一道闸(不发放/不显示/不可用,
存档残券也隐身);四层实现 + 全部断言留在树上继续被单测背书,`t_ticket` 第一条锁着这个拍板。
收尾细账(1.1 翻开关时直接可用):每日发放早已接好(启动时结算 + seedmoney 开局兑现,
交接把它们记漏了);后补的**使用入口**:局内券托盘(`view/tray.gd`,overtime/redeal/boost)+
商店点唱券免费刷新 + boost **获得时掷值**的存位(FIFO,`ticket_rolls`)。
⚠ **广告出口顺延**:它是**加发**通道(`ticket.gd` 文件头有拍板:每日发放不用广告也不用领取按钮),
且要等 TapTap SDK 那一步 —— 不挡 1.0。
⚠ **交接的「券 18+24 条断言」是假账** —— tests/ 里从未存在过;`t_ticket` 域 2026-08-17 才是第一份。

### ⚑ 2026-08-17 晚 · 券使用入口的实现账(过单测前先记)

| 件 | 落点 | 要点 |
|---|---|---|
| boost 掷值 | `core/ticket.gd` | **获得时掷**(用户拍板「拿到手就知道」), `ticket_rolls` 与张数同长 FIFO;发放收拢到 `grant_with_roll` 一个入口 |
| 计分通道 | `core/beat.gd` | `flags.meta_mult` 缺省 1.0 = 探针逐字节不变;raw_score 不吃加成(复读/回放读原始分);**记分留在 core**(判生死同一条线) |
| 整手重发 | `core/phrase.gd::redeal_hand` | **不是弃牌**:不计弃牌计数器/不占预算/不开 action_track —— 否则周转/早弃被一张券白喂(零挂机成长);**封印卡不换**(券不许破解脸规则) |
| 托盘 | `view/tray.gd` + `layout.gd` + `ui.json tickets` 节 | chips 用 Button(刚踩过两条命中 bug, 不自造命中测试);**只占自身矩形**(PASS ≠ 透明);数据注入制(探针闸挡不了目视);教学关不显示 |
| 加时 | `view/phrase.gd` | 三个钟(dur/warning/lock)**走同一套 GameConfig 钩子重推**, 不手加三个数 |
| 点唱券 | `view/shop.gd` + 编排器 | shop 只分流不消耗;**不推付费阶梯**(券买的是「多看一次」);淘碟照常喂(刷新真发生了, 代价是券) |
| 打点 | 编排器 | `Tape.on("ticket", ...)` 四处:overtime(at/dur)· redeal(重发后的手)· boost(mult/total)· juketicket |
| 测试 | `t_ticket`(新域)+ `t_phrase` + `t_run` | 掷值不变量 · redeal 非弃牌 · meta_mult 缺省零痕迹 |

---

---

## 2026-08-16 · 补账:第二批六件 + 分级门 9 轮 + S1 921→560→420(2026-08-21 从 TODO 搬来,原文未改)

> ⚠ 同上:升级系统、加分族 A 案、教学关重做、双色调拆分、`min_run` 新通路、`bot_targets` 重锚都发生在这一天,
> CHANGELOG 此前零记录。TODO 原文整段搬迁。

## ⚑⚑ 2026-08-16 深夜 · 第二批的账(六件功能 + 分级门跑完三关)

### ✅ 这一批做完并验过的

| 事项 | 验证 |
|---|---|
| **加分族 A 案** —— 12 张换成 `bonus_target_pct`(每拍目标 × pct) | 换算 `pct = 旧数额/416`(灯牌锚在 12%), **只改尺度不重新定价** |
| **快闪回池** —— 窗口 `只有第1段` → **每段第1拍** | ⚠ **不是照原则加数额**:`docs/design/jokers.md` 写着「加数额是假修」, 它的死因是商店最早 S1 过半才开 ⇒ 改的是**窗口形状** |
| **Director 接线** | 300/300 逐字节等价已验(含随机流位置);⚠ 接线时抓到 `Director` 绕过 `min_run` 门的 bug |
| **券:数据 + 纯逻辑 + 存储 + 存档** | 5 张券 · ~~18 + 24 条断言~~ ⚠⚠ **假账(2026-08-17 查证)**:tests/ 里从未有过任何券断言 —— `t_ticket` 域 08-17 才是第一份。交接说「有测试」要先 grep 到再信 |
| **教学提示条自动躲开所有区域** | ⚠ 第一版漏了「标签画在容器之外」⇒ 躲避矩形上放宽 44px |
| **D1/D2 两条模型分叉** | 三探针补传 `section_target` · `curve.gd` 显式 `UNLOCK_ALL_RUN` |

### 🚪 分级门(**9 轮才绿**)

| 门 | 结果 |
|---|---|
| 单测 | ✅ **1755 / 0**,四条判据全满足 |
| `pair.gd` | ✅ 配对差 +0.0 / −0.1,**与上轮逐字相同** ⇒ 加分族换通道没让求解器和游戏分叉 |
| sim | ❌ **尺子自检红** —— 见下 |
| kit | ⏭ **建议跳过单卡**:这批改了 13 张, 每张 12 分钟 ≈ 2.6h, 而全量门(含全部 kit)才 4.5h |
| `gate.sh` 全量 | ⏳ **等夜里跑**(用户 2026-08-16:「等我睡觉的时候跑 sim」) |

**门抓到的三件**(不跑门会一路带到试玩):
1. **真 bug**:`tools/bot.gd` 变量重名 `best_cost` —— 我加升级那段引入的;
2. **真 bug**:求解器给那 12 张加分卡估值 **0** —— `_amt()` 不认识新通道 ⇒ bot 永远不买;
3. **我的设计错**:`bonus_target_pct` 缺 `section_target` 时 `push_error` + 返回 0 ——
   ① 卡在模型里**隐身** ② 日志刷 **120 万行** ERROR。⇒ 改成退回**一局平均每拍目标**,
   与 `bot.gd::_avg_beat_target()` / 测试助手**三处同源**, 都从 `SECTION_TARGETS` 推导。

### ~~⏳ 夜里要跑的两件~~ ✅ 都做完了(08-16/17 补账)

1. ~~**`gate.sh` 全量**~~ —— 08-16 14:51 全绿过一趟;之后又改 14 处 ⇒ **08-17 晚重挂在跑**
2. ~~**`bot_targets` 重锚**~~ —— ✅ 已做(尺子 random 7.5% → **4.0%**,第一批)。
   原始症状留档:sim 尺子自检红 `random 3.4% → 7.5% > 5%` 哨兵线。
   ⚑ **哨兵在干活, 不是坏消息**:bot 变强是这批的**预期效果**(会升级 + 加分族中后期给得多 + 快闪回池)。
   ⚠⚠ **红的是 `sim.json` 的 `bot_targets`(仪器刻度), 不是 `run.json` 的 `section_targets`(玩家面对的)**
   ⇒ **不影响试玩**, 影响的是「sim 这把尺子还准不准」。
   ⚑ 顺带印证金币出口有效:局末余额 **34.7◆ → 8.5~10.6◆**, 钱真的被花掉了。

### ⚠ 这一批新欠的

| 债 | 性质 |
|---|---|
| ~~**券的发放**(广告 / 每日领取)~~ | ✅ 每日发放其实当时就接好了(交接记漏);广告 = **加发**通道, 顺延到 TapTap SDK 阶段, 不挡 1.0 |
| ~~**券的使用入口**(phrase / shop / run 三种 scope)~~ | ✅ 2026-08-17 收完(run 早在;phrase/shop 本轮补齐, 账见「⏳ 1.0 还差」下节) |
| ~~`bot_targets` 重锚~~ | ✅ 第一批已做(random 7.5% → 4.0%) |
| kit 13 张未单验 | 由全量门覆盖 |

---

## ⚑⚑ 2026-08-16 晚 · 账本(本轮改了 32 个文件, 门还没过完)

**⚠ 顺序已由用户改过一次**:原本是「改完 → 试玩 → 才跑门」,2026-08-16 晚用户补充
**「试玩我会最后做, 你得先把账补完」** ⇒ 现在是 **`补账 → 跑门 → 试玩`**。
理由不用猜:让他去玩一个没验过的包是浪费他的时间。

### ✅ 已补(第一步:两条**模型分叉**,必须在跑门之前)

**⚑ 为什么排在门之前**:门是**拿探针量的**, 而探针当时和游戏不是同一个世界 ——
先跑门等于用一把歪尺子量, 量完还得重量。

| # | 债 | 补法 |
|---|---|---|
| **D1** | `attrib.gd` / `warm.gd` / `model.gd` 三处直连 `Settle.run` **没传 `section_target`** | 三处补齐。⚠ 不补的话, 加分族 A 案的新通道在这些探针里**静默算 0**, 而 attrib 正是牌型频率与中性基准的仪器 ⇒ 会「测出」那族卡没用 = **自我实现的错误结论** |
| **D2** | `min_run` 造成的新分叉:探针缺省 `-1` = 全解锁, 而真人**第 1-9 局看不到禁回** | `curve.gd` 显式传 `UNLOCK_ALL_RUN = 10`。⚑ **目标分从此明确按「老玩家的世界」标定**;新手前 9 局池子更温和 ⇒ 偏差方向**安全**, 但它是真分叉, 写在调用点上 |

### ⏳ 第二步:**分级跑门**(不要直接上 4.5 小时)

```
单测        744s    ← 覆盖面最广、最便宜, 先跑
pair.gd     ~3min   ← 守「求解器 = 游戏代码」;升级放大最可能在这炸
kit 单卡×2  几分钟   ← SYNC5_KIT_ID=blacktone / redtone
sim         ~107s   ← 拿**新基线**(bot 会升级了, 旧读数不可比)
gate.sh     ~4.5h   ← 上面全绿之后再跑, 可过夜
```

⚑ **这个顺序省的不是时间, 是返工** —— 前四项加起来 ~15 分钟就能抓到大部分问题,
而全量门一旦红, 你还是得回来从单测查起。

### 本轮待验的四类改动(每一类单独都够格跑一次门)

**规则**(双色调拆黑调/红调)· **计分**(升级按增量放大)· **掷脸**(`min_run`)· **目标分**(S1 = 420)。

---

## ⭐ 下一个 session 从这里接(2026-08-16 交接)

**⏸ 全部挂在同一件事上:用户试玩。** 本轮(08-16 下午)改完的东西都**没有跑过 sim/门**
—— 那是用户拍的板(见文件顶「本轮的工作方式」):`改完 → 用户试玩 → 用户喊 → 才跑门`。

**⚠ 等着他给意见的三件**:

1. **S1 = 420 到底够不够低** —— 本轮压了**两次**:`921 → 560 → 420`。
   ⚠ 560 那版**压得不够而且是算得出来的**:零构筑 6 拍全打对子 = 540 **< 560** ⇒ 一次不失手才刚好过。
   420 的判据 = **允许失手两拍**(4 对子 + 2 高牌 = 440 过),而「基本没打」(390)照样死。
   ⚠ **只有真人试玩能验**(sim 读的是另一张表,见下)。
2. **教学关重做完了,等体感** —— 做中学 + 动作门 + 读提示时钟不走 + 白光高亮 + 压暗。
   ⚠ 高光位置本轮修过一次:那四个矩形原本**手抄在 `ui.json`**,而真值是运行时算的 ⇒ 必然对不上。
   现在从活部件取(`Hand.focus_rect()`)。
3. ~~**加分族的 10 倍差距**~~ ✅ **已动手(08-17 第一批 A 案落地,13 张走 `bonus_target_pct`)**。
   ⚑ **A 案已被证明是唯一解**,不是「倾向」:见下面「不是 21 张,是 14 张」那节。
   ⚠ S1 压到 420 之后每拍需求跨度 **10 倍 → 13.3 倍**,B 案(逐张调绝对值)数学上更不可能了。

**⚠ 本轮新挖出、归用户拍板的一件**:`norepeat`(禁回)在第 1 段,而它 ×0.5 罚重复牌型
—— **新手最可能的打法正是每拍打对子**,禁回下 6 拍全对子只有 315 分,**420 也过不了**。
**这个靠调目标分治不了。** 三条岔路见「🟡 需要先拍一个设计判断」那节。

**🟢 不依赖用户、随时可做的**:Director 接线 · 快闪 popup 复活 · 清理瘦身剩下的 ·
求解器 S2/S3/S6/S7 · 断点续玩 · 两条仪器债。

**⚠⚠ 别用 sim 验证 `section_targets` —— 它读的是另一张表(2026-08-16 实测坐实)**

我把 S1 从 921 改到 560(几乎腰斩),**跑完 sim 读数纹丝不动**:`twin 54.9%` / `random 3.5%`,
与改前**逐字相同**。原因:

| 表 | 谁读 | 谁面对 |
|---|---|---|
| `run.json` 的 `section_targets` | 游戏 · `curve.gd SYNC5_CURVE_BOT=perfect` | **真人** |
| `sim.json` 的 `bot_targets` | **`tools/sim.gd` 的九个队列** | 规则 bot |

⇒ **sim 结构上量不到这次改动。** 唯一能验证它的是:① 真人再试玩一次(最可靠);
② `SYNC5_CURVE_BOT=perfect godot --headless --path . --script res://tools/curve.gd`(完美玩家尺度)。

⚑ **这次没被骗到,是因为读数「一点没动」本身就是信号** —— 一个大改动配一个零变化,
说明仪器和改动不在同一条路径上。**这正是 memory 里那条「选错仪器会把结论量成反号」的形状。**
⚠ 顺带:`ui.json` 的 `key_w` 改动只影响渲染,已截图目视确认。

---


---

## 2026-08-15 · **第四轮补脸**:六秒靠的东西换了,而基线一动就掉出两条过期声明

用户 2026-08-14:「**tier4 可以补脸,只不过都是时间相关的就行**」——
覆盖 2026-08-07 那版「第四轮暂定只有一种脸」。`blinds.md` 自己早写着
「时间族目前只有赶场一张,想给第 4 轮加变化就得加脸」。

- **入池 3 张,全部「六秒 + 一层既有的时间约束」**:`overtime` 加场(最后 2 秒不能弃牌)·
  `teardown` 拆台(最后 2 秒不能交换)· `closing` 打烊(每拍只能弃一次)。
  **零新参数、零新引擎代码** —— 四个 param 都在白名单里且已被求解器覆盖。`fixed_tiers` 清空。
- ⚑ **六秒仍然始终成立,但靠的东西换了**:从「只有一张脸」变成「**每张脸都自带缩时**」。
  判据 = 进第四轮的脸 `time_penalty` 必须 > 0,`tests/t_face.gd` 有断言。
  理由是 §6 删掉「延音」的那条:**把六秒还回去 = 破坏第四轮主机制**。
  ⚠ 我本来想走的捷径(把轮 1 的 `lastcall`/`lockup` 用 `tiers` 放进轮 4)**正是这个错** ——
  那样掷到它们的那局 S4 会变回 8 秒,终章反而更软。
- ⚠⚠ **三张读数 bit-identical(−135.3 ±103.9, z=−1.30, 1.8%),而这本身就是读数**:
  模型把三张全当成了纯 `rush`。门列了两种可能,这三张**不属于同一种**(都当场查了):
  `overtime`/`teardown` 是 **① 规则没接进模型** —— `discard_open`/`swap_open`
  **只在 `view/phrase.gd` 里被读**,模型没有时钟,**结构上量不到**;
  `closing` 是 **② 约束不 binding** —— 求解器不建模弃牌次数,且它本来就一次批量弃牌。
  ⇒ 按 §6 铁律**测出近零不许改内容**,三张写进 `weak_upper_bound` 并附理由。
  ⚑ 更根本的一条在 `gates.md §7.4`:**新鲜感是唯一一个求解器天然感受不到的因子** ——
  这批脸的立项动机正是新鲜感,**按定义就量不到**,读数是预期内的。
  ⚠ 代价写明白了:定价也量不到 · 门从此不守它们 · 对完美玩家它们字面上就是 `rush`。

**⚑⚑ 基线一动,两条过期声明当场掉出来(反向保护在工作)**:

| 条目 | 实测 | 处置 |
|---|---|---|
| `lowend` 挂在 `weak_upper_bound` | **z=−4.00, 5.7%**,两条判据都过了 | 摘掉豁免 → 复验 **✓ 量到了** |
| `起始金币 −3 → 0` 挂在 `gate.gd` 的 `KNOWN_FLAT` | **+0.02 段**,正好压在 `FLAT_BAND` 边界 | 摘掉声明 → 复验 **✓**(行内断言本身一直是 ✓) |

**为什么同时过期(假说,有代码依据 + 一条独立读数,未 A/B)**:`closing` 的
`discard_actions: 1` 在 `core/phrase.gd::can_discard` 里被强制,而**规则 bot 走 `Phrase` 打牌**
—— 它对 bot 是 binding 的,对求解器不是。bot 约 1/4 的局在 S4 撞上它 ⇒ S4 变难。
⚑ **独立佐证**:尺子自检 `adaptive:twin` **47.3% → 43.6%(−3.7pt)**,方向与量级都对得上。

⚠ **没做的事:不动 `FLAT_BAND`。** 把 0.02 调宽能让门变绿,那是**为门绿改仪器**。
⚠ **认下的残留风险**:摘掉 `KNOWN_FLAT` 那条后,若效应漂回 0,抛硬币式的红绿会回来
(它当初被声明成零效应正是为了治这个)。治本是**把带宽从实测标准误推出来**,已记在 `gate.gd`。

**⚠⚠ 顺带量出来一条:门的耗时文档过期了一个数量级。**
实测 单测 744s · 覆盖自证+单调性+哨兵 **10503s(2h55m)** · `kit.gd` ~1h15m ·
流程/打点/重放/尺子 ~10min ⇒ **全量门 ≈ 4.5 小时**,而 STATUS 写的是「~15 分钟」。
根因是 **S10 把求解器拖慢约 2.4 倍**,而门里最贵的两段全走求解器。
⚠ 「快路径十几秒」也没了 —— `gate.sh <face_id>` **第一步就跑全量单测**,地板 12 分钟以上;
要只验脸就直接调底层 `SYNC5_GATE_FACE="a,b,c"`(**接逗号分隔的列表**,多张一次验完)。

**回归(全量门六步 + 单测,全绿)**:tests **1199/0** · 覆盖自证+单调性+哨兵 **EXIT=0**
(8299s,0 个 ❌)· 小丑牌覆盖自证 **EXIT=0**(61 张全部量到)· 流程 **0 bugs** ·
打点 ✓ · 决策重放 **78 个决策点 0 违规** · 尺子自检 ✓(random 0.8% ≤ 5%,最强 43.6% ≥ 3%)。

---

## 2026-08-14(晚)· **难度与节奏立卷**:三个索引被混成了一个词「关卡」

**起点是用户四句话**:「难度曲线也就是 death spec,应该可以数学化,大概是一个导数递增的
函数」·「问题在于不同关卡,不能一直难,要有节奏」·「**这里不是千人千面的不用读 context。
真正千人千面的只有随机出来的小丑牌**」·「**有大量只有一轮生效的盲注我不喜欢**」。

- **⚑ 诊断:「关卡」这个词同时指过三样东西**,而它们的「一步」差三个量级 ——
  **局内段**(~70s,`death_spec`,✅有实现❌没逻辑)· **跨局序列**(~5min,Director,
  ❌没实现✅有逻辑)· **解锁进度**(天/周,❌两样都没有)。
  **有逻辑的那层没实现,实现了的那层没逻辑** —— 这就是「逻辑不清晰」的确切来源。
  规格立在新的 [`docs/design/difficulty.md`](docs/design/difficulty.md)。
- **`death_spec` → `[0.05, 0.28, 0.50, 0.60]`**(增量 `+.23/+.22/+.10` = 123 陡、S4 平)。
  通关率 **13.7%** 与旧值 13.9% 持平,**换形状不连带重定任何东西**。
  ⚠⚠ **需要手工封顶的是 S4,不是 S1-S3**(反直觉):纯 sigmoid 外推 S4 到 0.73-0.80,
  通关率崩到 6-9% —— `∂通关率/∂d_s = −S/(1−d_s)`,**末段敏感度是首段 2.4 倍**。
  ⚠ 而「S1 死亡率低 → 拉长用户时间」**量级只有 +6.5% 封顶**(294s→313s):
  S1 本来只淘汰 10%。**这条仍该做,但买的是「不在 70 秒就出局」的不挫败,不是时长。**
- **⚑ 脸的轮次从单值 `tier` 放成集合 `tiers`**(缺省 `[tier]`,现有 30 张脸一行没改)。
  **不是新设计,是把 schema 放回它本来的宽度** —— `tools/price.gd` 文件头 2026-08-07 就写着
  「定价表大小 = **合法放置数** 4+5+7+6 = **22**,不是 12」:**12 张脸 22 个放置**,
  当年一张脸本来就能进多个段,定价仪器至今按「(脸, 轮)」在量。**窄的是 schema 不是仪器。**
  ⚑ 顺带解锁了 `research_pacing_retention.md §5.5` 判定「更接近原意」却搁置了半年的
  **起承転結 A 案**(一张主脸走完四段)—— 当时的阻碍正是脸不能跨段重复。
- **Director 大幅简化**:拍板不读 context ⇒ `levels.md` 的 `Inputs` /
  `Recent behavior model` / `Joker learning states` **三节作废**(它们全是读 context 的)。
  它从「自适应 AI 系统」降级成**一张设计期的 JSON 表**,`gates.md §9` 的依赖(池子搜索 +
  体感量)一并解除。⚑ **这大概也是它一直没实现的真正原因:被设计得太重了。**
- **「池子搜索」正式作废** —— 三个前提去掉两个(死亡率已由用户指定形状 / Director 不是
  优化器),第三个(留存代理数据)从来没有过。⚠ **它的消费者 `tools/pool.gd` 从未存在。**

**回归**:tests **1104 / 0**(`1104 − 8` = S10 之后的本树基线 1096,没有断言消失)·
`--import` 解析干净。⚠ **只跑了单测** —— 本批没碰 `faces.json`/`jokers.json`,
`death_spec` 也不进求解器路径;**给某张脸真的加 `tiers` 时才需要全量门。**

**⚠⚠ 两件我做错的,都记进了 LESSONS**:

1. **调研之前没查仓库。** 用户说「调研一下业内做法」,我做了 6 次网络搜索重新推导 ——
   而 `docs/design/research_pacing_retention.md`(27k,2026-08-07)早就逐项覆盖了,而且更深。
   **代价不只是时间**:我在重推的版本里把「单独的教程关卡」写进了「明确不做」,
   **而用户 2026-08-07 已拍板过相反的方案**(「教学总要时间,但教学只要一次,不影响整体
   节奏」= 教学单开一关,不砍段)。**拿自己的判断推翻用户已拍的板,而依据就在同一个目录里。**
2. **「注释承诺了一个不存在的机制」已是第 4 次**(`beat.gd` 漏步=崩 · `db.gd` 直接红 ·
   `shop.gd` 不许各写一份 · `price.gd` 指向 `pool.gd`)。**升格成检查项**:
   注释里出现文件名/类名/「不许·必须」时当场验证 —— 文件名 `ls`,机制找到那行断言。

---

## 2026-08-14 · **先验层**:用户「区分先验生成和后验生成」→ 一天里推翻了五条旧结论

**起点是用户的两句话**:「我们需要区分先验生成和后验生成……数学上最核心的是**我们自己
计算好各种情况的概率**」+「**我对之前强依赖后验是不太舒服的**」。

- **⚑ 诊断:先验的反面不是后验,是拍脑袋。** 账本 `p̂ 设计` 那一列是**散文不是算式**
  (`0.35(重复上一拍:需刻意保型)`),44 张卡有一半还空着 —— 经不起检验,
  于是全部信任压到 `p_bot` 上。**这是「可执行性替我排优先级」的第二次**:
  蒙特卡洛能跑,就跑了蒙特卡洛;而三件本该算的事(倍率表依据 / 规则牌 Δp / 每卡触发率)全去测了。
- **产物** = `tools/prior.gd`(三模式)+ `docs/design/prior.md`。判定走 `pattern.gd`、
  谓词走 `fx.gd`、抽卡做解析求解 —— **一行都不重写**,自带闭式自检与快慢路径逐次对账。
- **⚑ 少数据也能定价的数学原因**:`p_人 = p̂(可算) × 策略折扣 × 执行折扣 × 认知折扣`,
  **三个折扣跨卡共享** ⇒ 参数量从 `O(卡数)` 降到 `O(1)`,
  12 局的分母从「每卡 n=3」变成「全体 n=168 拍」。**先验不是凑合的初值,它是降维。**

**一天内被数据推翻的五条**(每条都在文档里躺了一段时间):

| 旧说法 | 实际 |
|---|---|
| 「Target 层不再区分顺子/同花(都 ×7)」 | 实装两天前就是 13/10 —— **注释没跟着改** |
| 「同花比顺子难 1.4 倍」 | 组合口径 **1.31**、策略后 **1.03**,表里是 1.25 —— **三个数打架** |
| 「完美玩家追最优 ⇒ `same_as_prev` 恒假」 | **是窄枚举塑不了形**,`SOLVER_BLIND` 第 ⑤ 类整类作废 |
| 「稀有档 5%、档内均分 ⇒ 一局见到 11%」 | 实现是每卡带权重 ⇒ **4.8%**,差 2.3 倍 |
| 「缓存档是下界」(我自己写的) | 排练先验 10.48% 而实测 6% —— **缓存里是打不出去的剩牌** |

**⚑ 用户拍板三件,当日实装过门**(tests 1096/0 · kit 3/3 · sim 三态分解):
**双色调 uncommon→rare** · **阶梯 ×13→×11** · **单色 ×10→×12**(顺子/同花**互换高低**)·
**定价口径写死为组合口径**(理由三条进 `CLAUDE.md` Target 两层原则 ③)。
- ⚠ **阶梯降价的真正理由不是"顺子不难"**:组合上顺子(8.89%)比同花(6.79%)**更容易**,
  而真人只打出 1.9% —— 那 7pp 落差**不是难度,是没认出来**。
  **旧的 ×13 补的是可读性,而倍率治不了可读性**(近道能把顺子抬到 27.5%,那才是药)。
- ⚠ **sim 三态分解意外发现**:双色调变稀有让 **twin/wolfpivot 这两个不吃同花的队列各掉 2.2–2.4pt**
  ⇒ **它不是同花流派的卡,是所有流派的通用首选**,比 9.8× 更硬的超模证据。

**⚑ S10 引擎手术**(用户「开始做吧」):`best_discard` 的枚举 `_subsets(base.keep)`(3 张)
→ **全部可见 8 张**。立项依据 = 先验层配对 **+26.6 ±1.7 · z=15.5 · 量级 9.3%**。
连带解掉三条债(回响/复读退出豁免名单 · 断舍离的「弃 4 张」进动作空间 ·
`best_beat` 那份没人调用且违反共用随机数的死代码)。
⚠ 代价:`tests` **<300s → 716s**;候选剪枝提速**被实测否掉**(M=6 只提速 2.9× 就丢 3.26%)。

**五项难度分解表第一次并排**(`tools/decomp.gd`)—— `solving.md` 说了很久的
「五项仪器都在跑,只是从没并排放过」。首批读数:**阶梯主要由 ①数值压力 + ⑤运气暴露承担,
②③④ 基本不参与** ⇒ **接近「同一种难收四次费」,而第二种"难"是运气变大不是技巧变高**。
⚠ 待用户审(这五项与「难」的关系没有外部锚)。

---

## 2026-08-13(夜)· 仪器债第一批:还债时挖出一个**没量过的占位值**

- **拆迁回池**(+1286,z=12.1)—— 债还掉了。但这批真正的收获不是它。
- **⚑⚑ `beat_budget.discards` 一直是 2,而真人实测**一拍最多弃 4 张**
  (校准依据是这个上界;「均值 2.30」是**条件均值**——只算有弃牌的 81 拍,
  无条件均值是 1.11)** ——
  **44% 的真实行为在模型里是不允许的**。它同时决定 bot 的弃牌上限、求解器的枚举范围、
  和所有反解出来的目标分,却从没有人量过它。校准到 4。
- **连锁三个,每个都比原来那笔债值钱**:`pair.gd` 把这个配置**抄成了字面量**
  (配对差假性 −16,看起来像求解器分叉)· 回响/复读在推演里的「测得到」被证明
  **一直是噪声**(预算小 → 求解器被迫重复牌型)· 赶场 −966 里一大半是
  **floor 离散化的假象**(`floor(2×6/8)=1` 是 −50%,`floor(4×6/8)=3` 才是真的 −25%)。
- **⚠ 反推:此前所有以 bot 为尺子的读数系统性偏保守,目标分跟着偏松** —— 带进重算。
- **三重唱的债被重新定性**:校准抬高基准后重量 z=1.81 / 量级 4.1%,**两条都不达标**。
  量级与样本量无关(我算完 power analysis 才想起来),所以这是**「效应不够」的设计结论,
  不是覆盖缺陷**。留在池外,债的条目撤销。
- **⚑ 由此得一条通则:校准仪器会重新裁决已经量过的卡,两个方向都可能。**
  同一次校准让回响/复读的「非零」变噪声、让三重唱从「差一口气」变「够不着」。
  **基线一动,所有以它为分母的量级都得重读。**

---

## 2026-08-13(夜)· 子波 3:商店成长族 + **一个从没实现过的玩家动作**

- **入池 3 张**(淘碟/收藏家/转型):成长挂在**商店事件**上 —— 刷新、购买、换旗。
- **需要第四种证物 `counter`**:四条既有通路**没有一条**量得到它们 ——
  score 通路关商店、coin 通路不量分、shop 通路原有三种证物都是**货架**读数而这三张不碰货架。
  证物改成「一局结束时那张卡的计数器终值」(零基线机械读数)。
  ⚠ 我第一版声明成 score 通路,理由是「那条臂会开商店」—— **kit 自己的文件头写着
  商店必须关掉**。三张全部触发 0%。
- **收藏家加上限 30**:实测它是**无脑卡**(买牌就涨,没有取舍),按 numbers.md 的 SOP 定 cap。
- **⚑ M5 查下去发现不是反馈问题,是动作根本不存在**:`early_settle()` 钩子在、
  三张卡(碎纸机/势头/冻结)等着它、bot 会用它 —— **而玩家没有任何办法触发它**。
  我一开始当「反馈缺失」在查,查到最后才明白它从来没被实现过。
  用户拍板交互 = **点唱片**:`view/vinyl_deck.gd` 从装饰变按钮(金环呼吸 + 中央剩余秒数)。
  ⚠ `mouse_filter` 要 **STOP 不能 PASS** —— PASS 会让点击穿透到手牌框。

---

## 2026-08-13(夜)· 引擎波次子波 2:计时族 + bot 时间信念数据化

- **入池 3 张**(谢幕/秒表/早弃)。时钟观测走 `flags` 交给 core(**core/ 不含时钟**
  是铁律,与 late/early 同一条路);弃牌时刻记在 `_notify_discard` —— 三条弃牌路径
  的共同出口,一处覆盖。
- **⚑ bot 时间旗数据化(重构点)**:旧版把概率写死在 `.gd`、**只认 finale/momentum
  两个卡名**(加卡忘改代码不报错 —— 那张卡在模型里就是「玩家从不为它调整打法」)、
  `elif` 造成隐式优先级(同时持两轴的卡时只有一张生效,从没人写下过这条规则)。
  现在偏置表进 `sim.json ev.timing`,多张取最大值、早晚两轴独立。
  ⚠ 行为改动:finale/momentum 的既有读数会移动。
- **一处卡面说谎修掉**:ui 文案写「前 4 秒」而实现复用了早锁线 3.5s。给早弃配了
  自己的旋钮(两条线概念不同:早锁问「你是不是不动了」,早弃问「弃牌是不是做完了」)。
- `SOLVER_BLIND` 具名表**第一次兑现预言**:三张新卡全被当场抓住(求解器没有时钟),
  逼作者逐张声明。旧的「−5 魔法数」会让它们悄悄吃掉余量而门照旧绿。

## 2026-08-13(夜)· 引擎波次子波 1 + **两道门的确定性修正**

- **⚑ 同一天连抓三个「靠运气变绿」的门**,判据同源(全部入 LESSONS):
  ① `gate.sh` 金币单调性哨兵 —— 量的效应恒为零,绿灯只看四舍五入的符号
  (08-12 `+0.00 ✓` / 08-13 `−0.00 ❌`,**同一个零**)。改成具名豁免制
  `KNOWN_FLAT`(逐条写 S9 依据 + 标真人待定)+ 零效应带 0.02 段 + **反向锁**
  (效应变真就红,要求删条目)+ **拼写保护**(key 对不上断言就红)。
  实测确认:两条真效应断言(目标分 −1.32 / 脸 −0.47)**没被豁免波及** —— 豁免只吃零。
  ② `tapeprobe.gd` 打点回归 —— 它走**真实一局、脸随机掷**,而它要做两次弃牌动作,
  掷到「一口气」(弃牌动作限 1)就报一个不存在的 bug:**平均八次跑绿七次**。
  修法 = 清脸(它验打点管线,脸的交互归 flow_probe)。三臂 A/B 闭合诊断。
  **判据:门的颜色不许由与被验对象无关的随机变量决定** —— 随机脸、四舍五入的符号、
  抽卡运气都算。⚠ 它比「永远红」更坏:红的时候第一个念头总是「我刚改的东西坏了」。

## 2026-08-13(夜)· 引擎波次子波 1:动作内容信号 + 四个仪器修正(含一次自查推翻)

- **入池 6 张**(定格/静物/串场/让位/分成/穷开心),**2 张挂仪器债**
  (断舍离:bot 一拍弃不满 5 张 · 打包:kit 单卡臂打不到 2×目标 —— 按铁律
  「不许为门绿改卡」撤出 json,机制与谓词留着)。新词汇:`swaps_eq` /
  `discard_batch_gte` / `section_doubled` / `per: face_discard|swapped_scoring` /
  操作码 `coins_factor` / 计数器 `pulse_on_early_finish` / **`hold` 新键族**(coin_cap)。
- **金币上限收口成两个口**(穷开心):`Economy.grant` 卡住收入(四个入账点全部改走它)
  + `Economy.cap_held` 在**装卡后**修剪存量。后者不做,卡面「上限 5」对一个已有 30◆
  的玩家就是假的 —— **D2 管的不只是词数,还有卡面不许说谎**。
- **⚑ 仪器三修(比新卡值钱)**:
  ① **bot 的试探性交换在污染动作计数** —— 每拍 15 次试探全是真调用,「本拍零交换」
  于是在模型里永不成立(静物触发 0%)。修成 `probe` + 显式补记后触发率 0%→21%。
  **连带**:单换/岔轨/限流三张脸读的正是这个计数器,它们的实测强度一直混着
  「bot 被自己的试探拖累」= 方向性高估。这是「求解的过程不许污染被求解的对象」
  第二次(第一次是 `Deck.peek_many`)。
  ② **求解器算得出来的量必须写进 ctx** —— `need_swaps` 与被弃的牌本来就在手里,
  只是没交给结算;接上后两张卡在 `Draft.card_value` 里从 0 变成有值,没改卡一个字。
  ③ **两处判据从魔法数换成具名豁免表**(`SOLVER_BLIND` / `WEAK_MAGNITUDE`,都带反向锁)
  —— 这一批一次吃掉 4 格数字余量而门还是绿的:**门的余量不许用匿名数字表达**。
  ④ **穷开心的估值臂漏了代价那一半**(只算 ×1.3、不算金币上限 → sim 实测 bot
  持有率 **100%**,无脑吃亏)。代价按「没收的购买力」折算后修好。
- **⚠⚠ 同日自查推翻一条我自己写的因果链(留档,因为这个形状最阴险)**:
  我一度把 `gate.sh` 金币单调性哨兵的红灯归因给上面那个 bug。修完哨兵**照旧红**,
  翻出穷开心入池**之前**的读数才看清:昨天 `+0.00 ✓`、今天 `−0.00 ❌`,
  **效应量级两次都是零**,绿灯只取决于四舍五入的符号 —— 哨兵在量一个 S9 早已裁定的
  游戏事实(「买不起只占 0.1%,钱从来不是约束」),不是在守护不变量。
  **我修了一个真 bug,却把一个无关的失败归因给它** —— 「修好了」的感觉是真的,
  解释是假的(「错得互相抵消」第五次)。两条判据入 LESSONS:
  **修完必须回头验症状真的消失** · **一个符号随噪声翻转的判据不是判据**。

## 2026-08-13 · 盲注八裁决落定 + 曲目硬门改税 + 外审收官

- **八条盲注裁决结案**(用户:「同意你的判断」,五条按审查意见落定,
  `docs/design/blinds_review.md` §6):退池维持(行为答案判据是宪法)· 红灯路线版
  (判据④裁掉算术版)· 延音不回池但理由更正(真依据 =「谁差这一秒」先例,
  不是 Codex 自立的六秒公理)· 聚光第六张版(万能版撞百搭 = 3 万能,
  破判定引擎 ≤2 硬顶 —— 硬约束不是偏好)· **曲目硬门 → 税**。
- **曲目手术**:种数配额从「检查表即死」改「缺一种目标升一档」(×1.25/种,
  `variety_penalty` 进 faces.json)。税挂 `Run.variety_mult` **单一真相**、
  悲观实时(段首欠满额,覆盖一种当场降档 —— HUD/商店缺口/判生死同一个数);
  顺手修平一处旧分叉:**旧硬门只活在 advance(),runloop 判生死根本没查种数**
  —— 半个「游戏里活、模型里死」,第六次同形状,这次在改造中发现而不是事故后。
- **外部审查全部收官**:V2(嵌套 schema,昨夜)+ V3(首页选角可重入,今日闩锁)
  —— 2026-08-09 那份审查的遗留项清零。
- 素材侧:流派批 8 张的生产规格在 `codex/joker-seven-assets` 分支(仅文档,
  图未生成),用户自理。

## 2026-08-12(夜)· 流派图谱立卷 + 流派批第一波落地

- **流派层第一次有了自己的文档**:42 条原作流派全目录(`docs/design/research_balatro_builds.md`,
  15+ 来源交叉)→ 三桶映射 + 9 个新流派候选(`docs/design/archetypes.md`)。诊断补齐了
  两条成长线的载体缺口:**数额线没有族内件**(双子玩家零张对子专属卡,凑的全是通用件)、
  **概率线的件买不到**(3/4 规则牌压在 5% 池权重里)。
- **落地第一波(轻手术件)**:增 8 —— 族内件四张(对唱/二重唱/三和弦/三重,
  contains 语义 = `kind_in` **零词汇**:顺/同花天然无对,葫芦双吃两族 = 原作 Duo+Trio
  直译)+ 后台(Bull)/替补(Splash 缓存版)/包厢(Baron 缓存版)+ 修剪(减牌手术,
  enable_wilds 的镜像);删 2(快闪/伴唱,素材按 wrecker 先例保留);
  四指/双色调降罕见(概率线曝光)。重手术(重播/罢演/保镖/shelf 三件套)延期挂 TODO。
- **顺手锁了三个静默失效口子**:acquire 内键 / deck_rule 值 / per 值此前**不校验**,
  拼错 = 规则牌悄悄不生效 —— 「规则在游戏里、不在模型里」栽过五次的形状,
  趁加 `trim_low` 一起补白名单。
- 立规格局:**流派用户拍板,数字后行** —— 本批定价全部按宪法三轴代公式(锚记在
  jokers.md),真人两态数据进账本后复价;修剪按宪法 §1 封锁不定价。
- **二波(同日深夜):shelf 三件套 + 第四条 proof 通路**。联票(货架 4 位、一店两购)/
  赞助(全场 −1◆,地板 1◆)/点唱机(必出规则牌,atlas 落选后凭流派层理由复活 ——
  它是 Oops! All 6s 的 Sync5 形态)。货架 API 收口 `Joker.slots_*`,游戏侧与 bot 侧
  消费同一个口;新 `shop` 通路 = 商店行为臂。**仪器课**:第一版证物(成交数↑/均价↓)
  双双死于钉槽混杂 —— 实验臂钉死一个槽的效应与被量效应同量级;换零基线机械读数
  (双购店/实收折扣,无卡在手≈0)一次过 z=24~62。与 coin 行为臂「总分→花费」
  是同一课的两次上课:**证物要选混杂进不来的量**。外部审查 V2(嵌套 schema)结案。

## 2026-08-12(晚)· 两条成长线入宪 + v3 全族过刀 + 发行线破土

- **宪法修订(用户口授两条成长线)**:「低波动高频牌型 → 组合放大数额;高波动低频
  牌型 → 凑手牌 + 构筑买概率」。由此收窄彩票判据(**彩票 = 不存在买得到的 Δp**,
  期望税只落真随机)、规则牌单列为**概率放大器**(计价 = Δp × 流派价值)、
  账本真人列拆两态。
- **仪器 bug 反转读数**:probbook 旧版持有集只认 buy 的 id,repl(in/out)换进不计、
  换出不删 —— 修正后回响真人 11%→33%(v2 的建成态假设 0.30 反而被印证),
  彩虹 25%→40%。**顺带发现规则牌 12 局零购买**:概率成长线的载体无人用过,
  阶梯 ×13 赖以成立的救援体系实际不存在 —— 下一轮试玩的头号观察点。
- **v3 全族过刀**(bonus_pct/改基牌/coins/mult):新增稀有度地板线(12.5 分/拍·◆),
  锚 = 真人 168 拍**反事实重放**(拿真人的手和倍率链代入卡效果)。8 动 4 不动:
  铁粉 cap 砍半 —— 持币 17.7◆ 恒触发 126 分/拍,**v1 灯牌同形的无脑卡最优在
  uncommon 层重现**;黑胶 204 分/拍 = 弃牌免费连带的兑现(economy.json 当年预言)。
  sim 漂移 ≤±0.5pt 而真人端刀口很深 —— bot 与真人尺度分叉的又一证据
  (砍的三张全不在 bot 胜利套件里)。coins 族暂缓(S9:金币边际近零,不跨量纲定价)。
- **速弹真机死条件(C10)**:view 侧 settle 不传 early,条件真机恒假而 sim 为真 ——
  「模型里活、游戏里死」,C7 的反方向。修法 = 判据收进 `_acted_early()` 一份。
  连带发现:kit 门量 bot 路径,**抓不到这类 view 侧分叉**。
- **发行线破土**:TapTap 路线定案(`docs/design/taptap.md`:离线+无内购 = 试玩版免版号,
  硬门槛防沉迷 SDK + 隐私合规)+ **Android 裸包首通**(115MB / arm64 / 零权限;
  etc2_astc 空错误坑在 Android 导出上二次踩中,Web 预设的注释救了自己)。
  剩用户拍板三件:厂商名 / 正式包名 / 发布 keystore 密码。

## 2026-08-10 ~ 08-12 · 试玩迭代周:三次判决反转 + 数值立宪 + Web 首发

**真人数据从零到有**,三天里把「等真人 Tape」的一串事项全部激活。转折们:

- **素材判决反转**(08-12):首页玻璃卡 PNG 当年 4× 放大判死「不能贴」,复盘发现用户觉得
  质感更好的设计稿贴的就是这张图 —— 显示尺寸+黑底下三宗罪全读不出来。教训进
  LESSONS 判据 5:素材成色要在显示尺寸+真实底色下判。首页大卡从此素材壳,顶栏贴同款膜。
- **Target 重锚到真人尺度**(08-12):A 案的覆盖面是拿 bot 量的(bot 顺子 20%,真人 1.9%),
  bot 等值 ≠ 真人等值 —— 单色/三连音 ×10、阶梯 ×13,双子锚定不动,极差 5.6×→1.3×。
- **数值立宪**(08-12,用户:「概率低=倍率高未必是资深思路」):`docs/design/numbers.md`
  三轴定价(行为/方差/时间)+ 三概率账本制度(设计/仪器/真人,probbook.py)+ 六步 SOP。
  bonus 族两轮定价,v1 犯了「无条件卡仍是天花板」的方向性错误,v2 翻正支配序
  (会玩>保底>赌狗);三张结构死卡(快闪/伴唱/排练)挂复审,不再用数字假修。
- **操作手感四修**(08-12):缓存行持久化(点选重建整行,拖拽死在被释放节点上——Tape
  实证失败挪牌连 deny 都没有)· lock_offset 归零+锁定浮字 · 翻牌 1.2s→0.65s ·
  挪牌落点整区吸附(以前只认卡面矩形,松手在卡缝就静默取消;顺带发现 Hand 是零矩形
  控件,拖放祖先链根本走不到它)。
- **Web 首发**(08-11):导出预设(单线程免 COOP/COEP)+ 中文字体语料子集两件套
  (SC 800KB + Symbols2 15KB,桌面系统字不变);手机浏览器全链路触屏实测通过。
  TapTap 调研:试玩版免版号路线成立,Android 导出是唯一入口。

## 2026-08-09 · `CLAUDE.md` 二次瘦身:只留原则

用户第二次提同一件事:「**待办 经验 STATUS CHANGELOG 都放到我们对应的文档,
而不要写在 CLAUDE.md,CLAUDE.md 只保留原则性的东西,现在我看 CLAUDE.md 还有很多杂项**」。
第一次(见下面「文档梳理」)只搬走了状态/待办/历史,**证据和推导留在了原地**,所以又长回去了。

这次把**判据本身**写进 `CLAUDE.md` 顶部那张分工表的下面 —— 下一个人往里加东西之前就会撞见:

> **去掉所有数字和日期,这句话还站得住吗?
> 站得住 = 原则(留下);站不住 = 它其实是证据 / 某次改动的经过(移走)。**

**587 → 250 行。** 去处:渲染手法与排版整节 → `docs/design/ui_meta.md`
(**它现在是现行 UI 的权威,不再回指 `CLAUDE.md`**)· 弃牌免费与牌型倍率的推导 → `docs/design/cards.md` ·
关卡结构/商店次数的实测与推导 → `docs/design/levels.md` · Target 倍率与商店定价 → `docs/design/jokers.md` ·
打点口径的完整推导 → `docs/design/telemetry.md` · 迁移四样的完整版 → `LESSONS.md` ·
「起因 / 上一版 / 返工史」→ 本文件 · Target 回池后的待测 → `TODO.md`。

⚠ **每一段都是逐字搬的,没有压缩、没有润色** —— 那些结论是用户反复返工换来的,
丢一条就要重踩一次。


## 2026-08-09 · 外部代码审查:两个 High,其中一个防护**从未生效过**

一次独立审查(REQUEST CHANGES,0 Critical / 2 High / 3 Medium / 2 Low)。两个 High 都属实。

### H1 `raisedbar` 的显示与判定不一致 —— 而且是当天的改动把它引爆的

判生死走 `Run.target()`(乘了 `SectionMod.target_mult`),而 **HUD(`view/phrase.gd`)
和商店盲注板(`view/shop.gd`)直接索引 `GameConfig.SECTION_TARGETS`**。
S3 基础目标 4198、实际 6297,玩家会看到错误的目标与进度条,
而**段中商店的「还差多少分」正是买牌的唯一依据**。

⚠ **这个 bug 此前是潜伏的**:`raisedbar` 原本 `tier: null`、不在任何池子里,
`target_mult` 永远是 1.0。**当天按用户拍板把它放进第三轮,等于给它上了膛。**

⚑ 形状:`LESSONS.md` 记的第 5 次事故就是「`sim.gd` 判生死漏乘 `target_mult`」,
而 `Run.target()` 就摆在旁边。**这是同一个函数被绕过的第六次。**
已改成展示侧一律消费 `run.target()`,乘法只留 `Run.section_target_for` 一处。

### H2 `Beat` 的状态机防护**从未生效过**

`_expect()` 只 `push_error` 就返回,而 **`push_error` 在 Godot 里不中断执行**。
重复 `settle()` 会**再结算一次**(金币与段分二次入账,而 `lock_and_settle()` 对重复调用
返回缓存结果,所以第二次输入完整、看不出异常);乱序 `phrase_end()` 会**再跑一次成长钩子**。

⚠⚠ 比 bug 更糟的是:`core/beat.gd` 顶部白纸黑字写着「**漏步 = 崩**」,
`LESSONS.md` 也写着解药是「让漏步会崩」——**那句话是假的,而它被反复引用为已解决**。
已改成真正的 fail-closed(`_expect` 返回 `bool`,调用方早退),两处假注释一并改掉。

⚑ **新经验:防护措施本身也要有测试。** 这个项目对「读数」的怀疑极其彻底
(配对、标准误、A/B 注入),对「安全网」却是**默认信任** —— 而安全网失效同样静默。
判据:**写下「这里会崩」之前,先真的触发一次看它崩不崩。**

### Low 两条 + 文档纠偏

- `_tape_section()` 在段首 `phrase == null` 时把金币记成 0(真值 6)—— 改读 `run.coins`
- `tests/t_run.gd` 那条断言**算术没错**(4 段 × 每段 2 档期 = 8),错的是注释把**档期**说成
  **实际次数**。已补一条锁「实际 7 次」的断言 —— 此前锁了会算的那个数,没锁真正发生的那个
- `docs/design/cards.md` 的 Two Pair ×3 是**被 2026-08-06 晚重定价推翻的旧决定**
  (代码是 ×2),已就地标注;`docs/design/levels.md` 9 处「8 商店」改为 7

### 一条我不同意的(记录分歧)

审查把 `DB._load()` 校验失败仍缓存返回列为 Medium。**`db.gd` 头部写的是
"lands in `load_error()` and tests go red" —— 意图本就是测试期门禁**,
而 `tests/t_db.gd` 第一条就断言 `load_error() == ""`。真正错的是 `CLAUDE.md`
把它写成「硬校验…直接红」,读起来像运行时拒绝。
⚠ 运行时 fail-closed 也不是白拿:数据随游戏发布,坏了就开不了机。
**改措辞还是改行为,待用户拍板**(TODO.md P0.5 的 V1)。

**回归**:665 passed / 0 failed · `gate.sh` 全过(396s)· `pair.gd` 三关 **0/2000 手不一致 + 0.0 / +0.0**。


## 2026-08-09 · 小丑牌有门了,以及一条被数据改写的结论

### C4:`tools/kit.gd` —— 小丑牌的覆盖自证

脸有门、小丑牌没有,而**构筑值 4.2 倍**(分数大头在小丑牌上)。现在有了:
每张牌在 `jokers.json` 声明 `proof` 通路(`db.gd` 强制必填 + 逐张断言),按**用什么仪器证明**分三条:

| 通路 | 仪器 | 张数 | 为什么必须单列 |
|---|---|---|---|
| `score` | 规则 bot 配对 A/B, 关商店 | 16 | 效果直接改分 |
| `solver` | **完美玩家** | 4 | 改 `Deck.rules` / 牌堆, **规则 bot 未必会去用新规则**(脸那边 `freshsheet` 两种 bot 符号是反的) |
| `coin` | 金币臂 | 3 | 只给钱不给分, **分数臂里按定义恒等于 0**(与 `raisedbar` 同形) |

⚑ **`solver` 通路换了个证物**:那四张没有 `effects`, 触发率恒为 0(仪器盲区)。
改问「**它该造出来的东西真的出现了吗**」—— 装了四指之后四张顺子真的多了
(成手率 10→36%)。**结果:23/23 全部量到。**

### ⚑ 一条我自己在门上犯的错:覆盖 ≠ 平衡

`coin` 通路我原本还设了「行为臂」(钱变没变成购买力)当硬判据, 三张经济卡全红。
查下去红的原因不是覆盖坏了(金币臂 z=+28/+51/+469, 钱明明白白到账),
而是**这笔钱在 24 拍 / 7 商店的结构下换不出分** —— 那是**平衡结论不是覆盖缺陷**。
让覆盖门判平衡, 它**永远红**, 下一个人就不看了, 比没有门更糟。**已降级为参考行**
(照旧打印、进 ⚠ 清单, 不判罚)。判据是不是该硬, 问它**失败时你会去改什么**。

### S9:金币影子价做反了 —— 修了,但结论被数据改写

`bot._draft` 的 `lam = coin_score_ratio × 本局均分` **随分数上涨、不随剩余机会衰减**,
而收益边 `ev × horizon` 随剩余拍收缩 —— 两边同时朝「别买」走, 门槛 ev 从 3 爆到 ∞,
后半程购买完全停止(`tools/wallet.gd` 200 局:满槽后整局只再买 0.27 次, 78% 不愿换,
而货架最好的比手里最弱的强 3.8 倍、买不起只占 0.1%)。
⚠ 同一个东西两把尺子:bot 41 分/◆ vs 求解器 `COIN_TO_SCORE` 0.3 分/◆, **差 137 倍**。

**已修**:`sim.json ev.coin_decay = 1.0`(`lam ×= (horizon/20)^decay`)。选 1.0
**不是因为它涨分** —— 扫描 8 个点**没有一个达到 |z| ≥ 3**。选它是因为它是
**horizon 从判据里整个约掉**的那个物理点。(选 decay=8 就是拿 z=2.50 的不显著涨分定参数。)

⚑ **真正的收获是结论变了**:原链条「lam 做反 → 不买牌 → 经济卡是空气 → 目标分不可信」,
**第三环不成立**。修好后 bot 确实开始买了, **总分几乎不动** —— 剩 3 拍买一张好卡也就
~139 分 / 全局 15000。**后期钱花不出去不是定价错, 是那时没什么值得买的了。**
按铁律「测出近零不许改内容」:**不改 `tipjar`/`interest`**。
⚠ 尺子位移实测 9 个队列全在 1 个标准误内 —— 先前担心的「全量重测」没有发生。

### 商店是 **7 次不是 8 次** —— 而且方向和以为的相反

`tools/wallet.gd` 发现末段的段末商店剩余拍 = 0(买什么都不产生分数)。
查下去:**游戏里本来就没有这一次**(`view/phrase.gd` 在 `finale` 那一支直接走结算屏),
多出来那次**只存在于 `tools/runloop.gd`** —— **模型比游戏多一次商店**,
是「规则在游戏/模型里不一致」的**反方向**一例。Tape 实测 **37/37 完整局 = 段中 4 + 段末 3**。
已修;全仓库「8 次商店 / 后 4 次替换」→ 7 次 / 后 3 次。

### `raisedbar` 进第三轮(用户拍板),以及一条设计原则

⚠ 量出一件反直觉的事:**加一张更难的脸, 最强队列的通关率反而涨了**(twin 38.1% → 42.2%)。
因为脸是从池子里随机掷的, 加它等于**稀释掉 `rerun` 的出场机会** —— 而 `rerun` 是
**唯一专克 twin 的脸**(对 twin 70% 致死, 对 mono 只有 30%)。

⚑ **纯加量的旋钮诚实, 但不中立**:它只抬门槛不改问题, 而**门槛对高分构筑最容易跨**
(raisedbar 对 twin 只有 41%, 对中游 56%)。往任何一轮加纯加量之前, 先问:
这一轮的克制关系靠谁承担, 稀释得起吗。
**数据当面复核过, 用户看过仍选第三轮 —— 知情决定, 别当 bug 改掉。**


## 2026-08-09 · 重构:补上缺的两层真相

**主线是一句判据:找到那个被抄了 N 份的东西,把差异收敛;验收 = 输出逐字节不变。**
`Beat`(6→1)和 `RunLoop`(14→1)已经用它成功过两次,这次接着用。

⚠ **按行数切是假重构** —— `runner.gd` 1695 行切成 5 个 340 行,抄写量不变、耦合不变,
只多了 5 个文件。**每一刀都要能回答「收口的是哪一份真相」。**

### 四层真相,这次补齐后两层

| 层 | 唯一真相 | 何时收的 |
|---|---|---|
| 一拍的转移 | `core/beat.gd` | 早先(6→1) |
| 一局的循环 | `tools/runloop.gd` | 2026-08-08(14→1) |
| **一次实验的骨架** | **`tools/probe.gd` + `tools/shot.gd`** | **本次** |
| **一份统计** | **`tools/stat.gd`** | **本次**(`_mean` 11 份 · `_var` 8 份 · `_paired` 6 份 → 1) |

⚑ **探针文件数基本不变(38 → 41),这是对的** —— 它们是**不同的实验**,各占一个文件本来就该如此。
膨胀的从来不是文件数,是**每个文件里那份抄来的地基**。

### 同批落地

- `tests/runner.gd` **1695 → 59 行** + 19 个 `t_*.gd` 域文件(一个域 = 一份契约)
- `view/phrase.gd` **960 → 727 行**:搬出装配(`StageLayout`)、视觉反馈(`StageFeedback`)、
  **替换态(`ReplaceFlow`)** —— 后者原本是 4 个散落成员由 3 个函数分头维护,
  而它是后 4 次商店 100% 会走的主线交互。⚠ 两条铁律不动:打点与经济动作仍只在编排器
- `data/faces.json` **16728 → 3363 B**:散文占了 **77.6%**,全部搬进 `docs/design/blinds.md` 第 7 节,
  `db.gd` 显式**拒绝散文键**(不加这道门它会慢慢长回来)

### ❌ R4「经济职责散落」查证**不成立**,退役

两条依据都是假的:① view 侧 4 处金币动作**本来就全在编排器里**,铁律已满足;
② 「双宿主」不是事故 —— `Phrase.new` 的 **27 个调用点里绝大多数根本没有 `Run`**
(探针拿裸数字构造 `Phrase` 去量单拍)。
**`Phrase` 能脱离 `Run` 独立存在是有意的接缝**,把它焊死等于让每个探针都先造一整局。

### 两条方法论(这次现学的)

1. **并行的约束是「文件所有权」,不是「步骤顺序」。** 我第一版依赖图写成 `stat → 切测试`,反了 ——
   `tests/runner.gd` 是**每个会改行为的步骤都要动的共享资源**,必须先切开,后面才能各改各的域。
   同理:两个 agent 都要跑截图探针时,它们抢的是根目录同一批 `_shot_*.png`。
2. **并行时不许动别人正在量的东西。** 探针在做「输出逐字节对比」期间改 `data/` 或 `core/`,
   量到的 diff 是假的。

### 回归

单元测试 **634 passed / 0 failed** · `gate.sh` **全过(520s)** · `pair.gd` 三关配对差 **+0.0** ·
`flow_probe` 0 bugs · 截图目视对账(局内 / 商店替换态 / 首页)。


## 2026-08-09 · 文档重组:30 篇 → 20 篇

**起因**(用户):「项目本身的文档、CLAUDE.md 都有点乱了……我喜欢项目干净可迭代。」

### 根目录四份

`STATUS.md`(现在是什么样)· `TODO.md`(接下来做什么)· `CHANGELOG.md`(经历过什么)·
`LESSONS.md`(踩过什么坑)。**`CLAUDE.md` 从 1126 行压到 580 行,只留原则。**

### `docs/design/` 按主题重命名,编号全部去掉

用户定的结构:导览 / 小丑牌 / 盲注 / 生成 / 求解 / UI+META / 关卡 / 技术 / 初衷,
加两篇拍板补充(牌与牌型、打点)。

```
建模线 22+23+24+25 → solving.md + generating.md + solving_history.md
02+03 → cards.md          00+01+12 → vision.md        09+10 → ui_meta.md
04+07+08+15 → levels.md   13+14+20 → tech.md          05+06 → jokers.md
11 → telemetry.md         19 → gates.md               21 → capability.md
18 → solver_roadmap.md(历史)   16/17 → history_*.md(作废)
盲注设计原本**不存在** —— 从 faces.json 的 tier/proof/families 规格新写了一篇
```

**全仓库 100+ 处引用已重映射**(含 `core/deck.gd`、9 个探针、`tests/runner.gd`、记忆文件),
所有 markdown 链接验证有效,测试 589 passed / 0 failed。

### 两条文档约定(用户定的第一条,第二条是踩坑总结)

1. **验证跟着被验的东西走** —— 每篇设计文档自带验证节,不单开一篇「验证方案」。
   单开的话写完设计的人不会去翻它;放同一篇里,**改设计时自然看得见**。
2. **被推翻的内容必须就地标注** —— 同一篇里旧新方案并排躺着而读者无从分辨,
   是 `capability.md` 那条矛盾躺了一整轮没被发现的直接原因。


## 2026-08-09 · 文档梳理

拆出 `STATUS` / `TODO` / `CHANGELOG` / `LESSONS` 四份,`CLAUDE.md` 只留**原则**。
起因:CLAUDE.md 涨到 1126 行,状态、待办、经验、历史全混在里面。

---

## 2026-08-08 · 建模的重新设计

**这一天的主线是:承认建模本身有问题,然后重建。**

### 求解器的目标函数换了

```
旧:  max Σ 分数
新:  max E[总分]  s.t. 每段 ρ_n ≥ T_n        ← 用户定
```

**约束不需要显式写** —— 没达标 run 就结束了,后面的分拿不到,约束编码在边界条件里。
「有压力全力以赴 / 没压力留余力 / 必输赌一把」三种局面**全部自动涌现**,不需要词典序或权重。

⚠ 起因是核实到一个硬事实:**段分每段清零、通关工资固定,所以超出目标的分数价值精确为零** ——
而旧目标在已经稳过的段里仍在拼命拿分。

### 状态设计推倒重来(原 `docs/design/25`,现已并入 `docs/design/solving.md`)

| | 旧 | 新 |
|---|---|---|
| 构筑 | 标量「构筑档」 | **倍率向量当索引**(诊断实测标量档在档内已无区分力,d=0.078) |
| 缓存 | 抽象成档 | **不抽象** —— 它在决策时是已知的,不需要枚举 |
| 分层 | 全部制表 | **参数 / 索引 / 状态 / 在线** 四类 |

⚑ **发现:加一张新卡可能改变状态设计。**`encore` 需要 `prev_kind` 维、`setlist` 需要
`first_kind` 维。**这推翻了 `docs/design/capability.md` 的「加词汇不会产生新的能力缺口」** ——
那句话只在「效果只依赖当前」时成立。由此产出「内容的模型成本表」(见 TODO.md)。

### 工程

- **`tools/runloop.gd`** —— 一局的编排 **14 份 → 1 份**(不是记忆里的 7 份;`gate` 3 份、
  `formal` 6 份,而后者是同一天我自己加的)。判据 = 逐份**逐字节不变**。
  ⚑ **起因与收敛口径**(2026-08-09 从 `CLAUDE.md` 迁来的原文):
  起因:docs/design/tech.md 治好了「一拍」那层之后,「一局」那层实测被抄了 **14 份**
  (sim/curve/coin/blind/addit/price 各 1、**gate 3**、**formal 6**)——
  而 formal 那 6 份是同一天加的,那个文件顶部就写着「别在这里重写规则」。
  **现在 14 → 1**,差异收敛到 5 个键:**谁在打 / 有没有商店 / 判不判生死 / 脸从哪来 / 录什么**。
- **`ε` 决策噪声**进玩家参数 `θ`。实测 **λ 全谱只值 645 分,而 ε 那一维值 1894 分(2.9×)**;
  ε≥8 时等于随机挑,而那就是规则 bot 的水平 —— **规则 bot 选切法的水平约等于随机**。
- **`tools/replay.gd`** L2 决策重放门(建模侧完整性,和 Tape 的「局面完整性」差一层)。
- **前瞻走真实跨拍转移**(`docs/design/capability.md` 缺口 ②)—— 精确枚举 C(3,n) 驱逐结果取期望 + 补牌。
- **门的判据改成两条**:`|z| ≥ 3` **且** 量级 ≥ 5%,配 `weak_upper_bound` 显式豁免位
  (照 `fixed_tiers` 的思路:豁免必须是有意的)+ **反向保护**(声明了却量到了 → 报警)。
- **`tools/draft.gd`** 买牌求解化(默认关)。给 0 的卡从 14/23 降到 3/23。

### 新文档

`22_Formal_Model`(建模地基)· `23_Open_Modeling_Questions`(待建模的问题,按「归谁管」分五类)
**⚠ 这四篇 2026-08-09 已合并成 `docs/design/solving.md` + `generating.md` + `solving_history.md`。**
· `24_DP_Solver`(目标函数与 DP)· `25_Solver_State`(状态与转移,待审)

---

## 2026-08-07 · 内容扩批 + 一套规则

- **脸系统 6 → 12 张**。调研全量 28 张 boss 后的核心结论:**原作 72% 的 boss 改「输入」
  (手里有什么牌/看得见什么),我们原来 6 张全在改输出**。而给最终分乘一个无条件常数
  **argmax 完全不变** —— 那是难度旋钮不是脸。
- **不完全信息求解**(盖牌族的前置件):**用信念选,用真值记账**。
- **`tools/gate.sh`** —— 加了内容就要过的门。每张脸必须声明 `proof` 通路。
- **可加性检验** —— 脸的难度**可以相加**(六对残差 ≤2%),我预判会破,错了。
- **`core/beat.gd`** —— 一拍的编排六份 → 一份,逐份对账逐字节一致。
- **`tier` 成为脸的基础属性**(用户拍板),`pools` 手写表删除。
- **`tools/price.gd`** —— 每(脸, 轮)一个价。

### ⚠ 这一天最贵的一次错:拿模型倒推内容

`lostpage`/`freshsheet` 在求解器下测出正分,我**改了两个机制**去把数字掰成负的。
用户叫停:「**为了可以求解,我们要扭曲多少游戏内容,这是个非常本末倒置的做法**」
「**不允许修改游戏内容的情况下建模**」。全部已还原,并立为铁律。

---

## 2026-08-06 · 节奏定案 + 路线切换

### 结构

**4 场 × 1 盲注 × 6 拍 × 8 秒,每 3 拍一次商店**(≈4.9 分钟/局)。

⚑ **关键是把商店从盲注上解耦** —— 总拍数、商店数、槽位压力三项与旧方案相同,
差别只在**生死判定点 8 → 4**,以及商店能开在盲注**中途**。
段中商店时缺口已知,买牌从**赌**变成**解题**。

⚠ **槽位是瓶颈不是容量目标**:8 商店 : 4 槽 = 2:1 才逼出「买新替旧」(Balatro 是 1:5)。

### 路线:训练 → 求解

**上界不需要「学」,只需要「解」。** `docs/design/history_adversarial.md`/17` 的参数化训练路线作废,
删掉两块最贵的活(CEM 训练机器人 13-40h、回归拟合)。

**跨拍 DP 随后被用户否掉,改「平衡贪心」** —— 理由是缓存只有 3 张、**未来的小丑牌没信息**。
(⚠ 2026-08-08 部分回到 DP,但状态设计完全不同 —— 当初的否定理由**只挡住买牌那一层**。)

### 规则

- **弃牌免费**(推翻「1 金币/张」)——「唯一的闸门是 8 秒钟」。连带五处变化。
- **点击 = 纯选择,拖拽 = 对调**,两种手势不许混。
  被删掉的那条捷径(2026-08-09 从 `CLAUDE.md` 迁来的原文):
  **拖拽 = 对调**。曾经有一条「已选 1 张手牌 + 点缓存 = 立刻对调」的捷径, 它把第二次点击**吞掉**了,
  跨区多选根本选不出来 —— 交换已经有正式手势, 捷径纯属多余, 已删。测试里有这条契约(含 A/B 验证)。
- **Target 回池** —— 删掉专属概率,「不应该有任何卡有固定概率」。
- **牌型倍率抄 Balatro** 再按**本作实测的 `P(≥牌型)` 累计频率**修正两处。
  ⚠ 我连错两次才找对指标:出现频率有「中转站伪影」(三条稀有是因为它是**路过**)。

### 其他

- **打点系统** `core/tape.gd`(21 类事件)。口径铁律:**只记事实,不记特征**。
- **玻璃卡 UI** —— 建立「采样对账」闭环(`tools/glass.gd`)。配色收敛了,**光影没有**。

---

## 2026-08-05 · 配置化 + 拆分 + 真人试玩

- **配置化**:六份 `data/*.json` + 效果 DSL + `core/db.gd` 硬校验。**sim 报告逐字节一致**(纯搬家已证明)。
- **大文件拆分**:`phrase.gd` 1342 → 726;`sim.gd` 882 → 146+bot+report。
- **公式改 chips×mult**(真人试玩二改):`(chips + Σ改基牌) × 牌型倍率 × ... + Σ奖励分`。
- **SECTION_TARGETS 换人锚**(真人试玩三改):机器人是严重悲观下界,
  `bot_targets` 降级为**影子表**只给 sim 判生死。
- **首页实装** + 局内盲注卡(两轮返工:横条 → 加高 → 终稿改成**逐层同构 `PaperCard`**)。
- **修掉一个潜伏 bug**:结算屏无重入守卫,连点会**整段盲注被跳过**。

---

## 2026-08(早期) · 从零到可玩

- **候选牌机制作废** → 弃牌重抽制。
- 手牌恒 5 张 / **缓存恒满 3 格**(2 格不好玩)。
- 小丑牌 roster:Target 5 + Support 18,16 条设计原则,**20 轮模拟校准**。
- 八主角被动 + 行走系统。
- 结算三段式(FLY/MERGE/BURST)+ 结算两屏。
- **美术方向锁定:霓虹舞台**(写实酒馆/疯狂动物城/AI 底图全部否掉并回滚)。
