# Sync5 · 待办

> 现状看 [STATUS.md](STATUS.md) · 变更史看 [CHANGELOG.md](CHANGELOG.md) ·
> 经验看 [LESSONS.md](LESSONS.md) · 原则看 [CLAUDE.md](CLAUDE.md)
>
> **⚑ 这份文件只回答一件事:接下来做什么。**
> **做完的事一律搬 [CHANGELOG.md](CHANGELOG.md),不在这里留经过** ——
> 2026-08-15 它长到 871 行,一半是已完成事项的叙述,而那些叙述 CHANGELOG 里已经有了。
> **一个 871 行的待办清单不能当待办用。**
>
> 三堆的分法 = **谁能解**,因为这是唯一影响「现在能不能推进」的属性。

---

## ⚑⚑ 本轮的工作方式(2026-08-16 用户拍板)

> 「最近几天有点慢,其中一个原因是**每次小修改都要全量 sim**,我希望这次不要了,
> **等改完,我让你 sim 再 sim**。」

**⇒ 改动期间不跑 sim / 不跑 `gate.sh` / 不跑 kit 全量。** 只跑**秒级**的:
单卡 JSON 校验、`tests/t_*.gd` 里相关那几个、截图。

**⚑⚑ 更严的一条(2026-08-16 用户补充):「改完也别跑 sim,等我先玩。」**
**⇒ 顺序是 `改完 → 用户试玩 → 用户喊 → 才跑 sim/门`。** 改完不是跑门的信号,**试玩才是**。

⚑ **这个顺序有理由,不只是省时间**:真人试玩是**唯一**能验证 `section_targets` 的证据
(sim 读的是另一张表,见下节实测),而它同时也是**最高质量**的证据——
S1 重锚就是这么来的。**先拿最贵的仪器量,再决定要不要跑便宜但盲的那个。**

⚠ **但交付前必须自己确认「能跑起来」** —— 让用户打开一个崩溃的包比跑一次门贵得多。
判据 = **JSON 过 `db.gd` 校验 + 相关那几个单测 + 真机截图**,这三样不算「跑门」。

⚠ **这不是降低验收标准,是把验收挪到批次末尾** —— 判据仍是那三条门,只是**一批过一次**。
⚠ 而且它现在有硬理由:`gate.sh` 实测 **4.5 小时**、单测 744s(S10 之后),
**「小改一次、验一次」在这个耗时下根本不成立** —— 每天的时间会全部烧在门上。
⚠ 顺带,**sim 本来就量不到很多东西**:它读 `sim.json` 的 `bot_targets`,
量不到 `run.json` 的 `section_targets`(见下节实测)。**跑得勤 ≠ 量得准。**

---

## 🎵💰 1.1 · 商业化 + 英文化 + 音乐(2026-08-17 用户三次拍板收敛)

> **版本切分**:「第一版只有游戏体验,第二版我再加商业化和音乐」⇒ **1.0 = 门 + 试玩 + 打包**。
> **发行路线(同日补拍)**:「发 tap 是为了**给朋友试玩**,我后面是**发美国的**」⇒
> **发行三件用户已自己搞定**(只需要给包)· **防沉迷 SDK 明确不做**(见「明确不做的」表)。
>
> **1.1 真正的四件(用户原话清单 + 08-17 晚补一条)**:
>
> 1. **商业化** —— ⚑ **含券系统整体上线**(2026-08-18 用户拍板「第一版去掉券,第二版
>    商业化的时候加」):翻 `data/tickets.json` 的 `enabled` 一个开关就回来 —— 四层实现、
>    使用入口、托盘、42+ 条断言全在树上被单测背书,`t_ticket` 第一条锁着「1.0 必须是关的」。
>    再加**广告出口**(券的加发通道,`ticket.gd` 文件头有拍板:每日发放不依赖广告)
> 2. **英文化** —— 目标市场是美国。⚑ 底子比想象好:卡面 fx **本来就是英文**(≤7 词铁律),
>    小丑牌/券的 `name` 字段也是英文;要英文化的是 UI chrome/教学文案/盲注脸名,
>    **全部在 `data/*.json` 的中文字段里**(ui/tutorial/faces/jokers 的 cn 系列)——
>    配置化那条铁律等于提前给 l10n 铺了路。⚠ Web 中文字体子集工具(`fontsubset.sh`)
>    在纯英文版里可以整个跳过, 包还能瘦一截。
> 3. **加音乐** —— 见下面音乐节(BPM 已算死 120, 拍长偶数秒约束;素材已入库)。
> 4. **收集用户数据回传记录**(2026-08-17 用户补)—— Tape 采集侧本地已齐(21 类事件、
>    「能重放任意时刻」判据),缺的是**回传通道**:上传时机/端点/失败重试 + 合规
>    (隐私政策用户已办好, 正好为此铺路)。⚑ 它直接服务两个卡了很久的洞:
>    **发挥系数(D2)与会话边界(D4)只有真人数据能填** —— 回传上线后不再依赖用户本机拷日志。

> 用户原话:「每一关不是换场景,**是换音乐**,有节奏,只不过节奏要更好卡到我们的秒数(8 秒、6 秒)。」
> ⇒ **四段 = 四首同 BPM 的曲子, 编曲越来越满**(小酒吧 → 俱乐部 → 剧场 → 体育场),
> 切段时鼓点不断、只是层次变厚。**不换场景, 换的是听觉的密度。**

### ⚑⚑ BPM 已经算死了:**120(4/4)**, 这是算术不是口味

8 秒与 6 秒的最大公约数是 **2 秒** ⇒ **小节长度必须整除 2 秒** ⇒ 120 BPM(小节正好 2.000s)。
跑遍常用 BPM 只有 120 与 240(同一网格)成立;140 的 6 秒是 3.5 小节、90 的是 2.25 小节
—— **听感上就是「音乐还没走完就被切断」**。

**⚑ 实测游戏里全部拍长, 结论比预期更强**(2026-08-17 查 `faces.json` + `tutorial.json`):

| 拍长 | 出现在 | 120 BPM 下 |
|---|---|---|
| 6.0s | 四张 tier-4 的脸(`rush`/`overtime`/`teardown`/`closing`, `time_penalty: 2.0`) | 3 小节 ✅ |
| 8.0s | 正式局全部 | 4 小节 ✅ |
| 10.0s / 12.0s | 教学关 | 5 / 6 小节 ✅ |

**⇒ 派生出一条该写进设计的约束:拍长一律取偶数秒。**
只要守住它, 120 BPM 就**永远**对齐, 以后再改拍长也不会破坏音乐。

### ✅ 素材已到库(2026-08-17,用户自产 10 首,`assets/audio/`)

**10 × 8 秒循环段**,自带规格声明(README.txt):**120 BPM · 4/4 · 恰好 8.000s ·
首 kick 在 0.000s · 无全局淡入淡出 · 尾部循环回绕** —— 与下面算死的全部约束**逐条对上**,
连「首个瞬态偏移」都预答了(声明 0.000s;⚠ **接线时仍要量一遍波形验证**,声明≠测量)。
场地分布:小酒吧 ×2 · 俱乐部 ×3 · 剧场 ×2 · 体育场 ×3 —— 正好盖四段「编曲越来越满」的弧。
剩余工作 = **接线**(AudioStream 播放 + 段切换不断拍 + 6 秒脸的 3 小节对齐),归 1.1。

### ~~素材:用户找~~(下面的筛选条件与站点表**已用不上**,历史保留)

**筛选条件三条**:① **120 BPM · 4/4** ② **loop(循环段)不是 track(成品曲)** ——
我们要的是能无缝循环的 2/4 秒小节段, 不是 3 分钟完整曲 ③ **许可允许商用**(要上 TapTap)。

| 站 | 许可 | 备注 |
|---|---|---|
| [Pixabay 120-bpm](https://pixabay.com/sound-effects/search/120-bpm/) | 自家 License, **不要求署名** | ⚑ **首选** —— 唯一同时有 BPM 筛选 + 免署名 + 可直接下 wav/mp3 |
| [OpenGameArt CC0](https://opengameart.org/content/cc0-music-0) | **CC0** | 专为游戏做的, 质量参差 |
| [Kenney](https://www.hackingtons.com/free-game-audio.html) | **CC0 1.0** | 偏音效, 音乐少 |
| [Looperman](https://www.looperman.com/loops/tags/free-120-bpm-loops-samples-sounds-wavs-download) | 逐个上传者不同 | 120 BPM loop 最多, ⚠ **必须逐曲看条款** |
| [Free-Stock-Music](https://www.free-stock-music.com/loops.html) | 多数 **CC-BY(要署名)** | 只有一部分 CC0 |

⚠⚠ **「Royalty-free」不是一种许可, 是一种商业模式** —— 同站不同曲许可可能完全不同,
**必须逐曲看许可页**, 不能信首页那句宣传语。

### ⚠ 接线时必须做的一件事:**测首个瞬态的位置**

很多免费 loop 开头有几十毫秒静音或渐入, **那样每次循环都会漂**, 8 秒拍上漂几十次就散了。
⇒ 拿到文件后先把波形打出来、量出首个瞬态偏移, 在代码里补偿。
⚑ 这和「临摹前先把素材的像素剖面打出来」是同一个手法, 只是量的对象从像素换成波形。

### 现状:**零基建**

`grep` 过全仓 —— 没有任何 `AudioStream` / `AudioServer` 调用, `assets/` 下零音频文件,
`docs/design/` 里只有 3 处无关提及。**这条是从零开始, 所以外部调研是该做的**(不是重推已有文档)。

---

## ⭐⭐ 2026-08-17 交接 · 下一个 session 从这里接

### 🚨 第一件事:**门已经过期了,必须重跑**

`gate.sh` 在 **14:51 全绿**(7 步一趟连续跑完)。但那之后**又改了 14 处**(全是真人试玩报的 UI/流程 bug),
所以**现在的树没有任何一趟连续的门背书**。已跑过的只有:单测 1759/0 · 流程回归 0 bugs · 若干针对性探针。

⇒ **下一步 = 重跑一次全量门(~4h)**, 之后才谈得上 1.0。

### ✅ 这个 session 做完的(两批)

**第一批(功能, 已过门)**:S1 目标分 921→560→**420** · 禁回 `min_run: 10`(**新开「脸按局数解锁」通路**)·
教学关重做(做中学 + 动作门)· 教学高亮改**白光 + 压暗** · **双色调拆黑调/红调** ·
**升级系统**(金币的主出口, 5 级 `[4,7,11,16]◆`, 按**增量**放大)· 加分族 **12 张改跟随尺度** ·
快闪回池(窗口改成每段第 1 拍)· Director 接线(**300/300 逐字节等价**已验)· 券的四层(数据/纯逻辑/存储/存档)·
`bot_targets` 重锚(尺子 random 7.5% → **4.0%**)

**第二批(真人试玩的 14 条 bug, 未过门)**:见下。

### 🐛 真人试玩抓到 14 条, 门一条都抓不到

| 症状 | 根因 | 谁的 |
|---|---|---|
| 替换小丑牌点不动 | `Hand` 全屏 PASS 盖住槽位 —— **PASS ≠ 透明**(赢命中测试, 事件传给**父节点**不漏给兄弟) | 既有 |
| 手牌弃不了(只能弃缓存) | 主角轨道框 `PanelContainer` 默认 STOP, 盖住手牌行 | **修上一条引出的** |
| 牌不刷出来 / 没教学 | `_reset_run` 把 `phrase` 清空后, 开场金 `phrase.coins +=` **空引用** | **我造的** |
| 教学关弹盲注公示卡 | `_enter_section` 缺教学关的闸 | 既有 |
| 教学中途退回首页 | 旧设计:走完就 `_open_home()` ⇒ **玩家一局都没打过就被弹出去** | 设计错 |
| 回首页再开局留着旧卡 | `run.reset()` 漏了「回首页再开局」这条路径 | 既有 |
| 不能同时选多张 / 手牌弃不了 | **能力压制**:`unlock` 把部件关着 ⇒ 玩家体感是「点了没反应」= 和 bug 一样 | 设计错 |
| 高亮反而变暗 / 好丑 | 光画在内容下面(不染花色的代价)⇒ 小圆钮本身没变亮, 只有周围变暗 | 我造的 |
| 文案里 `**` 星号原样显示 | `draw_string` 不解析 markdown | 我造的 |
| 开头要点一下才走 | 我加的「动手才起钟」—— **冻结是看不见的**, 「给你时间」和「卡住了」长得一样 | 我造的 |
| 同一句提示重复太多 | 软门**没有兜底**(给时钟做了 30s 兜底, 忘了给步进做) | 我造的 |

**⇒ 已全部修完, 但只过了单测 + 流程回归, 没过全量门。**

### ⚠⚠ 这个 session 最贵的教训:**七种「绿是假的」**

| # | 现象 | 真相 |
|---|---|---|
| 1 | 退出码 0 | 脚本 parse 失败, `SceneTree` 空转十几分钟 |
| 2 | `load()` 非 null | 类解析其实失败 —— **只有 `--check-only --script` 算数** |
| 3 | `1706 passed, 0 failed` | **一个域中途抛类型错被掐断, 47 条没跑**, 框架把「没跑」记成「没失败」 |
| 4 | `SCRIPT ERROR = 0` | 日志里 **120 万行 `ERROR:`** —— `push_error` 出的是 `ERROR:` 不是 `SCRIPT ERROR:` |
| 5 | 门在跑 | **我同时改源码** ⇒ 那一趟验的是一棵不存在的树, **日志里一个异常都没有** |
| 6 | 全量门绿 | **门覆盖不到的地方**(输入命中测试)绿了也不代表没坏 |
| 7 | 探针全过 | **我亲手把代码移出了测量范围** —— 券的探针闸让 `consume_ticket` 恒 false, 那行永远不执行 |

**⇒ 判据升到四条同时满足**:`退出码 0` **且** `failed 0` **且** `SCRIPT ERROR 0` **且** `^ERROR 0`
(剩余 `ERROR` 还要**逐条看过来源**), **外加通过数要涨**。全部写进 LESSONS。

### ⚠ 三条探针盲区(今天一半 bug 的共同成因)

1. **探针不点屏幕** ⇒ 输入命中测试、层序、`mouse_filter` 一律无覆盖
2. **探针进不了教学关**(`_is_probe()` ⇒ `seen_tutorial()` 恒 true)
3. **探针拿不到券**(同一道闸)⇒ 券相关代码零覆盖

⇒ **待办:给教学关补一个真走那条路径的探针**(照 `tutorsheet.gd` 的办法, 在 `choose_character`
**之前**把 `run.tutorial` 按上)。⚠ 今天两次「截图里教学关冒出 BOSS 脸」都是**探针按晚了**的假象, 不是 bug。

### 🎵 1.1 音乐(用户自己生成中)

**BPM 已算死 = 120(4/4)**, 见本文件「1.1 · 音乐」节。四条生成提示词已给用户。
⚑ 派生约束:**拍长一律取偶数秒**, 守住它 120 BPM 就永远对齐。

### ⏳ 1.0 还差(2026-08-17 晚更新;流程 = 我做完 → **用户验** → 验完出包)

**我的(✅ 08-18 凌晨全部做完, 等最终验证绿)**:
✅ 全量门跑完 —— **唯一红点 `lowend`** 已按门的指引进 `weak_upper_bound` 豁免
(z=−3.31 显著但只占基准 4.0% < 5%;根因 = 两对 ×3 把价值从点数和挪向牌型倍率,
低音税的正是点数和 ⇒ 咬合面真的变小 + 基准被抬高, 理由全文在 `docs/design/blinds.md`)·
✅ **教学关真路径探针**(`SYNC5_PROBE_FRESH=1` 让探针走真人同款入口;tutorsheet 已换新办法)·
✅ **每日发券提示**(第一拍飘字「今日券 · X」, 文案在 ui.json;方法留了探针注入口)·
✅ **目录整理**:`resources/` 拆三路后删除(music→`assets/audio` · 设计稿+素材+handoff→
`docs/mockups/` **整体搬**, html 与它引用的 assets 不拆散)· `design/` → `docs/design/` ·
全仓 100+ 文件路径指针一次改写(.gd 注释/JSON/根 md/docs 内链深度/记忆), 零残留零双前缀 ·
`docs/.gdignore` 把设计材料挡出 Godot 扫描与出包(此前 mockup 素材一直被白白 import)。
✅ **教学高亮重做**(08-18 凌晨, 用户:「需要操作的区域提亮, 其他地方变暗;
有时候包了一层形状不对的光晕, 怪」)—— 病根 = **光和暗的形状是两套逻辑**:
光会画圆(弃牌键)、暗只会挖方 ⇒ 圆光外面套一圈没压暗的方角;多块高亮取并集 ⇒
手牌与缓存**之间的走道**也漏亮。修法:形状收成一份(`Widgets.focus_radius`,
光/暗/提亮三层共用)· 压暗层**逐块挖形状洞**(含反向圆角补片, 纵向重叠退回并集兜底)·
新增 **`TutorLight` 加法混合提亮层**画在内容之上 —— 历史上两版「最上层加光」翻车都是
**普通混合往灰里拉**, add 是加光不加灰, 这才是能把不透明小圆钮真正照亮的位置。
已跑真路径 tutorsheet 6 步逐张目视 ✓(圆钮圆光 · 双区域各自开洞 · 走道正确压暗)。
⏳ 收尾中:`lowend` 豁免后的单脸重验(在跑)→ 提交 → 交用户验 →
**出 Android 包**(TapTap 给朋友试玩)。单测已重跑 **1868/0**(目录搬迁后全绿)。
**用户的**:和弦/排练两张**条件坏了**待拍(改条件 or 删)· 教学关**新手数据**(找个没玩过的人)·
3 张插画(见 🔴)

✅ **券系统 2026-08-17 收完 → ⏸ 2026-08-18 按拍板下线到 1.1**(「第一版去掉券,第二版
商业化的时候加」)。下线方式 = 「重看教学」同款先例:**关开关不删代码** ——
`data/tickets.json` 的 `enabled: false`,SaveState 五个入口同一道闸(不发放/不显示/不可用,
存档残券也隐身);四层实现 + 全部断言留在树上继续被单测背书,`t_ticket` 第一条锁着这个拍板。
收尾细账(1.1 翻开关时直接可用):每日发放早已接好(启动时结算 + seedmoney 开局兑现,
交接把它们记漏了);后补的**使用入口**:局内券托盘(`view/tray.gd`,overtime/redeal/boost)+
商店点唱券免费刷新 + boost **获得时掷值**的存位(FIFO,`ticket_rolls`)。
⚠ **广告出口顺延**:它是**加发**通道(`ticket.gd` 文件头有拍板:每日发放不用广告也不用领取按钮),
且要等 TapTap SDK 那一步 —— 不挡 1.0。
⚠ **交接的「券 18+24 条断言」是假账** —— tests/ 里从未存在过;`t_ticket` 域 2026-08-17 才是第一份。

### ⚑ 2026-08-17 晚 · 券使用入口的实现账(过单测前先记)

| 件 | 落点 | 要点 |
|---|---|---|
| boost 掷值 | `core/ticket.gd` | **获得时掷**(用户拍板「拿到手就知道」), `ticket_rolls` 与张数同长 FIFO;发放收拢到 `grant_with_roll` 一个入口 |
| 计分通道 | `core/beat.gd` | `flags.meta_mult` 缺省 1.0 = 探针逐字节不变;raw_score 不吃加成(复读/回放读原始分);**记分留在 core**(判生死同一条线) |
| 整手重发 | `core/phrase.gd::redeal_hand` | **不是弃牌**:不计弃牌计数器/不占预算/不开 action_track —— 否则周转/早弃被一张券白喂(零挂机成长);**封印卡不换**(券不许破解脸规则) |
| 托盘 | `view/tray.gd` + `layout.gd` + `ui.json tickets` 节 | chips 用 Button(刚踩过两条命中 bug, 不自造命中测试);**只占自身矩形**(PASS ≠ 透明);数据注入制(探针闸挡不了目视);教学关不显示 |
| 加时 | `view/phrase.gd` | 三个钟(dur/warning/lock)**走同一套 GameConfig 钩子重推**, 不手加三个数 |
| 点唱券 | `view/shop.gd` + 编排器 | shop 只分流不消耗;**不推付费阶梯**(券买的是「多看一次」);淘碟照常喂(刷新真发生了, 代价是券) |
| 打点 | 编排器 | `Tape.on("ticket", ...)` 四处:overtime(at/dur)· redeal(重发后的手)· boost(mult/total)· juketicket |
| 测试 | `t_ticket`(新域)+ `t_phrase` + `t_run` | 掷值不变量 · redeal 非弃牌 · meta_mult 缺省零痕迹 |

---

## ⚑⚑ 2026-08-16 深夜 · 第二批的账(六件功能 + 分级门跑完三关)

### ✅ 这一批做完并验过的

| 事项 | 验证 |
|---|---|
| **加分族 A 案** —— 12 张换成 `bonus_target_pct`(每拍目标 × pct) | 换算 `pct = 旧数额/416`(灯牌锚在 12%), **只改尺度不重新定价** |
| **快闪回池** —— 窗口 `只有第1段` → **每段第1拍** | ⚠ **不是照原则加数额**:`docs/design/jokers.md` 写着「加数额是假修」, 它的死因是商店最早 S1 过半才开 ⇒ 改的是**窗口形状** |
| **Director 接线** | 300/300 逐字节等价已验(含随机流位置);⚠ 接线时抓到 `Director` 绕过 `min_run` 门的 bug |
| **券:数据 + 纯逻辑 + 存储 + 存档** | 5 张券 · ~~18 + 24 条断言~~ ⚠⚠ **假账(2026-08-17 查证)**:tests/ 里从未有过任何券断言 —— `t_ticket` 域 08-17 才是第一份。交接说「有测试」要先 grep 到再信 |
| **教学提示条自动躲开所有区域** | ⚠ 第一版漏了「标签画在容器之外」⇒ 躲避矩形上放宽 44px |
| **D1/D2 两条模型分叉** | 三探针补传 `section_target` · `curve.gd` 显式 `UNLOCK_ALL_RUN` |

### 🚪 分级门(**9 轮才绿**)

| 门 | 结果 |
|---|---|
| 单测 | ✅ **1755 / 0**,四条判据全满足 |
| `pair.gd` | ✅ 配对差 +0.0 / −0.1,**与上轮逐字相同** ⇒ 加分族换通道没让求解器和游戏分叉 |
| sim | ❌ **尺子自检红** —— 见下 |
| kit | ⏭ **建议跳过单卡**:这批改了 13 张, 每张 12 分钟 ≈ 2.6h, 而全量门(含全部 kit)才 4.5h |
| `gate.sh` 全量 | ⏳ **等夜里跑**(用户 2026-08-16:「等我睡觉的时候跑 sim」) |

**门抓到的三件**(不跑门会一路带到试玩):
1. **真 bug**:`tools/bot.gd` 变量重名 `best_cost` —— 我加升级那段引入的;
2. **真 bug**:求解器给那 12 张加分卡估值 **0** —— `_amt()` 不认识新通道 ⇒ bot 永远不买;
3. **我的设计错**:`bonus_target_pct` 缺 `section_target` 时 `push_error` + 返回 0 ——
   ① 卡在模型里**隐身** ② 日志刷 **120 万行** ERROR。⇒ 改成退回**一局平均每拍目标**,
   与 `bot.gd::_avg_beat_target()` / 测试助手**三处同源**, 都从 `SECTION_TARGETS` 推导。

### ~~⏳ 夜里要跑的两件~~ ✅ 都做完了(08-16/17 补账)

1. ~~**`gate.sh` 全量**~~ —— 08-16 14:51 全绿过一趟;之后又改 14 处 ⇒ **08-17 晚重挂在跑**
2. ~~**`bot_targets` 重锚**~~ —— ✅ 已做(尺子 random 7.5% → **4.0%**,第一批)。
   原始症状留档:sim 尺子自检红 `random 3.4% → 7.5% > 5%` 哨兵线。
   ⚑ **哨兵在干活, 不是坏消息**:bot 变强是这批的**预期效果**(会升级 + 加分族中后期给得多 + 快闪回池)。
   ⚠⚠ **红的是 `sim.json` 的 `bot_targets`(仪器刻度), 不是 `run.json` 的 `section_targets`(玩家面对的)**
   ⇒ **不影响试玩**, 影响的是「sim 这把尺子还准不准」。
   ⚑ 顺带印证金币出口有效:局末余额 **34.7◆ → 8.5~10.6◆**, 钱真的被花掉了。

### ⚠ 这一批新欠的

| 债 | 性质 |
|---|---|
| ~~**券的发放**(广告 / 每日领取)~~ | ✅ 每日发放其实当时就接好了(交接记漏);广告 = **加发**通道, 顺延到 TapTap SDK 阶段, 不挡 1.0 |
| ~~**券的使用入口**(phrase / shop / run 三种 scope)~~ | ✅ 2026-08-17 收完(run 早在;phrase/shop 本轮补齐, 账见「⏳ 1.0 还差」下节) |
| ~~`bot_targets` 重锚~~ | ✅ 第一批已做(random 7.5% → 4.0%) |
| kit 13 张未单验 | 由全量门覆盖 |

---

## ⚑⚑ 2026-08-16 晚 · 账本(本轮改了 32 个文件, 门还没过完)

**⚠ 顺序已由用户改过一次**:原本是「改完 → 试玩 → 才跑门」,2026-08-16 晚用户补充
**「试玩我会最后做, 你得先把账补完」** ⇒ 现在是 **`补账 → 跑门 → 试玩`**。
理由不用猜:让他去玩一个没验过的包是浪费他的时间。

### ✅ 已补(第一步:两条**模型分叉**,必须在跑门之前)

**⚑ 为什么排在门之前**:门是**拿探针量的**, 而探针当时和游戏不是同一个世界 ——
先跑门等于用一把歪尺子量, 量完还得重量。

| # | 债 | 补法 |
|---|---|---|
| **D1** | `attrib.gd` / `warm.gd` / `model.gd` 三处直连 `Settle.run` **没传 `section_target`** | 三处补齐。⚠ 不补的话, 加分族 A 案的新通道在这些探针里**静默算 0**, 而 attrib 正是牌型频率与中性基准的仪器 ⇒ 会「测出」那族卡没用 = **自我实现的错误结论** |
| **D2** | `min_run` 造成的新分叉:探针缺省 `-1` = 全解锁, 而真人**第 1-9 局看不到禁回** | `curve.gd` 显式传 `UNLOCK_ALL_RUN = 10`。⚑ **目标分从此明确按「老玩家的世界」标定**;新手前 9 局池子更温和 ⇒ 偏差方向**安全**, 但它是真分叉, 写在调用点上 |

### ⏳ 第二步:**分级跑门**(不要直接上 4.5 小时)

```
单测        744s    ← 覆盖面最广、最便宜, 先跑
pair.gd     ~3min   ← 守「求解器 = 游戏代码」;升级放大最可能在这炸
kit 单卡×2  几分钟   ← SYNC5_KIT_ID=blacktone / redtone
sim         ~107s   ← 拿**新基线**(bot 会升级了, 旧读数不可比)
gate.sh     ~4.5h   ← 上面全绿之后再跑, 可过夜
```

⚑ **这个顺序省的不是时间, 是返工** —— 前四项加起来 ~15 分钟就能抓到大部分问题,
而全量门一旦红, 你还是得回来从单测查起。

### 本轮待验的四类改动(每一类单独都够格跑一次门)

**规则**(双色调拆黑调/红调)· **计分**(升级按增量放大)· **掷脸**(`min_run`)· **目标分**(S1 = 420)。

---

## ⭐ 下一个 session 从这里接(2026-08-16 交接)

**⏸ 全部挂在同一件事上:用户试玩。** 本轮(08-16 下午)改完的东西都**没有跑过 sim/门**
—— 那是用户拍的板(见文件顶「本轮的工作方式」):`改完 → 用户试玩 → 用户喊 → 才跑门`。

**⚠ 等着他给意见的三件**:

1. **S1 = 420 到底够不够低** —— 本轮压了**两次**:`921 → 560 → 420`。
   ⚠ 560 那版**压得不够而且是算得出来的**:零构筑 6 拍全打对子 = 540 **< 560** ⇒ 一次不失手才刚好过。
   420 的判据 = **允许失手两拍**(4 对子 + 2 高牌 = 440 过),而「基本没打」(390)照样死。
   ⚠ **只有真人试玩能验**(sim 读的是另一张表,见下)。
2. **教学关重做完了,等体感** —— 做中学 + 动作门 + 读提示时钟不走 + 白光高亮 + 压暗。
   ⚠ 高光位置本轮修过一次:那四个矩形原本**手抄在 `ui.json`**,而真值是运行时算的 ⇒ 必然对不上。
   现在从活部件取(`Hand.focus_rect()`)。
3. ~~**加分族的 10 倍差距**~~ ✅ **已动手(08-17 第一批 A 案落地,13 张走 `bonus_target_pct`)**。
   ⚑ **A 案已被证明是唯一解**,不是「倾向」:见下面「不是 21 张,是 14 张」那节。
   ⚠ S1 压到 420 之后每拍需求跨度 **10 倍 → 13.3 倍**,B 案(逐张调绝对值)数学上更不可能了。

**⚠ 本轮新挖出、归用户拍板的一件**:`norepeat`(禁回)在第 1 段,而它 ×0.5 罚重复牌型
—— **新手最可能的打法正是每拍打对子**,禁回下 6 拍全对子只有 315 分,**420 也过不了**。
**这个靠调目标分治不了。** 三条岔路见「🟡 需要先拍一个设计判断」那节。

**🟢 不依赖用户、随时可做的**:Director 接线 · 快闪 popup 复活 · 清理瘦身剩下的 ·
求解器 S2/S3/S6/S7 · 断点续玩 · 两条仪器债。

**⚠⚠ 别用 sim 验证 `section_targets` —— 它读的是另一张表(2026-08-16 实测坐实)**

我把 S1 从 921 改到 560(几乎腰斩),**跑完 sim 读数纹丝不动**:`twin 54.9%` / `random 3.5%`,
与改前**逐字相同**。原因:

| 表 | 谁读 | 谁面对 |
|---|---|---|
| `run.json` 的 `section_targets` | 游戏 · `curve.gd SYNC5_CURVE_BOT=perfect` | **真人** |
| `sim.json` 的 `bot_targets` | **`tools/sim.gd` 的九个队列** | 规则 bot |

⇒ **sim 结构上量不到这次改动。** 唯一能验证它的是:① 真人再试玩一次(最可靠);
② `SYNC5_CURVE_BOT=perfect godot --headless --path . --script res://tools/curve.gd`(完美玩家尺度)。

⚑ **这次没被骗到,是因为读数「一点没动」本身就是信号** —— 一个大改动配一个零变化,
说明仪器和改动不在同一条路径上。**这正是 memory 里那条「选错仪器会把结论量成反号」的形状。**
⚠ 顺带:`ui.json` 的 `key_w` 改动只影响渲染,已截图目视确认。

---

## 🔴 只有用户能解(挡着一整族)

| # | 事项 | 挡着什么 |
|---|---|---|
| D2 | **发挥系数**(完美玩家 → 真人) | ⚠ **不许拍初值、不许用机器人代算**(用户 2026-08-07:「我后面会完整玩很久,到时候用我的系数」)。它挡着**整套目标分** |
| D4 | 会话边界数据 | ✅ **打点已实装**(`sess: {id, gap, runs_prev}` 进 Tape meta)—— 现在只差**你正常玩**。目标函数换成留存后,**这是唯一能直接观测目标的量** |
| — | **教学关的新手数据** | 现有合格 Tape 全是熟练玩家(用户本人)。**找个没玩过的人跑一遍**就有第一份新手动作时刻分布。教学关已带 `tutorial: true` 标记 |
| ~~—~~ | ✅ **发行三件用户已自己搞定**(2026-08-17:「已经搞定了,只需要你给我包」) | 剩余动作只有:1.0 验收通过后**出 Android 包**(TapTap 给朋友试玩用)。⚠ 正式发行目标是**美国**,TapTap 只是试玩渠道 |
| — | **3 张小丑牌插画** | `jukebox` 点唱机 · `blacktone` 黑调 · `redtone` 红调(双色调拆分的两张,manifest 的 art_subject 已冻结:S-15 / S-61)。`verify_joker_assets` 对它们报缺 = 待办信号 |
| ~~C1~~ | ✅ **两对 ×2 → ×3 已恢复**(2026-08-17) | 用户 08-06 白天的拍板(「two pair 和 pair 怎么是一个倍率」→ ×3)当晚被「实测累计 + 0.75 幂」覆盖回 ×2(算出 2.47);而 **08-14 用户已把定价口径换成组合** —— 同一套压缩在组合累计上 = `2×(92.88/53.22)^0.75 ≈ 3.04`,与拍板相符 ⇒ 恢复。终版表 **`1/2/3/3/4/5/6/7/8/8`**,两对与三条共 mult、chips 20<30 保扑克序(原作本就让多牌型共 mult)。<br>⏳ **剩余归用户的只有顶端三档**(四条/同花顺/皇家 ×7/8/8):缺口真实(压缩外推四条 ≈×18)但合计只占 0.43% 的拍;抬它要先定压缩指数 = 设计判断,走 `numbers.md` §5。<br>⚠ 三条/顺子**不倒挂** —— 那是恰好概率的中转站伪影(同一个错的第 3 次,记在 pattern.gd)。 |

---

## 🟡 需要先拍一个设计判断

| # | 事项 | 岔路 |
|---|---|---|
| ~~—~~ | ✅ **`norepeat`(禁回)已按局数上锁**(2026-08-16 用户拍板:「禁回必须在第十关以后出现就好了」,并确认「关」= **跨局**)。`min_run: 10` ⇒ 第 10 局起才可能出现。<br>⚑ **顺带开了一条新通路:脸可以按局数解锁。** 以前脸**只按「局内第几段」(`tiers` 1-4)索引**,没有任何跨局维度。现在 `faces.json` 支持 `min_run`,`SectionMod.roll_run(rng, run_index)` 按它筛候选。<br>⚠ **`run_index` 是显式参数, `core/` 绝不自己读存档** —— 偷读会让同一份纯逻辑在「这台机器玩过几局」下产出不同结果,那正是 2026-08-15 `flow_probe` 事故的形状。**缺省 `-1` = 全解锁**,所以**探针逐字节不变**(实测 200/200 相同,含随机流位置)。只有 `view/phrase.gd` 传真实局数。<br>⚠ 实装时抓到一个自己的 bug:`faces` 是**数组**不是字典,第一版 `.get(id,{})` 会静默返回空 ⇒ `min_run` 恒 0 ⇒ **门形同虚设且不报错**。已改走 `_entry()`。<br>⏳ **`min_run` 现在只有禁回一张在用** —— 别的脸要不要也按局数放,等试玩。 |
| ~~—~~ | ✅ **twotone 已解:拆成黑调/红调**(2026-08-16 随第二批实装,`db.gd:685` 有账)—— 正是当初三条岔路里「只对一色生效」那条:一张只认 ♠♣、一张只认 ♥♦,各自 rare。单张先验实测 **5.3×** 同花放大(两张都装 = 9.6× ≈ 老 twotone),`t_rules.gd` 锁了拆分后的断言。⚠ 收尾三件 2026-08-17 补齐:art manifest(黑调占 S-15 旧位、红调**追加队尾** S-61,避免 40+ 张 code 位移)· `joker_slot.gd` 字形/倍率牌 · `jokers.md` roster 行。插画待用户出(见 🔴) |
| ~~—~~ | ❌ **「重看教学」不做**(2026-08-16 用户拍板)。⚠ 机制本身留着(`SaveState.clear_tutorial()` + `-- --fresh` 启动参数),**只是不给它 UI 入口** |
| M1 | meta 层 `docs/design/ui_meta.md` | ✅ **冲突已解除**(用户 2026-08-15:「有道具后简单点正常,**但关卡难度设计不用带这个**」)。剩下的是做不做、什么时候做 |

---

## 🟢 可以直接做(按建议顺序)

### 1 · ⭐ 清理与瘦身(2026-08-15 用户提,**进行中**)

> 「项目需要清理、瘦身……**标准是可持续迭代,易读懂,结构清晰,符合后续迭代需求**。」

**A · 文档:思考过程搬走,只留调研 + 最新结果**(用户原话:「中间过程放在 CHANGELOG 就行」)

| 内容 | 归哪 |
|---|---|
| 外部调研、实测数字、当前结论、规格 | **`docs/design/`**(留) |
| 「起因是…」「我第一版这么错的」「后来推翻了」 | **`CHANGELOG.md`**(搬走) |
| 「别再退回去」的可复用判据 | **`LESSONS.md`**(留,它按定义就是结果) |

⚠ **不是无脑删** —— 有些 ⚠ 是**载重**的。判据:**去掉之后读者会不会重犯那个错?**
会 → 是结果(留 / 搬 LESSONS);不会 → 是过程,搬 CHANGELOG。

- ✅ `TODO.md` **871 → 本文件**(已完成事项的叙述搬走;CHANGELOG 早有同一批记录)
- ✅ `DESIGN.md` → `docs/design/visual.md`(它是 design spec 却躺在根目录当孤儿,**无人引用**)
- ⏳ `docs/design/difficulty.md`(462)· `docs/design/blinds.md §2.4/§5` · `docs/design/prior.md`(462)
  —— 都是我这两天写的,污染最重
- ⏳ `core/` 与 `tools/` 里的长注释(同判据)

**B · 代码:清理冗余**

⚠ **别按行数切**;判据 = **找那个被抄了 N 份的东西**,验收 = **每份调用方逐字节不变**。

- ✅ ~~`view/phrase.gd` 的替换态~~ —— **本条已过期**:`view/replace.gd`(`ReplaceFlow`,84 行)
  2026-08-12 就建好了,`phrase.gd` 里散落成员**现为 0**,接口正是当初提议的
  `replace.enter(j)` / `replace.exit()`,经济动作与打点也确实留在编排器。
  ⚠ 2026-08-15 量出来才发现 —— **待办自己过期是这次清理的第 6 例**
- ✅ **货架抽卡 2 份 → 1 份**(2026-08-15):`_weighted_pick`/`_shelf_weight` 在
  `view/shop.gd` 与 `tools/bot.gd` 各一份且**逐字节相同**,而 shop 那份的注释写着
  「不许各写一份」—— **它自己就是第二份**(第 5 次「注释承诺不存在的机制」)。
  收进 `Economy.weighted_pick`,**收算法不收入口**(`wallet.gd` 的 SpyBot 靠
  `super._weighted_pick` 覆盖 bot 那份记货架)
- ⚠ **量出「代码是屎山」这个判断不成立**:`view/phrase.gd` 1077 行 = 代码 724 + 注释 232
  + 空行 121,而 2026-08-09 重构后是 727 行 —— **代码量没变(727→724),长的全是注释**,
  函数体最大 78 行。**下次别按体积找问题,按「被抄了 N 份」找。**
- ⏸ **R6 建模五件套**(`formal`/`dp`/`dpcheck`/`dpdiag`/`udp`,1364 行)——
  **解锁条件 = S3 结案**。它们是 S3 正在用的仪器,**在仪器还在用的时候改仪器**这个项目吃过亏
- ⏸ **R7** 五件套要不要进一步合并 —— R6 之后再评估,**默认答案是「不合」**

### 2 · ⭐ 小丑牌的两条原则(2026-08-15 用户拍板)

**① 一轮限定的卡不该删,该是「窗口窄 ⇒ 效果强」**

> 「不是应该删除,而是**如果仅限一轮,效果就要比较强**;如果是**累计效果的,效果保留现状**。」

⚠ 前提更正:此前删的两张,理由都不是「仅限一轮」——

| 卡 | 实际死因 | 这条原则救不救得了 |
|---|---|---|
| **快闪 popup** | 结构死卡:**窗口 × 商店时机错位** | ✅ 救得了 —— 正是「窗口太窄」,该加强 |
| 伴唱 backup | 包厢的下位重复(同读缓存人头) | ❌ 死于重复 |
| 三重唱 trio | 真人 **0/168 拍**从未触发 | ❌ 条件本身不会发生 |

⇒ ① 写进 `docs/design/jokers.md` 设计原则(✅ 2026-08-17 补,第 17 条);
② ~~快闪按新原则复活~~ ✅(已回池:窗口改**每段第 1 拍** + `bonus_target_pct: 0.8` —— 改的是
窗口形状不是数额,因为它的死因是窗口×商店时机错位);③ 复审现役(现 63 张)窗口/时机限定的卡 —— **还没做**,
⚠ 副歌欠补偿那条(只有开场一半)也归这里,待用户定提到多少。

### ⚑ 2026-08-16 真人试玩把它坐实了,而且分成**两个**诊断

> 「有部分小丑牌我几乎不会选,主要是**只有本轮生效**的小丑牌,**奖励不太够**;
> 对于**加分而不是分数带乘子**的也不爱选……这俩的特点都是**效果小**,
> **前者短、且奖励不足,后者加分太少了**。」

**⚠ 别当成一件事** —— 「效果小」是共同**症状**,但成因不同,旋钮也不同:

| 类型 | 成因 | 该动什么 |
|---|---|---|
| **一轮限定**(窗口短) | 窗口短 **且** 数额没为「短」补偿 | **窗口不动,数额往上** —— 正是上面那条原则。⚑ 补偿倍数应当≈「它比常驻卡少覆盖多少拍」 |
| **加分族**(`bonus` / `additive`,非 `mult`) | 数额绝对值太小 | bonus 族数额。⚠ 它在乘法链**之后**落地 ⇒ **前期是神、后期自然过气**,这是杀死万能填充牌的 Balatro 机制,**不是缺陷** |

### ⚑⚑ 2026-08-16 算出了决定性的那个数:**加分族与乘子族差 10 倍以上**

```
新 S4 目标 5600 / 6 拍  ⇒  中后期每拍要 ~930 分
bonus 40(串场)   在 90 分的拍上 = +44%    在 930 分的拍上 = +4%
mult ×1.5                                  在 930 分的拍上 = +465 分
                                            ↑ 差 10 倍以上
```

⇒ **加分族不是「弱」,是衰减太快** —— 从 +200% 掉到 +4%,而**玩家停留最久的正是中后期**。
**21 张非成长加分卡全在这条线上**(清单见下)。

⚠⚠ **所以「后期过气是设计」这句只在前期够强时成立,不能拿它当挡箭牌。**
用户两轮都报了同一件事(「加分太少」→「还有大量一轮生效但加成很弱的」),**这是真问题**。

**⚑ 三条候选修法(未拍板,按侵入性排)**:

| | 做法 | 代价 |
|---|---|---|
| **A** | **数额跟随尺度** —— bonus 改成「本段目标分的 x%」而不是绝对值 | 机制改动,要过 SOP + 门;但一次修好全部 21 张 |
| **B** | 逐张调大绝对值 | 前期会爆炸(bonus 190 在 90 分的拍上已经是 +211%) |
| **C** | 把一部分加分卡改成乘子卡 | 改变卡的身份,且乘子族本来就强 |

### ⚑⚑ 2026-08-16 复核:**不是 21 张,是 14 张**,而且 A 案被证明是唯一解

**① 清单纠正 —— 上面那 21 张混进了两个族,别一起调:**

| 族 | 落点 | 张数 | 行为 |
|---|---|---:|---|
| **奖励分** `bonus` | 乘法链**之后**,平的 | **14** | ❌ **会衰减,就是这条线** |
| **改基牌** `additive` | 加在 chips 上,**吃全部倍率** | 6 | ✅ **会放大,结构上没问题** |

**14 张(要改的)**:回响 · 尾声 · 周转 · 和弦 · 灯牌 · 变奏 · 全员 · 彩虹 · 清流 · 排练 ·
静物 · 串场 · 让位 · 早弃。
**6 张(别动)**:暖色 · 冷色 · 低声部 · 对唱 · 三和弦 · 后台 —— 例如三和弦 `+25 chips`
打葫芦(×6)配一张 ×2 小丑 = **+300 分**,它跟着乘法链一起长。

**② A 案不再是「倾向」,是唯一同时满足用户两个要求的形状**(2026-08-16 用户补充
「第一个 section 做简单点」之后):

```
重锚后每拍需求   S1 93 → S4 933 = 10 倍跨度   (S1 越简单, 跨度越大)

固定加分调到 S4 够用(+30% = 280)  ⇒ 在 S1 是 +300%  ← 打穿刚调简单的那关
固定加分调到 S1 合理(+30% = 28)   ⇒ 在 S4 是   +3%  ← 仍是废牌
```

⇒ **没有任何固定数能同时成立。** 用户原话「除非你加很多分」方向对,
但那个「很多」**必须跟着尺度走**。⇒ **B 案数学上不可能,不是「代价大」。**

**③ 现役 14 张实际给多少**(期望/拍 = 数额 × 触发率,率来自先验层与真人 Tape):

| 卡 | 数额 | 触发率 | 期望/拍 | 占 S4 一拍(933) |
|---|---:|---:|---:|---:|
| 全员 fullcast | 320 | 21.8% | 70 | **+7.5%** ← 全族最强 |
| 回响 encore | 240 | 25.0% | 60 | +6.4% |
| 彩虹 rainbow | 180 | 30.8% | 55 | +5.9% |
| 灯牌 neonsign | 50 | 100% | 50 | +5.4% |
| 清流 nopair | 190 | 22.9% | 43 | +4.6% |
| 周转 turnover | 30/弃 | 1.11 弃/拍 | 33 | +3.5% |
| 排练 rehearsal | 200 | 10.5% | 21 | +2.2% |
| 静物 stilllife | 60 | 21% | 13 | +1.4% |
| **和弦 chord** | 140 | **4.0%** | **5.6** | **+0.6%** |

对照:一张 `×1.5` 乘子在同一拍 = **+465**。⇒ **全族最强的也只有 +7.5%,没有一张够格占 4 槽之一。**

**④ 其中 2 张是另一种死法,A 案救不了**:**和弦(4.0%)· 排练(10.5%)** ——
不是数额小,是**条件几乎不发生**,乘以接近零的触发率怎么调都是零。
⇒ 这两张该**改条件或删**,和另外 12 张不是一件事。

### ⚑ 实现侧已查清的三件(2026-08-16 读代码,**动手前别重推**)

1. **`bonus_pct` 不能复用** —— 它看着像「百分比加分」,实际是 `settle.gd:113` 的
   `ctx.mult * (1.0 + ctx.bonus_pct)`,是公式里的 `(1 + Σ%)` **乘子项**(开场/副歌那族)。
   ⇒ 要加的是**新通道**(建议名 `bonus_target_pct`),落在 `ctx.bonus`(乘法链之后),
   数额 = `section_target / PHRASES_PER_SECTION × pct`。
2. **`ctx.section_target` 已经有了** —— `settle.gd:62` 已从 `extra` 取,`fx.gd:104` 已有谓词在读。
   **不用新造管道。**
3. ⚠⚠ **但三个探针没传它** —— `tools/attrib.gd:57` · `tools/model.gd:115` · `tools/warm.gd:77`
   直接调 `Settle.run`,`extra` 里**没有 `section_target`**(`settle.gd:62` 默认 0)。
   ⇒ **新通道在这三个探针里会静默算成 0**,而 attrib 正是牌型频率/中性基准的仪器、
   warm 是养牌价值的仪器 —— **那会「测出」这族卡没用,一个自我实现的错误结论。**
   ⇑ **这正是 memory 里「规则在游戏里不在模型里,而且不报错」的形状,第 6 次。**
   `core/beat.gd:101` 传了(游戏 + 走 `Beat` 的探针都安全),漏的只有这三个直连调用点。
   ⇒ **改这条时必须同时补这三处,并让缺失时 `push_error` 而不是返回 0。**

✅ **已动手(2026-08-17 第一批,A 案落地)**:12 张换 `bonus_target_pct`(+复活的快闪 = **13 张**
走新通道),换算 `pct = 旧数额/416`(灯牌锚 12%),**只改尺度不重新定价**;
D1 三探针(attrib/model/warm)已补传 `section_target`,缺失时退回一局平均每拍目标(三处同源)。
**剩余只有和弦/排练两张**(条件死,A 案救不了)待用户拍 —— 见「⏳ 1.0 还差」。

### ⚑ 牌型倍率:要调的是哪几格(2026-08-16 查证,C1 的展开)

**⚠⚠ 最要紧的一条:整张表是用「策略后实测」口径算的,而口径 2026-08-14 换成了「组合」,
表没跟着重算 —— 只抽查了一对(同花 vs 顺子)。** `pattern.gd:105-110` 原话是
「本表的 5 vs 4 = 1.25, 落在组合口径 1.31 的手感带内, 维持不动」——**那是一次点检,不是重算。**

现值(rsum≈35 的中性拍,`score = (chips + rsum) × mult`):

| 牌型 | chips | 倍率 | 一拍 ≈ | 组合(恰好) | 组合累计 `P(≥)` | 判断 |
|---|---:|---:|---:|---:|---:|---|
| 高牌 | 5 | ×1 | 40 | 7.12% | 100.00% | 地板,不动 |
| **对子** | 10 | **×2** | **90** | 39.66% | 92.88% | 基准锚 |
| **两对** | 20 | **×3** | **165** | 25.98% | 53.22% | ✅ **已调(08-17 恢复 ×3)** |
| 三条 | 30 | ×3 | 195 | 5.13% | 27.24% | ✅ 带内 |
| 顺子 | 30 | ×4 | 260 | 8.89% | 22.11% | ✅ 带内 |
| 同花 | 35 | ×5 | 350 | 6.79% | 13.21% | ✅ 已点检 |
| 葫芦 | 40 | ×6 | 450 | 5.99% | 6.42% | ✅ 带内 |
| **四条** | 60 | **×7** | 665 | 0.34% | 0.43% | ⚠ **结构性低估** |
| **同花顺** | 100 | **×8** | 1080 | 0.08% | 0.09% | ⚠ 同上 |
| **皇家** | 140 | **×8** | 1400 | 0.01% | 0.01% | ⚠ 同上 |

**① 两对 ×2 → ×3 —— ✅ 已恢复(2026-08-17,见 🔴 表 C1 行的账)**

对子 → 两对 稀有 **1.74 倍**(累计 92.88% → 53.22%),回报只 **+22%**(90 → 110)。
⚠ **用户 2026-08-06 就提过**(「two pair 和 pair 怎么是一个倍率」),当时改成了 ×3,
**但 08-06 晚那次「按实测频率修正」把它算回了 2.47 ⇒ 落回 ×2** —— 修过又被覆盖掉了。
⚠ 影响面**全表最大**:两对是策略后最常打出的牌型(**33.5%**)。
⚠ `core/pattern.gd:56-61` 的注释仍写着「我们只在两处偏离 ① Two Pair 2 → 3」,
**与表里的 ×2 不符**(`docs/design/cards.md:213` 已划掉更正,代码注释没跟上)。

**② 四条 / 同花顺 / 皇家(×7/8/8)—— 唯一不随口径选择而消失的缺口**

葫芦 → 四条 稀有 **14.9 倍**,回报只 +48%。用能复现中段现值的压缩指数(≈0.41)外推,
四条应当 **≈×18**,现值 ×7。⚠ **但三者合计只占 0.43% 的拍** ⇒ 缺口真实、影响面近零。
⚠ `pattern.gd:90` 当初不动的理由是「顶端三档没有数据」—— **先验层 2026-08-14 已经给了数,这个理由过期了**;
但另一条理由仍然成立:**抬顶端是普涨风险最高的动作**。

**③ 别动三条/顺子** —— 见上面 C1 的更正,那个「倒挂」是恰好概率的中转站伪影。

⚠⚠ **不能简单「按组合口径重算一遍」** —— 组合累计在高段掉得比实测快得多,
0.75 次幂压缩会把葫芦推到 **×14.8**、四条推到 **×112** = **普涨**,
而普涨的后果 `pattern.gd:86` 记着:构筑价值塌(log₂ 版翻车,baseline 7.9% → 41.9%)。
⇒ **要先定压缩指数,那是设计判断,归用户。** 走 [`numbers.md`](docs/design/numbers.md) §5 六步 SOP。

### ⚠ 「窗口窄」那一类:**结构上只有 2 张**,别和上面这条混

| 卡 | 窗口 | 效果 | 摊到每拍 |
|---|---|---|---|
| `opener` 开场 | 每段第 1 拍 = 4/24 | +150% | **+25%/拍** |
| `chorus` 副歌 | 每段末拍 = 4/24 | +75% | **+12.5%/拍** |

⇒ **副歌只有开场的一半**,而 v3 只提了开场。**它是欠补偿的那张,待用户定提到多少。**
⚠ 用户体感里「一轮生效」的卡远不止 2 张 —— 那说明他指的是**上面那类(打完这拍就没了)**,
不是窗口窄。**两个成因不同,别一起调。**

⚠⚠ **加分族要小心「按后期体感调数额」** —— 玩家不爱选它很可能发生在**中后期**,
而那时它本来就该过气。**先分拍位看它在前期是不是也没人要**,再决定动不动数额:
如果只有后期不要,那是**设计生效**,不是失衡。
⚠ 两类都必须走 [`numbers.md`](docs/design/numbers.md) §5 六步 SOP(锚 = 稀有度地板线
12.5 分/拍·◆),并过小丑牌的门(`SYNC5_KIT_ID=<id>`)。

**② 曝光:最差 3-4 轮出一次** —— ✅ **已落地**(权重 70/25/5 → 35/30/25,特定 rare 卡 21.2 → **4.0 局**一次)。

⚑ **它换掉了一条设计原则,不是调了个旋钮**(守恒律推导 `numbers.md §9.1`):

```
21 个货架位是不可增的预算 ⇒ 「4 局见一次」要求单卡份额 ≥1.361%
61 张吃掉 83.0% ⇒ 只剩 17% 当阶梯预算 ⇒ 曝光阶梯最多 1.6:1(旧值 10.5:1)
```

**⇒ 稀有度从曝光轴上摘掉**,此后只剩**价格**(4/6/9◆)与 **EV 地板**两个作用。
⚠ **加卡会稀释所有人**(全体按 1/W 下降),而**没有任何测试锁着它** ——
加卡前先跑 `SYNC5_PRIOR_MODE=shelf godot --headless --path . --script res://tools/prior.gd`。

### 3 · 教学关剩余

| # | 事项 |
|---|---|
| ~~—~~ | ✅ **分区「指向」已实装(2026-08-16 查证,读实现)** —— `Widgets.TutorHint._draw_ring()` 画霓虹描边(外发光 + 主描边,不做遮罩/压暗,理由写在函数头);区域矩形在 `data/ui.json` 的 `tutor_focus`(`hud`/`hand`/`cache`/`discard` 四块),`core/db.gd` 拿 `Tutorial.regions()` 当白名单;编排器 `view/phrase.gd:337-342` 做「区域名 → 矩形」的翻译(core/ 不认识像素)。**部件注释里甚至写着「那正是第一版的毛病(光说不指)」** ⇒ 待办本身过期。**这是「待办自己过期」的第 8 例。** |
| ~~—~~ | ✅ **教学关重做完(2026-08-16)** —— 用户「非常拉跨」+ 外部调研后。**做中学 + 动作门**:每步一个 `require`,**做出来才推进**(旧版一拍过去就下一步,做没做都一样);**扑克不用教**,只教本作特有的三件(8 秒 / 缓存 / 弃牌免费),删掉「牌型越大得分越高」那句;弃牌与缓存**各给两拍**当练习位。脚本 6 步 → **7 步**,规格 = `docs/design/difficulty.md` §4.1–4.3。⚠ **等用户试玩给意见** |
| N5 | **量重开摩擦**(几次点击 / 几秒)—— 调研标为「便宜的留存杠杆」,Balatro 的零摩擦重开是核心机制之一,**一直没量过** |

### 4 · Director 接线(✅ 主通道 08-17 第一批已接;剩两个子项,都不挡 1.0)

**2026-08-17 复查(读实现)**:`core/run.gd:98` 已走
`Director.roll_run(run_index, _blind_rng, face_ranking)`(**300/300 逐字节等价已验** ——
`face_ranking` 空表时逐字节退回旧掷法,注释在 run.gd:94);`runs_total()` getter 已有,
`view/phrase.gd:225` 传真实局数。**上一版「接线零进度」的描述已过期(待办自己过期第 9 例)。**

| | 事项 | 现状 |
|---|---|---|
| ① | **脸的难度排序数据** —— `tools/price.gd` 出数喂给 `face_ranking` 入参(仪器读数不抄进 `data/`,理由在 `core/director.gd:19-25`) | ❌ **没人喂** —— 结构接好了、排序表还是空的 ⇒ 当前行为 = 旧掷法。喂它才是 Director 真正开始「导」的那一刀 |
| ~~②~~ | ~~「第几局」的持久化~~ | ✅ 第一批(`runs_total()` + `view/phrase.gd:225`) |
| ③ | 商店读 `Director.shelf_weights()` | ❌ 没接(`director.gd:107` 有定义,零调用方) |

### 5 · 求解器(P2)

> ⚠⚠ **这一节整体可信度存疑,动手前先查证。** 2026-08-15 的清理里,**7 条待办被查出早已完成**
> (替换态 · S4 · C2 · C3 · C4 · D1 · M5),其中 S4 还长期挂着「怀疑是 S3 主因」的标签 ——
> **一个错的候选主因比没有候选更糟**,它会把排查引到已经做完的地方。
> ⇒ **本节每一条动手前先花五分钟读实现**,别信描述。

| # | 事项 |
|---|---|
| S3 | **通关率低估 8.4pp,主因未定位**。⚑ 仪器已验对:`dpcheck.gd` 用 `player="perfect"` ⇒ S10 确实影响它。⚠ **重跑要预留 2 小时以上**(跑到 1h20m 仍在建表,中途不输出只能靠 CPU 判活)。2026-08-15 为让 CPU 主动杀掉,随时可重跑 |
| ~~S4~~ | ✅ **先验「剩余商店次数」—— 查证已实装**(2026-08-15)。**别再当成 S3 的候选主因。**<br>① 不假设「不再买牌」:`dp.gd` **每个段末都应用 `trans[n][b]` 的经验转移矩阵**;<br>② 按剩余商店次数建模:`trans` 按**段号 n** 索引,而商店均匀分布 ⇒ 段号即剩余次数;<br>③ 段中商店(7 次里的 4 次):后缀样本是**真实每拍分数**,段中买的牌**已在数据里**;<br>④ 构筑档在**段首**取样(`dpcheck.gd:103`「事前可知」)—— DP 状态不能条件于未来,这是对的。<br>⚑ 实装比 `solving.md §3.5` 的规格**更强**:规格说「估平均提升」,代码用的是**完整转移分布**。<br>⚠ 残留的只是「同档内买得好/买得差分不开」= **档太粗**,而那条已测过:`d=0.078 无效`。 |
| ~~S5~~ | ✅ **查证:实装了、量过了、被有意关着**(2026-08-15)。`tools/draft.gd` 132 行;`tools/bot.gd:256` 写着「求解版只在 `cfg["solve_draft"]` 打开时启用 —— **规则 bot 永久保留手写表当回归基线**」。而本文件 S9 节自己记着「**已排除**:`solve_draft` 不是解药 —— 配对花费 +0.70◆(z=1.26 不显著)」。⚠ 真正开着的问题只剩「**要不要默认打开**」,而证据说对 bot 影响不大。⚠ 原描述里「给 0 的卡 14/23 → 3/23」是 **23 张时代**的数,roster 现在 61 张 |
| ~~S8~~ | ❌ **作废** —— 它是「池子从手写改搜索」,而**池子搜索本身 2026-08-14 已裁定不做**(三个前提去掉两个,消费者 `tools/pool.gd` 从未存在)。**一项依赖已取消工作的待办,不是待办** |
| S2 | 三个状态缺失。⚑ **`first_kind` 已查证(2026-08-15,读实现)—— 真缺口,但贵且窄,建议先不做**:<br>· **算分是对的** —— 求解器把 `extra` 原样传给 `Settle`,而 `core/beat.gd:104` 已把 `first_kind` 放进去(`pair.gd` 配对差 **+0.0** 印证);<br>· **缺的是规划** —— 求解器不知道「这一拍打什么会锁死整段」,所以在 `setlist` 下会**低估**完美玩家;<br>· ⚠ **代价 ×11 状态**(项目自己的成本表),**收益只覆盖 28 张脸里的 1 张**(`setlist`,tier 2 的 1/8)⇒ 性价比差,除非 S3 的排查指到它;<br>· ⏳ `prev_kind`(`solver/dp` 有 1 处)与 `coins` **仍未查证** |
| S6 | 两个不动点的收敛性。**测不到收敛就停下报告**。⚠ **未查证** |
| S7 | 闭环重放扩到脸 / 经济。⚠ **只做了初查**:`tools/replay.gd` 已经认识脸(缓存容量随 `smallstage` 变),所以**可能已部分完成**。**未查证** |

### 6 · 其余

| # | 事项 |
|---|---|
| — | **给 1/2/3 三轮填 `tiers`**(跨轮复现 = 起承転結 A 案)。前置 = 每 (脸, 轮) 过价;⚠ 要走全量门(**4.5 小时**) |
| — | **断点续玩**(移动 Web 刚需:iOS 内存回收重载后局面全丢)。⚑ **存档层已建**(`core/save.gd`),往上加键即可;Tape 的「能重放任意时刻」判据 = 存档 schema 现成 |
| C5 | 新卡的状态成本静态检测 —— 引用了状态里没有的量 = **状态缺失**,当场报出代价 |
| M3 | `gig_clocks` 场曲线(正式局仍全平 8/8/8/8)。⚠ 动它会与 `death_spec[0]=0.05` **两处放水叠加** |
| M4 | 槽位显示「这张牌本局贡献多少分」—— 后 3 次商店 100% 是替换场景却没有数值依据 |
| M6 | 终幕专属画面(等素材) |
| — | ⚠ **仪器债**:`tools/probbook.py` **不产出曝光列**,probbook 里那节是手写的、**下次重刷会被冲掉** |
| — | ⚠⚠ **仪器债:`tools/sim.gd` 不可复现**(2026-08-15 A/A 实测,见下) |

### ⚠⚠ `tools/sim.gd` 抖 ~0.1pt —— 一个「看起来有确定性保障、实际没有」的仪器

**A/A 实测**(同一份代码连跑两次):队列 4 `31.3 → 31.2`,队列 8 `41.9 → 42.0`。
它**有显式种子**(`_rng.seed = 90000 + r` · `deck_seed = run_idx*7+1`),所以看起来是确定性的。

**⇒ 两条后果**:

1. **项目铁律「逐字节不变」不能用在 sim 上** —— 而我 2026-08-15 验收货架收口时正是这么用的,
   差点把仪器的抖动当成自己改坏了。**对 sim 量到的东西,验收只能是「在抖动带内」。**
2. 抖动 ~0.1pt **远小于**决策用的 ±3pt 漂移带 ⇒ **既往结论不受影响**,不用回头重算。

**已排掉三个候选**(别重查):① `core/`+`tools/` 里裸用全局随机的只有 `Economy.weighted_pick`
的 `else` 分支(shop 侧,sim 不走);② 无 `shuffle()` / `pick_random()`;
③ `core/run.gd:46` 的 `Deck.new()` 不带种子(走 `randomize()`),**但 `runloop.gd:91` 随即
用带种子的 Deck 替换掉它,而 `randomize()` 只给自己那个实例播种、不消耗共享流** ⇒ 惰性。

⚠ **要不要修取决于你要它做什么**:当判据(±3pt 带)它够用;
当**回归基线**(逐字节对照)它不够 —— 而收口/重构的验收恰恰需要后者。

---

## ⚑ 内容的模型成本表(加卡前先查这张)

| 需要的记忆 | 例子 | 状态代价 |
|---|---|---|
| **无** —— 只看这手牌 | 牌型倍率 · 加分项 · 规则牌 | **0** |
| **上一拍** | `encore` · `norepeat` · `rerun` | ×11 |
| **本段首拍** | `setlist` | ×11 |
| **本段累积分** | 缺口 `g` | **0**(已在状态里) |
| **本局累积计数** | 成长牌 | ×25,**每张一维** |
| **本局的集合** | 「打过 5 种牌型 → ×2」 | **×1024** |
| **有序历史** | 「按打出顺序…」 | **爆炸** |

⚠ **这不是「贵就不做」**,是把代价摆出来让设计者判断。有些代价值得付。
**区别在于:「这卡测出来 0 分所以改它」是迁就模型(禁止);「这卡要 1024 倍状态,你要不要付」是定价(应该)。**

---

## 明确**不做**的

| 事项 | 为什么 |
|---|---|
| **重算目标分表** | 它是**占位**,是整套重新设计的产物,不是一次算术 |
| **玻璃卡光影** | 用户 2026-08-06 中止。要动先看 `LESSONS.md` 的根因 |
| 写实化 / 换美术场景 | 用户已全部否掉并要求回滚 |
| skip-blind / Tag 奖励 | 用户拍板明确不做 |
| 参数化训练路线 | 已作废 —— 上界不需要「学」,只需要「解」 |
| 雨的效果 | 用户明确不喜欢(局内舞台背景;失败屏的雨是设计稿钦定的,别误删) |
| **池子搜索 / `tools/pool.gd`** | 三个前提去掉两个,第三个从来没有过;**消费者从未存在**。不是「还没做」,是「不做了」 |
| **用价格压超模卡** | S9 裁定**金币边际近零**(买不起占 0.1%、局末余额 34.7◆)—— 弱杠杆,且绕开 SOP 就是特例累积 |
| **catch-up meta**(挣扎的玩家获得加速) | 与「DDA 不可见」冲突。要做也得**看起来像奖励,不是像同情** |
| **防沉迷 SDK** | 2026-08-17 用户拍板不做:TapTap 只是**给朋友试玩**的渠道,正式发行走**美国**——防沉迷是国内正式上线的门槛,试玩版用不上 |
