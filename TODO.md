# Sync5 · 待办

> 现状看 [STATUS.md](STATUS.md) · 经验看 [LESSONS.md](LESSONS.md)
> **最后更新:2026-08-12**

---

## 当前优先级(2026-08-12,试玩迭代期)

**大顺序不变(重构✅→内容设计→求解器),内容设计进入「用户试玩 ↔ 数值迭代」循环,
发行线并行。**

1. **试玩迭代**(用户玩,Tape 说话):
   - 观察点:回响真人触发率(仪器修正后读 33%@n15,混着修正不算激励生效,继续攒)·
     锁定体感 · 阶梯 ×13 · 彩虹 p 40%@n15(n≥30 坐实则 180 要下调)·
     **规则牌 12 局零购买** —— ✅ **2026-08-14 先验层结案了一半**:
     「稀有度深」**不成立**(近道/四指在 uncommon 档,**12 局有 95.0% 会见到**,
     `prior.md` §5.7 解析读数)⇒ **玩家看见了却不买,问题是「收益不可读」**。
     ⚑ 不可读的确切内容也有了:近道把顺子 **8.89% → 27.5%(3.1 倍)**,
     而卡面只写 `Straights may skip one rank`。**对症措施是把 Δp 显示出来,不是降稀有度**;
   - ✅ **v3 已落地**(2026-08-12,`design/numbers.md` §8):8 张动刀
     (开场/复读/贵宾/低音谱 ↑,铁粉/暖冷色/黑胶 ↓),4 张带内不动,
     coins 族暂缓、规则牌封锁;过门全绿(948/0 · kit 9/9 · sim 漂移 ≤±0.5pt)。
     **等用户试玩反馈回炉**;
   - ✅ **流派批第一波已落地**(2026-08-12 夜,`design/archetypes.md` §5):
     增 8(族内件对唱/二重唱/三和弦/三重 + 后台/替补/包厢 + 修剪)、
     删 2(快闪/伴唱 —— 结构复审就此结案,**排练保留**至 trio 回池)、
     改 2(四指/双色调降罕见);过门读数见 STATUS。
     **新观察点**:① 后台 bot 尺度 +46/拍 是本批最强,防铁粉同形(持币恒触发);
     ② 规则牌曝光三措施落了两条(降稀有度+修剪实装),下轮 Tape 看购买数是否 >0;
     ③ 族内件的真人含对/含三条率进账本后按 SOP 复价;
   - ✅ **流派批二波已落地**(同日深夜):shelf 三件套(联票/赞助/点唱机)+
     新 proof 通路 `shop`(商店行为臂,零基线证物;第一版证物死于钉槽混杂,
     教训记 jokers.md)。概率线曝光三措施全落地;外部审查 **V2 就此结案**
     (acquire/shelf 双白名单)。
   - **流派批延期项(别偷跑,各有前置)**:重播(结算链手术)· 罢演/保镖(盲注审
     + 自毁钩子 + 生死判定手术)· 红毯/暗场(kit 风险同 wrecker:规则 bot 不追色)·
     化妆间(交互审)· 地下室/堆料(growth 配额二选一)· FIVE_KIND(远期);
   - ✅ **8 条盲注设计裁决已落定**(2026-08-13 用户:「同意你的判断」):五条待拍板
     项按审查意见结案(退池维持/红灯路线版/延音不回池理由更正/聚光第六张版/
     **曲目硬门→税**);曲目手术已实装(`Run.variety_mult` 单一真相,悲观实时,
     缺一种 ×1.25;顺手修平「runloop 判生死没查种数」的半个模型分叉),
     tests 1004/0 · 单脸门 ✓ · sim 漂移 ≤0.1pt;
   - 哪族数值再硌手,按 `design/numbers.md` 六步 SOP 走(bonus_pct/chips/coins 未过刀)。
2. **发行线**:TapTap 路线已研究完 → **[design/taptap.md](design/taptap.md)**(2026-08-12):
   离线+无内购走「正式上线(试玩版)」,**免版号/免软著/免 ICP** 成立;
   硬门槛 = **防沉迷 SDK**(TDS 合规认证,Godot 无官方支持要包 Android 插件,2-4 天)
   + **隐私政策链接必填**(政策页 + 首启弹窗)。
   - ⚑ **等用户拍板三件事**:中文厂商名 · 包名(定了不能改) · 隐私政策托管处;
   - 顺序:入驻(1-3 工作日)→ 预约页先开(不用包体/防沉迷)→ ~~keystore/导出预设/出包~~
     ✅(2026-08-12)→ **真机/模拟器验**(模拟器未装,或用户真机侧载)→
     防沉迷插件+接入视频 → 物料终版(实机截图≥50%、录屏 15s+ 且前 5 秒核心玩法)→
     提审试玩版(5-7 天);全链约 2-3 周。
   - ✅ **Android 裸包已通**(2026-08-12):`build/sync5-dev.apk` 115MB,
     arm64-v8a、minSdk 24 / targetSdk 35、**零权限**(隐私检测友好)、竖屏。
     排除项与 Web 同一份(原画不进包)。工具链修了四处:editor_settings 的
     android_sdk_path 指错、java_sdk_path 为空、project.godot 缺 `handheld/orientation=1`
     (Android 默认横屏)、缺 `import_etc2_astc=true`(不开则 can_export 空错误,
     Web 预设注释里那个坑的 Android 版)。
   - ⚠ 现在是**调试签名 + 占位包名 `com.sync5.dev`**:上传 TapTap 前要
     ① 用户拍板正式包名(传包后不可改)② 生成**发布 keystore**(密码用户自持,
     丢了永远不能更新,必须备份)。
   Web 已做 512/768 瘦图止血(120MB);再瘦走 WebP 供 itch.io 外网试玩。
   - **断点续玩**(移动 Web 刚需):iOS 内存回收重载后局面全丢 —— 拍/段界把
     Run+Phrase 状态存 user://(Web 端落 IndexedDB,天然持久),启动检测到残局
     问"继续上一局?"。Tape 的事实完整性判据(能重放任意时刻)= 存档 schema 现成。
3. **⚑ 把「真人反事实率」工具化**(新,2026-08-13 手算过一次就值回票价):
   现在是一次性脚本 —— 扫合格 Tape,回答「**假如持有这张卡,真人会触发几次**」。
   它和 probbook 不同:probbook 只能算**已进池且被买到**的卡,这个能算**还没做的卡**,
   所以它是**新卡该不该做 / 仪器债该不该还**的前置。
   一次扫描挡掉了一件白干的仪器工程(三重唱真人 0/168)。
   做法:`tools/counterfact.py`,输入 = 谓词名,输出 = 真人触发率 + 分母 + 样本量。
   ⚠ 只对「条件能从 Tape 事实字段重建」的谓词有效,重建不了的要显式报「不可测」。
4. **内容余量**:杂项 3 张。⚑ **两张的 ⚠ 已做完算术,结论都和原来记的不一样**
   (完整推导 = [`design/jokers.md`](design/jokers.md) 杂项裁决节,**建议待你拍板**):
   - **渐强** —— 藏分**不成立**(成本 ~350 vs 收益 ~135,差 2.6 倍;350 是定价硬上限)。
     真问题是**条件与玩家决策完全无关**(「比上一拍高」= 随机序列的上升沿,约 50%,
     与玩家水平无关)→ **一张伪装成条件卡的无条件卡**,决策层等价于霓虹灯牌但体感更差。
     ⚠ 它没有安全形状:改成「本段逐拍递增」决策密度就上来了,但**藏分那时是真的**。
     **我的建议:不做。**
   - **透牌** —— 不是「belief 仪器未建」,是**认错了通路**:它没有 effects,
     属 solver 族(证物 `*score`,同修剪)。欠的是**求解器弃牌枚举的一个分支**
     (用实际补牌而非分布期望)。**建议:可以做,并进 S5**(与「求解器一拍只弃 ≤3 张」
     那条结构盲区是同一个引擎的两处扩张,一起做比分两次便宜)。
   - **加签** —— 无疑点,纯排期。
   - ✅ **子波 3 + M5 已落地**(2026-08-13):商店成长族 3 张(淘碟/收藏家/转型)
     + 第四种证物 `counter`(零基线计数器终值;既有四条通路一条都量不到它们);
     收藏家按 SOP 加 cap 30(实测是无脑卡)。
     **M5 查下去发现不是反馈缺失,是「提前收工」这个玩家动作从来没被实现过** ——
     钩子在、三张卡等着它、bot 会用它,而玩家没有任何办法触发。交互 = **点唱片**。
   - ✅ **子波 2 已落地**(计时族 3 张 + bot 时间信念数据化 + 早弃专属窗口)。
   - ✅ **子波 1 已落地**(2026-08-13):入池 6(定格/静物/串场/让位/分成/穷开心),
     **2 张挂仪器债**(断舍离欠 bot 弃牌策略块 · 打包欠 kit 构筑臂);
     经过与三个仪器副产品记 `design/jokers.md`。
   - ✅ **gate 增量模式已落地**(2026-08-13):`./tools/gate.sh --changed` 从 git diff
     认出改动的卡/脸,只验它们;**碰到机制文件(core/** 、bot/solver/runloop/draft、
     run/economy/sim.json)自动升级为全量**并说明理由 —— 判据是「改内容就增量,
     改机制就全量」。全局回归(流程/打点/重放/尺子 ~190s)增量也照跑。
     ⚠ 增量会打印跳过了多少张(no silent caps),且**提交/发布前仍要跑一次全量**。
     实测耗时分布(修前 1818s):**脸 944s(52%)/ 卡 611s(34%)/ 尺子 168s** ——
     ⚠ 我原本以为瓶颈只在 kit,量完才知道脸的门才是最大那块。
     ⚠ **增量的实际省时还没在真实场景量到**(当前工作区碰了机制文件,一律走全量);
     逻辑各部分已分别验过(多 id 过滤实测 / id 提取三场景 / 机制升级端到端)。
   - ✅ **仪器债第一批已还**(2026-08-13):拆迁**回池**(+1286 z=12.1);
     还债时挖出 **`beat_budget.discards` 是个没量过的占位值** —— 真人实测一拍
     **最多弃 4 张**(「均值 2.30」是只算有弃牌的拍的**条件均值**,无条件是 1.11 ——
     校准依据是那个**上界 4**),而配置是 2,**44% 的真实行为模型不允许**。校准到 4。
     三个连带:pair.gd 把它抄成字面量(配对差假性 −16)· 回响/复读的旧「非零」被证明
     是噪声(新增 SOLVER_BLIND 第 ⑤ 类)· 赶场进 weak_upper_bound(floor 离散化的假象)。
     ⚠ **反推:此前所有以 bot 为尺子的读数系统性偏保守,目标分跟着偏松** —— 带进重算。
   - **仪器债剩两笔,都已用真人 Tape 确认「值得还」**(反事实率,推导见 jokers.md):
     **打包** ← kit **构筑臂**(真人 **25%** 的段达到 2× 目标 —— 每 4 段触发 1 次,活卡);
     **断舍离** ← bot 的**大批量弃牌**策略(真人 **8.6%** 一次弃 4 张;
     ⚠ 我已为它调过三次 bot,**再调就是为门绿改仪器** —— 该动的是 `_best_plan`
     的策略空间,不是参数)。
   - ❌ **三重唱结案,不回池** —— 两个**独立**的死因:真人 **0/168 拍**
     (缓存满 3 张的拍有 150 个,一次三同点都没有;随机概率 0.24%),
     **加上** bot 侧重量得 **z=1.81 且量级 4.1%**
     (旧读数 +376/5.3% 是**旧基线**下的;手速预算 2→4 抬高基准、摊薄量级)。
     量级那条 **加多少样本都过不去**(量级 = 效应/基准, 与 n 无关)——
     ⚠ **两条死因互相独立**,所以「等真人数据」这个出口也关死了 ——
     真人那条**就是**真人数据。要它回池只有一条路:**换一个真的会发生的条件**
     (缓存三张同点数在真人手里 168 拍发生 0 次,而缓存满 3 张的拍有 150 个)。
     ⚠ **不许为了让它过门而调高数额**。
   - 流派批 8 张卡素材:7 张已接线入库 ✅,**只欠点唱机(S-60)一张图**。
5. **⭐ 生成与求解这条大项(用户既定)**。地基 = [`design/prior.md`](design/prior.md) +
   [`design/generating.md`](design/generating.md) + [`design/solving.md`](design/solving.md)。

   ### ✅ **先验层已落地**(2026-08-14,用户提出「区分先验生成和后验生成」当天)

   **诊断**:先验的反面不是后验,是**拍脑袋**。账本 `p̂ 设计` 那一列是**散文不是算式**
   (`0.35(重复上一拍:需刻意保型)`),一半的卡还是空的 —— 所以信任全压到 `p_bot` 上。
   ⚑ **这是「可执行性替我排优先级」那个老毛病的第二次**:蒙特卡洛能跑 → 就跑蒙特卡洛。

   产物 = `tools/prior.gd`(判定走 `core/pattern.gd`、谓词走 `core/fx.gd`,**零重写**;
   闭式自检 + 快慢路径逐次对账常驻)。N=20 万实测:

   - **牌型倍率表的依据里策略占多少,而且不均**:策略把同花抬 **58%**、顺子只抬 **24%**(差 2.4 倍);
   - **⚑ 8 张规则牌的定价封锁解除**(那张表本来因"真人持有 0 拍"全空,而 Δp **零数据需求**):
     近道 **+18.57pp(3.1×)** · 四指 +17.61pp(3.0×) · **双色调 +59.47pp(9.8×)**;
     另有两个没量过的结构事实 —— **挤出**(双色调的 Δ顺子族 **−6.10pp**,同花吃掉顺子)与
     **超可加**(近道+四指 +48.64pp > 各自之和 36.18pp,**多 34%**);
   - **谓词 `p̂` 补齐 23 张**;**30 张先验算不出来**,缺的字段全是行为与状态
     (`discards`/`coins`/`counter`/`prev_kind`/`early_*`)—— **分界线画对了**;
   - **⚑ 第二把尺子已经开始干活**:灯牌/替补/贵宾/暖色/冷色 **5 张两条独立路径对上**,
     同时抓到 **3 处不一致**(见下)。

   **⚑ 为什么先验能解决"数据永远不够"**:
   `p_人 = p̂(可算) × 策略折扣 × 执行折扣 × 认知折扣`,而**三个折扣是跨卡共享的**。
   ⇒ 要估的参数从 `O(卡数)` 降到 `O(1)`,同一批 12 局的分母从"每卡 n=3"变成"全体 n=168 拍"。
   ⚠ 而且**先验是规范性的**:后验对齐"玩家现在什么样",设计要回答"希望玩家去到哪" ——
   没有先验,偏差就没有参照物,只能跟着现实走,**那是 DDA 不是设计**。

   **✅ 首批裁决已落地(2026-08-14 用户拍板当日)**:
   **双色调 uncommon→rare** · **阶梯 ×13→×11** · **单色 ×10→×12**(顺子/同花**互换高低**)·
   **定价口径写死为组合口径**(`CLAUDE.md` Target 两层原则新增 ③ + `numbers.md` §1 修订)。
   过门:tests **1100/0** · kit **3/3** · sim 三态分解**倍率效应零外溢**。(⚠ 该数是当日更早的读数;S10 之后本树基线是 **1096**,难度批后 **1104**)
   - ⚑ **意外收获**:三态分解显示双色调变稀有让 **twin / wolfpivot 这两个不吃同花的队列各掉 2.2–2.4pt**
     ⇒ **它不是同花流派的卡,是所有流派的通用首选** —— 比 9.8× 更强的超模证据;
   - ⚠ 连带:**同花流派净 −0.6pt**(倍率 +1.7 被"买不到双色调"的 −2.3 吃掉)。
     按铁律**不补偿**(在噪声内,且两个改动各自独立正确),但**列入真人试玩观察点**;
   - ⚠ 撤销了「规则牌曝光」三措施的一半(twotone 降罕见),**近道/四指维持 uncommon** ——
     判据:要救的是**顺子线**(组合 8.89% 而真人只打出 1.9%),不是同花线(6.79% vs 7.4%,没缺口)。

   **挂查三条(先验层自己抓出来的)**:
   - 低声部/低音谱 **先验 36.21% vs 账本 74%,差一倍**(n=569,不是样本问题);
   - **"缓存档是下界"这个标注我写反了** —— 排练先验 10.48% 而实测 6%。
     根因:**缓存里装的是打不出去的剩牌,不是选留的好牌**。方向是谓词相关的;
   - 全员:**组合 21.77% · 规则 bot 12% · 完美玩家 26.8%** ——
     规则 bot **连"零策略最优切牌"都达不到**,与「bot 选切法约等于随机」独立印证。
     ⇒ 先验层顺带给了能力谱一个**不需要任何玩家参数的锚点**。

   ### ✅ **弃牌一步转移已落地**(同日,`SYNC5_PRIOR_MODE=discard`)

   **「用时间买频率」第一次有了兑换率**(400 手 × 12 组补牌,配对):
   弃牌预算 `b` = 0→4,期望分 **182.9 → 333.2(+82.2%)** ——
   ⚑ **时间不是"影响因素之一",是最大的那一个**(对照:整张 Target 倍率表的族间极差才 2 倍)。
   边际 +25.4 / +26.0 / +19.0 / +11.8 pp,**第四次弃牌仍值 11.8pp**
   ⇒ `beat_budget.discards` 从 2 校准到 4 拿回的东西不小。
   - ⚠ **b=4 时顺子族反而回落**(15.26%→13.47%)而同花继续升 ⇒ **弃牌能力越强越主动放弃顺子**。
     ⚠⚠ 这和「真人顺子 1.9%」是**两个不同机制**:真人**打不出来**,高手**主动不打**。别混成一个结论;
   - 高牌 6.00%→0.72%:弃牌抬的主要是**地板**不是天花板。

   ### ✅ **S10 引擎手术已落地**(2026-08-14,用户「开始做吧」)

   `Solver.best_discard` 的枚举 `_subsets(base.keep)` → **全部可见 8 张**,返回值语义随之
   从「keep 下标」改为「visible 下标」(两个调用点已同步:`bot.gd` / `pair.gd`)。
   **验证**:tests **1096/0** · `pair.gd` 配对差 **+0.0 / −0.1(z=−1.20 不显著)** ——
   「求解器 = 游戏代码」契约仍成立。

   ⚑⚑ **它连带解掉了三条挂着的债,而且是测试自己喊出来的**:
   - **回响 / 复读** 退出 `SOLVER_BLIND` —— 反向保护(声明了却量到 ⇒ 报警)当场抓到。
     ⚠⚠ **第 ⑤ 类「完美玩家打法族」整类作废**:旧理由说「完美玩家追最优 ⇒ 牌型每拍在变 ⇒
     `same_as_prev` 恒假」,**真相是保持牌型靠主动弃牌塑形,而窄枚举塑不了形**。
     **这一类把「求解器做不到」写成了「最优玩家不会这么做」—— 差着一整个枚举空间。**
   - **断舍离**的「一次弃 4 张」进了动作空间(`t_draft.gd` 那句「扩求解器的弃牌集是 S5 的活」已过期)。
   - ⚠ **`best_beat` 那份死代码**:它本来就是全枚举,但**没有任何调用方**,
     且每个子集**独立抽补牌**(违反共用随机数铁律)。两份各对一半 —— 现在正确的枚举空间
     与正确的随机数合到了 `best_discard` 一份里。`best_beat` 待清理(未动)。

   ⚠ **代价与未决**:
   - `tests` **<300s → 716s**(约 2.4 倍),`gate.sh` 全量会到 ~35 分钟 —— 日常走 `--changed`;
   - **候选剪枝提速已被实测否掉**(`prior.md` §5.6d):M=6 只提速 2.9× 就丢 **3.26%**,
     而整个扩枚举才拿回 9.3%。根因:**「边际价值低的牌该弃」这个启发式是错的** ——
     弃牌的价值在「腾空间搏更大牌型」(拆对子追同花),不在「扔没用的牌」;
   - ⚠ **选择偏差未量**:候选 7 → 162 而 `lam_samples` 仍是 2,
     `generating.md §6.1` 警告过「搜得越多,选出的越可能是运气好的」。
     共用随机数缓解了一部分,但**没有量过**。修法是两阶段(粗筛 → top-k 大样本重估);
   - **S3 诊断待重跑** —— 这是立项时的主要动机。

   ⚑⚑ **哪把尺子会动、哪把不会(A/B 实测,别搞错)**:
   `best_discard` **只在 `_play_perfect` 路径上**。而 `data/sim.json` 的队列**全部是 `adaptive`**
   (规则 bot,走手写弃牌),所以:

   | 仪器 | bot | 受影响? |
   |---|---|---|
   | `sim.gd` 九个队列 | adaptive | ❌ A/B 实测漂移 **≤0.1pt** —— **这是正确读数,不是 bug** |
   | `curve.gd` 默认 → `sim.json bot_targets`(影子表) | adaptive | ❌ |
   | **`curve.gd SYNC5_CURVE_BOT=perfect` → `run.json section_targets`** | 求解器 | ⚑ **会动,而那是真人面对的表** |
   | **游戏本身** | — | ✅ **完全不受影响**(`core/`/`view/` 不加载 `tools/solver.gd`,可直接试玩) |

   ⚠ 所以 **`section_targets` 因这次改动更过期了**:求解器变强 ⇒ 重算会让目标分**更高(更难)**。
   但按既定原则**现在不重算**(那张表是占位,是整套重新设计的产物)。
   ⚑ 这也给 CLAUDE.md 那条「目标分是完美玩家尺度、对真人偏难,**就让它偏着**」
   加了一档:**偏的幅度变大了**,回头重算时要带上这一笔。
   ⚠⚠ **差点栽在这里**:第一版 A/B 我用 sim 跑,看到 0.0pt 差点报「零影响」——
   而真相是**那个仪器结构上就量不到它**。memory 记着「选错仪器会把结论量成反号」,这次是第 N 次。

   ### ~~⚑⚑ 新仪器债(本轮量出来的,**S3 的强候选主因**)~~ ✅ 已做,见上

   **`Solver.best_discard` 的枚举是 `_subsets(base.keep.size())`** ——
   它只能从「最优切法留在缓存的那 3 张」里选弃牌,**结构上弃不了 4 张,也弃不了要打的 5 张**。
   先验层不受此限,配对一比:**+26.6 ±1.7 · z=15.5 · 量级 +9.3%** —— **两条判据都过**。

   ⚠ 这与 S3(通关率低估 8.4pp)方向一致、量级吻合,但 **9.3% 是单拍分数、8.4pp 是通关率,不能直接比**,
   **所以不断言它是主因**。**验证路径**:把枚举扩到全部 8 张 → 重跑 S3 诊断 → 看偏差是否收敛。
   ⚑ 这与 **P2-S5 的「透牌」那张卡是同一处引擎扩张**(TODO 第 4 条早就写着"一起做比分两次便宜")——
   现在有了它值多少分的数,**这一项的优先级应当提到 S3 前面**。

   ### ✅ **货架曝光已落地**(同日,`SYNC5_PRIOR_MODE=shelf`,**解析零采样**)

   ⚠⚠ **抓到一处文档-实现分叉,实际比文档稀 2.3 倍**:`jokers_atlas.md §0` 的稀释数学
   假设「先掷档再档内均分」,而实现是「**每卡带权重一起不放回抽**」。
   实际:**rare 档占比 3.9%(非 5%),一局期望只出 0.82 张 —— 平均连一张稀有卡都见不到**;
   特定稀有卡一局 **4.8%**(文档手算 11.0%)。已在 atlas 就地更正。
   ⚠ common/uncommon 的实际占比恰好≈名义权重**纯属两档张数相同的巧合**,
   改任一档张数三档比例全变,**而没有任何测试锁着它** —— 值得补一条断言。
   - **双色调升 rare 的代价量化**:一局 22.1% → **4.8%**,12 局 95% → 44.9%;
   - ⚠ **新仪器债**:`view/shop.gd::_weighted_pick` 的注释写着「不许各写一份」,
     **而它本来就是两份**(shop + `tools/bot.gd:522`)——
     和 `beat.gd`「漏步=崩」、`db.gd`「直接红」同一个毛病:**注释承诺了不存在的机制**。

   **下一步(先验层)**:① 构筑路径那 9 个点(买牌决策依赖策略,**只有货架那一层是纯先验且已做完**);
   ② 倒挂率(存在的最高牌型 vs 分数 argmax 的牌型);
   ③ L-B 精确枚举(优先级低,L-C 误差已小于任何设计判据的分辨率)。

   ### 以下是后验侧的三个卡点(未变)

   ### ⚑⚑ 2026-08-14 用户的两条设计输入(**这两条改变了这条线的下一步**)

   **① 「关卡间难度设置的逻辑不清晰,否则就自动生成了」** —— 用户举例:
   「前三关教学关简单点,后面难一关,又简单两关,又难一关」。
   ⚑ **这个判据本身是本条最有价值的东西**:*逻辑清晰 ⇔ 能自动生成*。拿它一量,得到一个反差:

   | 层 | 有逻辑吗 | 有实现吗 |
   |---|---|---|
   | **局内 4 段**(`death_spec` 管) | ❌ **没有** —— 四个手拍的数,没有任何东西回答「为什么 S3 是 0.45」 | ✅ |
   | **跨局序列**(用户例子说的那层) | ✅ **有** —— `levels.md` 七状态 Director + 转移条件 + 权限边界 | ❌ 一行代码都没有(M2) |

   **有逻辑的那层没实现,实现了的那层没逻辑 —— 这就是「不清晰」的确切来源:两层错配。**
   ⚠ 用户的例子与 Director 的状态序列**逐项对得上**(教学=Establish · 难一关=Pressure ·
   简单两关=Recovery+Experiment · 又难=Payoff)—— 他是独立重新发明了它,说明那个设计站得住。
   ⚠ 已定的铁律别忘:**Director 不许调目标分**(玩家看得见),只能调**脸的排布与货架** ——
   所以「自动生成关卡」≠「自动调 `death_spec`」。

   **② 「第一个 section 死亡率可以很低,拉长用户时间」**(留存导向,与目标函数一致)。
   ⚠ 与数据一致:S1 现在只要求零构筑上限的 46%(裸打就够)。**待落进新的 `death_spec`。**

   ### ⚠ 顺手查到:两张目标分表的口径对不上

   | 段 | `run.json section_targets`(应 perfect) | `sim.json bot_targets`(应 adaptive 影子表) | 比值 |
   |---|---:|---:|---:|
   | S1 | 921 | 496 | 1.86× |
   | S2 | 2909 | 995 | **2.92×** |
   | S3 | 4650 | 4004 | **1.16×** |
   | S4 | 5626 | 4741 | 1.19× |

   `curve.gd` 注释说两表该「差一个量级」,`solving.md §3.4` 实测完美玩家是规则 bot 的 **1.42–1.56 倍**
   —— 而实际比值在 **1.16 到 2.92** 之间乱跳,**S3/S4 甚至低于实测比值**。
   ⇒ **两张表不是同一次生成的**;它们本来就是占位,现在又多一条「口径也对不上」。
   ⚑ 连带:五项分解里 **① 数值压力的分母不可信**,而 **②③④⑤ 只依赖分数分布,不受影响**。

   **⚠ 先纠正一个词**:「生成关卡的能力」比这个词小得多 —— **现在能生成的是一个数**。
   `tools/curve.gd` 不死局录 100 局分 → 查分位数 → **反解目标分**(反解免费:
   贪心不看缺口 → 分数分布与目标分无关)。配置的其余维度(脸的排布/经济/货架)**全手写**,
   ⚠ **而那是设计,不是待自动化的技术债**(用户 2026-08-08 明确;generating.md §9
   有一张「别再当成缺口」的表,每条都是我误判过一次的)。

   **分工已写死**(generating.md §7):设计者定脸的排布 → 难的**性质** ·
   生成器定目标分 → 难的**量** · **生成器报告实测形状 → 让设计者看见自己的设计
   产生了什么**。⚑ 文档自己说**第三行才是最有用的地方,而第三行没做**。

   **三个卡点**:
   ① 两张目标分表(`run.json section_targets` / `sim.json bot_targets`)是**占位**,
      要跟着新目标函数重新设计 —— **别当成待修的 bug**;
   ② 求解器通关率**低估 8.4pp**(S3,主因未定位)→ **相对排序可信,绝对难度不可信**;
   ③ θ(真人参数)零数据 → **`τ` 手速这一维 2026-08-14 已量到**(见下),
      `λ` 前瞻 / `ε` 分布形状 / `d` 深度仍是零。

   ### ✅ **五项分解表已建并跑出首批读数**(2026-08-14,`tools/decomp.gd`,n=20)

   `solving.md §II-1b` 有完整表与推导。**三条结论**:

   1. **③ 技巧惩罚不参与阶梯,而且不是样本问题** —— `S4−S1 = +450 ±1491`(z=0.3),
      达到判据需 **n≈2000 局** ⇒ **效应本身太小**。但它绝对值在四段都有 1100–2700,
      所以「会玩一直有回报」,**只是回报不随段号变大**。
   2. **② 上界压缩非单调**:S1 0.90 → **S3 0.683** → S4 1.014 ——
      **S3 的脸压上界最狠,S4 的脸几乎不压**。⚠ 我的检验只比首尾,**漏掉了中间形状**,待补相邻对比较。
   3. **⑤ 运气暴露单调上升 0.31 → 0.84(近 3 倍)** —— **越到后面越像抽签**(无 se,只读趋势)。

   **⇒ 初步答案(待用户审)**:阶梯主要由 **①(数值压力)+ ⑤(运气暴露)**承担,②③④ 基本不参与。
   **接近「同一种难收四次费」,而且第二种"难"是运气变大,不是技巧要求变高。**

   ⚠⚠ **第一版判读列是错的,已修**:我用 `(max−min)/mean` 判「在动」,**没对照噪声** ——
   违反「没有标准误的差值等于没有结论」;而且**问错了问题**(该检验段间差,不是每段的值)。

   ~~**⚑ 我建议的下一步 = 五项分解表(成本是汇总,不是研发)**~~:
   solving.md 把难度拆成五项并写着「**五项的仪器全都已经有了,而且都在跑 ——
   只是从来没有被并排放在一张表上**」:① 数值压力(算术)· ② 上界压缩(`gate.gd` 配对臂)
   · ③ 技巧惩罚(`formal.gd` 的 Δ)· ④ 信息惩罚(`blind.gd` 的 ORACLE A/B)
   · ⑤ 运气暴露(`formal.gd` 的 V)。
   **做出来能一眼回答**:现在四段的阶梯是不是**同一种难收了四次费**(四级全靠 ① 涨、
   ②③④ 一条平线)。
   ⚠⚠ **但 solving.md 自己标注:五项与「难」的关系没有任何外部锚,「这一条是本文最需要
   你审的」** → **判断权在用户,模型给形状给不了好坏。**
   ⚠ ⑤ 已实测**当前配置空间拧不动它**(跨配置标准差 0.032/均值 12%)——
   作为描述有效,**作为优化目标无效**。

   **τ 的真人实测(2026-08-14,12 局 168 拍)** —— 直接摘掉 generating.md §III-1 的「零数据」:

   | 量 | 真人 | 模型给的 |
   |---|---|---|
   | 一拍**总动作数** | **2.12** | **7**(弃 4 + 换 3)→ **模型的动作空间是真人的 3.3 倍** |
   | 交换次数 | 1.01(最大 4) | 预算 5,有效上限 3(缓存格数) |
   | 弃牌张数 | 1.11(有弃牌的拍 2.30,最大 4) | 曾是 2,08-13 校准到 4 |
   | 动作时刻(8s 拍) | 中位 **5.0s**,每拍最后一个动作中位 **5.7s** | 模型无时钟 |

   ⚠ **两个方向相反的偏差**:弃牌维模型曾**低估**真人(已修),交换维**高估**真人 ——
   **「校准后目标分偏松还是偏紧」不能从这两个数直接推,要实测**。
   确定的只有:求解器两维都不受手速限制 → 目标分是完美玩家尺度、对真人偏难。
   ⚠ **12 局撑得起拍级统计量(动作数/触发率),撑不起局级的(通关率/构筑成型率)** ——
   而发挥系数是**局级**的,所以仍要等用户「完整玩很久」。

---

---

# ⭐ 难度与节奏(2026-08-14 新立,规格 = [`design/difficulty.md`](design/difficulty.md))

**用户当天拍板四件**:① `death_spec` 形状「1 简单,2、3 逐步难,123 更陡,因为 1 容易」·
② **Director 不读 context**,是设计常量表(「真正千人千面的只有随机出来的小丑牌」)·
③ **小丑牌解锁取消**,一开始全开;**盲注逐步解锁** · ④ 「有大量只有一轮生效的盲注我不喜欢」。

### ✅ 已落地

- **`death_spec` → `[0.05, 0.28, 0.50, 0.60]`**(通关 13.7%,与旧值 13.9% 持平)。
  ⚠ **需要手工封顶的是 S4 不是 S1-S3**(纯 sigmoid 外推 S4 到 0.73-0.80,通关率崩到 6-9%;
  `∂通关率/∂d_s = −S/(1−d_s)`,末段敏感度是首段 2.4 倍);
- **脸的轮次从单值 `tier` 放成集合 `tiers`**(`core/modifier.gd` + `core/db.gd`,
  缺省 = `[tier]`,**现有 30 张脸一行没改、行为逐字节不变**)。
  ⚑ 依据是仓库里早就有的:`price.gd` 文件头写着「定价表大小 = **合法放置数** 4+5+7+6 = **22**,
  不是 12」—— **12 张脸 22 个放置,窄的是 schema 不是仪器**;
- **三处过期注释已更正**:`run.json` 的「留作池子搜索的起点」· `gates.md §6/§9` ·
  `levels.md` Director 的三节(`Inputs` / `Recent behavior model` / `Joker learning states` 作废);
- **`tools/price.gd` 的空指针已清** —— 它指着 `tools/pool.gd`(**从未存在**)和
  `faces.json` 的 `prices` 键(**白名单里没有,贴进去直接红**)。
  ⚑ 这是本项目第 **4** 次「注释承诺了一个不存在的机制」(前三:`beat.gd` 漏步=崩 ·
  `db.gd` 直接红 · `shop.gd` 不许各写一份)。

### 回归

**tests 1104 / 0**(退出码 0,无脚本错误)· `--import` 解析干净 ·
`1104 − 8 = 1096` = S10 之后的本树基线,**没有断言悄悄消失**。
⚠ **只跑了单测** —— 本批没碰 `data/faces.json` / `data/jokers.json`,`death_spec` 也不进
求解器路径,所以没跑 `gate.sh`/`kit`/`pair`/`sim`。**真正给某张脸加 `tiers` 时必须走全量。**

### ✅ 第四轮补脸(2026-08-14 用户:「tier4 可以补脸,只不过都是时间相关的就行」)

**新拍板覆盖 2026-08-07 那版「第四轮暂定只有一种脸」。** 入池 3 张,`fixed_tiers` 清空:

| id | 六秒之外 | params(**全是既有的,零新引擎代码**) |
|---|---|---|
| `overtime` 加场 | 最后 2 秒不能弃牌 | `time_penalty` + `discard_lock_last` |
| `teardown` 拆台 | 最后 2 秒不能交换 | `time_penalty` + `swap_lock_last` |
| `closing` 打烊 | 每拍只能弃一次 | `time_penalty` + `discard_actions` |

⚑ **六秒仍然始终成立,但靠的东西换了**:从「只有一张脸」变成「**每张脸都自带缩时**」。
判据 = 进第四轮的脸 `time_penalty` 必须 > 0(`tests/t_face.gd` 有断言),
理由是 `blinds.md §6` 删掉「延音」的那条:**把六秒还回去 = 破坏第四轮主机制**。

⚠⚠ **`t_face.gd` 里有两条断言因为 `fixed_tiers` 清空而双双变成空测**(一条恒真一条恒假)
—— **空测比没测更糟,它看起来还在守**。已改成直接测那个 float/int 坑本身(用真实 JSON 解析)。

### ⏳ 待做(按依赖)

1. **给脸填 `tiers`** —— 前置 = 每 (脸, 轮) 各自过价(`tools/price.gd`)。
   ⚠ 改 `tiers` 是改内容不是改机制,但它会动 sim 读数 —— 走 `gate.sh --changed`;
   ⚠ **`tiers`(跨轮复现)的落点是 1/2/3 三轮**,与「第四轮补脸」是两件事(后者已做,见下)。
2. **解锁曲线**(C 轴)—— 每轮 3 → 5 → 6 → 8 张,按累计局数。
   ⚑ **顺序判据 = 按「离本轮难度中位数的距离」由近到远** ⇒ **只加方差不加均值**,
   不用为每个解锁阶段重算目标分。排序用的数 = `price.gd` 的输出;
3. **Director**(M2)—— ⚑ **依赖已解除**:不读 context 之后它就是一张 JSON 表,
   不再需要「池子搜索」和体感量。序列表草案在 `difficulty.md §3`;
4. ⚑ **起承転結 A 案现在合法了** —— `research_pacing_retention.md §5.5` 判定
   「A · 一局一弧(一张主脸走完四段)更接近原意」,当时的阻碍正是**脸不能跨段重复**,
   而那是 `tier` 单值造成的。做 A 案时要一并落**结论 14**:
   规则不是「不许重复」,是「**重复必须有意,不能偶然**」。

### ⚠ 明确不做 / 已作废

- **小丑牌解锁** —— 用户 2026-08-14 取消(「可以一开始都有」);
- **池子搜索 / `tools/pool.gd`** —— 三个前提去掉两个,第三个从来没有过。**不是「还没做」,是「不做了」**;
- ⚠ **但曝光问题没跟着消失**:17 张 rare 每张平均 **21 局才见一次**(一局 21 个货架卡位 ×
  rare 实际占比 3.9% ÷ 17 张),学会一张卡要见 ~3 次 ⇒ **63 局 ≈ 5 小时**。
  而 08-14 刚把**双色调升 rare**(一局见到率 22.1% → 4.8%)。
  规则牌是流派**入口**不是终点装饰品 —— 这条与「让玩家学会规则牌」正面对冲。
  **剩三条路:调稀有度权重 / 缩 roster / 接受「稀有就是稀有」。走 `numbers.md` SOP,别顺手改。**

---

# ⭐ 新手引导(2026-08-14 用户点名:「第一关做好新手引导」)

⚑ **前置已拍板(2026-08-07)**:「**教学总要时间,但教学只要一次,不影响整体节奏**」
⇒ **教学单开一关,允许突破 4.9 分钟,不占用局内节奏**
(`research_pacing_retention.md` 结论 10)。

⚠ **我 2026-08-14 一度把「单独的教程关卡」写进「明确不做」,那是错的** ——
依据就在同一个 `design/` 目录里,而我先去查了一遍网络才发现。已改。

**设计草案 = [`design/difficulty.md`](design/difficulty.md) §4。** 骨架:

| # | 事项 | 备注 |
|---|---|---|
| N1 | **教学关本体** | 教**操作**(出牌 / 弃牌补牌 / 拖拽对调 / 点击多选 / 商店);正式局 S1 只教**决策**。⚠ 不判生死、不进 `curve.gd`,**不算进 13.7%**。⚑ 形状 = 起承転結 的「起」= 「安全的地方、**无惩罚**地理解机制」 |
| N2 | **拍长 12s → 10s → 8s** | ✅ **脚本层已落地**(`data/tutorial.json` 的 `seconds`)。⚑ 诊断:新手的敌人不是「不知道按钮在哪」,是**8 秒来不及想** —— 真人熟练玩家一拍最后一个动作中位已经 **5.7s / 8s**,新手只会更慢。⚠ 契约锁住了「只收不放 + 最后一拍必须等于正式拍长」 |
| N3 | **部件按「第一次真正有用」出现** | ✅ **脚本层已落地**(`unlock`,累积生效)。判据不是列清单讲一遍。⚠ 门禁挡住「有部件从没被解锁过」= 静默死锁 |
| N4 | **提示一行,不打断** | ✅ **脚本层已落地**(`command` 中文 + `signal` 英文 ≤7 词)。不暂停 / 不弹窗 / 不点确认 |

### ✅ 已落地(2026-08-14):**数据层 + 纯逻辑层**

- `data/tutorial.json`(6 步脚本,**你直接改这个文件调教学**)· `core/tutorial.gd`(`Tutorial`,
  引擎无关、不含时钟、不 import view)· `core/db.gd::validate_tutorial` 四条结构门禁 ·
  `tests/t_tutorial.gd`(**一个死数字都不抄**,全部从 DB 推导)· schema 进 `design/tech.md`。

### ⏳ 还没做的(视图侧,下一刀)

- **接线**:`view/phrase.gd` 读 `Tutorial` 决定拍长、灰掉未解锁的部件、贴提示行;
- **入口**:教学关怎么进(首页?首次启动?)—— 与下一条绑在一起;
- ⚠ **「只做一次」需要持久化** —— 一个 `user://` 标记。它和 TODO 里挂着的**断点续玩**
  是同一个存档层,**别各写一份**;
- ⚠ **改了视觉就要渲染出来自己看**(CLAUDE.md),所以视图那一刀必须带截图对账;
- N5 ⏳ **量重开摩擦**(几次点击 / 几秒)—— `research_pacing_retention.md` 结论 11
  标它「便宜的留存杠杆」,一直没量过。
| N5 | ⏳ **量重开摩擦** | 结论 11 标它「便宜的留存杠杆」,Balatro 的零摩擦重开是核心机制之一。**一直没量过** |

⚠ **待量**:12 秒够不够 —— 现有 12 局合格 Tape **全是熟练玩家(用户本人)**,**新手数据为零**。

---

## 顺序:~~重构~~ ✅ → **内容设计(下一个)** → 求解器

用户 2026-08-09 定的:「**设计牌型应该在求解器之前,而设计之前甚至可以先重构。
我喜欢项目干净可迭代。**」

**重构那一段 2026-08-09 已完成**(六步走了五步,第六步查证不成立而退役,详见 P0)。
**下一个是 P1 内容设计**,其中我判断 **C4(小丑牌的覆盖门)应当排第一** ——
脸有 `gate.sh` 守着,小丑牌**完全没有门**,而分数大头恰恰在小丑牌上(构筑值 4.2 倍)。
这正是「规则在游戏里、不在模型里」已经踩过五次的形状。

⚑ **而这个顺序有一条技术理由支持它**(2026-08-08 发现):

> **加一张新卡/新脸,可能改变求解器的状态设计。**
>
> `encore`(与上一拍同牌型)需要 `prev_kind` 维;`setlist`(定调)需要 `first_kind` 维;
> 若有一张「打过 5 种牌型 → ×2」,需要 ×2¹⁰ = 1024 倍状态。
>
> **所以先建求解器再加内容 = 内容会推翻状态设计 = 白建。**

---

# P0 · 重构(干净可迭代)

## 判据先行 —— 决定怎么切

> **找到那个被抄了 N 份的东西,把 N 份的差异收敛成几个键;
> 验收 = 每份调用方的输出逐字节不变。**

这条判据本项目成功用过两次(`Beat` 6→1、`RunLoop` 14→1),手法见 [LESSONS.md](LESSONS.md) 的对账条。

⚠ **按行数切是假重构**:`runner.gd` 1695 行切成 5 个 340 行,抄写量不变、耦合不变,
只是多了 5 个文件。**每一刀都要能回答「收口的是哪一份真相」。**

## 四层真相 —— **2026-08-09 已全部收口**

| 层 | 唯一真相在哪 | 收口于 |
|---|---|---|
| 一拍的转移 | `core/beat.gd` | 2026-08-07(6→1) |
| 一局的循环 | `tools/runloop.gd` | 2026-08-08(14→1) |
| 一次实验的骨架 | `tools/probe.gd` + `tools/shot.gd` | **2026-08-09**(32 处 `quit()` / 样板 → 1) |
| 一份统计 | `tools/stat.gd` | **2026-08-09**(`_mean` 11 · `_var` 8 · `_paired` 6 → 1) |

⚑ R1 的形状**不是「38 个文件合并成 20 个」**,而是**补上缺的这两层** ——
探针文件数基本不变(38 → 41),它们本来就是**不同的实验**,各占一个文件是对的。

## ✅ 本轮六步 —— **已完成**(2026-08-09,旧 id R1-R5 已并入)

| # | 旧 id | 做了什么 | 结果 |
|---|---|---|---|
| ① | R1a | `tools/stat.gd`(`Stat`) | 25 处抄写 → 1 份;`tests/t_stat.gd` **42 条 bit-identical 断言**逐位相等 |
| ② | R5 | `faces.json` 散文 → `design/blinds.md` §7 + `db.gd` 拒绝散文键 | **16728 → 3363 B**(-80%) |
| ③ | R4 | ~~金币单一宿主~~ | ❌ **查证不成立,退役**(见下) |
| ④ | R2 | `tests/runner.gd` 按契约归属切域 | **1695 → 59 行** + 19 个 `t_*.gd` |
| ⑤ | R1b | `tools/probe.gd`(`Probe`)+ `tools/shot.gd`(`Shot`) | 14 个数值探针 `extends Probe`,11 个截图探针调 `Shot`;`tools/` 6935 → 6777 行 |
| ⑥ | R3 | `view/phrase.gd` 搬出三块 | **960 → 727 行** + `StageLayout` 182 / `ReplaceFlow` 82 / `StageFeedback` 55 |

**回归**:单元测试 **634 passed / 0 failed** · `gate.sh` **全过(520s)** · `pair.gd` 三关配对差 **+0.0** ·
`flow_probe` 0 bugs · 截图目视对账(局内 / 商店替换态 / 首页)。

⚑ **新探针从此不许再抄地基**:`extends Probe` · 统计走 `Stat` · 截图走 `Shot` · 一局的循环走 `RunLoop` ·
一拍的转移走 `Beat`。**五层都有唯一真相了,再抄就是第六份。**

⚠ **①的仪器选择是有意的**:`Stat` 是**纯函数替换**,一条 bit-identical 单测就能证明它不可能
改变任何读数 —— 不必为它重跑 `addit`(10 min)和 `gate`(8 min)。**用最便宜的仪器验最贵的事**,
全套 `gate.sh` 只在最后跑一次。

## ❌ ③(R4「经济职责散落」)—— 2026-08-09 查证**不成立,退役**

**别再提这一项。** 两条依据都实测过了:

**① 「金币动作分散在多处」这个说法不成立。** 写点只有 6 处,而且已经收口:
`core/beat.gd`(入场费 + 结算收入)· `core/phrase.gd`(弃牌,恒 0)· `core/run.gd`(初始化)·
`view/phrase.gd` **4 处**(工资 / 买 / 刷新 / 替换)。
铁律「经济动作只发生在编排器」管的是 **view 侧**,而 view 侧 4 处**全部在编排器里** —— 已经满足。

**② 「双宿主」不是事故,是有意的接缝。** `Phrase.new(deck, cache, coins)` 有 **27 个调用点,
其中绝大多数根本没有 `Run`**(`attrib`/`blindtime`/`pair`×3/`lam`/`model`×2/`warm`×2 + 约 15 处测试,
一律直接传裸数字 99/999/20/0/6)。`core/phrase.gd` 开头明写它 engine-agnostic、
`cache` 是「external run-owned reference」——
**`Phrase` 能脱离 `Run` 独立存在,正是探针能量「一拍」而不用造一整局的原因。**
所以两个候选方案都不成立:删宿主要改 ⏸ 冻结的 `tools/formal.gd`(它读 `p.coins`),
代理到 `Run.coins` 要求每个调用点都有 `Run`,而 20 多处没有。

⚠ 顺带记一条**我判断错过的**:`Phrase.can_discard` 里那条钱检查自 2026-08-06 弃牌免费后恒为真,
我一度称它「死代码」——**不对**,`discard_cost` 仍是 `economy.json` 的活配置,删它等于**删一个旋钮**。

⚠ **⑤ 与 ⑥ 不能同波**:⑤ 要重跑 8 个 `*_sheet.gd` 截图探针,⑥ 要截图对账,
两者抢同一批根目录的 `_shot_*.png`(CLAUDE.md:多 agent 并行时别同时跑同一个截图工具)。

⚠ **⑥ 的期望要摆正**:铁律钉死了「打点只在编排器打」「经济动作只发生在编排器」,
所以**不能切成三个编排器**,只能把「不是编排的部分」搬走。960 → ~680 **不是大胜**,
价值在「剩下的每一行都是编排」。
其中**替换态是真问题**:它现在是 4 个散落成员(`replace_pick`/`replace_prompt`/`replace_preview`
+ `joker_views` 的 `tappable`/`pick_mode`/`accept_joker_drop`)由 3 个函数分头维护
(进入 `_on_shop_replace` / 退出 `_end_replace` / 成交 `_on_slot_tapped`),
而它是**后 3 次商店 100% 会走的主线交互**。它应该是一个对象:编排器只说
`replace.enter(j)` / `replace.exit()`,**经济动作与打点仍留在编排器手里**。

## 依赖

⚠ **约束是文件所有权,不是步骤顺序** —— 两个 agent 同时编辑同一个文件必然打架。
`tests/runner.gd` 是**每个会改行为的步骤都要动的共享资源**,所以它必须**先**切开
(我第一版把依赖写成 `①stat → ④切域`,反了)。

```
④切域(独占 tests/) ──┬──> ②_why (core/db.gd + data + t_db)
①stat (独占 tools/) ─┼──> ③coins(core/phrase,run + view/phrase + t_phrase,t_economy)
                      └──> ⑤probe/shot(tools/) ──┐
                                                  ├──> [R6] 建模五件套收口
S3 结案 ──────────────────────────────────────────┘
③coins **必须在 ⑥ 之前** —— ⑥ 会大改 phrase.gd 的经济回调(同一个文件)
⑥phrase.gd 独占一波:六步里**唯一会碰运行时行为**的一步, 且要截图对账
```

⚠ **并行时不许动别人正在量的东西**:探针在做「输出逐字节对比」期间,
不能改 `data/*.json` / `core/`(它们是被量的对象)—— 量到的 diff 会是假的。

## ⏸ 本轮明确不动(带解锁条件,别在解锁前动手)

| id | 事项 | 为什么现在不动 | **解锁条件** |
|---|---|---|---|
| **R6** | 建模五件套 `formal`/`dp`/`dpcheck`/`dpdiag`/`udp`(**1364 行**)的骨架与统计收进 `Stat`/`Probe` | 它们看着重复,实际是**五个不同的实验**(主张验证 / U 表 / V 表 / 样本外验证 / 档内诊断),重叠的只有骨架和统计 —— 而骨架已被 `RunLoop` 收过一次。更要紧:`dpdiag`/`dpcheck` 是 **S3 正在用的诊断仪器**,**在仪器还在用的时候改仪器**是本项目吃过亏的形状 | ① S3(通关率低估 8.4pp)**结案** ② 本轮 ① 与 ⑤ 已落地(否则合并没有落点) |
| **R7** | 建模五件套是否该进一步**合成更少的文件** | 需要先看它们在 `Probe` 之上还剩多少各自的东西 —— 现在判断不了 | R6 完成之后再评估,**默认答案是「不合」** |
| — | 玻璃卡光影 / 重算目标分表 / `raisedbar` 归属 | 不属于重构,要用户拍板 | 见本文件「明确**不做**的」与 C6 |

⚠ 五个被推迟的文件**顶部已就地标注 ⏸**(`grep -n ⏸ tools/*.gd`)——
索引会过期,文件顶上的标注不会。

---

# P0.5 · 外部审查(2026-08-09)剩余项

⚑ **两个 High 已修**(见 CHANGELOG)。下面三条是同一份审查的 Medium,**未修**。

| # | 事项 | 我的判断 |
|---|---|---|
| ~~V1~~ | ✅ **`DB._load` 的校验语义** —— 2026-08-09 已定:**保持运行时宽容,把选择写死、把文档说准** | **不是行为缺陷,是文档缺陷**。`db.gd` 的意图本就是测试期门禁(`t_db.gd` 第一条断言 `load_error() == ""`),错的是 `CLAUDE.md`/`STATUS.md`/`tech.md` 把它写成「硬校验…直接红」,读起来像运行时拒绝 —— **和 `beat.gd` 那句「漏步 = 崩」同一个毛病:注释承诺了一个不存在的机制**。⚠ 代价不对称是保持宽容的理由:漏网坏数据只影响一次构建,开不了机影响每个玩家;而 `_load` 返回 `{}` 会让调用方四处炸,不是一次干净的失败。⚠ **要改成运行时严格,先做单点失败出口**(启动时统一查 `load_error()` 并显示一屏可读错误),否则只是把静默换成雪崩 —— 理由与改法已写进 `core/db.gd` 文件头 |
| ~~V2~~ | ✅ **`acquire` / `shelf` 子结构校验已补**(2026-08-12 流派批) | acquire 白名单+deck_rule 值+per 值(一波),shelf 白名单(二波),全在 `core/db.gd` |
| ~~V3~~ | ✅ **首页选角可重入已闩**(2026-08-13) | 双保险:`_on_home_start` 挡二层 PickWalker,`_front_latch` 挡 picked 双发;`_open_home` 复位。flow_probe 的双 choose 路径核过(每轮经 `_open_home`) |

---

# P1 · 内容设计(牌型 / 小丑牌 / 盲注)

| # | 事项 | 备注 |
|---|---|---|
| C1 | **牌型倍率表要不要再调** | 现值 `1/2/2/3/4/5/6/7/8/8`,依据是 `P(≥牌型)` 累计频率。**改它会连带改所有卡的价值** |
| C2 | **小丑牌扩批**(现 23 张) | 原作规模是 150+。⚠ 每张要过「记忆成本表」(见下) |
| C3 | **盲注扩批**(现 12 张在池) | 原作 28 张。调研已做完(`design/research_balatro_bosses`) |
| C4 | ⚠ **小丑牌的覆盖门 —— 现在完全没有** | 脸有 `gate.sh`,小丑牌没有。而**构筑值 4.2 倍**,分数大头在小丑牌上 |
| C5 | **新卡的状态成本静态检测** | 读新卡的 `when` / 新脸的 `params`,引用了状态里没有的量 = **状态缺失**,当场报出代价 |
| ~~C6~~ | ✅ `raisedbar` **= 第三轮**(2026-08-09 用户拍板) | 原话「适合第三轮,放第二轮会无聊」。⚠ **副作用已量过并当面复核,用户看过数据后仍选第三轮**:它稀释了 `rerun`(唯一专克 twin 的脸),净效果 twin 38.1%→42.2%、其余微降。依据与那张死亡率表在 `design/blinds.md` 的 `raisedbar` 节 |
| ~~C7~~ | ✅ **第 8 次商店** —— 2026-08-09 已解决,**而且方向和以为的相反** | 游戏里**本来就没有**第 8 次:`view/phrase.gd` 在 `finale` 那一支直接走结算屏。Tape 实测 **37/37 完整局 = 段中 4 + 段末 3 = 7 次**。多出来那次只存在于 `tools/runloop.gd`(无条件开末段商店)—— **模型比游戏多一次商店**,「规则在游戏/模型里不一致」的反方向一例。已修;文档里的「8 次商店 / 后 4 次替换」全部改为 7 次 / 后 3 次 |
| ~~C8~~ | ✅ **彩票档复核已裁定**(2026-08-12) | **全员移出彩票 → 半可控**(判据在定义层:条件=牌型集,规则牌抬的就是它);但建成态重定价被两态数据封锁(规则牌真人 0 拍),**现值 320 维持,数据到位后重定(预判下调)**。**彩虹留彩票,180 不动**:`core/fx.gd` 的 `all_suits` 不认万能牌 —— 百搭抬不动。裁定与证据 = `design/numbers.md` §8.3 |
| ~~C9~~ | ✅ **probbook 两态仪器已落地**(2026-08-12) | 真人列拆两态 + 规则牌 Δp 牌型频率表;顺手修了 held 追踪 bug(`repl` 是 in/out 键,旧版换进不计换出不删 —— 回响 11%→33%、彩虹 25%→40%)。**封锁转为数据侧**:规则牌真人持有 0 拍,两态列空 = 仍不定价。剩仪器债:bot 列未拆态 |
| ~~C10~~ | ✅ **速弹真机死条件已修**(2026-08-12) | `_settle` 补传 `early`,判据抽 `_acted_early()` 一份与 `_advance` 共用;pair/kit/tests 全绿。**遗留归 M5**:早锁无 UI 反馈,真人早锁率仅 8%(bot 78%)—— 速弹/惯性要真正活起来得先做可见性(numbers.md §8.4) |

## ⏳ Target 回池后的待测(2026-08-09 从 `CLAUDE.md` 迁来的原文)

  ⚠ **改动会改变构筑弧形状**(「前期吃经济、后期转流派」),而且**必须等 sim 修好段中商店之后
  再测**——现在 sim 一局只掷 3 次、真机 6-7 次,任何换旗数值在当前 sim 上都测不准。
  8◆ 买入直接顶掉旧的(无回收)这条不变。

## ⚑ 内容的模型成本表(加卡前先查这张)

| 需要的记忆 | 例子 | 状态代价 |
|---|---|---|
| **无** —— 只看这手牌 | 牌型倍率 · 加分项 · 规则牌 | **0** |
| **上一拍** | `encore` · `norepeat` · `rerun` | ×11 |
| **本段首拍** | `setlist` | ×11 |
| **本段累积分** | 缺口 `g` | **0**(已在状态里) |
| **本局累积计数** | 成长牌 | ×25,**每张一维** |
| **本局的集合** | 「打过 5 种牌型 → ×2」 | **×1024** |
| **有序历史** | 「按打出顺序…」 | **爆炸** |

⚠ **这不是「贵就不做」** —— 是把代价摆出来让设计者判断。有些代价值得付。
**区别在于:「这卡测出来 0 分所以改它」是迁就模型(禁止);「这卡要 1024 倍状态,你要不要付」是定价(应该)。**

---

# P2 · 求解器

| # | 事项 | 备注 |
|---|---|---|
| S1 | ~~`design/25` 待审~~ | ✅ 已审并合并进 `design/solving.md` 第三部分 |
| S2 | **三个状态缺失** | `prev_kind` · `first_kind` · `coins`。见 `design/solving.md` §3 |
| S3 | **通关率低估 8.4 个百分点** | **主因未完全定位**。已排掉:段内卷积(无效)、幸存者偏差(降 8%)、构筑档太粗(d=0.078 无效)。⚑ **2026-08-14 新增强候选:S10 的弃牌枚举盲区(+9.3%, z=15.5)** —— 先做 S10 再回来看这条 |
| ~~**S10**~~ | ✅ **弃牌枚举已扩到全部可见牌**(2026-08-14) | 立项依据 = 先验层配对 **+26.6 ±1.7 · z=15.5 · 量级 9.3%**。**连带解掉三条挂着的债**(见下)。⚠ 代价:`tests` **<300s → 716s**;候选剪枝提速已被实测否掉(§5.6d)。**S3 诊断待重跑** |
| S4 | 先验「剩余商店次数」那条 | **免费**(`t` 已决定),且怀疑是 S3 的主因 —— 原来「假设不再买牌」是单向低估 |
| S5 | 买牌求解化 | `tools/draft.gd` 已有雏形,`solve_draft` **默认关**。给 0 的卡从 14/23 降到 3/23 |
| S6 | 两个不动点的收敛性 | 分布↔价值表 · 构筑转移↔买牌决策。**测不到收敛就停下报告** |
| S7 | 闭环重放扩到脸/经济 | `design/gates.md` |
| S8 | 池子从手写改搜索 | 可加性检验已通过,这一项已解锁 |
| ~~S9~~ | ✅ **金币影子价做反了** —— 2026-08-09 已修(`coin_decay = 1.0`) | ⚑ **但收益远小于预期,结论因此改写了** —— 见下 |

## ⚑ S9 · 金币影子价:诊断已定死(`tools/wallet.gd`,200 局)

**症状**:经济卡 `tipjar`/`interest` 在模型里是空气(`tools/kit.gd` 的 coin 行为臂 ❌)。
钱到手了(金币臂 +9.4◆/+108.6◆)却一分没多花。

**证据**(SpyBot 与原厂 Bot 逐局同分自检过,监听器只记账):
```
满槽第一次发生在第 4.00 次商店(共 8 次), 200/200 局都满
满槽之后整局只再买 0.27 次;满槽时 78% 不愿换
  货架最好 ev 46.5  vs  槽里最弱 ev 12.2   ← 差 3.8 倍
  新卡打不过旧卡的只占 10.1%                ← 九成情况新卡确实更好
  买不起只占 0.1%                          ← 钱从来不是约束
局末余额 34.7◆ —— 零终局价值的一笔钱
```
⚠ 而 `design/levels.md` 押的正是「**后 3 次商店 100% 是替换场景,那是构筑弧的发动机**」——
**bot 恰恰在设计押注的那一半完全罢工。** 不是口味问题,是缺陷。

**根因**(`tools/bot.gd:210`):`lam = coin_score_ratio × 本局均分`
—— **随分数上涨、不随剩余机会衰减**(实测 17.7 → 54.3),而收益边 `ev × horizon`
随 `phrases_left` 收缩。门槛 ev 从 3 爆到 ∞,后半程购买停止。
金币零终局价值,本该越到后期越不值钱,**模型做反了**。

⚠ **同一个东西两把尺子**:bot 买牌按 **41 分/◆**,求解器 `Draft.COIN_TO_SCORE`
按 `coin.gd` 实测取 **0.3 分/◆** —— **差 137 倍**。又一次「同一件事两套平行机制」。

**已排除**:`solve_draft` **不是解药** —— 配对实测花费 +0.70◆(z=1.26 **不显著**);
它让总分 +2565(z=2.97)但没让 bot 多花钱,因为门槛在 `lam` 那边、两条路径共用。

### ✅ 结论(2026-08-09,`tools/decay.gd` 扫描 300 局/点、配对同种子)

`lam = coin_score_ratio × 本局均分 × (horizon/20)^coin_decay`,**已定 `coin_decay = 1.0`**。

⚑ **但那不是因为它涨分 —— 它几乎不涨分。**

| decay | 总分 | Δ vs 0 | z | 通关段数 | z |
|---|---|---|---|---|---|
| 0.00(旧) | 15026 | — | — | 2.670 | — |
| **1.00** | 15031 | +4.3 | **0.15** | 2.680 | 0.69 |
| 8.00 | 15176 | +149.5 | 2.50 | 2.703 | 1.27 |

**没有一个点达到本项目自己的判据 |z| ≥ 3。** 选 1.0 的理由是**它是有物理意义的那个点**:
成本 `lam×price` 与收益 `ev×horizon` 同阶于 horizon,**horizon 从判据里整个约掉** ——
买不买只取决于「这张卡每拍值不值这个价」,与什么时候买无关。
(选 decay=8 就是拿一个 z=2.50 的不显著涨分去定参数 = 把噪声当信号。)

病理确实治好了:满槽后还买 **0.32 → 0.97 次**、不愿换 **76% → 56.5%**。

### ⚑⚑ 真正的结论:**经济卡近乎无价值是游戏事实,不是模型缺陷**

原来的因果链是「lam 做反 → 不买牌 → 经济卡是空气 → 目标分不可信」。
**前两环成立,第三环不成立。** 修好之后 bot 确实开始买了,**总分几乎不动** ——
剩 3 拍时买一张好卡也就 `ev×3 ≈ 139` 分,占全局 15000 的 0.9%。
**后期的钱花不出去不是因为定价错,而是那时候本来就没什么值得买的了。**

而且多出来的钱大半买了 `interest`(0.16 → 0.53 次),**产币卡生的钱同样没有终局价值** ——
局末余额反而从 39.1◆ 涨到 47.2◆。

⚠ 所以按铁律「**测出近零不许改内容**」「**模型只定目标分,不当内容裁判**」:
**不改 `tipjar`/`interest`**。`kit.gd` 的 coin 行为臂对它们报 ❌ 是**正确读数**,
不是待修的 bug —— 它如实说出了「这两张卡在 24 拍 / 7 商店的结构下换不出分」。

⚠ 尺子位移实测:9 个队列全部在 1 个标准误内(n=1000 时 se ≈ 1.5pp)——
**先前担心的「全量重测」代价没有发生。**

## ⚠ 如果 S3 的主因仍找不到

**别继续加状态维度**。改用最笨但最确定的办法:**同一个种子,在不死局和真判生死两种模式下逐帧对比轨迹**。今天我一直在猜维度,应该早点用这个。

---

# P3 · 等真人数据(只有用户能解)

| # | 事项 |
|---|---|
| D1 | **手速** `beat_budget.discards` —— 实测差一次动作让全部读数偏 31% |
| D2 | 发挥系数(完美玩家 → 真人)· 人群权重 |
| D3 | 三因子权重(成长感/赌博感/新鲜感) |
| D4 | 会话边界打点(一次坐下玩几局、隔多久回来)—— **目标函数换成留存后,这是唯一能直接观测目标的量** |
| D5 | `design/telemetry.md` 的指标与验证两节(分析侧,一行没有) |

---

# P4 · 未做的整块

| # | 事项 | 备注 |
|---|---|---|
| M1 | **meta 层 `design/ui_meta.md`** | 日常小丑牌/角色碎片/收藏/皮肤,一行代码没有。⚠ 调研判定它是 **D7 的主要风险**。**2026-08-09 用户加了三层生命周期(日 / 赛季 / 永久),赛季层是新的** —— ⚠⚠ 其中「局内高分换赛季道具」**与求解器目标函数冲突**,判据分岔待用户拍板,见 `design/ui_meta.md` 那一节 |
| M2 | Director `design/levels.md` + **[`difficulty.md`](design/difficulty.md) §3** | 设计完了,没建。⚑ **2026-08-14 大幅简化**:拍板不读 context ⇒ 三节作废,它降级成**一张设计期的 JSON 表**,依赖(池子搜索 / 体感量)一并解除 |
| M3 | `gig_clocks` 场曲线 | 四场全平 8/8/8/8,难度弧那部分没做。⚑ **2026-08-14 找到落点** = 新手引导 N2(教学关拍长 12→10→8),不必先解决正式局的场曲线 |
| M4 | 槽位显示「这张牌本局贡献多少分」 | 后 3 次商店 100% 是替换场景却没有数值依据 |
| M5 | `early_settle()` 提前锁定 | 钩子在,UI 没接 |
| M6 | 终幕专属画面 | 等素材 |

---

# 明确**不做**的

| 事项 | 为什么 |
|---|---|
| **重算目标分表** | 它是**占位**,是整套重新设计的产物,不是一次算术 |
| **玻璃卡光影** | 用户 2026-08-06 中止。要动先看 `LESSONS.md` 的根因 |
| 写实化 / 换美术场景 | 用户已全部否掉并要求回滚 |
| skip-blind / Tag 奖励 | 用户拍板明确不做 |
| 参数化训练路线(`design/history_adversarial.md`/17`) | 已作废 —— 上界不需要「学」,只需要「解」 |
| 雨的效果 | 用户明确不喜欢(局内舞台背景;失败屏的雨是设计稿钦定的,别误删) |
