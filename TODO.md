# Sync5 · 待办

> 现状看 [STATUS.md](STATUS.md) · 变更史看 [CHANGELOG.md](CHANGELOG.md) ·
> 经验看 [LESSONS.md](LESSONS.md) · 原则看 [CLAUDE.md](CLAUDE.md)
>
> **⚑ 这份文件只回答一件事:接下来做什么。**
> **做完的事一律搬 [CHANGELOG.md](CHANGELOG.md),不在这里留经过** ——
> 2026-08-15 它长到 871 行,一半是已完成事项的叙述,而那些叙述 CHANGELOG 里已经有了。
> **一个 871 行的待办清单不能当待办用。**(2026-08-26 洁癖清理又剪了一轮:✅ 与作废项全部出清。)
>
> 三堆的分法 = **谁能解**,因为这是唯一影响「现在能不能推进」的属性。

---

## ⭐ 从哪接(2026-08-26)

-2. **⚑⚑ 用户排定的接续路线(2026-08-26)**,按序执行:
   ① 全量门跑完 → **push**;
   ② 加**超级百搭**(四张 JOKER 入堆 + 付费洗牌钓卡;引擎前提 = Pattern ≥3 张万能解析捷径);
   ③ **删除卡牌升级系统**(用户拍板;理由原话:「升级太复杂等于要做好几套经济,
     现在能做是因为没考虑实质问题。删掉是为了减负,不太会影响游戏乐趣」——
     每个升级档都是一套要独立平衡的经济, 那笔账从未真付过)——连带:商店升级栏 UI、
     economy.json joker_upgrade、升级测试、fx 的 increment_scale 通道、
     CLAUDE.md「金币两个出口」改为「买牌 + 洗牌」;金币主出口易位, 经济数值重标并进 ⑥;
   ④ **增量门**处理新卡(超级百搭动了万能解算 = 机制改动, --changed 会自动升全量, 有心理准备);
   ⑤ 新卡**前端特效**(由 Claude 调用 Claude Design 出素材/规格);
   ⑥ **数值批**:卡牌数值 + 经济系统数值(numbers.md 六步 SOP + probbook 补行);
   ⑦ **商业化**:激励视频广告双入口 —— 刷新次数不够看广告补刷 · 体力不足看广告领;
     **不做内购**。**体力已拍板:开局扣一点**(体力从「只显示」转正为真闸门;
     data/profile.json 的 energy_max 从此是真容量);广告 SDK 选型(TapTap 生态)归用户侧确认。

-1. **⚑⚑ 对抗批已实装未量**(细账 CHANGELOG 08-25b;设计总纲 = docs/design/versus.md):
   - **仪器统跑进行中**(08-26 全量门在跑):跑完 `tools/rankgen.py` 重生成 ranking →
     暂存脸补 tier 入池(faces.json 加 tier 即入)→ sim 报表加**骨架归因 + 伏击率 + 必字三查**
     (versus.md 验证节);
   - **蒙色/蒙点入池前必修**:bot 属性级信念(现按整张全盲保守近似);
   - 挂起七张深钩子卡:试音/延音踏板/指挥棒/定金(交互 UI)· 幸运币(逐事件掷点)·
     定存(跨拍牌身份, 防 deck 回收对象复用)· **超级百搭**(见路线 ②;拍板点 = 与百搭共存还是取代);
   - 悬案:串烧/连击(挂牌型成长口子)· 金融组谨慎筐;
   - 新卡定价全部是方向锚:numbers.md 六步 SOP + probbook 补行未走(并进路线 ⑥)。

0. **等用户的**:试玩(对抗批)· 回传端点 · release keystore + 正式包名 · App 备案/软著材料 ·
   新画风参考素材(搁置中,素材到了再启动切换框架)。

---

## ⚑ 工作方式(2026-08-16 用户拍板,至今有效)

**改动期间不跑 sim / 不跑 `gate.sh` / 不跑 kit 全量**,只跑秒级的(JSON 校验 · 相关单测域 · 截图);
顺序 = **改完 → 用户试玩 → 用户喊 → 才跑 sim/门**。理由:真人试玩是唯一能验 `section_targets` 的仪器
(sim 读的是 `bot_targets` 另一张表),且门实测 4.5 小时、单测 12 分钟,「小改一次验一次」不成立。
交付前仍必须自己确认「能跑起来」(JSON 过 `db.gd` + 相关单测 + 真机截图)。

---

## 🔴 只有用户能解(挡着一整族)

| # | 事项 | 挡着什么 |
|---|---|---|
| D2 | **发挥系数**(完美玩家 → 真人) | ⚠ **不许拍初值、不许用机器人代算**(用户 2026-08-07:「我后面会完整玩很久,到时候用我的系数」)。它挡着**整套目标分** |
| D4 | 会话边界数据 | 打点已实装(`sess` 进 Tape meta)—— 现在只差**你正常玩**。目标函数换成留存后,**这是唯一能直接观测目标的量** |
| — | **教学关的新手数据** | 现有合格 Tape 全是熟练玩家(用户本人)。**找个没玩过的人跑一遍**就有第一份新手动作时刻分布。教学关已带 `tutorial: true` 标记 |
| C1 | **顶端三档牌型倍率**(四条/同花顺/皇家 ×7/8/8) | 缺口真实(压缩外推四条 ≈×18)但合计只占 0.43% 的拍;抬它要**先定压缩指数** = 设计判断,走 `numbers.md` §5。推导全文在 `docs/design/jokers_history.md`(2026-08-16 节) |
| — | **和弦 / 排练两张条件死卡** | 触发率 4% / 10.5%,A 案救不了 —— **改条件还是删**,归你。⚑ 对抗批又添两张同族候审:**静场**(条件誓约 +23±58 触发 8%)· **和声**(bot 不管理缓存单花色)—— 低频答案是三死法之一,**不为绿改数值**,判归你 |
| — | **副歌欠补偿** | 窗口窄该补强(jokers.md 原则 17),提到多少归你 |
| — | **3 张小丑牌插画** | `jukebox` 点唱机 · `blacktone` 黑调 · `redtone` 红调(manifest 的 art_subject 已冻结:S-15 / S-61)。`verify_joker_assets` 对它们报缺 = 待办信号 |
| — | **语言切换 UI 要不要做** | 现在只有 `SYNC5_LANG` env / 存档 `lang` 键 / 系统语言自动 |
| — | **新画风参考素材** | 2026-08-24 提出后**用户暂令搁置** —— 素材/方向到位前不动 |

---

## 🟢 可以直接做(按建议顺序)

### 1 · 路线 ①~⑦ 之外的小件

| # | 事项 |
|---|---|
| N5 | **量重开摩擦**(几次点击 / 几秒)—— 调研标为「便宜的留存杠杆」,Balatro 的零摩擦重开是核心机制之一,**一直没量过** |
| — | 断点续玩**验收**:真机杀进程/杀标签页后点回来(实装已完,2026-08-24) |
| — | Web 出包前重跑 `fontsubset.sh`(en-dash/弯引号是新字,语料扫 `data/*.json` 自动收进) |
| — | 复审现役卡里窗口/时机限定的(原则 17:窗口窄 ⇒ 效果强) |
| — | **bot 动作粒度:多选弃 = 1 动作**(2026-08-26 门尸检定案)—— bot 把弃牌张数当动作次数计(`beat_budget.discards: 4`),弃 6 结构性不可达,拆迁(wrecker)因此挂 kit `UNREACHABLE` 声明;真人一次跨区多选 6 张只是一个动作。修完粒度反查会喊删声明。**⑥ 数值批前置**(不修则拆迁定价无仪器) |
| — | **db 交叉校验 `discard_bias`** —— 值必须等于该卡 `effects.when.discards_gte`(2026-08-26 抓到手抄漂移:卡改弃6后 sim.json 还是 3,触发恒 0%;这是「两个家」第 N 例,该上校验锁) |
| C5 | 新卡的状态成本静态检测 —— 引用了状态里没有的量 = **状态缺失**,当场报出代价 |
| M3 | `gig_clocks` 场曲线(正式局仍全平 8/8/8/8)。⚠ 动它会与 `death_spec[0]=0.05` **两处放水叠加** |
| M4 | 槽位显示「这张牌本局贡献多少分」—— 后 3 次商店 100% 是替换场景却没有数值依据 |
| M6 | 终幕专属画面(等素材) |

### 2 · Director 留给迭代的

price.gd 全表精修排序(挂过夜跑;08-26 全量门跑完 rankgen 先顶上)·
跨轮 tiers 复现表(要用户拍哪张脸进哪几轮;前置 = 每 (脸, 轮) 过价,要走全量门)。
context 线仍在等的:C5 死亡段细粒度(等试玩)· C2 A/B(等回传)· 疲劳轴(待留存数据)。

### 3 · 求解器(P2)

> ⚠⚠ **这一节动手前先花五分钟读实现,别信描述** —— 2026-08-15 的清理查出 7 条待办早已完成,
> 其中 S4 还长期挂着「怀疑是 S3 主因」的错标签:**一个错的候选主因比没有候选更糟**。

| # | 事项 |
|---|---|
| S3 | **通关率低估 8.4pp,主因未定位**。仪器已验对(`dpcheck.gd` 用 `player="perfect"`)。⚠ **重跑要预留 2 小时以上**(中途不输出只能靠 CPU 判活) |
| S2 | 三个状态缺失。`first_kind` 已查证 = 真缺口但贵且窄(代价 ×11 状态,收益只覆盖 `setlist` 一张脸)⇒ 先不做,除非 S3 指到它;`prev_kind` 与 `coins` **未查证** |
| S6 | 两个不动点的收敛性。**测不到收敛就停下报告**。⚠ 未查证 |
| S7 | 闭环重放扩到脸 / 经济。初查:`tools/replay.gd` 已认识脸,**可能已部分完成**。未查证 |
| — | ⚠ 建模五件套(`formal`/`dp`/`dpcheck`/`dpdiag`/`udp`)的清理**解锁条件 = S3 结案** —— 仪器还在用时不动仪器 |

### 4 · 仪器债

| 事项 | 现状 |
|---|---|
| `tools/sim.gd` 抖 ~0.1pt(A/A 不可复现) | 根因候选已定位(2026-08-21 评审):`RunLoop.play` 从不给 `Run._blind_rng` 播种。修法一行,修完 A/A 一次验证。全文在 [LESSONS.md](LESSONS.md)「sim 不可复现」节 |
| `tools/probbook.py` 不产出曝光列 | probbook 里那节是手写的、**下次重刷会被冲掉** |

---

## ⚑ 内容的模型成本表

已搬 [`docs/design/capability.md`](docs/design/capability.md) 附录(加卡前先查那张表)。

---

## 明确**不做**的

| 事项 | 为什么 |
|---|---|
| **重算目标分表** | 它是**占位**,是整套重新设计的产物,不是一次算术 |
| **局外 build 任何形式的回归**(主角/券/META/成就) | 2026-08-24 用户拍板整体删除「仅保留局内玩法,我们应该专注」——**别再提议加回** |
| **内购** | 2026-08-26 用户拍板:商业化只做激励视频双入口 |
| **玻璃卡光影** | 用户 2026-08-06 中止。要动先看 `LESSONS.md` 的根因 |
| 写实化 / 换美术场景 | 用户已全部否掉并要求回滚 |
| skip-blind / Tag 奖励 | 用户拍板明确不做 |
| 防沉迷 SDK | 用户拍板(发行面向海外) |
| 参数化训练路线 | 已作废 —— 上界不需要「学」,只需要「解」 |
| 雨的效果 | 用户明确不喜欢(局内舞台背景;失败屏的雨是设计稿钦定的,别误删) |
| **池子搜索 / `tools/pool.gd`** | 消费者从未存在。不是「还没做」,是「不做了」 |
| **用价格压超模卡** | S9 裁定金币边际近零 —— 弱杠杆,且绕开 SOP 就是特例累积 |
| **catch-up meta**(挣扎的玩家获得加速) | 与「DDA 不可见」冲突。要做也得**看起来像奖励,不是像同情** |
| **给锁配钥匙式纯对策卡** | 2026-08-25 对抗批拍板(versus.md):单卡对单盲注 = 商店彩票,**组合破题**才是解 |
