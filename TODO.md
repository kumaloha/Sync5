# Sync5 · 待办

> 现状看 [STATUS.md](STATUS.md) · 变更史看 [CHANGELOG.md](CHANGELOG.md) ·
> 经验看 [LESSONS.md](LESSONS.md) · 原则看 [CLAUDE.md](CLAUDE.md)
>
> **⚑ 这份文件只回答一件事:接下来做什么。**
> **做完的事一律搬 [CHANGELOG.md](CHANGELOG.md),不在这里留经过** ——
> 2026-08-15 它长到 871 行,一半是已完成事项的叙述,而那些叙述 CHANGELOG 里已经有了。
> **一个 871 行的待办清单不能当待办用。**(2026-08-26 洁癖清理又剪了一轮:✅ 与作废项全部出清。)
>
> 三堆的分法 = **谁能解**,因为这是唯一影响「现在能不能推进」的属性。

---

## ⭐ 从哪接(2026-08-26)

-2. **⚑⚑ 用户排定的接续路线(2026-08-26)**,按序执行:
   ① ✅ 全量门跑完(五红三过期已尸检处置,CHANGELOG 08-26b)→ **已 push**(d41a858);
   ② ✅ **超级百搭已实装**(CHANGELOG 08-26c:捷径引擎 + 注入表 + 洗牌动作 + UI + bot 臂 +
     素材全管线;单测 2344/0 · 截图目视过)。**剩一个拍板点归用户:与百搭共存还是取代**
     (当前缺省共存,两卡可叠 2+4 张);
   ③ ✅ **升级系统已整体删除**(CHANGELOG 08-26d:core/view/tools/data/tests/docs 六面清,
     金币出口 = 买牌 + 洗牌);金币主出口易位, 经济数值重标并进 ⑥;
   ④ **增量门**处理新卡(超级百搭动了万能解算 = 机制改动, --changed 会自动升全量, 有心理准备);
   ⑤ 新卡**前端特效**(由 Claude 调用 Claude Design 出素材/规格);
   ⑥ **数值批** —— **首位新增(2026-08-27 用户两洞见, cards.md 倍率 v3)**:
     prior 层出 `P(≥k|弃牌预算)` 与 `CV(k)` 两列 → 牌型倍率按「难度×(1+λ·波动溢价)」重标
     (λ 归用户手感);弃牌条件卡成本重估(拆迁三案待拍:抬数额/触发返还[推荐]/降条件);
     —— **⚑ 2026-08-26 用户四条规定压缩解空间**(原话与连带账在
     [levels.md 经济 v2](docs/design/levels.md) + [difficulty.md §2.5](docs/design/difficulty.md)):
     卡价平化 ≈3◆ · **开局 10◆ + 弃牌 1◆/张(推翻弃牌免费,κ 回归求解器 = 机制级)** ·
     每拍牌型金币为主收入 · 10-run 周期机制课程(考试关 = 加码族/复合)。
     调研 agent 已派(match-3 gate 节奏 / Balatro 经济细账);数字待先验层建模,
     numbers.md 六步 SOP + probbook 照走;
   ⑦ **商业化**:激励视频广告双入口 —— 刷新次数不够看广告补刷 · 体力不足看广告领;
     **不做内购**。**体力已拍板:开局扣一点**(体力从「只显示」转正为真闸门;
     data/profile.json 的 energy_max 从此是真容量);广告 SDK 选型(TapTap 生态)归用户侧确认。

-1. **⚑⚑ 对抗批已实装未量**(细账 CHANGELOG 08-25b;设计总纲 = docs/design/versus.md):
   - **仪器统跑进行中**(08-26 全量门在跑):跑完 `tools/rankgen.py` 重生成 ranking →
     暂存脸补 tier 入池(faces.json 加 tier 即入)→ sim 报表加**骨架归因 + 伏击率 + 必字三查**
     (versus.md 验证节);
   - **蒙色/蒙点入池前必修**:bot 属性级信念(现按整张全盲保守近似);
   - 挂起七张深钩子卡:试音/延音踏板/指挥棒/定金(交互 UI)· 幸运币(逐事件掷点)·
     定存(跨拍牌身份, 防 deck 回收对象复用);
     ✅ 低频卡族已按拍板「改条件放宽」处置(和弦/和声→全同色 · 静场→只禁弃牌 · 排练实测已活不动)·
     ✅ 超级百搭已拍「取代」百搭(wildcard 删, 特例价 4◆);
   - **⚑ 盲注扩池三轴(2026-08-26 用户补拍,排门后)**:方案 = **盲注包预扩容,Director
     照旧从包里找**(不做生成时动态改数)。① 刻度档位 + ② 纯分数墙的**第一批设计表已列**
     ([blinds.md §2.6](docs/design/blinds.md):17 个新档,池 33 → ~47,数字是方向锚)——
     门跑完落 faces.json + 文案 + 增量门定价;
     ✅ ③ **复合双盲注引擎已落地(2026-08-27)**:形状不是「run_faces 一段两条」而是
     **一条 `combo: [甲, 乙]` 的脸**,参数在 `SectionMod._entry` 一处合并 ⇒ Beat/Phrase/
     Settle/BlindCard 全部零改动(见 [blinds.md §2.7](docs/design/blinds.md));
     db 守恰两成分 / 异轴 / 成分先登过场 / 同键冲突。**试点一条**(低音限流, T3,
     ranking 是 bootstrap 位等 price 统跑正位)。**剩下的**:复合的**排布**
     (Director 怎么挑、一局最多几张)+ 更多复合对 —— 下一批;
   - 悬案:串烧/连击(挂牌型成长口子)· 金融组谨慎筐;
   - 新卡定价全部是方向锚:numbers.md 六步 SOP + probbook 补行未走(并进路线 ⑥)。

0. **等用户的**:试玩(对抗批)· 回传端点 · release keystore + 正式包名 · App 备案/软著材料 ·
   新画风参考素材(搁置中,素材到了再启动切换框架)。

---

## ⚑ 工作方式(2026-08-16 用户拍板,至今有效)

**改动期间不跑 sim / 不跑 `gate.sh` / 不跑 kit 全量**,只跑秒级的(JSON 校验 · 相关单测域 · 截图);
顺序 = **改完 → 用户试玩 → 用户喊 → 才跑 sim/门**。理由:真人试玩是唯一能验 `section_targets` 的仪器
(sim 读的是 `bot_targets` 另一张表),且门实测 4.5 小时、单测 12 分钟,「小改一次验一次」不成立。
交付前仍必须自己确认「能跑起来」(JSON 过 `db.gd` + 相关单测 + 真机截图)。

**⚑⚑ 跑门的时机纪律(2026-08-26 用户复盘「门拖了一周工期」后立)**:
- **全量定价档一周当一轮用**:只在 ① 数值批前后 ② push/发布里程碑前 ③ 用户喊 时跑;
- **绝不背靠背两轮全量** —— 机制批与内容批**能合并的合并成一轮**(08-26 就亏在
  批末统跑后又为超级百搭+删升级单起一轮,本该攒到扩池批一起);
- 机制小改 → 回归档(提速②落地前 = 攒着等合并);内容新增 → 单脸/单卡门(分钟级);
- **门跑着不并行其他仪器**(08-26 实测:并行单测/kit/pair 把 4.9h 拖到 5.5h+);
- 提速工程:① 两门并行 + 跳重复单测(门后立即落)② 回归/定价分档 ③ SHARD 分片(⑥ 前后)。

---

## 🔴 只有用户能解(挡着一整族)

| # | 事项 | 挡着什么 |
|---|---|---|
| ⭐ | **19 张卡没有「实测保底值」** | 机器人判断买不买时, 除了手写公式还会参考实测数据当下限(公式算低了就用实测的)。这 19 张没有:**① 12 张信息不够** —— 它们的效果取决于「这拍弃了哪几张牌」, 而记录里只有「弃了 3 张」这个数字, 没有牌面 ⇒ 要改记录方式, 独立一批。**② 6 张成长卡** —— 价值是一局里慢慢累积的(每次换旗 +40%, 换五次 +200%), 而这把尺子每次只看一拍, 看不见累积。**③ 3 张给钱的卡** —— 有数据(每拍 0.96/3.37/2.45 金币), 但要换成「分」才能和别的卡比, 而换算系数(1 金币值多少分)是**今天砍收入之前测的**, 现在钱少了一半肯定更值钱, 用旧系数会算低。⇒ **宁可空着** —— 用过时系数算出来的数看着有根据、其实是错的, 比空着更容易骗到下一个看它的人 |
| ⭐⭐⭐ | **08-27~08-30 的探针读数全部作废, 要重跑** | 根因已修(`runloop` 的「达标即收工」在 `o.targets` 为空时恒真 ⇒ **不传目标表的探针一局只打 4 拍**, 应有 24;细账 CHANGELOG 08-30 九 + LESSONS 同名条)。**受影响 = 11 个探针**:`curve`(**关卡分那张表作废**)· `kit`(单卡门, 已重跑消耗牌 + 版税族)· `price` → **`ranking.json`** · `gate.sh` · `coin`(κ 已重测)· `addit` · `blind` · `wallet` · `decay` · `decomp` · `udp`。⚠ **`sim.gd` 不受影响**(它传表), 覆盖率/通关率/经济读数仍成立。⇒ **下一轮全量门时一起重跑**, 别零敲碎打(门时机纪律) |
| ⭐ | **估值地板只覆盖 33 张** | `ev.measured`(evbook×0.5)修好了 23 张 EV 为正却没人买的卡(经过见 CHANGELOG 08-30)。⚠ ~~进赢局 35%→52%~~ **那个百分比作废** —— 它是数 `winning kits` 前 3 名那一行数出来的(CHANGELOG 08-30 七)。真实读数用 `sim` 的**覆盖率**节。⚠ **剩下的**:成长/金币/decay 卡 `cf.gd` 量不到 ⇒ 转型/淘碟/收藏家那一族仍靠手写信念。地板权重 0.5 未标定 |
| — | ✅ **`royalty` 已解决:装机率 0.0% → 10%** | 根因不是弱也不是 κ —— `_amt` 的通道白名单漏了 `coins_factor`(全池唯一用它的卡)⇒ `(0−1)=−1` ⇒ **EV 为负 ⇒ 永不购买**。它其实值 **+1191.7 分/局(15.5%)= 一张 common 的两倍**。已落两道机械:`parity.py` 第 ⑤ 层(通道白名单 vs jokers.json) + bot 的「EV ≤0 = 永不购买」警告。**自然口径覆盖率 68/69 → 69/69** |
| ⭐ | **⚠ 挑高(highroller)是净负的 —— 归你拍板** | 单卡门:机制**看得见**(零普通卡货架 +3.5 店, z=26.0), 但**总分 −1555.9(z=−5.96, 占基准 20.3%)**、商店花费 −3.9◆。「下次货架不出普通卡」在**钱刚收紧**的经济里 = 「货架只出买不起的卡」⇒ **玩家花 3◆ 买一张让自己变弱的卡**。三条修法:降价 / 改成「多一格」/ 换效果。⚑ 这是**内容判断不是数值** —— 我不动 |
| ⭐ | **商店类 6 张消耗牌要重定价(证据齐了, 差拍板)** | **12 张全部过了单卡门**(2026-08-30, 三张证物挂错也修了 —— 加急/挑高/砧座此前借的键根本不会动)。现价一律 3~4◆, 而实测量级差 **8 倍**:`superwild +5732(118%)` · `chorus +1668(34%)` · `trim +1185(24%)` · `jackpot +1109(23%)` · `opener +931(19%)` · `popup +730(15%)` · `sponsor +820(11%)` · `encorecall/doublebill/jukebox/anvil` 分差不显著 · **`highroller −1556`**。⚠⚠ **超级百搭/修剪的数字要打折看** —— kit 的「每拍补满」对牌堆手术卡是**复利**(24 次注入 4 张万能), 不是玩家用法;定价仍以 `ccf.gd` 的单次 Δ 为准 |
| ⭐ | **条件卡的估值公式系统性低估:分母用了「均分」而它们只在大牌上触发** | `fullcast` 的臂是 `fixed_rate × _amt × score_mean`,而它 `kind_in [顺子/同花/葫芦/同花顺/皇家]` —— **那几拍的分远高于均分**。实测:公式给 **6.7 分/拍**,而 evbook 实测 **68.8**(差 10 倍),现在完全靠 `ev.measured` 地板撑着。同族还有 `rainbow` / `nopair` / `hush` 等所有 `kind_in` / 条件触发的卡。⇒ 正解是把「触发拍的条件均分」和「全局均分」分开记(`st` 里加一列),而不是继续加地板。⚠ **这条挡着「没有实测地板的条件卡」全部** |
| — | **`fullcast` 的 `_amt` 也是 0(和版税同病, 但被地板盖住了)** | 它的通道是 `mult`(倍数), 而它的臂按「数额 × 均倍率」写 ⇒ `_amt=0` ⇒ 公式恒 0。装机率 23% **全部来自 `ev.measured` 地板** —— 地板一撤这张卡就死。⇒ 给它单独一条臂 `fixed_rate × (_amt − 1.0) × score_mean`(与版税同形), 并让 `_amt` 认识 `mult`。⚠ 改完读数不会动(地板仍占优), 这是**拆地雷**不是调平衡 |
| — | **冷门尾巴(装机率 <5%)剩 7 张 support** | `vinyl 1% · tipjar 1% · loadeddice 1% · harmony 3% · blindplay 3% · triad 4% · stageexit 4%`(另 3 张 `shredder/kaleido/wrecker` 是 **Target**,多数 cohort 用 `cfg.target` 钉死旗子 ⇒ **与 support 不可比**)。已分清成因:**tipjar 是 κ**(低估 2.5 倍,见上)· **loadeddice 是组合卡**(贪心 bot 只看「现在装上值多少」,看不见「先买 A 再买 B」——**不是它弱**)· `stageexit` evbook 标着「ctx 缺·读数无效」· 其余是条件窄。⇒ **没有第二个「版税」那类的结构 bug**(parity 第 ⑤ 层已全绿) |
| — | **消耗牌没有稀有度轴** | 12 张等权重上架。够用, 但如果以后要做「稀有消耗牌」(砧座这种量级 vs 加急这种)就得加。⚠ 先别加 —— 12 张的池子再分档太细 |
| ⭐⭐ | **关卡分:curve 已反解, 待验证(⚠ 原定的验证方法是空转)** | ⚠⚠ **`[64, 106, 345, 1422]` 这张表作废** —— 它是 `curve.gd` 在**四拍局**上反解的(截断 bug, 见本表第一行)。「curve 段分中位 228/153/345/882 vs sim 500~3500 差 3~4 倍」那个口径谜题**真身就是它**。⇒ 修完要**重跑 curve**(⚠ perfect 尺度此前 94 分钟是四拍的, 打满后按拍数比例约 **6 倍**, 做好过夜的准备)。原记录:~~跑出 `[64,106,345,1422]`(5614 秒), 通关率 14.3% 与设计谱 13.7% 吻合~~。⚠⚠ ~~下一步:填进 run.json 跑一次 sim~~ **做不了** —— `sim.gd` 的 `TARGETS` 来自 `sim.json bot_targets`,`run.json` 的表在 sim 里**从不参与判生死**(08-16 已实测坐实过一次:S1 921→560 读数逐字不变)。**能验的三条**:① **真人试玩**(唯一真仪器,**归用户**)· ② 把 curve 的 **adaptive** 输出填进 `bot_targets` —— 验的是**方法**,且**重标仪器刻度 ⇒ 历史读数全部不可比**,单独作一批 · ③ curve 自洽(已做)。⚑ 实测难度形状:`deaths S1:2% S2:0% S3:34% S4:24%` vs 设计 `[5%,28%,50%,60%]` —— **S2 完全没有压力**。⚠ 另注:`run.json` 现行 `[420,510,960,1680]` 是**在被污染的 `BASE_CHIPS` 上反解的, 已知作废**。详见 docs/design/difficulty.md 末节 |
| — | **转型:持有它的局换旗仍略少**(0.78 vs 未持有 0.99) | 「换旗不占购买名额」解开了大头(修前 0.65 vs 0.94,装了增益卡反而更少),但**残余挤压未定位**。kit 单卡门在改造队列下量到 1.7 次/局(= 物理上限),说明机制本身通了 ⇒ 剩下的是 bot 决策层面的东西,不阻塞设计 |
| — | **同花偏弱**(29.6%,六条路线倒数第二) | 它的 CV=1.087 是全场最高,同期望下硬生死线多杀它一截。现行 `mono 5.6` 已经比公式值 4.54 高 23% 在补这个差,再补要动方差补偿的系数(λ 性质,**归用户手感**) |
| D2 | **发挥系数**(完美玩家 → 真人) | ⚠ **不许拍初值、不许用机器人代算**(用户 2026-08-07:「我后面会完整玩很久,到时候用我的系数」)。它挡着**整套目标分** |
| D4 | 会话边界数据 | 打点已实装(`sess` 进 Tape meta)—— 现在只差**你正常玩**。目标函数换成留存后,**这是唯一能直接观测目标的量** |
| — | **教学关的新手数据** | 现有合格 Tape 全是熟练玩家(用户本人)。**找个没玩过的人跑一遍**就有第一份新手动作时刻分布。教学关已带 `tutorial: true` 标记 |
| — | **奖励分族的早段仍弱(0.33×)** | 2026-08-28 已整族 ×1.35,按**晚段**(它的黄金期)与改基族拉平 → 晚段 0.99×。早段 0.24→0.33 仍低,但那是**机制特性不是病**:两族互补(改基早期强 / 奖励分后期起来)。⚠ 真要补早段,得压平段间斜率 —— 这一族写的是「目标分的百分比」而目标分 420→5600 涨 13 倍,压平要么改 DSL 操作码(过 jokers.md 门槛)、要么改百分比的基准。**是不是问题,取决于「S1-S2 该不该有奖励分族的位置」** |
| ⭐ | **新口径的 P 表出来了,倍率怎么跟** | **口径已于 2026-08-28 修好**(`vol.gd` 改成手牌 5 张计分 + 缓存 1 次交换;旧口径 `SYNC5_VOL_POOL=8` 可复现 v3)。新读数:顺子 P(≥) 从 33.3% → **9.5%**(真人实测 8%,吻合)· 同花 25.3% → 8.7% · 葫芦 17.6% → 4.8%;**CV 同步变了**(顺子 0.78 → 1.42)。⇒ 倍率 v3 公式 `mult(k) = base(P≥k|b) × (1 + λ·CV(k))` 的**两个输入都换了**,终表 1/2/3/7/8/9/12/15/18/18 要不要重算 —— **λ 归你手感**,而且下游连着 Target 卡倍率 / `kind_coins`(尺 = −log₂P) / `curve.gd` 目标分反解,量级足以重排策略地形 |
| ⭐ | **行为类条件卡的 p 从来没算过** | `probbook.md` 64 条里只有 33% 有 `p̂`;`numbers.md` 自己写着行为类(弃牌/持币/时机/跨拍)先验算不出、必须等实测,而实测是 3~6 拍。**对照:定格 33% 给 +30%,谢幕 83% 给 +60%** —— 条件难一倍半的给一半的钱,且谢幕与无条件的灯牌同额还不递减(违反 numbers.md §3.1「无条件卡必须是地板」)。按 §3.1 公式 p=0.83 只该比地板高 10%。**要么补一把能算行为类的仪器,要么认下「这族按薄实测定价」** |
| ⭐⭐ | **κ 第一次可用:71.7 分/◆(z=8.70)—— bot 的信念是它的 2.5 分之一, 待校准** | 2026-08-30 在**修好截断之后**重测(旧的 0.6 与 0.0 都是四拍世界的读数, 作废)。bot 心里一枚金币 = `coin_score_ratio(0.1) × 本局均分` ≈ **28 分**, 实测 **71.7** ⇒ **低估 2.5 倍**, 而产币卡的 EV 全部经过它。⇒ 校准 = 把 `coin_score_ratio` 抬到 ~0.25。⚠⚠ **没做, 因为它会让所有以 bot 为尺的读数失效**(sim.json 自己的注释就写着这条), 该**和目标分重算并成一批** —— 反正 11 个探针本来就要重跑。⚠ `discard_kappa: 30` 是从旧值推的第二个家, 一起动。下面这条是它的旧版记录:<br>~~`tools/coin.gd` 测出 **κ = 0.6 分/◆**,而按「4◆ 买一张 EV 800~2000 分的卡」推算,钱若稀缺该值 ~200 分/◆ ⇒ **差 350 倍**。sim 佐证:局末余额 34~66◆(健康带 = 个位数),`lonewolf` 队列高达 **65.8◆**。⚑ **根因不是发太多钱,是三个出口全封顶**:弃牌受边际收益递减自限(弃 1 张就够)· **买卡受槽位 4 个硬限**(买满后钱没出口,替换还折半回血)· 刷新 bot 几乎不用。而收入按 `−log₂P` 随牌型发,**玩家越强收入越高**。⇒ 修法三选一(减收入 / 提价 / 加出口),⚠ 加复杂出口与用户 08-26「删升级系统:太复杂等于要做好几套经济」的拍板冲突。**这条挡着所有金币 delta 的标定**(§2.5)~~ |
| — | **3 张卡的通道错配**(2026-08-28 全池审查) | 全员(11%)· 清流(17%)· 三重(18%)——**难度罕见却用加分通道**,按 §2.55 该走乘数。⚠ 改通道 = 改机制不只是数值,单独成批。(快闪/开场/副歌的 1/6 是**时机罕见**,加分是对的;黑胶 common+加法成长**合规**,别误伤——两条例外已写进 §2.55) |
| C1 | **顶端三档牌型倍率**(四条/同花顺/皇家 ×7/8/8) | 缺口真实(压缩外推四条 ≈×18)但合计只占 0.43% 的拍;抬它要**先定压缩指数** = 设计判断,走 `numbers.md` §5。推导全文在 `docs/design/jokers_history.md`(2026-08-16 节) |
| — | **副歌欠补偿** | 窗口窄该补强(jokers.md 原则 17),提到多少归你 |
| — | **3 张小丑牌插画** | `jukebox` 点唱机 · `blacktone` 黑调 · `redtone` 红调(manifest 的 art_subject 已冻结:S-15 / S-61)。`verify_joker_assets` 对它们报缺 = 待办信号 |
| — | **语言切换 UI 要不要做** | 现在只有 `SYNC5_LANG` env / 存档 `lang` 键 / 系统语言自动 |
| — | **新画风参考素材** | 2026-08-24 提出后**用户暂令搁置** —— 素材/方向到位前不动 |

---

## 🟢 可以直接做(按建议顺序)

### 0 · 玩家旅程审计的缺口工单(2026-08-27,[journey.md](docs/design/journey.md),按疼痛排序)

| # | 事项 |
|---|---|
| ❌ | **弃牌键亮价**:fee 徽章接 DISCARD_COST × 选中张数 —— 经济 v2 后弃牌是暗扣钱,最疼最小 |
| ⚠ | **教学关补经济 v2**:弃牌花钱/成牌挣钱一个字没教,新玩家第一次被扣会懵 |
| ⚠ | **牌型金币入账反馈**:主收入通道要像分数一样被看见(+N◆ 飘字/chip 脉冲) |
| ⚠ | **商店里的整局四脸路线**:调度解法落到决策点(商店盲注板加缩略行) |
| ⚠ | 重开摩擦量化(N5 并档)· 洗牌键发现性(等真人) |

| — | **数额章带中文单位 ⇒ 每改一次数值就要手补 lingo key**(2026-08-28 一天撞三次:`10%/弃`→`26%/弃`→`22%/弃`) | `manifest.json` 的 `amount` 形如 `22%/弃`,含中文 ⇒ `t_lingo` ① 层要求它在对照表里。数值一改 key 就变,旧的成孤儿。三条修法:① `t_lingo` 对 `^\d+%/(弃\|张)$` 这种**机械可译**的模式改成校验规则而不是查表;② 数额章把单位拆出去(美术侧);③ 写个 `lingosync` 从 manifest 活取补键。**①最省** |
### 1 · 路线 ①~⑦ 之外的小件

| # | 事项 |
|---|---|
| N5 | **量重开摩擦**(几次点击 / 几秒)—— 调研标为「便宜的留存杠杆」,Balatro 的零摩擦重开是核心机制之一,**一直没量过** |
| — | 断点续玩**验收**:真机杀进程/杀标签页后点回来(实装已完,2026-08-24) |
| ⭐ | **启动图标接线** —— 素材已并进来(`assets/app_icon/v4/`, 1024×1024, 08-30 并的 codex-sync5-game-icon), 但 `export_presets.cfg` 的四个 `launcher_icons/*` **仍是空串** ⇒ **现在出包没有图标**。上架前必做, 顺带跑 `--import` |
| — | Web 出包前重跑 `fontsubset.sh`(en-dash/弯引号是新字,语料扫 `data/*.json` 自动收进) |
| — | 复审现役卡里窗口/时机限定的(原则 17:窗口窄 ⇒ 效果强) |
| — | **bot 动作粒度:多选弃 = 1 动作**(2026-08-26 门尸检定案)—— bot 把弃牌张数当动作次数计(`beat_budget.discards: 4`),弃 6 结构性不可达,拆迁(wrecker)因此挂 kit `UNREACHABLE` 声明;真人一次跨区多选 6 张只是一个动作。修完粒度反查会喊删声明。**⑥ 数值批前置**(不修则拆迁定价无仪器) |
| — | **db 交叉校验 `discard_bias`** —— 值必须等于该卡 `effects.when.discards_gte`(2026-08-26 抓到手抄漂移:卡改弃6后 sim.json 还是 3,触发恒 0%;这是「两个家」第 N 例,该上校验锁) |
| C5 | 新卡的状态成本静态检测 —— 引用了状态里没有的量 = **状态缺失**,当场报出代价 |
| M3 | `gig_clocks` 场曲线(正式局仍全平 8/8/8/8)。⚠ 动它会与 `death_spec[0]=0.05` **两处放水叠加** |
| M4 | 槽位显示「这张牌本局贡献多少分」—— 后 3 次商店 100% 是替换场景却没有数值依据 |
| M6 | 终幕专属画面(等素材) |

### 2 · Director 留给迭代的

price.gd 全表精修排序(挂过夜跑;08-26 全量门跑完 rankgen 先顶上)·
跨轮 tiers 复现表(要用户拍哪张脸进哪几轮;前置 = 每 (脸, 轮) 过价,要走全量门)。
context 线仍在等的:C5 死亡段细粒度(等试玩)· C2 A/B(等回传)· 疲劳轴(待留存数据)。

### 3 · 求解器(P2)

> ⚠⚠ **这一节动手前先花五分钟读实现,别信描述** —— 2026-08-15 的清理查出 7 条待办早已完成,
> 其中 S4 还长期挂着「怀疑是 S3 主因」的错标签:**一个错的候选主因比没有候选更糟**。

| # | 事项 |
|---|---|
| S3 | **通关率低估 8.4pp,主因未定位**。仪器已验对(`dpcheck.gd` 用 `player="perfect"`)。⚠ **重跑要预留 2 小时以上**(中途不输出只能靠 CPU 判活) |
| S2 | 三个状态缺失。`first_kind` 已查证 = 真缺口但贵且窄(代价 ×11 状态,收益只覆盖 `setlist` 一张脸)⇒ 先不做,除非 S3 指到它;`prev_kind` 与 `coins` **未查证** |
| S6 | 两个不动点的收敛性。**测不到收敛就停下报告**。⚠ 未查证 |
| S7 | 闭环重放扩到脸 / 经济。初查:`tools/replay.gd` 已认识脸,**可能已部分完成**。未查证 |
| — | ⚠ 建模五件套(`formal`/`dp`/`dpcheck`/`dpdiag`/`udp`)的清理**解锁条件 = S3 结案** —— 仪器还在用时不动仪器 |

### 4 · 仪器债

| 事项 | 现状 |
|---|---|
| `tools/sim.gd` 抖 ~0.1pt(A/A 不可复现) | 根因候选已定位(2026-08-21 评审):`RunLoop.play` 从不给 `Run._blind_rng` 播种。修法一行,修完 A/A 一次验证。全文在 [LESSONS.md](LESSONS.md)「sim 不可复现」节 |
| `tools/probbook.py` 不产出曝光列 | probbook 里那节是手写的、**下次重刷会被冲掉** |

---

## ⚑ 内容的模型成本表

已搬 [`docs/design/capability.md`](docs/design/capability.md) 附录(加卡前先查那张表)。

---

## 明确**不做**的

| 事项 | 为什么 |
|---|---|
| **重算目标分表** | 它是**占位**,是整套重新设计的产物,不是一次算术 |
| **局外 build 任何形式的回归**(主角/券/META/成就) | 2026-08-24 用户拍板整体删除「仅保留局内玩法,我们应该专注」——**别再提议加回** |
| **内购** | 2026-08-26 用户拍板:商业化只做激励视频双入口 |
| **玻璃卡光影** | 用户 2026-08-06 中止。要动先看 `LESSONS.md` 的根因 |
| 写实化 / 换美术场景 | 用户已全部否掉并要求回滚 |
| skip-blind / Tag 奖励 | 用户拍板明确不做 |
| 防沉迷 SDK | 用户拍板(发行面向海外) |
| 参数化训练路线 | 已作废 —— 上界不需要「学」,只需要「解」 |
| 雨的效果 | 用户明确不喜欢(局内舞台背景;失败屏的雨是设计稿钦定的,别误删) |
| **池子搜索 / `tools/pool.gd`** | 消费者从未存在。不是「还没做」,是「不做了」 |
| **用价格压超模卡** | S9 裁定金币边际近零 —— 弱杠杆,且绕开 SOP 就是特例累积 |
| **catch-up meta**(挣扎的玩家获得加速) | 与「DDA 不可见」冲突。要做也得**看起来像奖励,不是像同情** |
| **给锁配钥匙式纯对策卡** | 2026-08-25 对抗批拍板(versus.md):单卡对单盲注 = 商店彩票,**组合破题**才是解 |
