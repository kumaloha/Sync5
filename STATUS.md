# Sync5 · 当前状态

> **这份文档只回答一件事:现在是什么样。**
> 待办看 [TODO.md](TODO.md) · 变更史看 [CHANGELOG.md](CHANGELOG.md) · 经验看 [LESSONS.md](LESSONS.md)
> 规则与美术的**原则**在 [CLAUDE.md](CLAUDE.md) · 设计规格在 `docs/design/`
>
> **最后更新:2026-09-01**(增量见文末「增量快照」节,数字冲突以那节为准)

---

## 一句话

**5 秒乐句 × 扑克 Roguelite**,Godot 4.6.2 / GDScript,竖屏 720×1280。
Lumines 的节奏推进 + Balatro 的构筑。一局 4 段 × 6 拍 × 8 秒 ≈ 4.9 分钟。

---

## 回归状态(2026-08-09)

| 项 | 状态 | 命令 |
|---|---|---|
| 单元测试 | **3330 passed / 0 failed**(2026-08-31 平衡批后)。⚠ 耗时 ~31 分钟 | `./tools/unittest.sh`。⚑ 跑之前先跑四条秒级检查:`parity.py --check && evsync.py --check && counts.py --check && docscan.py` |
| 单元测试 | **3172 passed / 0 failed**(2026-08-30:消耗牌开轴 + 经济收口 + code review 15 个 bug;含 `t_consumable` **119 条**)。⚠ **耗时 ~31 分钟**(从 12 分涨上来)—— 08-29 删掉 goldenvoice 的 SOLVER_BLIND 豁免后 `t_draft` 要真跑一次求解器推演。⚠⚠ 那条豁免 **08-30 又加回来了**(收入重构后金币贴地, 「持币 ≥6」再次不成立)—— **它的成立与否取决于经济参数**, 别当永久声明 | `./tools/unittest.sh`(四判据唯一一份)。⚑ **跑之前先跑三条秒级检查**:`parity.py --check && evsync.py --check && counts.py --check` |
| CI | `.github/workflows/tests.yml`(push/PR 跑 `--import` + `unittest.sh`;**尚未在 GitHub 上验证过一次**,首跑可能要调 Godot 下载链接) | 推送即触发 |
| 小丑牌覆盖门 | **64 张**(2026-08-30 三批转生:首批 9 张 +帕奇欧 · 二批 4 张规则牌 · 三批预支);单卡 kit 直跑 ~10 秒(不含单测)。✅ **kit 已认识消耗牌**(12→16 张全部量到) | `SYNC5_KIT_ID=<id> godot --headless --path . --script res://tools/kit.gd` |
| 内容门 | **08-26 全量门跑完**(4.9h,对抗批新基线):五红三过期全部尸检处置(声明/仪器层,内容零改;细账 CHANGELOG 08-26b),ranking 已由 rankgen 重刷 | `./tools/gate.sh`(增量 = `--changed`) |
| 求解器一致性 | **三关配对差 +0.0**(08-16 分级门) | `godot --headless --path . --script res://tools/pair.gd` |
| 流程/打点/重放 | 0 违规 | 已并进 `gate.sh` |

---

## 三层的完成度

### ① 游戏本体 —— **可玩,已封盘**

一局能完整走完:首页 → 开局(体力/教学判定)→ 4 段 × 6 拍 → **7 次商店**(段中 4 + 段末 3)→ 结算两屏。
⚠ **不是 8 次** —— 末段没有段末商店(Tape 实测 37/37 局)。旧文档里的「8 次」按 7 读,
多出来那一次曾经只存在于 `tools/runloop.gd`,2026-08-09 已修。

| | 现役 |
|---|---|
| 小丑牌 | **64 张** · **消耗牌 17 张**(2026-08-30 开轴 + 二批四张规则牌 + 三批预支, 见 [consumables](docs/design/consumables.md))。⚠ 这一行历史上写错过多次(23/61/63/76 都出现过)—— **一律以 `data/*.json` 的计数为准, 别手抄** |
| Boss 脸 | **28 张在池**(按 `tier` 计 8/8/8/4,以 `faces.json` 有无 `tier` 为准)+ 5 张无 tier 未入池(unplugged/static/rotation/cover/freshsheet)。⚠ 此前三处写着 29/30/28 打架(2026-08-21 评审),以本行为准 |
| 主角 | ~~8 个~~ **已删除**(2026-08-24 局外 build 整体删除,含被动层与全部立绘素材) |
| 结构 | 4 段 × 6 拍 × 8 秒,每 3 拍一次商店 |

**UI/美术已完成并暂告段落**(霓虹舞台风格已锁定)。唯一未达标的是玻璃卡的**光影**
—— 用户 2026-08-06 明确表示不满意后中止,现状与根因记在 `docs/design/` 与记忆里。

### ② 模型(求解器) —— **能跑,但绝对值不可信**

```
core/pattern.gd + settle.gd    计分       ← 游戏与模型**共用同一份**
core/beat.gd                   一拍的转移  ← 游戏 + 全部探针共用
tools/runloop.gd               一局的循环  ← **只给探针用**(游戏是实时异步的)
tools/solver.gd                拍内枚举 C(8,5)=56 切法 × 弃牌子集, 调真实计分
tools/bot.gd                   玩家策略(完美玩家 / 规则 bot)
```

**拍内是精确的**(与 draw poker 的领域标准一致,`pair.gd` 逐手验过)。
**跨拍是 λ-平衡贪心**(近似)。**构筑是手写规则**(最粗)。

⚠⚠ **当前最重要的限制:通关率的绝对值系统性低估约 8.4 个百分点**(z≈−3.5,
加样本加档数都修不掉)。原因已部分定位(见 [LESSONS.md](LESSONS.md) 与 `docs/design/solving_history.md`),
**但主因仍未完全找到**。

> **所以:模型给出的相对排序可信,绝对难度不可信。**

### ③ 生成器 —— **只生成目标分,而且那两张表是占位**

`tools/curve.gd`:不死局录分 → 按 `death_spec` 查分位数 → 反解目标分。

⚑ **2026-08-14 补锚**:generating.md 的 `θ` 里 **`τ`(手速)那一维不再是零数据** ——
真人一拍 **2.12 个动作**(弃牌 1.11 + 交换 1.01),而模型给 **7**(弃 4 + 换 3)——
**模型的动作空间是真人的 3.3 倍**;动作时刻中位 5.0s、每拍最后一个动作中位 5.7s(8s 拍)。
⚠ 两个方向相反的偏差:弃牌维模型曾**低估**真人(已校准),交换维**高估**真人
(能用 3 次,真人只用 1.01)——**净方向要实测,不能从这两个数直接推**。
仍零数据:`λ` 前瞻 · `ε` 分布形状 · `d` 深度。

⚠⚠ **`sim.json bot_targets` 是仪器刻度(占位),`run.json section_targets` 是玩家面对的那张**。
⚠ **现行值 = `[420, 510, 960, 1680]`**(2026-08-30 核 `data/run.json`)——
不是本文此前写的 `[420,1500,3100,5600]`(那是 08-15~17 真人重锚的值,**08-29 已被两次改动覆盖**:
先按杠杆标准降到 420/1280/2400/4200,当天又在**被污染的 `BASE_CHIPS`** 上反解成现行值)。
⇒ **现行表是已知作废、待重定的**(状态见 `docs/design/difficulty.md` 末节)。
整套目标分体系仍要跟着新目标函数(留存最大化)重新设计 —— 但**别再说「真人数据为零」**。

而配置 `c` 的其余维度(脸的排布、经济、货架)**全是手写的** —— 那是**设计**,
不是待自动化的技术债(用户 2026-08-08 明确)。

---

## 数据与配置

**所有数值与内容在 `data/*.json`**,`core/db.gd` 校验 ——
未知键/坏引用**在测试里直接红**(⚠ 是**测试期门禁**,运行时不拒绝启动;理由见 `core/db.gd` 文件头)。

| 文件 | 管什么 |
|---|---|
| `jokers.json` | 小丑牌(效果 DSL,`core/fx.gd` 解释) |
| `faces.json` | Boss 脸(参数表 + `tier` + `proof` 通路 + `weak_upper_bound`)。**纯数据,散文一律在 `docs/design/blinds.md`,`db.gd` 拒绝散文键** |
| `run.json` | 关卡结构 · 目标分 · `death_spec` · `beat_budget` |
| `economy.json` `sim.json` `ui.json` `tape.json` | 经济(含 `joker_upgrade` 升级曲线)/ 机器人信念 / 界面坐标文案(blindcard·jokercard 与脸·卡**交叉校验**)/ 打点开关(含 `upload` 回传节) |
| ~~`characters.json` `tickets.json` `assets.json`~~ | **已删除**(2026-08-24 局外 build 整体删除) |
| `director.json` | B 轴剧本 + `context` 四开关(novelty/streak_shift/returning/explore_shelf) |
| `ranking.json` | **仪器输出**(`price.gd` → `rankgen.py`),手改无效;校验守池脸完备性 |
| `lingo.json` | 中→英对照表(**改英文文案改这里**;`t_lingo` 守完整性) |
| `tutorial.json` `boons.json` | 教学脚本(4 步,`require` 动作门)/ 盲注 boon |

**改卡改平衡 = 改 JSON**,不用动代码。

---

## 工具链(`tools/`,55 个 .gd + 4 .py + 1 .sh)

| 工具 | 干什么 | 耗时 |
|---|---|---|
| ⚠⚠ **08-27~08-30 的探针读数** | **全部作废, 要重跑** —— `RunLoop` 的「达标即收工」在 `o.targets` 为空时恒真 ⇒ **不传目标表的 11 个探针一局只打 4 拍**(应有 24)。受影响:`curve`(关卡分那张表)· `kit` · `price` → `ranking.json` · `gate.sh` · `coin` · `addit` · `blind` · `wallet` · `decay` · `decomp` · `udp`。⚠ **`sim.gd` 不受影响**(它传表)。已修 + `tests/t_run.gd::_t_probe_runs_full` 锁住 | — |
| **`gate.sh`** ⚑ | **加了内容就要过的门**(测试+脸覆盖自证+**小丑牌覆盖自证**+单调性+哨兵+流程+打点+重放+尺子) | ⚠⚠ **实测 ~4.5 小时**(2026-08-15 逐步计时,见下)。~~15 分钟~~ 那个数是 **S10 之前**的,**过期了一个数量级** |
| **`unittest.sh`** | 全量单测 + **四判据唯一一份**(exit 0 · 0 failed · SCRIPT ERROR 0 · 非白名单 ^ERROR 0 · 通过数 ≥ 2400);gate.sh 与 CI 都调它 | ~13 分钟 |
| `pair.gd` | 守「求解器 = 游戏代码」,三关递进 | ~3 分钟 |
| `curve.gd` | 生成器:录分 → 反解目标分 | — |
| **`prior.gd`** ⚑ | **先验层(2026-08-14 新)· 三个模式**:①(默认)牌型分布 / 谓词 `p̂` / **规则牌 Δp**;② `SYNC5_PRIOR_MODE=discard` **弃牌兑换率**(b=0→4 期望分 +82%)+ 求解器弃牌盲区;③ `SYNC5_PRIOR_MODE=shelf` **货架曝光**(解析零采样)。判定/谓词/抽卡**一律不重写**,全部调 `core/pattern.gd` + `core/fx.gd`,或对实现做解析求解。自带闭式自检、快慢路径逐次对账、跨模式交叉自检。规格 = [`docs/design/prior.md`](docs/design/prior.md) | ①~440s @20万 · ②~150s @400手 · ③ 秒级 |
| `sim.gd` | 全队列通关率,**自带尺子自检**(非零退出)+ **卡面覆盖率四道闸门**(见下行) | **~275s**(14 队列 × 1000 局;~~107s~~ 已过期) |
| **`run.sh probe <名>`** ⚑(2026-08-30 新) | 带锁跑**任意**探针(`tools/<名>.gd`)。此前锁只覆盖 `sim`/`unittest` 两个, 而 tools/ 下五十多个探针都是长跑 ⇒「长探针一律走 run.sh」这条纪律对其余的**根本没法遵守** | 随探针 |
| **`lift.gd`** ⚑(2026-08-30 新) | **逐卡因果通关率** —— 把一张卡钉进支援槽 1(每拍复位), 与「三个槽全自己买」的基准**配对**比通关率。⚠ 与 `report.gd` 既有的 `support lift` 是两回事:那条是**观察性**的(bot 有钱才买得起 ⇒ 反向因果)且有 `n≥50` 门槛把冷门整族滤掉。⚠⚠ 第一版用 `cfg.prefer` 不成立 —— 一张卡被抽到的概率只有 ~30%, 实测**持有率 0~7%**, 量的是噪声。⇒ 钉槽位, 持有率 100% | ~23 分钟(61 张 × 1000 局/臂) |
| **覆盖率报表** ⚑(`Report.print_coverage`, 2026-08-30 新) | 逐张卡列**上架 → 装上 → 赢局里装过 → 赢局末仍在槽位**,把「没进赢局」拆成四种该改的东西完全不同的原因,并印**装机率全表**(连续量)。⚠ 默认**自然口径**(摘掉 `dead:N` 强制试用队列 —— 那是因果通道, 不能同时当观察值)。**现读数:自然口径 69/69 = 100% 进过赢局**(`royalty` 的病根 = `_amt` 白名单漏了 `coins_factor` ⇒ 负 EV ⇒ 永不购买, 已修 + 落成 parity 第 ⑤ 层) | 随 sim |
| `price.gd` | 每(脸, 轮)一个价 | ⚠⚠ **2026-09-04 查明单局贵在哪(tools/pace.gd)**:完美玩家装了小丑牌后每拍 ~2.1 s(`best_score` 每个 8 选 5 组合过一次 `Settle.run`, 1.8 万次/拍 —— 固有), 单局 45 s ⇒ 52 放置 × 150 局 × 2 臂 ≈ **190 h 单核**。`SYNC5_PRICE_SHARD=i/n` 分片 + `rankgen.py` 多日志合并 ⇒ ~~10 核过夜~~ 实测十片满载一天(每个 godot 不止一核)。**09-04 夜:求解器结算记忆**(`Solver.memo_begin`, 同拍同槽位同组合只结算一次, 逐位精确)⇒ 空载单局 ~10 s。**09-05 实测六片全表 11 h**:M5 是 4 性能核 + 6 能效核, 六片有两片在能效核上 ⇒ **分片固定 4 片**, 全表估 5~6 h;日常只跑改过的脸。`ranking.json` 09-05 13:03 重生成(基准 16370)。~~09-01「19.4 小时 7 行」~~是四链抢机器的污染读数 |
| `addit.gd` | 可加性检验(脸能不能相加) | ~10 分钟 |
| `coin.gd` `blind.gd` `warm.gd` `lam.gd` `attrib.gd` | 金币影子价 / 盖牌族 / 养牌价值 / λ 扫描 / 分数归因 | — |
| **`decomp.gd`** ⚑ | **五项难度分解(2026-08-14 新)**:回答「**难在哪**」而不是「多难」。四臂共用随机数(基准/无脸/ε 退化/λ=0/ORACLE),① 数值压力 ② 上界压缩 ③ 技巧惩罚 ④ 信息惩罚 ⑤ 运气暴露 **第一次并排在一张表上**。⚠ 表只让形状可见,**不说好坏**(判断权在设计者) | ~70s/局 × 5 臂 |
| `formal.gd` `dp.gd` `dpcheck.gd` `dpdiag.gd` `udp.gd` | `docs/design/solving.md` 的建模验证 | 各 10-25 分钟 |
| `replay.gd` | **L2 决策重放门**(建模侧完整性) | 秒级 |
| **`hundred.gd`** | Director 100 关 × 4 墙验证(不重复/池内/min_run/非空) | 分钟级 |
| **`rankgen.py`** | `ranking.json` 的**唯一合法来源**(吃 `price.gd` 日志;时间族/改目标分族设计性压最狠端 + 池脸兜底) | 秒级 |
| `probbook.py` | 概率账本重刷(⚠ 仪器债:不产出曝光列,分母恒为持有拍) | 秒级 |
| **`cf.gd`** ⚑ | **反事实重放:每张支援卡的实测 EV(分/拍)**(2026-08-28 固化 —— numbers.md §8.1 用过这把尺,当年手算没留下)。同一拍跑两次 `Settle.run` 只差 slots,**判定一行不重写**(照走 `core/fx.gd`)。**两个口径**:空构筑(可比)/ **真实构筑**(装在玩家当时的牌组上 = §8.1 的口径,**定价看它**);再按**早段/晚段**拆(检验「奖励分后期过气」那句 —— 实测两头都反)。量不到的分类标注不出值:ctx 缺 · 成长/decay/hold 卡(state 跨拍)· 金币通道(单独报 ◆/拍)。**产出 `docs/design/evbook.md`(手改无效)**。⚠ 绝对值有系统偏差,**以灯牌归一 + 只在族内比** | ~2 分钟 |
| **`tau.py`** ⚑ | **真人 Tape 里的时间读数(2026-08-28 新)**:① τ 分量(`τ_pick` 0.56 · `τ_disc` 0.66 · **`τ_swap` 1.44**)与认知时间(开拍→首个动作 **中位 3.37s / 8s**)· ② **熟练度趋势 = 判据**(随熟练降 = 教学问题;不降 = 结构性难度该进定价 —— 实测 19 局跨 22 天是平的)· ③ 每拍动作构成与**实际弃牌 1.28 张**(先验层定价用 b=4)· ④ 按持有 Target 分组的命中率(干净口径,不按结算牌型分组)。`solving.md §3` 那个「τ 待 Tape 量」自此有读数 | 秒级 |
| `tapeserver.py` / `webserve.py` | Tape 回传参考接收端 / 手机试玩 HTTPS 服务(会话级进程,要重起) | — |
| 各 `*_sheet.gd` `shoot.gd` `glass.gd` | 截图探针 | — |
| **`counts.py`** ⚑ | **文档里的卡数 vs `data/*.json` 的真值**(2026-08-30 新)。⚑ 立它是因为 STATUS 那一行历史上写过 **23/61/63/76 —— 没有一个对**(真值 69), 而它自己的注释就写着「以 data 计数为准」。⚠ 自动豁免**历史记录**(带日期/口径/冻结/曾的行)—— 那些写的是当时的数, 改了是篡改证据 | 秒级 |
| **`run.sh`** ⚑⚑ | **长探针的带锁启动器**(2026-08-30 新)。记启动时的工作区指纹, 结束时比对 —— 跑着的时候文件被改过就**退 90 并宣告读数作废**。⚑ 立它是因为「跑着别改」这条纪律 2026-08-30 一天内**靠自觉守了五次都没守住**(四次半新半旧, 一次 `Stack underflow`)。`--status` 看锁, `--unlock` 强解 | 随被跑的探针 |
| **`parity.py`** ⚑⚑ | **四层一致性扫描**(2026-08-30 新, 每一层都是当天某个 bug 教出来的):① 规则入口(函数名在 view/ 与 tools/ 的调用数)· ② action 键(数据驱动, 上一层扫不到)· ③ **写了没人读**(三张消耗牌因此在游戏里是空白的)· ④ **卡面 vs 数据**(一天撞三次「卡面说谎」)。「规则在游戏里、不在模型里」本项目栽过**七次**, `core/joker.gd` 早写着方法(「grep 就能数清两侧是否对齐」)—— **有尺没用**, 所以做成可执行的:数每个规则入口在 view/ 与 tools/ 的调用数, 一侧为零就红。首用即抓到帕奇欧(只接了游戏侧, sim 里是纯废卡)。已进 `gate.sh`。⚠ **加新钩子时要同时加进 ENTRIES 表** —— 那张表本身就是这条纪律 | 秒级 |
| **`ccf.gd`** ⚑ | **消耗牌的反事实定价**(2026-08-30 新)。⚠ **不能用 `cf.gd`** —— 那把尺量「装着一整局的卡」(每拍取差), 消耗牌**一局只生效一次**, 直接套会高估 24 倍。本尺只算单次 Δ分。首用即抓到 5.3 倍定价错(快闪 174.7 分/◆ vs 副歌 33.1, 价格却一样)。⚠ 牌堆/商店类**量不到**(不改这一拍的分), 显式标注不出值 | ~1 分钟 |
| **`evsync.py`** ⚑ | **evbook 的实测 EV → `sim.json` 的 `ev.measured`**(2026-08-30 新)。⚑ **2026-09-04 起它的用途变了**:重放估值的卡(39 张)拿它当**先验**(无历史时 = 它), 手写臂那 17 张仍当地板(权重 0.5)。⚠ 现在**读 cf 的备注列**, 「本尺不适用 / 读数无效」的行不进表(穷开心 112.4 曾进表当地板 ⇒ 装机率 43% 而 lift −13pt)。**手改无效** —— 改数据的唯一路径:`cf.gd` 刷 evbook → `evsync.py` 同步 → 跑 sim。`--check` 在 `gate.sh` 与 `t_db` | 秒级 |
| **`pace.gd`** ⚑(2026-09-04 新) | **完美玩家 + 商店一局逐拍计时** —— 回答「price / curve 单局为什么贵」。首跑答案:无小丑牌 70 ms/拍(分数记忆之前 112), 装了小丑牌 2000+ ms/拍(固有, 见 price 行)。`SYNC5_PT_N` 局数 · `SYNC5_PT_NOSHOP=1` 关商店对照 | ~1 分钟/局 |
| **`Bot._card_ev_replay`** ⚑⚑(2026-09-04 新) | **bot 买牌估值 = 反事实重放**:在本局已打过的每一拍上跑两次 `Settle.run`(装/不装), 差值均值 + `blend_w` 收缩到先验。效果卡 39 张走它, 成长/衰减/持有/概率/货架 17 张留手写臂(`DB.replay_valued` 按数据判, 与 `validate_sim` 共用)。历史在 `st.hist`(`RunLoop._tally` 记, `Beat.settle` 现在把结算 ctx 随 outcome 返回)。⚠ 它把「加了新操作码忘了教 bot」这一类事故对效果卡整个关掉 | 每店 ≤8 卡 × 24 拍 × 2 结算 |
| **`cfg.prefer`**(sim cohort) ⚑ | **强制 bot 试用指定的一批卡**(2026-08-30 新)。用途:分辨**「内容真的弱」与「仪器看不见」** —— 一条 cohort、五分钟, 而按错误结论重做内容要几天。首用即翻案:46 张「从没进过赢局」的卡, 强制试用后通关率 42.7~45.4%, **全部高于正常路线均值**。⚠⚠ **它是实验者的干预, 不能同时当观察值** —— 覆盖率报表因此默认把 `dead:N` 摘出去(不摘的话「有没有人装过」恒为真)。⚠ `dead:1..4` 的 44 张名单是**修 `ev.measured` 之前**列的, **已过期**(现在这四条队列通关 34.5~39.7%, 与正常路线均值持平而非高于) | 随 sim |

#### lift 第二趟(**2026-09-04 16:xx, 调数后 + kit 地板 1.0**)

基准 13.2%。r **0.18** · ρ **0.48**(零点 0.13 / 0.40;手写臂 0.22 / 0.33)。skint 装机 2 → 20%(+13.2pt, 买对了)· momentum 9 → 18 ·
fastforward 11 → 16 · vinyl 4 → 11。**仍不买的强卡:ensemble +43.5pt 装机 4%**(它是 10◆ 特例价, bot 局末余额中位 3◆ —— 买不起, 不是估值问题;白送 +43.5pt 与 10◆ 自洽)。
最弱 recycle −3.2(十倍点数版)。

#### lift 逐卡因果通关率(**2026-09-04 换尺零点**, 快照 3 = 调数前, 61 张 × 1000 局/臂, 5992 s)

基准(bot 自选)13.5%。**与 bot 装机率的相关**:Pearson r **0.13**(08-30 手写臂 0.22)· Spearman ρ **0.40**(0.33)——
线性没抬、排序略抬;真变化在**两头**:「高用低值」那一桶(skint 43%/−13 · perkeo 84% · digger 60% · rehearsal 80%)
**清空了**(bot 不再重仓坏卡), 「低用高值」还在, 而且**全是手写臂的卡**:ensemble **+42.9pt**(装机 3%)· skint
**+13.3**(2%;08-30 是 −13 —— 经济收紧后金币上限不再咬人, 它的 `hoard` 先验 34.7◆ 已过期)· curtain · tipjar · vinyl。
⇒ **重放修好了效果卡那 39 张, 剩下的病集中在 17 张手写臂的先验上** —— 下一步用 kit 的总分效应给它们当地板(kit 量得到
成长/持有卡的到达值, cf 量不到)。最弱:recycle −4.1(z −3.1, 调数前的 2× 点数版)。中位 +3.1pt。

#### sim 全队列基线(**2026-09-04 换尺零点** —— 估值改重放 + 09-03 五选一修复 + evbook 重刷;快照 3 = **调数前**)

⚠ 与 09-01 表**不可比**(尺子换了):这一趟是零点。括号里是相对 09-01 的位移, 只看方向。
最大位移 **lonewolf −5.5pt**(独狼队列的支援卡由重放估值挑, 与 09-01 的手写臂挑法不同 —— 待 lift 出来看它的支援卡换了什么)。

| 队列 | full clear | 队列 | full clear |
|---|---:|---|---:|
| baseline(无小丑) | 0.0% | adaptive:triplet | 14.3%(−2.2) |
| adaptive:twin | 5.3%(−3.0) | adaptive:lonewolf | 3.9%(**−5.5**) |
| adaptive:stair | 9.3%(+0.8) | adaptive:wolfpivot | **23.3%**(−1.2) |
| adaptive:mono | 15.7%(+0.8) | adaptive:anytarget | 13.5%(−1.0) |
| random | 0.0% | nocons | 6.4%(+0.7) |

尺子自检 ✅(random 0.0% ≤ 5%, 最强 23.3% ≥ 3%)。经济带:局末余额中位 3◆ 达标 · 弃牌 0.80 张/拍 ·
**软破产拍 25.1%(stair, 带 ≤15%, 偏离 +10pt)** · 牌型金币斜率为正。
覆盖:64/64 上架且装过;「装过但从没赢」2 张(rehearsal / earlyout);冷门(装机率 <5%)23/64 ——
其中 harmony 3% / recycle 2% / stageexit 3% / mirror 4% 是**调数前**的读数;**调数后 sim(同日 10:4x)**:通关率全队列位移 ≤0.3pt, 装机率 harmony → 12% / mirror 5% / stageexit 4% / recycle 3%, 冷门 23 → 22 张。

#### sim 全队列基线(**2026-09-01 新刻度零点**,`tools/sim.gd` 1000 局/队列)

**留基线是 SOP 第 6 步的一部分**(2026-08-28 补)—— 漂移要有上一趟的读数才算得出来。

⚠⚠ **这一趟换了尺子, 所以它是零点而不是一次漂移读数**:`sim.json bot_targets`
从 `[390, 330, 1520, 3190]` 换成 `[369, 768, 2106, 5147]`(旧表 **S2 比 S1 还低**,
「S2 完全没有压力」的根因是表非单调, 不是难度调错)。**2026-09-01 之前的所有 sim 读数
与之后的不可比。**

| 队列 | full clear | 队列 | full clear |
|---|---:|---|---:|
| baseline(无小丑) | 0.0% | adaptive:triplet | 16.5% |
| adaptive:twin | 8.3% | adaptive:lonewolf | 9.4% |
| adaptive:stair | 8.5% | adaptive:wolfpivot | **24.5%** |
| adaptive:mono | 14.9% | adaptive:anytarget | 14.5% |
| random | 0.1% | nocons | 5.7% |

**同刻度上的两步漂移**(2026-09-01 当天,尺子未动 ⇒ 可比):
① **付费洗牌退役** —— 只打到 `lonewolf`(11.8 → 7.7,≈3.7 SE):它发誓不弃牌 ⇒ **不弃牌就拿不到新牌**,
洗牌是它唯一的换手手段。其余队列 ±0.5 内。经济几乎没动(bot 本来就很少攒得出 3◆ 来洗)。
② **消耗牌全部自动触发** —— 14 条队列里 **10 条上移**,均值 ≈ +1.1pt。
机制上是必然的:使用率 **96.6% → 100%**(买 = 用),差的就是那批「买了没打成」的。
⚠ 对 bot 只值 1pt,是因为 **bot 从不忘**;真人侧的摩擦(忘记点、点错拍、没注意到栏位)
这把尺子**结构上量不到** —— 那一半只能靠试玩。

尺子自检 ✅(random 0.1% ≤ 5% · 最强 24.5% ≥ 3%)。
**难度形状终于对上了**:S2 死亡率 **16~44%**(设计谱 28%)—— 上一趟是 0~5%;S3 26~59%(设计 50%)。
**消耗牌的因果值**:`anytarget` 14.5% vs `nocons` 5.7% ⇒ **+8.8pt**(同配置同种子, 唯一干净的一条)。
经济健康带:① 局末余额中位 **2◆ 达标** ② 弃牌 0.72 张/拍(1◆ 对 bot 无感, 未下移)
③ 软破产 **29.18% 偏离 +14.2pt**(带 ≤15%)④ 牌型金币斜率为正 达标。
⚠ ③ 那条带是「余额中位 49◆」的富裕时代定的, 在精瘦经济下它测的是 **bot 的花钱策略**
(贪心 bot 永远花到 0), 不是玩家体验 —— 更直接的量是「想弃但弃不起」的拍占比, 是不是病归真人试玩。

<details><summary>上一趟(2026-08-28,旧刻度,不可与上表比)</summary>

| 队列 | full clear | 队列 | full clear |
|---|---:|---|---:|
| baseline(无小丑) | 0.0% | adaptive:triplet | 48.5% |
| adaptive:twin | 11.5% | adaptive:lonewolf | 0.0% |
| adaptive:stair | 15.8% | adaptive:wolfpivot | 10.5% |
| adaptive:mono | 9.4% | adaptive:anytarget | 17.7% |
| random | 1.0% | | |

经济健康带:①局末余额中位 49◆ **偏离 +40◆** ②弃牌 0.99 张/拍 ③软破产 0.01% ④斜率为正。

</details>

## ⚠⚠ 门的实测耗时(2026-08-15 逐步计时)—— **上表的秒数整体过期,别信**

| 步骤 | 实测 | 备注 |
|---|---:|---|
| 单元测试 | **~55 分钟**(2026-08-27 实测) | S10 前 <300s → 744s → 现 ~3300s。⚠⚠ **距 `unittest.sh` 的 `timeout 3600` 只剩 ~5 分钟余量** —— 多 agent 并发时已撞到天花板作废重跑过一次。下次扩测试要么提 timeout、要么分片 |
| 覆盖自证 + 单调性 + 哨兵(`gate.gd` 全量) | **10503s ≈ 2h55m** | 贵在 solver 通路每张脸都跑完美玩家 |
| 小丑牌覆盖自证(`kit.gd`,69 张) | **~1h15m** | 2026-08-13 记的是 611s @57 张 |
| 流程 / 打点 / 重放 / 尺子 | ~10 分钟 | 这一段没怎么变 |
| **合计** | **≈ 4.5 小时** | 文档里写的 15 分钟**差一个数量级** |

⚑ **根因是 S10(扩弃牌枚举,2026-08-14)**:它把求解器拖慢约 2.4 倍,而门里最贵的两段
(脸的 solver 通路、卡的 solver 通路)**全部走求解器**。
⚠ **连带,「快路径」这个说法也没了** —— `gate.sh <face_id>` 与 `SYNC5_KIT_ID=<id>`
文档里写「十几秒」,实际因为**第一步就跑全量单测**,地板是 **12 分钟以上**。
⚠ 想只验一张脸而不重跑单测,直接调底层:
`SYNC5_GATE_FACE="<id1>,<id2>" godot --headless --path . --script res://tools/gate.gd`
(**接逗号分隔的列表**,多张一次验完 —— 三张脸各跑一次门会白烧两遍单测)。

**四层真相各只有一份**(2026-08-09 补齐后两层):
`core/beat.gd` 一拍的转移 · `tools/runloop.gd` 一局的循环 ·
**`tools/probe.gd`(`Probe`)+ `tools/shot.gd`(`Shot`)一次实验的骨架** ·
**`tools/stat.gd`(`Stat`)一份统计**。

⚠ **探针数量本身不是问题** —— 41 个文件 / 6777 行,它们是**不同的实验**,各占一个文件是对的。
真正要守的是「别再抄第五层」:新探针一律 `extends Probe`、统计走 `Stat`、截图走 `Shot`。

---

## 真人数据:**有了但薄**(2026-08-12 起)

磁盘上 1067 局 Tape 日志**全部是探针产物**(时长 >60s 的只有 1 局,而真人一局 ≈ 294s)。

**采集侧已实装并验过**(`core/tape.gd`,21 类事件,两个探针守着),**分析侧一行没有**。

⚠ 因此这一整族**全部零输入**:发挥系数 · 手速 `beat_budget` · 人群权重 · 三因子权重 · 留存。
**在它们到位前,模型的绝对值不该被当真。**

---

## 设计文档地图

**目录页 = [`docs/design/README.md`](docs/design/README.md)。**

2026-08-09 去号按主题命名;篇数与状态**只在 README 里维护**(这里不再抄第二份)。
2026-08-26 文档洁癖清理:退役篇已删(meta/候选图谱/GPT 提示词包/评审原始稿),
现役篇的主角/券/旧数字已扫(细账 CHANGELOG);任何残留过时处**以代码与 `data/*.json` 为准**。

---

## ⚑ 2026-09-04 下午 · 修尺子批之后:**bot 侧读数第三次换刻度, 这次换的是尺子本身**

bot 的买牌估值从手写臂改成反事实重放(39 张), `Pattern.score_five` 加了分数记忆(结果逐位不变, 只是快),
预支还款收进 `Run.repay_debt`(游戏侧此前每段重复扣款), κ 拆两侧(缺省相等)。
⇒ **lift / cf→evbook / price→ranking / curve / sim 基线全部要重跑**, 重跑前**别再动 bot/core**。
全量单测在工作区**快照**上跑(做法见 CHANGELOG 09-04 下午「验收」):结果 **3337 通过 / 0 失败 · rc=0 · SCRIPT ERROR 0 · 非白名单 ERROR 0**(06:47, 快照含全部改动;含重放批但不含预支收口的快照 1 是 3333/0)。
秒级检查(parity / evsync / counts)绿;分域单测:t_draft 新七条 · t_pattern/t_wild 76/0 · t_run/t_phrase/t_db 457/0。

## ⚑ 2026-09-04 上午 · 第一次拿对的尺子调数(N2/N3 七条红 + N4 判定)

五张卡回到本稀有度正常带(kit:digger 22.5% · harmony 17.6% · recycle 14.3% · stageexit 7.1% · mirror 5.9%),
三张脸按**真人段级咬合率**定门槛(trilogy 4 种 9.0% ✓ · trilogy4 税 0.5 13.6% ✓ · ration 10 N=400 9.5% ✓),
countdown 进 `weak_upper_bound`。细账 CHANGELOG 同日上午条目。
**重跑批进行中**(快照 3, 07:5x 起):sim 基线 · lift · price 10 分片(单局 45 s ⇒ 约 11~15 h)。
⚠ 快照 3 是**调数之前**的数据 —— sim/lift 出来的是「换尺后、调数前」的基线, 调数后要再跑一次 sim(5 分钟)+ lift;
price 里 ration/trilogy/trilogy4 三个放置要用 `SYNC5_PRICE_ONLY` 单独重量后并进 rankgen。

## ⚠⚠ 2026-08-31 晚 · 试玩批之后:**所有以 bot 为尺的读数再次作废**

「提前结束」整块退役(每段打满 6 拍, 少一个经济出口)· 唱片退役(消耗品栏搬进它的位置)·
商店 **5 选 1**(3 小丑 + 2 消耗抢同一次购买 = 供给收紧)。三层都动了 ⇒
**门 / sim / lift / price→ranking 全部要重跑**, 下面那一节的快照是**这批之前**的。
⚑ 本批只跑了单测。细账 CHANGELOG 08-31(二), 交接记忆 `sync5-session-20260831b`。

## ⚑ 2026-08-31 快照(交接用;⚠ 试玩批之前)

**小丑牌 64 张 · 消耗牌 17 张**(三批转生:首批 9+帕奇欧 · 二批四张规则牌 · 三批预支)。
覆盖率**自然口径 64/64 = 100%**(「上架但没人装」0/64), 消耗牌 17/17 全部用到;
尺子自检 ✅(random 2.0% ≤ 5%);各路线通关率 17~46%, 地形极差 **44.4 pt**;
局末余额中位 10◆;消耗牌这一层值 **+18.8pt**。
⚠⚠ **08-27~08-30 期间所有以 bot 为尺的读数曾整体作废**(`RunLoop` 截断 + 三批转生),
本快照是修复并重跑之后的。全量门 / price→ranking / curve(bot 尺)在 08-31 会话末重跑中。
⚠ `curve.gd perfect`(真人尺度那张表)**未跑** —— 修复后按拍数比例约 9 小时, 且它的产物
无论如何要等真人试玩才能验。

## 三条一眼就该知道的现状

1. **`run.json section_targets`(现行 `[420, 510, 960, 1680]`,已知作废待重定)与 `sim.json bot_targets`(仪器刻度)是两张表** ——
   **别用 sim 验前者**(它结构上读的是后者:`sim.gd` 的 `TARGETS` 来自 `DB.sim()["bot_targets"]`,
   一路传到 `RunLoop.Opts.targets`,`run.json` 的那张在 sim 里**从不参与判生死**)。
   ⚠⚠ 这条 08-16 实测坐实过一次(S1 921→560 跑完 sim **逐字不变**),而 08-30 的交接
   仍写着「把 curve 反解的表填进 run.json 跑一次 sim 验证」——**那一步是空转**。
   死亡率形状 `death_spec` 是设计常量。
2. **模型绝对值不可信,只信相对排序** —— 通关率低估 8.4 个百分点,主因未完全定位
   (探针世界缺 boon 那半已于 08-22 修复,新基线起 boon 缺省掷)。
3. **真人样本薄**(用户本人十来局,新手零局)—— 发挥系数仍禁拍初值、禁用机器人代算;回传通道已建但端点未部署。
   ⚠ 本节 2026-08-21 重写:旧版三条(「目标分占位 / 真人数据为零」)已被同文件下文推翻却没打标。


---

## 增量快照(2026-08-10 ~ 08-25,与上文冲突时以本节为准)

- **2026-08-25 · 对抗批实装完毕、未量**(细账 CHANGELOG 08-25b;设计总纲 docs/design/versus.md):
  盲注池 28→26(红灯/接线退役)+ **11 张新脸机制完工暂存**(无 tier, 待 ranking 重跑入池);
  一口气/限流/打烊按**张数**重铸 · 封条/双封随机封;小丑 **13 新 + 3 改**(63→76 张在库,
  素材全配, 82 条资产校验绿);新 DSL:chance/target_streak + 四个 hold 键 + 两个 per 源;
  序列杀伤预算(attack_axes 自动推导, 探针/Director 共用)· 开局亮整局(特写路线行)·
  胜负音效(victory/defeat.mp3)。流程探针 43/43 · hundred 400 放置 ✅。
  ⚠⚠ **所有仪器读数再次过期**(RNG 消耗点新增:随机封/掷类脸/赌具预掷;三张脸语义变了):
  sim/curve/gate/price/ranking 全部要重跑, bot_targets 大概率再重锚。
  ⚠ 三个素材库目录(expansion_20260825/redesign_20260825/fh100_dedup_kept_47)已进导出排除。
- **2026-08-24 · 局外 build 整体删除**(细账 CHANGELOG 08-24):用户拍板「仅保留局内玩法」——
  主角/券/META 资产(宝石·唱片·彩带机·应援团·赛季)/成就·荣誉·档案栏**代码与素材全删**(172+ 文件,含
  assets/characters 77MB);保留图鉴、Director 千人千面、教学关、局内金币。结算链收窄(无主角被动层)、
  开局入口 `choose_character(i)` → `start_run()`、存档 v2 清退役键、曲池十首全开、首页页签 4→2。
  ⚠⚠ **仪器又是新基线**(少一次抽主角的 RNG 消耗 + 结算链变短):sim/curve/gate/price 旧读数全部不可比。
  上文各节凡提到主角/券/资产/荣誉的,一律按本条为准。画风切换一事搁置未动。
- **2026-08-21 · 全面评审 + 按批次修复**(细账 CHANGELOG 08-21;评审报告 `docs/review_20260821.md`):
  A 玩家可见 ✅(8 张卡升级无效 · 重开绕过 Director/min_run · 教学商店没收 · 资产页错位 · 四小件)·
  B 发布线代码侧 ✅(导出过滤 + .gdignore;**签名/包名/版本归用户**)· D 校验补洞 ✅ · C 仪器 ✅(gate.sh 四判据 ·
  gate.gd 曲目税 · trilogy 摘豁免 · RunLoop boon 入参/播种 · pair 退出码)· E 文档 ✅(TODO 776→254)· **F 性能未动**。
  批 A+D 宽探针 1976/0;**全量门仍未跑 —— C 批改了门与 RunLoop,下一次全量门是新基线,不可与旧读数直接比**。
- **2026-08-21 深夜 · 外部审查逐条复核修完**(细账 CHANGELOG 同日晚节):HIGH 四条属实全修(`Run.reset_section_state()` 一份 ·
  `fork()` 补全 + 逐字段断言 · `upload` 可选 · 五处文档矛盾 + tech.md 四个旧示例块删)· H5 CI 补上 · MEDIUM 全修(tapeserver 路径门 ·
  存档原子写/.bak/版本键 · probbook/replay 扫 sent/ · shop 升级槽钉位 + 按钮只建一次 · gate.sh 正则与未跟踪文件)。
  **四判据抽成 `tools/unittest.sh` 唯一一份**;全量单测 **2445/0**。全量门仍未跑。
- **2026-08-22 · 「能修的修完了吗」批**(细账 CHANGELOG 08-22):F 性能 ✅(首页/图鉴/荣誉/盲注板拆「静态层 + 动效层」,Shader 只编译一份)·
  探针世界有 boon(`RunLoop.Opts.boon` 缺省掷;**所有仪器新基线**)· replay 只取真人局 + 真注入(此前几天验的是机器局空集)·
  popup 补臂 + 反向校验 · context 四阈值进 `director.json context_tuning` · 商店 boost 改注入 · 分数滚动复活。
  首页档案栏**全真**(08-22 拍板落地):参与度等级(`assets.json profile.levels`,零数值校验锁)· 称号 · 头像/名字跟当前主角 · ◈ 真钱包;
  ◆ 跨局金币 chip 删;成就页读终身计数 `SaveState.stats()`。meta.md §8 已从待拍转为实装记录(⚠ 本段所记的档案栏/META 已于 08-24 随局外删除退役,meta.md 08-26 删)。
- **2026-08-20 · 设计稿推荐项全落地**(用户:「一切先按你的设计来,我的工作是试玩」):
  回归局(≥3 天回来首局 = 温和+熟脸)· Boon novelty · 探索型货架(没用过的 Target ×1.5,
  weighted_pick 空 boost 逐字节不变有断言)· 点唱机资产(首页音景)· 赛季脚手架
  (下架≠没收)· META 快照进 Tape.begin。四开关全开。探针 1028/0。
  **price 全表已出**(20.6h,26 放置实测,ranking.json 已重刷精修版;顺手抓出并修掉
  trilogy 静默丢脸 —— rankgen 兜底规则 + validate_ranking 反向完备性);
  全量门仍未跑(试玩后)。
- **2026-08-19 深夜 · 两条拍板**:streak_shift **开**(「就是要千人千面,但不要被察觉」)·
  **资产 v2 推倒重做**(「没有回本周期…买了你就想玩,玩了你就想买」——
  分红删掉,资产 = 唱片×6(场馆歌单加曲)/彩带机/应援团/券合约×2;
  `Music.track_pool` 收口为免费首曲+已购唱片)。探针 1020/0,资产页 v2 截图 ✓。
- **2026-08-19 晚 · 追加两件(用户当日新需求)**:**Director 读 context**
  (SaveState 战绩圈 + faces_seen → `roll_run(..., ctx)`;novelty 开 / streak_shift 关;
  推翻 08-14 拍板已就地标注;新断言在 t_director)· **META 资产循环**
  (`data/assets.json` + `core/asset.gd` + 荣誉页资产子页 + 结算收入行;宝石/券双出口,
  不碰局内金币;新测试域 `t_asset`)。批次探针 1011/0,资产页/结算行截图 ✓。
  全量门与全量单测**仍未跑**(累积三批,等用户喊)。
- **2026-08-19 · 进入 1.1,一批四件全落地**(细账在 [CHANGELOG.md](CHANGELOG.md) 08-19 节):
  **券上线**(`tickets.json enabled: true`)· **音乐卡拍 v2**(`Music.sync_beat()`,
  波形实测 10/10)· **英文化**(`core/lingo.gd` + `data/lingo.json` ~300 条,
  探针钉死 cn,`SYNC5_LANG=en` 强制;新测试域 `t_lingo`)· **回传通道**
  (`core/uplink.gd` + `view/beacon.gd` + `tools/tapeserver.py`,**缺省关**等端点;
  新测试域 `t_uplink`;`SaveState.install_id()` 新键)。
  ⚠ **本批只跑了秒级验证**(快速探针 455/0 · 英文 7 屏 + 中文对照目视):
  单测全量套件与门都没跑(批次纪律,等用户喊)——上面回归表的 1868/0 是 08-17 的数,
  两个新域会让它变大。⚠ en 模式 `DB.ui()/tutorial()/run()` 返回**翻译副本**,
  cn/探针原样 —— 拿 DB 数据做逐字节对照的探针不受影响(探针恒 cn)。

- **2026-08-15 ~ 17 · 教学关重做 + 升级系统 + 券 + 三处重锚**(细账在 [TODO.md](TODO.md)
  「2026-08-17 交接」与 [CHANGELOG.md](CHANGELOG.md),此处只留读数):
  `section_targets` 由**真人试玩重锚** `[420, 1500, 3100, 5600]`(⚠ **当时的值**,08-29 起已两次被覆盖,现行见上;增量递增、终点不动,
  是对「目标分由生成器算」的显式例外,理由在 run.json)· **升级系统**(金币主出口,5 级)·
  加分族 **13 张**改 `bonus_target_pct`(跟随尺度)· **双色调拆黑调/红调**(roster → 63)·
  禁回 `min_run: 10`(新开「脸按局数解锁」通路)· **教学关重做**(做中学 + 动作门,
  走完接着把这一局打完)· **券系统完整**(每日发放 + 四个使用入口 + 局内托盘 `view/tray.gd`)·
  **两对 ×2 → ×3 恢复**(组合口径下 ≈3.04 = 用户 08-06 拍板值)· Director 主通道接线
  (`run.gd:98`,排序表未喂时逐字节退回)· 真人试玩报了 **14 条 UI/流程 bug** 已全修
  (门一条都抓不到 —— 输入命中/流程/可读性三类,LESSONS 有「八种绿是假的」判据)。
  回归:单测 **1868/0** · `gate.sh` 08-16 14:51 全绿过一趟,之后又改 14 处 ⇒ **08-17 晚重跑中**。
  **版本切分(用户拍板)**:1.0 = 纯游戏体验(门 → 用户验 → 出包);
  1.1 = 商业化 + 英文化 + 音乐(发行三件用户已自办 · 防沉迷 SDK 不做 · 正式发行目标美国)。

- **2026-08-14(晚)· 难度与节奏立卷** —— 规格 = 新的 [`docs/design/difficulty.md`](docs/design/difficulty.md)。
  **三个索引之前被混成了一个词「关卡」**:局内段(~70s)/ 跨局序列(~5min)/ 解锁进度(天)。
  - `death_spec` **[0.10,0.30,0.45,0.60] → [0.05,0.28,0.50,0.60]**(通关 13.9%→**13.7%**,持平);
  - 脸的轮次 **单值 `tier` → 集合 `tiers`**(缺省 `[tier]`,**30 张脸一行没改、行为逐字节不变**);
  - Director **三节作废**(读 context 的那三节),降级成一张 JSON 表;**依赖全部解除**;
  - **「池子搜索」作废**,`tools/pool.gd` **从未存在**(第 4 次「注释承诺不存在的机制」);
  - **「一局四张脸」收口 7 → 1**(`SectionMod.roll_run`)+ 加回「重复必须有意」守卫
    —— `blinds.md §3` 删 `arc` 时留的后手,放开 `tiers` 就必须兑现;
  - **第四轮补脸 1 → 4 张**(用户 2026-08-14:「tier4 可以补脸,只不过都是时间相关的就行」,
    覆盖 08-07 那版「暂定只有一种脸」):`overtime` 加场 / `teardown` 拆台 / `closing` 打烊,
    全部「六秒 + 一层既有的时间约束」,**零新参数、零新引擎代码**;`fixed_tiers` 清空。
    ⚑ 六秒仍始终成立,靠的从「只有一张脸」换成「**每张脸都自带缩时**」(`t_face.gd` 有断言);
  - ⚠ **`gate.sh <face_id>` 不再是「十几秒」** —— 它第一步就跑全量单测,而 S10 之后单测
    是 ~716s,所以**单脸门的地板变成 12 分钟以上**。CLAUDE.md 命令表那行已过期;
  - ⚠ 新曝光算术:**17 张 rare 每张平均 21 局才见一次**(21 卡位 × 3.9% ÷ 17)——
    学会一张要见 ~3 次 ⇒ **≈5 小时**。**小丑牌解锁已被用户取消,所以这个洞没有解法在手上。**

- **2026-08-13(夜)· 引擎波次子波 1**:小丑牌 **54 张现役 / 68 冻结**。
  入池 6(定格/静物/串场/让位/分成/穷开心),**2 张挂仪器债**(断舍离/打包)。
  **四个仪器修正**(经过在 `docs/design/jokers.md`,判据在 LESSONS):
  ① bot 试探性交换污染动作计数(静物触发 0%→21%;连带**单换/岔轨/限流三张脸
  的历史强度是方向性高估**)· ② 求解器算得出的量必须进 ctx(两张卡从 0 变有值,
  没改卡)· ③ 两处判据从魔法数换具名豁免表(`SOLVER_BLIND` / `WEAK_MAGNITUDE`)·
  ④ **穷开心的估值臂漏了代价那一半**(只算 ×1.3 不算金币上限 → bot 持有率 100%;已修)。
  ⑤ **两道门的确定性修正**(与④无关,同日连抓三个「靠运气变绿」的门):
  金币单调性哨兵改具名豁免制(`KNOWN_FLAT` + 零效应带 0.02 段 + 反向锁 + 拼写保护;
  两条真效应断言未被波及,已实测确认)· `tapeprobe` 清脸(它走真实一局、脸随机掷,
  掷到「一口气」就假红,**八次绿七次**;三臂 A/B 闭合,修后 6/6 绿且事件数一致)。
  回归:tests **1059/0** · kit 6/6 · pair +0.0 ·
  sim 通关率带内(twin 36.5% / random 0.4%)。
  ⚠ **既有 sim 基线已移动**:probe 修正改了 bot 行为,08-12 之前的队列读数不可直接比。
- **2026-08-13**:八条盲注裁决落定(五条按审查意见,CHANGELOG 有账)· **曲目改税**
  (`Run.variety_mult` 单一真相,缺一种 ×1.25 悲观实时;顺手修平 runloop 判生死
  没查种数的半个模型分叉)· 外部审查 V3 闩锁收官(V1/V2/V3 全清)。
  回归:tests **1004/0** · 单脸门 trilogy ✓ · sim 全队列漂移 ≤0.1pt。
  流派批 8 张图:分支上只有生产规格,**图未生成**,用户自理中。

- **roster**:小丑牌 **48 张现役 / 68 张已冻结**(Target 7 + Support 41;流派批两波
  +8 新 id,快闪/伴唱撤出 json 素材保留;16 张待引擎波次 + wrecker/trio 仪器债);
  盲注 **24 压力 + 赶场 + 4 boon** 全实装,`gate.sh` 上次全量绿在盲注批(08-11)。
- **真人数据**:**不再是零** —— 2013 份 Tape 里分拣出 **11 局合格真人局**
  (分拣判据与账本见 `tools/probbook.py`),已用于 Target 重锚与 bonus 族定价。
- **数值制度**:定价宪法 `docs/design/numbers.md`(三轴模型+六步 SOP)+ 概率账本
  `docs/design/probbook.md`(设计/仪器/真人三列,`python3 tools/probbook.py <sim日志>` 重刷)。
  bonus 族已按 v2 落地(支配序:会玩>保底>赌狗);**复审名单:快闪/伴唱/排练**(结构死卡)。
- **美术**:全部接线(主角八人立绘/行走/舞步、小丑牌 source 原画直出、盲注指纹卡、
  首页玻璃素材壳+顶栏膜);previews/cards 两目录已退役不进包。
- **Web 版**:`godot --headless --path . --export-release Web build/web/index.html`
  (先 --import;模板 4.6.2 已装机)。本地试玩:`python3 tools/webserve.py`(**HTTPS**,自签证书自动生成),
  手机同 WiFi 开 `https://<Mac IP>:8765`,首次点"继续访问"即可(Safari:显示详细信息→
  访问此网站;Chrome:高级→继续前往);换 WiFi 后删 `build/cert/` 重跑。包 147MB(pck 116MB;瘦身后账:立绘转 WebP)。
- **Android**:**裸包已通**(08-12 晚):`godot --headless --path . --export-debug
  "Android" build/sync5-dev.apk` → 115MB,arm64-v8a、minSdk 24 / targetSdk 35、
  **零权限**、竖屏。⚠ **1.0 实际以 debug 签名 + 占位包名 `com.sync5.dev` + `0.1.0-dev` 出货**(`build/sync5-taptap.apk`,08-19);
  传 TapTap 前必须换 release keystore(凭据操作归用户)+ 正式包名 + 版本号 —— **包名上传后不可改**。
  **发行口径(2026-08-17/19 拍板,取代 taptap.md 旧结论)**:TapTap 只是**给朋友试玩**的渠道,正式发行走**美国**;
  **防沉迷 SDK 不做**;国内试玩需要 **App 备案**(用户自办)+ 软著(材料包我可出),版号不需要(免费试玩、无内购)。
  工具链四处修正已留注释(editor_settings 两处路径、project.godot 的 `handheld/orientation` 与 `import_etc2_astc`)。
- **新工具**:`tools/probbook.py`(概率账本)· `tools/art/fontsubset.sh`(Web 中文字体子集,
  文案加新字要重跑)· `tools/art/glassprobe.gd` / `glassfilm.gd` / `blind_fp_extract.py`。
- **数值 v3 已落地**(08-12 晚,`docs/design/numbers.md` §8):宪法修订(玩家两条成长线
  入纲:彩票 = 不存在买得到的 Δp;规则牌 = 概率放大器按 Δp×流派价值)→ 未过刀族
  全部过刀:8 张动(开场+150%/复读+80%/贵宾20/低音谱15 ↑;铁粉 cap15%/暖冷色+3/
  黑胶+1 ↓)、4 张带内不动、coins 族暂缓(S9)、规则牌封锁(真人持有 0 拍)。
  锚 = 12 局 168 拍真人反事实重放 + 稀有度地板线 12.5 分/拍·◆。
  过门:948/0 · kit 9/9 · pair ✓ · sim 全队列漂移 ≤±0.5pt。
- **probbook 两态仪器**(08-12 晚):真人列拆「持有/未持有规则牌」+ 规则牌 Δp 牌型
  频率表;**修了 held 追踪 bug**(repl 是 in/out 键,旧版换进不计换出不删)——
  回响真人 11%→**33%**、彩虹 25%→**40%**(样本仍薄)。C8 已裁定:全员移半可控
  (重定价待 Δp 数据)、彩虹留彩票(`all_suits` 不认万能牌,代码级证据)。
- **速弹真机死条件已修**(C10):`_settle` 补传 early、判据收进 `_acted_early()` 一份
  —— 之前「模型里活、游戏里死」(C7 反向)。**M5 已结案(08-13 夜)**:查下去发现
  **不是反馈缺失,是这个玩家动作从来没被实现过** —— 钩子在、三张卡等着它、bot 会用它,
  而玩家没有任何办法触发。用户拍板 = **点唱片**(金环呼吸 + 中央剩余秒数)。
  ⚠ 真人早锁率 8% 这个读数**从此作废**:它测的是「一个不存在的动作」的使用率。
- **流派批第一波已落地**(08-12 夜,`docs/design/archetypes.md`;原作 42 流派全目录 =
  `docs/design/research_balatro_builds.md`):增 8(族内件对唱/二重唱/三和弦/三重 +
  后台/替补/包厢 + 修剪)· 删 2(快闪/伴唱)· 改 2(四指/双色调降罕见)。
  **过门读数**:tests **979/0** · kit **8/8 量到**(trim +1319 z=6.22;仪器新增
  `*score` 牌堆手术判据,旧 witness 路径抽验 ✓)· pair **+0.0** · sim 自检 ✓
  (random 哨兵 0.6%)· 图鉴 45/67 目视 ✓(新卡无图走玻璃壳+文字兜底)。
  ⚠ **twin 队列 39.7%,较 v3 基线 +5.3pt 超 ±3pt 带,解释**:本批族内件(对唱/二重唱)
  与通用件(后台/替补)直接服务 bot 的主力牌型,是**内容新增的预期效果**而非定价漂移;
  真人目标分(run.json)未动。观察:bot 尺度通胀若推 random 哨兵破 1%,重锚 bot_targets。
  **8 张新卡素材未出**(manifest S-53..S-60 已冻结 id + art_subject,
  `verify_joker_assets` 对它们报缺 = 待办信号,不是门坏了)。
- **流派批二波(同日深夜)**:shelf 三件套(联票 4 位 2 购 / 赞助 −1◆ / 点唱机必出
  规则牌)+ **第四条 proof 通路 `shop`**(商店行为臂,零基线证物:双购店 +1.2/局
  z=25.5 · 实收折扣 +3.4◆/局 z=24.4 · 含规则牌店率 18%→98% z=61.8)。
  货架 API 收口 `Joker.slots_*` 五口 + `Economy.shelf_price`,游戏/bot 同源;
  商店 UI 支持 4 卡位窄版(`_shot_draft_four` 验收态)。
  过门:tests **1001/0** · kit 3/3 · pair +0.0 · sim 自检 ✓(random 0.5%,
  全队列相对一波漂移 ≤±1.3pt)。⚠ 仪器教训记 jokers.md:第一版证物(成交数/均价)
  死于钉槽混杂,货架证物必须选零基线机械读数。外部审查 **V2 结案**。
- **子波 1/2/3 + 仪器债第一批(08-13 夜)**:roster **39 → 61 张**。
  引擎侧补齐三族(动作内容 / 计时 / 商店事件)+ 第四种证物 `counter`;
  **提前收工这个玩家动作首次实装**(点唱片)。
  **⚑⚑ 最贵的一个发现:`beat_budget.discards` 是个没量过的占位值** —— 配置写 2,
  真人实测**一拍最多弃 4 张**(校准依据是这个上界;「均值 2.30」是只算有弃牌的 81 拍的
  **条件均值**,无条件均值 1.11),**44% 的真实行为模型不允许**。校准到 4 之后连锁翻出
  三处(pair.gd 抄字面量 / 回响·复读的旧非零是噪声 / 赶场的 floor 离散化假象)。
  ⚠ **反推:此前所有以 bot 为尺子的读数系统性偏保守,目标分跟着偏松。**
  过门:tests **1100/0** · pair +0.0 · 拆迁回池 +1286 z=12.1 ·
  sim 漂移 twin 43.7→47.3 / triplet 31.6→38.3 / **baseline 与 lonewolf 零变化**。
- **观察点(下轮 Tape 回答)**:~~① 回响触发率~~ ✅ **结案(08-13)**:反事实率把样本
  从 n=15 撑到 **n=156**,读数 33%→**25.0%**(差 0.66 个标准误 = 噪声,**看不到激励效应**)② 锁定体感 ③ 阶梯 ×13(修剪/降稀有度落地后按 Δp 复价,方向回落)
  ~~④ 彩虹 p 40%@n15~~ ✅ **结案(08-13)**:n=168 读 **29.8%**(旧读数同样是噪声),
  180 的定价**落在锚内不用动**;同批复核发现**无对 nopair 期望比锚低 23%**
  (190×20.2%=38.4),按锚复价 250~300,**待走 SOP**
  (⑤ 见上,已挪)  ——曝光三措施已落两条(四指/双色调降罕见 + 修剪实装)
  ⑥ v3 砍的三张(铁粉/暖冷色/黑胶)的体感,黑胶 +1 太寒酸的备选 = 「每 2 弃 +3」
  ~~⑦ 族内件真人率~~ ✅ **已量(08-13)**:真人含对 **79.8%** / 含三条 **15.5%**
  → 对唱在地板上 ✓,**三和弦与三重只值半价**(26.0/44.7 vs 地板 50/75),二重唱偏高 28%。
  ⚠ **根因是分母错了**:定价时用的是 bot 的三条率,而 bot 追难牌型比人狠得多。
  **待走 SOP**(其中一步 = 用装了卡的 Tape 复测三条率,排除激励效应)。
  后台仍是 bot 尺度最强(+46/拍,防铁粉同形)。
  ⑤ **规则牌购买(保留)**:合格局 35 次购买里规则牌 **0 张** —— 但 12 局全在
  08-06/11/12,**曝光三措施是 08-12 夜落地的,这批基本没测到**。
  ⑧ **早锁率(真人)** —— 点唱片上线后重新测。旧的 8% 不是基线,是空值;
  这个数直接决定碎纸机/势头/冻结三张卡值多少钱。
  ⑨ **规则牌 + 商店成长族**:淘碟/收藏家/转型的真人计数器终值(bot 侧刷新率与真人差得远)。
  ~~⑩ 缓存织构率~~ ✅ **结案(08-13)**:三重唱真人 **0/168 拍**(缓存满 3 张的拍有 150 个),
  与 bot 侧的量级 4.1% 是**两个独立死因** —— 不回池,不用再等数据。
