# Sync5 · 数值策划总纲

> 这份文档是**定价的宪法**:任何卡面数额/倍率的改动,先过这里的框架,再动 JSON。
> 事实层在 [probbook.md](probbook.md)(概率账本,`python3 tools/probbook.py` 重刷);
> 单次改动的经过记在 [jokers.md](jokers.md) 对应小节。
> 2026-08-12 立纲。起因:用户「概率低=倍率高未必是错的,但这未必是资深数值策划的思路」。
> 同日修订:玩家视角两条成长线入纲 → 彩票判据收窄 + 规则牌 Δp 计价(§1/§2/§3.1)。

---


## ⚑⚑ 小丑牌五因子定价(2026-08-27 用户拍板,与牌型倍率 v3 同一套理论)

> 用户:「小丑牌的倍率也应该有这个考虑,考虑**生效时间**(一轮还是一整局)、
> **生效机制**(累积和每次重新算)、**条件概率**、**条件成本**(金币),
> 来给一个最终的倍率……哦最后忘了说,**波动性**。」

每张卡的最终数额 = f(五因子):
1. **生效时间**:一轮限定 vs 整局持续(窗口窄 ⇒ 效果强,jokers.md 原则 17 的推广);
2. **生效机制**:累积(黑胶式,价值 ∝ 剩余拍数²量级)vs 每次重算(瞬时价值);
3. **条件概率**:先验层口径(触发率,vol.gd 同族仪器);
4. **条件成本**:经济 v2 后条件可能要花钱(拆迁弃 4 = 4◆)—— 金币成本按
   coin_score_ratio 折成分数当量入账;
5. **波动性**:条件越难触发 = 收益越离散,溢价同牌型层 λ·(1−P) 形状。

落地 = ⑥ 全池重标:每张卡算 E[贡献/拍] 与五因子修正,对齐目标带(垫底 <500 与
头部 +36097 的 70 倍离散度收敛到 3 倍量级);输入三张表 = kit 性价比榜 ·
P_b/CV(vol.gd)· 真人反馈台账(feedback_20260827.md,权重最高)。

## 0. 一句话纲领

**EV 拉平是会计,不是设计。** 一张卡的价格不由它的期望值决定,由三个问题决定:
它想让玩家**做什么**(行为轴)、做成时的**情绪是哪一档**(方差轴)、
它的黄金期在**一局的哪一段**(时间轴)。期望值只是这三个答案算出来的**结果**。

---

## 1. 三概率记录制度(用户拍板:概率计算和实际校验必须留记录)

每张有条件的卡,账本里记三个概率,**缺一列就不许定价**:

> **⚑⚑ 2026-08-14 修订(用户提出「区分先验生成和后验生成」)**:`p̂` 那一列曾是**散文不是算式**
> (`0.35(重复上一拍:需刻意保型)`),44 张卡有一半还空着 —— 于是它经不起检验,
> **全部信任压到 `p_bot` 上**,这就是"强依赖后验"的由来。
> **先验的反面不是后验,是拍脑袋。**
> 现在 `p̂` 由 [`prior.md`](prior.md) 的先验层**算出来**(`tools/prior.gd`,组合枚举,零数据)。
> 三条连带纪律:
> **① 能算的不许去测** —— 判据是「这个量依不依赖『人是什么样』」,不依赖的一律算;
> 去测一个确定的数 = 拿带噪声的尺子量它。
> **② 定价锚优先级:先验 > 真人 > bot。** 先验列**不受样本量限制**,所以
> 「缺一列不许定价」那条对**组合类条件**自动解除(规则牌 Δp 因此解封)。
> ⚠ 但对**行为类条件**(弃牌/持币/时机/跨拍)先验算不出来,那一列仍然必须等实测 —— 分界见 prior.md §2。
> **③ 三个折扣跨卡共享**:`p_人 = p̂ × 策略折扣 × 执行折扣 × 认知折扣`。
> 要估的参数从 `O(卡数)` 降到 `O(1)`,**这就是"少数据也能定价"的数学原因**。
> ⚠⚠ **实测远低于先验时,那不是难度,是玩家没认出来/来不及做 —— 倍率治不了它**
> (阶梯 ×13 就是这么来的,已改;完整案例见 [jokers.md](jokers.md)「第三次重锚」)。

| 列 | 是什么 | 怎么得 | 谁失真 |
|---|---|---|---|
| **p̂ 设计概率** | **组合可得性**:零策略、8 选 5 取最优下条件的达成率 | **`tools/prior.gd` 精确算**(旧口径是手推散文,已废) | 不含策略与手速,是**基线**不是预测 |
| **p_bot 仪器概率** | bot 持有期实测触发率 | `tools/sim.gd` 的 support trigger 表 | bot 的追法≠人 |
| **p_人 真人概率** | 真人 Tape 持有期 fired 率 | `tools/probbook.py` 扫 tape | 样本少、版本混 |

**三列差异本身是诊断**,不是误差:
- `p̂ ≫ 两个实测` → 教学或结构问题(玩家没看懂/条件成本高于收益),**先修卡不修价**;
- `p_bot ≫ p_人` → 水平相关条件(bot 会追人追不动),**定价锚真人**(A 案反转的教训);
- `p_人 ≫ p_bot` → bot 信念表落后,修 `data/sim.json`,别错怪卡。

**校验节律**:每次调价后、每个版本节点后重刷账本;账本每行带样本量,
样本 < 30 拍的真人列标 ⚠ 不作定价依据。

**真人列拆两态(2026-08-12 修订)**:受规则牌影响的条件卡,真人列分
**持有规则牌 / 未持有**两个条件概率 —— `Δp = p_有 − p_无` 是规则牌计价的直接输入(§2)。
⚠ 仪器(`tools/probbook.py`)未跟上前,两态列缺 = **规则牌不定价**,
与「缺一列不许定价」同一条纪律(挂 TODO C9)。

---

## 2. 通道族与量纲(不同族的分不是同一种分,跨族不许直接比大小)

| 族 | 结算位置 | 吃倍率? | 天然价值曲线 | 现役卡 |
|---|---|---|---|---|
| `bonus` 平加 | 乘法**后** | 否 | **贬值**(前期是神,后期过气 —— 杀万能填充的机制) | 回响/尾声/灯牌/彩虹… |
| `bonus_pct` 百分比 | 乘 (1+Σ%) | 是 | 恒值(跟总分走) | 副歌/开场/铁粉/复读… |
| `additive`/`chips_per_card` 改基牌 | 乘法**前** | 是 | **升值**(倍率越晚越大) | 黑胶/暖冷色/低音谱 |
| `mult`/`mult_add` 倍率 | 乘法链 | — | 升值且乘性 | Target 族/贝斯线/镜面 |
| `coins` 金币 | 经济轴 | — | 购买力曲线另算 | 小费罐/利息/独狼 |
| 规则牌(无数额) | 改判定 | — | **概率放大器:Δp × 流派价值**(见下) | 近道/四指/双色调/百搭 |

**族内比期望,跨族比定位。** bonus 族在 S1-S2 是主力、S3-S4 自然让位给 pct/chips
—— 这不是失衡,是设计(curve 字段就是在声明这件事)。

**两类放大器分开计价(2026-08-12 修订)**:
- **数额放大器**(Target/pct/改基牌):按**覆盖面**标定(§3.1 与 jokers.md 两层原则);
- **概率放大器**(规则牌):计价 = **Δp × 它救的那条流派的价值** ——
  按「把哪条流派的 p 从多少抬到多少」算,Δp 从账本两态列读(§1),不许拍脑袋。
  阶梯 ×13 那条线已经证明:**数字治不了低频答案,概率放大器才治得了** ——
  高倍流派的深度长在概率端,不在数额端。

---

## 3. 定价模型:三轴

### 3.1 行为轴 —— 可控性决定 EV 排序(本纲的核心修正)

按「玩家能不能用操作抬这个概率」分三类,**EV 支配关系必须是:玩好 > 保底 > 纯赌**:

| 类 | 定义 | EV 目标(以无条件卡 E₀ 为基准) | 理由 |
|---|---|---|---|
| **可控**(变奏/尾声/周转/清流) | 概率主要由玩家操作决定 | **E = E₀ × (1 + 0.6×(1−p))**,打成时严格高于保底 | 技术溢价归玩家,这是构筑深度的来源 |
| **半可控 / build-around**(和弦/回响) | 要先投资(建缓存/锁牌型)才可控 | 按**建成态** p≈0.8 代可控公式,再加 10-20% 建设税 | 建设期的机会成本要还给玩家 |
| **彩票**(全员/彩虹偏此类) | **不存在买得到的 Δp**(判据见下) | **E = E₀ × 0.7**,期望打折 | 它卖的是方差(爆一下的爽),不是期望;EV 再高就成了"数学上必买" |
| **无条件**(灯牌/暖冷色) | p = 1 | **E₀,全族最低** | 不改变任何决策的卡是新手保底,老手拿它=构筑深度归零 |

⚠ **2026-08-12 第一版重定价的自我纠错**:那一版把灯牌(60)留成了全族 EV 最高
—— 支配关系是反的,无脑卡最优。本纲下灯牌必须是**地板**。

**⚑ 玩家视角的两条成长线(2026-08-12 用户拍板,本轴的语义来源)**:
- **数额成长线**:高频低倍牌型(对子/两对),倍率低,深度在「用小丑牌组合把它放大」
  —— 载体 = 宽覆盖 Target + 吃倍率通道的 support,玩的是每拍吞吐的流水线;
- **概率成长线**:低频高倍牌型(顺/同花),深度在「凑手牌打一波大的,用构筑买 Δp」
  —— 载体 = 规则牌(近道/四指/双色调/百搭)+ 缓存 3 格 + 免费弃牌。

两条线都算「会玩」,支配关系仍是 **会玩(任一线)> 保底 > 真彩票**。

**⚑ 彩票判据(同日收窄)**:**彩票 = 不存在买得到的 Δp**。
凡规则牌/缓存/弃牌能抬的概率,归可控或半可控、按建成态 p 定价;
×0.7 期望税只落在**任何构筑都抬不动的真随机**上。
⚠ 待复核:全员(0.10)/彩虹(0.25)若能被百搭/双色调抬 p,
要移出彩票类按建成态重定价(挂 TODO C8)。

⚠ **别混两个「低波动」**:无条件保底卡(灯牌 —— 乘后平加,**故意不可放大**,
杀万能填充的机制)≠ 数额成长线(可放大、有构筑深度)。前者是地板,后者是「会玩」。

### 3.2 方差轴 —— 情绪档位,每档都要有人住

| 档 | 触发率带 | 体验 | 配额感觉 |
|---|---|---|---|
| 保底 | 100% | 每拍都亮,安心 | 每稀有度 1-2 张够了 |
| 节奏 | 40-75% | 大多数拍亮,配合操作 | **主力档**,过半 |
| 爆发 | 8-30% | 攒一个大的 | 每族 2-3 张,数额必须"值得等" |
| <5% | — | **死档**:低频答案,禁止入池(行为答案三死法) | 快闪/伴唱/排练在此,挂复审 |

### 3.3 时间轴 —— curve 字段的定价修正

- `burst`(bonus 族):按 **S1-S2 的分数尺度**定价(它的黄金期),别按全局平均;
- `growth`(黑胶/惯性/贝斯线):按**拍 18-24 的到达值**定价,初值可以看起来寒酸;
- `decay`(荧光棒):按**前 6 拍积分**定价;
- `fixed`(规则牌/改基牌):按它解锁的覆盖面 × 全程定价;
- `floating`(利息/铁粉/镜面):随外部状态,按典型持币/典型 Target 定价。

---

## 4. 心理数字规范

- 数额取**整五整十**;倍率一位小数以内;卡面数额 ≤4 字符(章要能读);
- `+` 是加分、`×` 是倍率、`%` 是百分比、`◆` 是钱 —— 通道符号不许混用;
- 公式出目标价后有 **±15% 手感修正带**,带内取顺眼的数;带外必须回来改公式系数,
  不许"就这张特殊"(特例累积=下一次的乱);
- 同稀有度内,数字的**位数**别跨两档(4◆ 的卡别同时存在 +25 和 +240 —— 后者该涨稀有度)。

---

## 5. 调价 SOP(六步,一步不许跳)

1. **刷账本**:`python3 tools/probbook.py`(真人列自动扫 tape;bot 列贴最近一次 sim);
2. **代公式**:§3.1 得 EV 目标 → 数额 = EV ÷ p(用定价锚列:可控卡锚真人,彩票锚 bot);
3. **手感带**:±15% 内取整五整十;
4. **落三处**:`data/jokers.json`(数值+fx 文案)、`assets/jokers/manifest.json`(数额章)、
   卡面中文若含数字则 `data/ui.json`;测试断言一律 `_bonus()`/`_tmult()` 推导,禁手抄;
5. **过门**:改动卡逐张 `kit` + 全量 `sim`(锚队列通关率漂移 >±3pt 要解释)+ `tests`;
6. **记账**:账本重刷入库,改动经过记 jokers.md,**本纲只在"原则变了"时才动**。

---

## 6. 概率账本

完整表在 [probbook.md](probbook.md),由 `tools/probbook.py` 生成
(真人列扫全部合格 Tape;bot 列解析指定的 sim 日志;含样本量与生成时间)。
**手改 probbook.md 无效** —— 它是仪器读数,不是意见。

---

## 7. 按本纲重推的 v2 目标价(bonus 族;**只推导,未实施,待拍板**)

E₀(灯牌)= 50。可控卡 E = 50×(1+0.6×(1−p)),锚真人概率;彩票 E = 35。

| 卡 | 类 | 定价 p | EV 目标 | 数额 = EV/p | 手感带取整 | 现值 | 动不动 |
|---|---|---|---|---|---|---|---|
| 灯牌 | 无条件 | 1.00 | 50 | 50 | **50** | 60 | ↓ 降为真地板 |
| 变奏 | 可控 | 0.70 | 59 | 84 | **85** | 65 | ↑ |
| 尾声 | 可控 | 0.60 | 62 | 103 | **100** | 80 | ↑ |
| 周转 | 可控 | 0.70×1.5张 | 59 | 56/张·率 | **30/张** | 25 | ↑ |
| 清流 | 可控 | 0.33 | 70 | 212 | **190** | 130 | ↑ |
| 回响 | 半可控 | 0.30(建成) | 71×1.1税 | 260 | **240** | 160 | ↑(与禁回族脸冲突已含在 p 里) |
| 和弦 | build-around | 0.80(建成) | 56×1.2税 | 84…按持续收租再压 | **150** | 140 | ≈ 带内不动 |
| 彩虹 | 彩票偏可控 | 0.25 | 40 | 160 | **170** | 180 | ≈ 带内不动 |
| 全员 | 彩票 | 0.10 | 35 | 350 | **320** | 240 | ↑ 爆发档要值得等 |
| 伴唱/排练/快闪 | 死档 | <0.05 | — | — | — | 200/200/200 | ⚠ 结构复审,不定价 |

**v2 相对 v1 的实质变化只有一个**:支配关系翻正 —— 可控条件卡打成时(59-71)
**高于**灯牌(50),彩票卡期望(35)**低于**灯牌但单发爆炸。

⚠ **v2 用的真人概率带仪器 bug**(2026-08-12 当日发现):probbook 旧版的持有集
只认 `buy` 的 `id` 键,`repl` 事件(in/out)换进的不计、换出的不删 —— 修正后
回响真人 11%→33%、彩虹 25%→40%。v2 的建成态假设(回响 0.30)反而被修正值印证,
数额不动;彩虹进 §8.3 观察。

### v2 落地记录(2026-08-12 用户拍板「按 v2 落」)

7 张落地(灯牌50/变奏85/尾声100/周转30/清流190/回响240/全员320),kit 全过、
948/0、哨兵绿(random 0.3%)。**锚队列漂移 +7.2pt(双子 27.6→34.8%),超 ±3pt 带,
解释如下**:漂移主要是 **bot 侧放大** —— bot 打双子队列时天然重复牌型,回响触发
37%(真人 11%),37%×240 ≈ 89/拍,是 bot 尺度上的最优卡;而真人端按实测触发率
折算,支援 buff 带来的分数通胀只有约 +2~4%。真人目标分(run.json)未动。
**观察点**:① 用户下一轮 Tape 里回响的真人触发率是否被 240 的激励抬起来
(设计预期 p̂=0.35,现状 0.11 —— 抬不起来说明"保型"的教学/可视化不够,
回 §1 的"先修卡不修价");② bot 尺度若继续通胀到 random 哨兵爬升,重锚
sim.json bot_targets。全员 lift −17~−33% 属预期(彩票卡不该是 bot 最优)。
③ 仪器修正后回响真人读数 33%(n=15)—— 观察点①已部分回答,但混着仪器修正,
**不是**激励生效的证据,继续攒样本。

---

## 8. v3:未过刀族过刀(2026-08-12;**已落地**,经过见本节末落地记录)

范围 = §7 之外的全部:bonus_pct / 改基牌 / coins / mult 族 + 规则牌 + C8 彩票复核。

### 8.0 本轮新增标定规则(待拍板):稀有度地板线

§7 的 E₀=50 是 **common(4◆)** 的地板。跨稀有度按购买价等比:**12.5 分/拍·◆** ——

| 稀有度 | 价 | 地板 E₀ |
|---|---|---|
| common | 4◆ | 50 |
| uncommon | 6◆ | **75** |
| rare | 9◆ | **110** |
| mirror 特价 | 11◆ | 137 |

行为轴系数(§3.1)原样叠在地板上。没有这条线,uncommon/rare 的支援卡没有基准可代。

### 8.1 真人尺度读数(定价锚;12 局 168 拍,scale 重放)

拍均分 **502** · 乘法部(score−bonus)**481** · 倍率链均值 **6.70**;
S1 375 → S2 437 → S3 749 → S4 1103(S3 n=24、S4 n=12,**薄**);
段首拍乘法部 **312** / 段末拍 **617**;零弃拍 **52%**;同型拍率 **25%**;
早锁率(act≤3.5s)**8%**;持币均值 **17.7◆**(≥4◆ 恒真 —— S9「钱不是约束」的真人版)。
**反事实重放** = 拿真人每一拍的实际手牌与倍率链,代入卡的效果算 Δ分/拍。
它不含行为适应(装了卡会改打法),对可控卡是**下界**。

### 8.2 v3 目标价表(实测 EV = 反事实重放全程均值,分/拍)

| 卡 | 稀有 | 类 | 实测 EV | EV 目标 | v3 提案 | 现值 | 动不动 |
|---|---|---|---|---|---|---|---|
| 副歌 | unc | 结构定时(段末 1/6) | 77 | 75 | +75% | +75% | ≈ **不动** |
| 开场 | unc | 结构定时(段首=全局最弱拍 312) | 42 | 75 | **+150%** | +80% | ↑ 近翻倍 |
| 复读 | unc | 半可控(同回响,建成 0.30) | 62 | 117 | **+80%** | +50% | ↑ |
| 荧光棒 | unc | 无条件 decay(前 6 拍积分 −6% 带内) | 77 | 75 | +60% | +60% | ≈ **不动** |
| 惯性 | unc | 可控 growth(真人早锁 8% vs bot 78%) | 22 | — | +10% | +10% | **先修可见性不修价**(M5 早锁无反馈,§1 先修卡) |
| 铁粉 | unc | **实测无条件**(持币 17.7◆ 恒触发,cap 常年顶满) | **126** | 75 | **cap 30%→15%** | cap 30% | ↓ **v1 灯牌同形**:无脑卡最优,砍半归地板 |
| 贵宾 | unc | 无条件改基 | 31 | 75 | **人头=20** | 15 | ↑ |
| 低音谱 | unc | 无条件改基 | 48 | 75 | **低牌=15** | 10 | ↑ |
| 暖色 | com | 无条件改基 | 118 | 50 | **+3/张** | +6 | ↓ 砍半(暖 118/冷 83 的差按噪声处理,同刀) |
| 冷色 | com | 无条件改基 | 83 | 50 | **+3/张** | +6 | ↓ |
| 低声部 | com | 半可控改基(低牌流,与低音谱成对) | 67 | 58 | +9/张 | +9 | ≈ 带内高沿,**不动** |
| 黑胶 | com | 可控 growth | **204**(拍18-24 达 **749**) | ~58 | **+1/弃** | +3/弃 | ↓ **弃牌免费的连带**(economy.json 预言兑现):+1 后全程 68、后期 250,仍是 common 顶部 |
| 贝斯线 | rare | 可控 growth(到达值 360 ≈ 110×3 带内) | 64 | — | ×0.25/12 | 同 | ≈ **不动** |
| 镜面 | rare | Target 从属(值 = 你的 Target 边际的一半) | — | — | — | ½ | **归 Target 族标定问题**,不按支援 EV 定价,观察 |
| 小费罐/利息 | — | coins 轴(利息实测 3.4◆/拍) | — | — | — | 2◆/1◆ | **暂缓**:S9 裁定金币边际近零,购买力曲线未建,跨量纲不定价 |
| 规则牌 ×4 | — | 概率放大器 | Δp 无读数 | — | — | — | **封锁**(§1):真人持有 **0 拍**,两态列空 |

⚠ 表内实测 EV 的样本 = 12 局 168 拍,S3/S4 段薄;所有「↑」卡涨完仍要过第 5 步门
(kit + sim 漂移 ±3pt + tests)再落地。

### 8.3 C8 裁定(彩票档按「买得到的 Δp」复核)

- **全员:移出彩票 → 半可控/build-around**。判据在定义层就成立:它的条件是牌型集
  (顺/同花/葫芦),四张规则牌抬的正是这些牌型的 p。但**建成态重定价被 §1 封锁**
  (规则牌真人持有 0 拍,Δp 两态列空)—— 现值 320 维持,数据到位后重定
  (方向预判:**下调**,建成态 p 高于 0.10)。
- **彩虹:留在彩票偏可控,180 不动**。代码级证据:`core/fx.gd` 的 `all_suits`
  **不认万能牌**(wild 被排除在花色统计外)—— 百搭抬不动它;弃牌追花色的那部分
  可控性已计入实测 p。⚠ 观察:仪器修正后真人 p = **40%**(n=15 薄),
  若 n≥30 后坐实,180 按彩票公式要**下调**(0.40×180 = 72 已高过可控档)。

### 8.4 本轮顺带发现(不属于定价,已挂 TODO)

1. **速弹真机死条件**:`view/phrase.gd` 的 `Beat.settle` 只传 `late` 不传 `early`,
   速弹的 `early_finish` 条件**在真机上恒假**;sim/bot 侧传了 —— **「模型里活、
   游戏里死」**,C7(第 8 次商店)的反方向同形状。kit 门量的是 bot 路径,抓不到这类。
2. **规则牌 12 局零购买**:概率成长线(§3.1 线 2)的载体无人用过 —— 阶梯 ×13
   赖以成立的救援体系实际不存在。是稀有度太深(3/4 张是 rare,池权重 5%)还是
   改判定卡的收益不可读,要用户试玩回答。
3. **早锁不可见**:真人早锁率 8%(bot 78%),而 M5 提前锁定没有任何 UI 反馈 ——
   惯性/速弹两张卡的地基都塌在这里,修可见性优先于修数额。

### v3 落地记录(2026-08-12 用户拍板「数值你都先做,我来实际体验后会给反馈」)

8 张三处齐落(jokers.json 数值+英文卡面 / manifest.json 数额章 / ui.json 中文含数字的
3 处:贵宾 20、铁粉上限 15%、低音谱 15 点;铁粉 cap 不在英文卡面上,fx 未动)。
**C10 速弹死条件同批修复**:`view/phrase.gd::_settle` 补传 `early`,判据抽成
`_acted_early()` 一份、与 `_advance` 共用 —— 速弹的条件从此真机/模型一致。

**过门读数**:kit 9/9 ✅(改动 8 张 + 速弹)· tests **948/0**
(顺带把 3 条手抄数额的断言改推导 —— 黑胶 ×2、贵宾 ×1,runner 补了通用口
`_do_amount`,LESSONS「断言禁手抄」的又一例)· pair ✅ ·
sim 全队列漂移 **≤±0.5pt**(twin 34.8→34.4,random 哨兵 0.3% 不动)——
远小于 ±3pt 门槛:v3 砍的三张(铁粉/暖冷色/黑胶)都不在 bot 的胜利套件里
(bot 押回响/清流/排练),漂移小属预期,**真人端的影响要大得多**(砍的恰是
真人反事实里超发的)。账本已按 SOP 重刷(sim_v7)。

**观察点(等用户 Tape)**:① 开场 +150% 会奖励「过段备牌」,段首拍分被玩家
抬起来后 EV 超 75 属技术溢价,**不回刀**;② 黑胶 +1 若体感太寒酸,备选方案是
step 化(「每 2 弃 +3」),数学等价砍半但保留「+3」的读感;③ 铁粉砍半后
若真人持币习惯变了(开始花钱了),按新持币分布重代公式。

---

## 9. 曝光轴 —— 一张卡多久出现一次(2026-08-15 用户拍板,已落地)

> 起因两句(用户当日):
> 「**规则牌的概率太低了,可以调高点,我们是手游,不是 steam**」
> 「**我们不需要玩几十局才出现一次的卡,最差也是 3-4 轮出一次**」

⇒ 本纲从此有**第四个轴**。§3 的三轴回答「这张卡该值多少」,曝光轴回答
**「玩家多久碰得到它」** —— **一张碰不到的卡,三轴算得再准也等于不存在**。
它同时是 §3.1「行为轴」的前置条件:**买不到的 Δp 不是可控,是彩票**。

读数一律来自 `SYNC5_PRIOR_MODE=shelf godot --headless --path . --script res://tools/prior.gd`
(**解析,零采样**,秒级),口径 = 7 商店 × 3 位、`target_mult=1.0`、**不含付费刷新 ⇒ 是下界**。

### 9.1 ⚑⚑ 守恒律:21 个货架位是不可增的预算

一局 = 7 次商店 × 3 位 = **21 次曝光**。不管权重怎么分,**全池"一局期望出现张数"之和恒等于 21**。
于是「每张卡一局见到率 ≥ p」等价于「每张卡的**单位货架份额** ≥ s(p)」,而份额之和 = 1:

| 靶子 | 单位份额下限 s = 1−(1−p)^(1/21) | **池子张数上限** |
|---|---:|---:|
| 3 局一次(p = 33.3%) | 1.912% | **52 张** |
| 3.6 局一次(p = 28%) | 1.552% | 64 张 |
| **4 局一次(p = 25%)** | **1.361%** | **73 张** |
| 5 局一次(p = 20%) | 1.057% | 95 张 |

现役 **61 张**,取靶子下沿(4 局):**61 × 1.361% = 83.0% 的份额被"达标下限"吃光,
只剩 17.0% 可以拿来做稀有度阶梯。**

⚑ **一句话**:**曝光阶梯 = 阶梯预算的函数,阶梯预算 = 1 − 张数 × s(靶子)。**
池子越大、靶子越紧,稀有度在曝光维度上就越平。**这不是调参口味,是守恒律。**

由它直接推出三个数(全部实测复核):

- 现状 70/25/5 的**实际**单卡曝光比 = **50.6 : 22.1 : 4.8 ≈ 10.5 : 4.6 : 1**;
- 把 17.0% 阶梯预算**全部**给 common(即 uncommon 权重压到与 rare 相等)时,
  曝光比的**理论上限 = 1.63 : 1 : 1**(穷举整数权重得 70/43/43);
- 要求三档**严格单调**且**都达标**时,可行域几乎是一个点:**≈1.4 : 1.2 : 1**。

⚠⚠ ⇒ **「每张卡 3–4 局一次」与「稀有度决定曝光」在数学上不相容。**
穷举验证:约束 `c ≥ 1.2u ≥ 1.44r` 且 rare ≥ 25%/局 ⇒ **可行解为空**;
放宽到 rare ≥ 20%(5 局)仍为空。**二选一,用户已选靶子。**

### 9.2 「手游不是 Steam」的数学形式

Balatro 用的也是 70/25/5,但它的一局曝光预算是**数量级更大的**(商店次数远多于 7,
外加补充包这一整条平行的发牌通道)。同一套权重:

```
曝光预算 ~100+ 次 × rare 3.9%  ≈  每局好几张稀有卡   ← Steam 尺度
曝光预算    21 次 × rare 3.9%  =   每局 0.82 张       ← 我们
```

⇒ **权重是照抄的,曝光预算不是。** 4.9 分钟一局的手游,内容必须在**局**这个尺度上浮出水面,
而不是在"几十小时"这个尺度上。

### 9.3 落地:`draft_rarity_weights` **70/25/5 → 35/30/25**

`data/economy.json` 一处,零代码改动。前后对比(`prior.gd` shelf 模式,两次读数):

| 量 | 前 70/25/5 | 后 35/30/25 | 变化 |
|---|---:|---:|---|
| **common 单卡** 一局见到 | 50.6%(2.0 局) | **33.4%(3.0 局)** | ↓ |
| **uncommon 单卡** 一局见到 | 22.1%(4.5 局) | **29.4%(3.4 局)** | ↑ |
| **rare 单卡** 一局见到 | **4.8%(20.6 局)** | **25.2%(4.0 局)** | **↑ 5.2×** |
| common 档占比 / 一局期望张数 | 70.8% / 14.87 | 41.5% / 8.72 | ↓ |
| uncommon 档占比 / 张数 | 25.3% / 5.31 | 35.6% / 7.47 | ↑ |
| rare 档占比 / 张数 | 3.9% / **0.82** | 22.9% / **4.81** | ↑ |
| 单卡曝光比 c : u : r | 10.5 : 4.6 : 1 | **1.4 : 1.2 : 1** | 阶梯几乎抹平 |

**三档全部落进 3.0–4.0 局的靶子带**,最差的一档(rare)= 3.97 局 —— 靶子达成。

**规则牌(用户点名的那条线)**:

| 卡 / 集合 | 档 | 前:一局 / 12 局 | 后:一局 / 12 局 |
|---|---|---:|---:|
| shortcut 近道 | uncommon | 22.1% / 95.0% | **29.4% / 98.5%** |
| fourfingers 四指 | uncommon | 22.1% / 95.0% | **29.4% / 98.5%** |
| twotone 双色调 | rare | 4.8% / 44.9% | **25.2% / 96.9%** |
| wildcard 百搭 · trim 修剪 | rare | 4.8% / 44.9% | **25.2% / 96.9%** |
| jukebox 点唱机 · doublebill 联票 | rare | 4.8% / 44.9% | **25.2% / 96.9%** |
| **判定规则牌 ×5 至少一张** | — | **48.1%** / 100% | **80.1%** / 100% |
| **全部规则牌 ×8 至少一张** | — | 63.8% / 100% | **92.8%** / 100% |

⚑ 这**否掉了 prior.md §5.7b 的一半结论**:那里断言「近道/四指 12 局 95% 会见到 ⇒
曝光不是问题,问题在收益不可读」。**12 局是 Steam 的分母。** 换成手游的分母(一局),
22.1% 意味着 **78% 的 session 里那条流派根本不存在** —— 撞设计画像
「随机不许决定『有没有玩点』」。**「收益不可读」仍然成立,但它不再是唯一的病因。**

### 9.4 三个连带效应(必须一起读)

1. **⚠ 8 张 Target 全在 rare 档** ⇒ 「一局至少见到一张 Target」**33.0% → 91.3%**。
   首张免费 Target 之后的**换旗**从"两局才有一次机会"变成"几乎每局都有",
   2026-08-06「Target 回同一个货架池」那条拍板的效果第一次真正兑现。
2. **⚑ 金币从死资源变回活约束**(正面)。S9 裁定「金币边际近零」(持币均值 17.7◆,
   ≥4◆ 恒真)是**因为货架上没东西值得买**。现在一局期望出 **4.81 张稀有卡**,
   4 槽全稀有 = 36◆,而保底收入只有 6 + 4×3 = 18◆ + 牌型金币 ⇒ **买不全,必须挑**。
   ⇒ **`joker_prices` 本次不动**(理由见 9.5)。
3. **⚠ 加卡即稀释**:单卡曝光 = w/W,池子每涨一张全体按 1/W 下降。
   jokers_atlas 的扩池计划(60 → 更多)会**直接吃掉阶梯预算**:按 §9.1,
   靶子下沿的池子上限是 **73 张**,现在 61 张 —— **只剩 12 张余量**。
   ⚠ **没有任何测试锁着这件事**(common/uncommon 占比≈名义权重纯属两档都是 22 张的巧合)。

### 9.5 ⚠⚠ 风险:twotone 双色调,以及"曝光是遮羞布"

**twotone 一局见到率 4.8% → 25.2%,比它 2026-08-14 之前当 uncommon 时(22.1%)还高。**
那次拍板把它 uncommon → rare,同时做了两件事:**抬价 6◆→9◆** + **砍曝光 22.1%→4.8%**。
本次改动**把后一半整个还回去并且超发**。它的效力是实测的 **9.8×**(同花 6.79% → 66.3%,
prior.md §5.2),同价位另两张只有 3.1×/3.0×。

⚑ **一般化的教训,值得写进纲里**:
> **曝光是遮羞布。把曝光拉平,所有超模卡会同时露出来** ——
> 因为「见不到」曾经在替它们做平衡,而那不是设计,那是运气在替你兜底。

**四个可选解,我一个都没做(全部超出本次文件边界或需要用户拍板)**:

| 解 | 做什么 | 判断 |
|---|---|---|
| **A 改效力** | 动 twotone 的 `deck_rule` 本身(如同色同花另加门槛) | **推荐**。曝光拉平后,平衡只能落在效力上。`data/jokers.json`,不在本次边界内 |
| **B 单卡权重覆盖** | 仿 `joker_price_overrides` 加 `joker_weight_overrides` | ⚠⚠ **与 2026-08-06 拍板正面冲突**:「不应该有任何卡有固定概率,大家都是一样的」。要做必须先推翻那条 |
| **C 抬价** | `joker_price_overrides.twotone` | 弱杠杆:S9 已裁定金币边际近零;且绕开 §5 SOP 就是"特例累积" |
| **D 接受 + 重标定** | 承认同花常态化,回 `cards.md` 重标 mono ×12 / stair ×11 与同花系倍率 | 自洽但代价大:牌型倍率表要跟着动一轮 |

同理需要复审的还有:**`fullcast` 全员(320)** —— §8.3 把它移出彩票档时,建成态重定价被
「规则牌真人持有 0 拍」封锁;曝光拉平后建成态会真的发生,**方向预判仍是下调**。

### 9.6 ⚠ 未过门(这次交付的已知缺口)

按 §5 SOP 第 5 步,本改动**只跑了 `prior.gd` shelf 模式**(解析,前后各一次,退出码 0),
**没跑 `sim` / `tests` / `gate.sh`**(本次会话被显式要求不跑,避免抢占 CPU)。
**落地前必须补的两条**:

1. **`sim` 全队列漂移**:bot 与玩家读同一张货架(`tools/bot.gd:522` 与 `view/shop.gd:322`
   各有一份同样的 `_shelf_weight`)。rare 曝光 ×5.2 会**系统性抬高 bot 构筑强度** ⇒
   通关率上行漂移是**预期内**的,但幅度要量;超 ±3pt 要解释,别默认接受。
2. **目标分表(`data/run.json`)的连带**:货架变强 ⇒ 分数通胀 ⇒ 目标分相对变松。
   ⚠ 但 CLAUDE.md 那条仍然管着:**发挥系数不许拍初值,等用户自己的 Tape** ——
   **不要用这次的 sim 去反推目标分**。

⚠ 另有一条仪器债:`tools/probbook.py` **不产出曝光列**,所以 probbook.md 里的曝光小节
是手写的、**下次重刷会被冲掉**。曝光的权威读数在 `prior.gd` shelf 模式与本节。
