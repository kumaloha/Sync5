# 激励视频变现(海外 Godot 版)· 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 两个由需求触发的激励视频入口 —— 商店里点了买不起的东西时弹「差 N◆ · 看广告 +3◆」,体力为 0 时开局按钮的墙变成「看广告 +1⚡ · 马上开局」看完直接进局;离线时游戏与今天一像素不差。

**Architecture:** 规则数全在 `data/*.json`;`core/` 只加纯函数(数额、上限、体力入账)与 `Run.ad_used` 一个字段;`view/ads.gd` 是适配层(假后端 + 动态查类的 AdMob 后端 `view/admob.gd`),游戏其余部分只认 `has_ad / load_ad / show_ad` 三个方法与 `rewarded / failed / closed` 三个信号;发放只在编排器 `view/phrase.gd`,Tape 记 `ad` 事实;bot 加 `ads` 臂量上界,parity 表守两侧对齐,Lua 镜像补孪生只为门绿。

**Tech Stack:** Godot 4.6.2 / GDScript · Lua 5.5 镜像(`lua/`)· Poing Studios `godot-admob-plugin` v5(Android,Gradle 构建)· 单测 = `tests/runner.gd` 域文件 · 秒级预检六道门。

**规格:** [docs/superpowers/specs/2026-09-08-ads-design.md](../specs/2026-09-08-ads-design.md)(以它为准;本计划的方法名与它 §4.1 一致:`has_ad / load_ad / show_ad`)。

---

## 跑测试的口径(每个任务都用)

```bash
# 只跑相关域(分钟级)。⚠ 新增 class_name(Ads)之后第一次要先 --import。
godot --headless --path . --import
SYNC5_TEST_DOMAINS=t_db,t_economy godot --headless --path . --script res://tests/runner.gd
```
成功的形状:末行 `=== RESULT: N passed, 0 failed ===`,且日志里没有 `SCRIPT ERROR`。
**看退出码要直接看**(`echo $?`,别隔管道)。

```bash
# 秒级预检(改了 core/ 或 data/ 之后每次都跑)
python3 tools/parity.py --check && python3 tools/evsync.py --check && python3 tools/counts.py --check \
  && python3 tools/luagen.py --check && python3 tools/mirror.py --check && lua lua/check.lua
```

---

## 文件地图

| 文件 | 动作 | 职责 |
|---|---|---|
| `data/economy.json` | 改 | `ad_coins` / `ad_coins_per_shop` / `ad_coins_per_run` |
| `data/profile.json` | 改 | `ad_energy` / `ad_energy_per_day` |
| `data/ads.json` | **新** | `test_mode` / `test_unit` / `android.rewarded_coins` / `android.rewarded_energy` |
| `data/ui.json` | 改 | `shop.ad_offer_text` / `shop.ad_offer_text_plain` / `shop.ad_offer_pos` |
| `data/lingo.json` | 改 | 四条新中文的英文;删「看广告补体力 · 敬请期待」 |
| `data/sim.json` | 改 | 一条 `ads: true` 的 cohort |
| `core/db.gd` | 改 | 三个校验 + `ads()` 访问器 |
| `core/config.gd` · `lua/core/config.lua` | 改 | `AD_COINS*` 三个常量 |
| `core/economy.gd` · `lua/core/economy.lua` | 改 | `ad_coins()` / `ad_coins_allowed()` |
| `core/save.gd` · `lua/core/save.lua` | 改 | 体力广告纯函数层 + 两个公开口 |
| `core/run.gd` · `lua/core/run.lua` | 改 | `ad_used` 字段 + snapshot/restore/reset |
| `view/ads.gd` | **新** | 适配层(`class_name Ads`),假后端 |
| `view/admob.gd` | **新** | AdMob 后端(动态查类) |
| `view/shop.gd` | 改 | `denied(why, need)` · offer 小键 · `ad_requested` |
| `view/phrase.gd` | 改 | 商店广告流 · 体力墙 · Tape |
| `view/home.gd` | 改 | 只改注释(胶囊不是入口) |
| `tools/bot.gd` · `tools/parity.py` | 改 | `ads` 臂 · ENTRIES 加 `ad_coins` |
| `tools/adsprobe.gd` | **新** | 无头驱动两条流(假后端) |
| `tests/t_db.gd` · `t_economy.gd` · `t_save.gd` · `t_run.gd` · `t_shop.gd` · `t_lingo.gd`(不动,自动覆盖)· **`t_ads.gd`(新域)** | 改/新 | 契约 |
| `tests/runner.gd` | 改 | DOMAINS 末尾追加 `t_ads` |
| `export_presets.cfg` | 改 | `gradle_build/use_gradle_build=true` |
| `docs/design/telemetry.md` · `docs/design/tech.md` · `STATUS.md` · `TODO.md` · `CHANGELOG.md` | 改 | 事件表 · schema · 出包行 · 路线 ⑦ · 变更史 |

---

### Task 1: 数据与校验(JSON 三份 + db.gd + GameConfig 常量 + Lua 数据)

**Files:**
- Modify: `data/economy.json`(`"reroll"` 块之后)
- Modify: `data/profile.json`
- Create: `data/ads.json`
- Modify: `core/db.gd:38-40`(`_ECO_KEYS`)、`core/db.gd:114-127`(profile)、`core/db.gd:373-393`(economy)、`core/db.gd` 新增 `ads()`
- Modify: `core/config.gd:111-123`、`lua/core/config.lua:40-47`
- Test: `tests/t_db.gd`

- [ ] **Step 1: 写失败的测试**(`tests/t_db.gd`,追加在 `run()` 末尾)

```gdscript
	# ---- 激励视频(2026-09-08, 规格 docs/superpowers/specs/2026-09-08-ads-design.md)----
	t.eq(int(DB.economy()["ad_coins"]), int(DB.economy()["joker_prices"]["common"]),
		"一次广告给的金币 = 一张卡的钱(economy.json 平价)")
	var eco_bad: Dictionary = DB.economy().duplicate(true)
	eco_bad["ad_coins_per_shop"] = 3
	eco_bad["ad_coins_per_run"] = 2
	t.check(DB.validate_economy(eco_bad) != "", "每店上限大于每局上限被拒(每店那条会是死数据)")
	t.check(DB.validate_profile({"energy_max": 5, "xp_per_level": 4}) != "",
		"profile 缺 ad_energy / ad_energy_per_day 被拒")
	t.check(DB.validate_profile({"energy_max": 5, "xp_per_level": 4, "ad_energy": -1, "ad_energy_per_day": 5}) != "",
		"ad_energy 负数被拒")
	t.eq(DB.validate_ads(DB.ads()), "", "ads.json 干净")
	t.check(DB.validate_ads({"test_mode": false, "test_unit": "x",
		"android": {"rewarded_coins": "", "rewarded_energy": ""}}) != "",
		"test_mode=false 时空 unit ID 被拒(真 ID 归用户填)")
	t.check(DB.validate_ads({"test_mode": true, "test_unit": "",
		"android": {"rewarded_coins": "", "rewarded_energy": ""}}) != "",
		"test_unit 不许为空")
	t.check(bool(DB.ads()["test_mode"]), "仓库里的 ads.json 恒 test_mode=true(出正式包才翻)")
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_db godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -12
```
Expected: 若干 `x FAIL`,含 `Invalid access to property 'ad_coins'` 或 `validate_ads` 不存在的 SCRIPT ERROR。

- [ ] **Step 3: 改数据文件**

`data/economy.json` —— 在 `"reroll": {...},` 之后插入:
```json
 "_comment_ads": "激励视频换金币(2026-09-08 用户拍板, 规格 docs/superpowers/specs/2026-09-08-ads-design.md):ad_coins = 一次广告给的金币(= 一张卡的钱, 与 joker_prices 平价同量);ad_coins_per_shop / ad_coins_per_run = 每店 / 每局上限, 只数真发出去的钱(失败/关掉不计)。⚑ 目标分与 bot 基线按零广告标定, 这三个数是纯盈余的封顶;验收带超了(通关率 +5pt / 每局多买 2 张)改这里, 不改机制。",
 "ad_coins": 3,
 "ad_coins_per_shop": 1,
 "ad_coins_per_run": 2,
```

`data/profile.json` —— 改成:
```json
{
	"_comment": "首页顶栏的两个数(2026-08-24 用户:「顶部样式参考图 · 体力可以存在 · 金币/紫宝石去掉」)。energy_max = 每日体力上限 —— ⚑ 2026-08-26 用户拍板「体力是开局扣一点」, 已转正为真闸门:正式开局扣 1 点, 不足不开局;教学关不扣;跨天回满。余额存在存档 energy/energy_day 两键(core/save.gd), 旧档缺键当满值。xp_per_level = 每级需要的通关段数(EXP = 累计通关段数, 参与度口径, 明确不挂分数 —— 沿用 08-22「首页数字全真」拍板)。",
	"_comment_ads": "看广告换体力(2026-09-08 用户拍板「局外看广告买体力」):ad_energy = 一次给几点(入账不超满值);ad_energy_per_day = 每日上限(= energy_max ⇒ 一天最多 10 局)。入口 = 体力墙本身(view/phrase.gd::_deny_no_energy), 首页胶囊只显示;未满才许看(不能囤);只有真发奖才计数。",
	"energy_max": 5,
	"xp_per_level": 4,
	"ad_energy": 1,
	"ad_energy_per_day": 5
}
```

`data/ads.json`(新):
```json
{
 "_comment": "激励视频 SDK 配置(2026-09-08;view/ads.gd + view/admob.gd 是它唯一的消费者)。test_mode=true 时两个 kind 一律用 Google 官方 Android 激励视频测试单元 ID(不用测试 ID 可能封号), 只在出正式包那一步由用户翻 false 并填真 ID。AdMob App ID 不在这里 —— 它按插件文档填在插件自己的设置里(进 AndroidManifest meta-data), 归用户。",
 "test_mode": true,
 "test_unit": "ca-app-pub-3940256099942544/5224354917",
 "android": {
  "rewarded_coins": "",
  "rewarded_energy": ""
 }
}
```

- [ ] **Step 4: 改 `core/db.gd`**

`_ECO_KEYS`(第 38 行)改成:
```gdscript
const _ECO_KEYS := ["starting_coins", "discard_cost", "section_clear_reward",
	"draft_rarity_weights", "joker_prices", "joker_price_overrides",
	"reroll", "kind_coins", "ad_coins", "ad_coins_per_shop", "ad_coins_per_run"]
```

`validate_economy` 的 `return ""` 之前插入:
```gdscript
	# 激励视频换金币(2026-09-08):三个数 ≥ 0, 每店上限不许超过每局上限(否则每店那条是死数据)。
	for k in ["ad_coins", "ad_coins_per_shop", "ad_coins_per_run"]:
		if int(d[k]) < 0:
			return "%s 必须 >= 0" % k
	if int(d["ad_coins_per_shop"]) > int(d["ad_coins_per_run"]):
		return "ad_coins_per_shop 不许大于 ad_coins_per_run"
```

`validate_profile` 整个换成:
```gdscript
static func validate_profile(d: Dictionary) -> String:
	var e := _keys_ok(d, ["energy_max", "xp_per_level", "ad_energy", "ad_energy_per_day"])
	if e != "":
		return e
	# 0 或负数不是「关掉」而是静默除零/恒零 —— 要关体力显示去改 view, 别改成 0
	if int(d["energy_max"]) < 1 or int(d["xp_per_level"]) < 1:
		return "energy_max / xp_per_level 必须 >= 1"
	# 看广告换体力(2026-09-08):两键 ≥ 0(0 = 入口永远不出现, 允许, 但不许负)
	if int(d["ad_energy"]) < 0 or int(d["ad_energy_per_day"]) < 0:
		return "ad_energy / ad_energy_per_day 必须 >= 0"
	return ""
```

`profile()` 之后新增访问器与校验:
```gdscript
## 激励视频 SDK 配置(2026-09-08;`view/ads.gd` / `view/admob.gd` 是它唯一的消费者)。
static func ads() -> Dictionary:
	return _load("ads", func(d): return validate_ads(d))


static func validate_ads(d: Dictionary) -> String:
	var e := _keys_ok(d, ["test_mode", "test_unit", "android"])
	if e != "":
		return e
	if typeof(d["test_mode"]) != TYPE_BOOL:
		return "test_mode 必须是 bool"
	if String(d["test_unit"]) == "":
		return "test_unit 不许为空(开发期一律用官方测试单元 ID)"
	if not d["android"] is Dictionary:
		return "android 必须是对象"
	var a: Dictionary = d["android"]
	var ea := _keys_ok(a, ["rewarded_coins", "rewarded_energy"])
	if ea != "":
		return "android: " + ea
	if not bool(d["test_mode"]):
		for k in ["rewarded_coins", "rewarded_energy"]:
			if String(a[k]) == "":
				return "test_mode=false 时 android.%s 不许为空(真 ID 归用户填)" % k
	return ""
```

- [ ] **Step 5: GameConfig 常量(两边)**

`core/config.gd`,`DRAFT_REROLL_STEP` 那行之后:
```gdscript
## 激励视频换金币(2026-09-08;规格 docs/superpowers/specs/2026-09-08-ads-design.md §2)
static var AD_COINS: int = int(_eco["ad_coins"])
static var AD_COINS_PER_SHOP: int = int(_eco["ad_coins_per_shop"])
static var AD_COINS_PER_RUN: int = int(_eco["ad_coins_per_run"])
```

`lua/core/config.lua`,`GameConfig.DRAFT_REROLL_STEP` 那行之后:
```lua
-- 激励视频换金币(2026-09-08)
GameConfig.AD_COINS = num.int(_eco.ad_coins)
GameConfig.AD_COINS_PER_SHOP = num.int(_eco.ad_coins_per_shop)
GameConfig.AD_COINS_PER_RUN = num.int(_eco.ad_coins_per_run)
```

- [ ] **Step 6: 重生成 Lua 数据,跑测试与预检**

```bash
python3 tools/luagen.py
SYNC5_TEST_DOMAINS=t_db godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -5
python3 tools/luagen.py --check && python3 tools/mirror.py --check && lua lua/check.lua
```
Expected:`=== RESULT: N passed, 0 failed ===`;三道门都绿(`mirror` 会看到 `lua/data/ads.lua` 已生成)。

- [ ] **Step 7: 提交**

```bash
git add data/economy.json data/profile.json data/ads.json core/db.gd core/config.gd lua/core/config.lua lua/data/ tests/t_db.gd
git commit -m "广告 1/12 数据与校验:economy 三键 / profile 两键 / ads.json(测试 ID 恒开)+ db 校验 + GameConfig 常量两边 + luagen"
```

---

### Task 2: `Economy.ad_coins` / `ad_coins_allowed`(+ Lua 孪生)

**Files:**
- Modify: `core/economy.gd`(`reroll_cost` 之前或文件末尾)
- Modify: `lua/core/economy.lua`(`return Economy` 之前)
- Test: `tests/t_economy.gd`

- [ ] **Step 1: 写失败的测试**(`tests/t_economy.gd` 末尾)

```gdscript
	# ---- 激励视频换金币(2026-09-08)—— 数从 JSON 推导, 不抄死 ----
	t.eq(Economy.ad_coins(), int(DB.economy()["ad_coins"]), "ad_coins 读自 economy.json")
	var ps: int = GameConfig.AD_COINS_PER_SHOP
	var pr: int = GameConfig.AD_COINS_PER_RUN
	t.check(Economy.ad_coins_allowed(0, 0), "开局第一店允许")
	t.check(not Economy.ad_coins_allowed(0, ps), "每店上限到了就不许(同一店第二次)")
	t.check(not Economy.ad_coins_allowed(pr, 0), "每局上限到了就不许(新店也不行)")
	t.check(Economy.ad_coins_allowed(pr - 1, 0), "每局差一次仍许")
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_economy godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head
```
Expected:SCRIPT ERROR `Function "ad_coins()" not found`。

- [ ] **Step 3: 实现**

`core/economy.gd` 末尾追加:
```gdscript
## ---- 激励视频换金币(2026-09-08, 规格 docs/superpowers/specs/2026-09-08-ads-design.md)----
## 数全部来自 economy.json;目标分与 bot 基线按**零广告**标定, 这里只管「给多少」与「还能不能给」。
## ⚠ 两侧都要调(view/phrase.gd 与 tools/bot.gd)—— `tools/parity.py` 的 ENTRIES 守着 `ad_coins`。
static func ad_coins() -> int:
	return GameConfig.AD_COINS


## 还能不能再发一次:每店与每局两个上限都没到。只数真发出去的钱(失败 / 关掉没看完不计)。
static func ad_coins_allowed(run_used: int, shop_used: int) -> bool:
	return shop_used < GameConfig.AD_COINS_PER_SHOP and run_used < GameConfig.AD_COINS_PER_RUN
```

`lua/core/economy.lua`,`return Economy` 之前:
```lua
-- 激励视频换金币(2026-09-08):数在 economy.json;两个上限都没到才发。
function Economy.ad_coins()
	return GameConfig.AD_COINS
end

function Economy.ad_coins_allowed(run_used, shop_used)
	return shop_used < GameConfig.AD_COINS_PER_SHOP and run_used < GameConfig.AD_COINS_PER_RUN
end
```

- [ ] **Step 4: 跑,确认绿 + 镜像门**

```bash
SYNC5_TEST_DOMAINS=t_economy godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
python3 tools/mirror.py --check && lua lua/check.lua
```
Expected:`0 failed`;mirror 绿(公开函数计数 +2)。

- [ ] **Step 5: 提交**

```bash
git add core/economy.gd lua/core/economy.lua tests/t_economy.gd
git commit -m "广告 2/12 Economy.ad_coins / ad_coins_allowed(+Lua 孪生):数额与两级上限, 只数真发出去的钱"
```

---

### Task 3: `SaveState` 看广告换体力(纯函数层 + 公开口 + Lua 孪生)

**Files:**
- Modify: `core/save.gd`(体力节之后、`profile()` 之前)
- Modify: `lua/core/save.lua`(`spend_energy_for_run` 之后)
- Test: `tests/t_save.gd`

- [ ] **Step 1: 写失败的测试**(`tests/t_save.gd` 的 `run()` 末尾)

```gdscript
	# ---- ⑤ 看广告换体力(2026-09-08 用户拍板「局外看广告买体力」)—— 纯函数层, 数从 profile.json 推导 ----
	var per_day := SaveState.ad_energy_per_day()
	var amt := SaveState.ad_energy_amount()
	t.check(per_day >= 1 and amt >= 1, "profile.json 广告体力两键 ≥ 1(否则入口永远不出现)")
	var a := {"energy": 0, "energy_day": day}
	t.check(SaveState._ad_energy_can_in(a, day, cap, per_day), "0 体力今天没看过 ⇒ 可以看")
	t.check(SaveState._ad_energy_in(a, day, cap, per_day, amt), "看完入账")
	t.eq(SaveState._energy_in(a, day, cap), mini(cap, amt), "入账 = min(amount, 缺口)")
	t.eq(int(a["ad_energy_used"]), 1, "只有真发奖才计数, 计 1 次")
	t.eq(String(a["ad_energy_day"]), day, "次数盖今天的日期章")
	var full := {"energy": cap, "energy_day": day}
	t.check(not SaveState._ad_energy_can_in(full, day, cap, per_day), "满值不许看(不能囤)")
	t.check(not SaveState._ad_energy_in(full, day, cap, per_day, amt), "满值入账被拒")
	t.eq(int(full["energy"]), cap, "被拒时字典原样")
	t.check(not full.has("ad_energy_used"), "被拒不计数")
	var near := {"energy": cap - 1, "energy_day": day}
	t.check(SaveState._ad_energy_in(near, day, cap, per_day, amt + 5), "差 1 点时看一次")
	t.eq(SaveState._energy_in(near, day, cap), cap, "永不超满值")
	var maxed := {"energy": 0, "energy_day": day, "ad_energy_day": day, "ad_energy_used": per_day}
	t.check(not SaveState._ad_energy_can_in(maxed, day, cap, per_day), "今日次数到顶不许")
	var tomorrow := {"energy": 0, "energy_day": next_day, "ad_energy_day": day, "ad_energy_used": per_day}
	t.check(SaveState._ad_energy_can_in(tomorrow, next_day, cap, per_day), "跨日次数清零")
	var legacy2 := {"seen_tutorial": true, "energy": 0, "energy_day": day}
	t.check(SaveState._ad_energy_can_in(legacy2, day, cap, per_day), "旧档缺广告键 = 今天 0 次")
	# 探针闸(公开口):测试自己就是探针 —— 探针恒满 ⇒ 永远不许, 画面上永远没有这个入口
	t.check(not SaveState.can_add_energy_from_ad(), "探针 can_add 恒 false")
	t.check(not SaveState.add_energy_from_ad(), "探针 add 恒 false 且不落盘")
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_save godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head -5
```
Expected:SCRIPT ERROR `ad_energy_per_day` not found。

- [ ] **Step 3: 实现 `core/save.gd`**(插在 `_spend_in` 之后、`profile()` 之前)

```gdscript
## ---- 看广告换体力(2026-09-08 用户拍板「局外看广告买体力」;规格 docs/superpowers/specs/2026-09-08-ads-design.md §3.3)----
##
## 入口 = **体力墙本身**(view/phrase.gd::_deny_no_energy), 首页胶囊只显示(二审删了胶囊入口)。
## 规则:未满才许(不能囤)· 每日次数上限(profile.json ad_energy_per_day)· 入账不超满值 ·
## **只有真发奖才计数** —— 由调用方保证:它只在 `rewarded` 信号里调 `add_energy_from_ad`。
## 存档新键 `ad_energy_day` / `ad_energy_used`;跨日清零;旧档缺键 = 今天 0 次(不需要升版)。
## 探针:`can_add` 恒 false(探针恒满 ⇒ 画面上永远没有这个入口, 截图稳定), `add` no-op false。
## 算术仍拆成纯函数层(`_ad_energy_*_in`), 与体力那一节同一理由:测试自己就是探针。

static func ad_energy_amount() -> int:
	return int(DB.profile().get("ad_energy", 1))


static func ad_energy_per_day() -> int:
	return int(DB.profile().get("ad_energy_per_day", 0))


static func can_add_energy_from_ad() -> bool:
	if _is_probe():
		return false
	return _ad_energy_can_in(_data(), _day_key(), energy_max(), ad_energy_per_day())


## 看完入账 + 落盘。false = 不许(满值 / 次数到顶)且什么都没发生。
static func add_energy_from_ad() -> bool:
	if _is_probe():
		return false
	if not _ad_energy_in(_data(), _day_key(), energy_max(), ad_energy_per_day(), ad_energy_amount()):
		return false
	_flush()
	return true


## 今日已看几次(纯函数):日期章不是今天 = 0。
static func _ad_energy_used_in(d: Dictionary, day: String) -> int:
	if String(d.get("ad_energy_day", "")) != day:
		return 0
	return maxi(0, int(d.get("ad_energy_used", 0)))


## 还能不能看(纯函数):未满 且 今日次数未到。
static func _ad_energy_can_in(d: Dictionary, day: String, cap: int, per_day: int) -> bool:
	return _energy_in(d, day, cap) < cap and _ad_energy_used_in(d, day) < per_day


## 入账(纯函数):不许 = false 且**不动 d**;许 = 加 min(amount, 缺口)、次数 +1、两处盖日期章。
static func _ad_energy_in(d: Dictionary, day: String, cap: int, per_day: int, amount: int) -> bool:
	if not _ad_energy_can_in(d, day, cap, per_day):
		return false
	var cur := _energy_in(d, day, cap)
	var used := _ad_energy_used_in(d, day)
	d["energy_day"] = day
	d["energy"] = mini(cap, cur + maxi(0, amount))
	d["ad_energy_day"] = day
	d["ad_energy_used"] = used + 1
	return true
```

- [ ] **Step 4: Lua 孪生**(`lua/core/save.lua`,`spend_energy_for_run` 之后)

```lua
-- 看广告换体力(2026-09-08):未满才许 · 每日上限 · 入账不超满值 · 只有真发奖才计数。
function SaveState.ad_energy_amount()
	return num.int(num.get(DB.profile(), "ad_energy", 1))
end

function SaveState.ad_energy_per_day()
	return num.int(num.get(DB.profile(), "ad_energy_per_day", 0))
end

function SaveState._ad_energy_used_in(d, day)
	if tostring(num.get(d, "ad_energy_day", "")) ~= day then return 0 end
	return num.maxi(0, num.int(num.get(d, "ad_energy_used", 0)))
end

function SaveState._ad_energy_can_in(d, day, cap, per_day)
	return SaveState._energy_in(d, day, cap) < cap and SaveState._ad_energy_used_in(d, day) < per_day
end

function SaveState._ad_energy_in(d, day, cap, per_day, amount)
	if not SaveState._ad_energy_can_in(d, day, cap, per_day) then return false end
	local cur = SaveState._energy_in(d, day, cap)
	local used = SaveState._ad_energy_used_in(d, day)
	d.energy_day = day
	d.energy = num.mini(cap, cur + num.maxi(0, amount))
	d.ad_energy_day = day
	d.ad_energy_used = used + 1
	return true
end

function SaveState.can_add_energy_from_ad()
	if is_probe() then return false end
	return SaveState._ad_energy_can_in(SaveState._data(), SaveState.day_key(),
		SaveState.energy_max(), SaveState.ad_energy_per_day())
end

function SaveState.add_energy_from_ad()
	if is_probe() then return false end
	if not SaveState._ad_energy_in(SaveState._data(), SaveState.day_key(), SaveState.energy_max(),
			SaveState.ad_energy_per_day(), SaveState.ad_energy_amount()) then
		return false
	end
	SaveState._flush()
	return true
end
```

- [ ] **Step 5: 跑,确认绿 + 镜像门**

```bash
SYNC5_TEST_DOMAINS=t_save godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
python3 tools/mirror.py --check && lua lua/check.lua
```
Expected:`0 failed`;mirror 绿(+4 公开函数)。

- [ ] **Step 6: 提交**

```bash
git add core/save.gd lua/core/save.lua tests/t_save.gd
git commit -m "广告 3/12 SaveState 看广告换体力:纯函数层(未满才许 / 每日上限 / 不超满值 / 只有真发奖计数)+ 探针恒 false + Lua 孪生"
```

---

### Task 4: `Run.ad_used`(字段 + reset + snapshot/restore + Lua)

**Files:**
- Modify: `core/run.gd:96`(字段)、`reset()`(第 106 行起)、`snapshot()`(第 255 行起)、`restore()`(第 360 行附近)
- Modify: `lua/core/run.lua`(new / reset / snapshot / restore 四处)
- Test: `tests/t_run.gd:438-450`

- [ ] **Step 1: 写失败的测试**(`tests/t_run.gd`,在 `var snap: Dictionary = sr.snapshot(9)` 之前一行加 `sr.ad_used = 1`;在 `t.eq(rr.section_kinds, ...)` 之后加)

```gdscript
	t.eq(rr.ad_used, 1, "restore 还原 ad_used(广告换金币的每局账, 2026-09-08)")
	var snap_old: Dictionary = snap.duplicate(true)
	snap_old.erase("ad_used")
	var r_old := Run.new()
	r_old.reset(1)
	r_old.ad_used = 5
	t.check(r_old.restore(snap_old), "缺 ad_used 的旧快照照样读得回来(v 不变)")
	t.eq(r_old.ad_used, 0, "旧快照缺键 = 0 次")
	r_old.ad_used = 2
	r_old.reset(1)
	t.eq(r_old.ad_used, 0, "reset 清零(新局从 0 数)")
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_run godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head -5
```
Expected:SCRIPT ERROR `Invalid assignment of property 'ad_used'`。

- [ ] **Step 3: 实现 `core/run.gd`**

第 96 行 `var coins: int = 0` 之后:
```gdscript
## 本局已看的换金币广告次数(每局上限的账;每店计数在编排器 `_shop_ads`, 与 `_shop_buys` 同归属)。
## 2026-09-08 规格 docs/superpowers/specs/2026-09-08-ads-design.md §3.2。
var ad_used := 0
```

`reset()` 里 `coins = GameConfig.STARTING_COINS` 那行之后:
```gdscript
	ad_used = 0
```

`snapshot()` 返回字典里 `"boon": run_boon, "coins": coins, "faces": faces_out,` 改成:
```gdscript
		"boon": run_boon, "coins": coins, "ad_used": ad_used, "faces": faces_out,
```

`restore()` 里 `coins = int(d.get("coins", 0))` 之后:
```gdscript
	ad_used = int(d.get("ad_used", 0))     # 旧快照没有这个键 ⇒ 0, 不需要升版(与 debt 同款)
```

- [ ] **Step 4: Lua 孪生 `lua/core/run.lua`**

- `Run.new` 里 `self.coins = 0` 之后加 `self.ad_used = 0`;
- `Run:reset` 里 `self.coins = GameConfig.STARTING_COINS` 之后加 `self.ad_used = 0`;
- `Run:snapshot` 返回表里 `coins = self.coins,` 之后加 `ad_used = self.ad_used,`;
- `Run:restore` 里 `self.coins = num.int(num.get(d, "coins", 0))` 之后加 `self.ad_used = num.int(num.get(d, "ad_used", 0))`。

- [ ] **Step 5: 跑,确认绿 + 金样门**

```bash
SYNC5_TEST_DOMAINS=t_run godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
python3 tools/mirror.py --check && lua lua/check.lua
```
Expected:`0 failed`;`check.lua` 全绿(金样不含 snapshot, 不需要重生成 —— `grep -n snapshot tools/golden.gd` 为空可以自证)。

- [ ] **Step 6: 提交**

```bash
git add core/run.gd lua/core/run.lua tests/t_run.gd
git commit -m "广告 4/12 Run.ad_used:每局账进 snapshot/restore(缺键 0, v 不变)+ reset 清零 + Lua 孪生"
```

---

### Task 5: `view/ads.gd` 适配层(假后端)+ 新域 `t_ads`

**Files:**
- Create: `view/ads.gd`
- Create: `tests/t_ads.gd`
- Modify: `tests/runner.gd:18-30`(DOMAINS 末尾追加)

- [ ] **Step 1: 登记新域 + 写失败的测试**

`tests/runner.gd` DOMAINS 的 `"t_eco",` 之后加:
```gdscript
	# t_ads:激励视频适配层 + 商店/体力两条流的契约(2026-09-08)。不碰共享状态, 追加安全。
	"t_ads",
```

`tests/t_ads.gd`(新):
```gdscript
extends RefCounted

## 激励视频(2026-09-08, 规格 docs/superpowers/specs/2026-09-08-ads-design.md)。
## 这一域锁三层:① 适配层 view/ads.gd 的假后端与模式选择;② 商店 offer(见 t_shop);
## ③ 编排器两条流的**纯判定**(能不能弹 / 三种发奖情形)—— 流本身由 tools/adsprobe.gd 无头驱动。
## ⚠ 测试自己是 `--script` 起的探针:缺省模式必须是 off(探针零广告痕迹), 显式 SYNC5_ADS 才能改。

func run(t) -> void:
	# ---- ① 适配层 ----
	var a := Ads.new()
	t.get_root().add_child(a)
	t.eq(a.mode(), "off", "探针缺省 off(截图与日志零广告痕迹)")
	t.check(not a.has_ad("coins"), "off 时没货")
	t.check(not a.has_ad("energy"), "off 时没货(energy)")
	t.check(not a.has_ad("banana"), "未知 kind 永远没货")
	var fails: Array = []
	var rewards: Array = []
	var closes: Array = []
	a.failed.connect(func(k: String, why: String) -> void: fails.append([k, why]))
	a.rewarded.connect(func(k: String) -> void: rewards.append(k))
	a.closed.connect(func(k: String) -> void: closes.append(k))
	a.show_ad("coins")
	t.eq(fails.size(), 1, "没货时 show 立刻 failed")
	t.eq(fails[0][1], "not_ready", "……理由 not_ready")
	# 假后端的三条路径(直接打 _fake_finish, 不等帧)
	a._mode = "fake"
	t.check(a.has_ad("coins"), "fake 有货")
	a._fake_finish("coins")
	t.eq(rewards, ["coins"], "fake 看完发奖")
	t.eq(closes, ["coins"], "……然后关(先奖后关, 与真 SDK 同序)")
	a._mode = "fail"
	a._fake_finish("energy")
	t.eq(fails.size(), 2, "fail 模式发 failed")
	t.eq(fails[1][0], "energy", "……带 kind")
	t.eq(rewards.size(), 1, "fail 模式不发奖")
	a._mode = "dismiss"
	a._fake_finish("coins")
	t.eq(rewards.size(), 1, "dismiss 模式不发奖")
	t.eq(closes.size(), 3, "dismiss 模式只关")
	t.eq(Ads.pick_mode_for(true, "", false), "off", "探针 + 无环境变量 = off")
	t.eq(Ads.pick_mode_for(true, "fake", false), "fake", "显式 SYNC5_ADS=fake 盖过探针(adsprobe 用)")
	t.eq(Ads.pick_mode_for(false, "", false), "fake", "桌面缺省 fake")
	t.eq(Ads.pick_mode_for(false, "off", true), "off", "显式 off 盖过 android")
	t.eq(Ads.pick_mode_for(false, "", true), "admob", "android 且插件在 = admob")
	t.eq(Ads.pick_mode_for(false, "banana", false), "fake", "未知环境值当没设")
	a.queue_free()
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_ads godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head -5
```
Expected:SCRIPT ERROR `Identifier "Ads" not declared`。

- [ ] **Step 3: 实现 `view/ads.gd`**

```gdscript
class_name Ads
extends Node

## 激励视频适配层(2026-09-08, 规格 docs/superpowers/specs/2026-09-08-ads-design.md §4.1)。
## 对外只有三件事:**有没有货(has_ad)/ 放(show_ad)/ 回调发奖(rewarded 信号)**。
## 游戏其余部分只认这三个方法三个信号;插件 API 只许出现在 view/admob.gd 里。
##
## 后端选一次(_ready, 见 pick_mode_for —— 纯函数, 测试直打):
##   · 显式 `SYNC5_ADS=off|fake|fail|dismiss` **最优先**(adsprobe 用 fake 驱动真流;off = 离线玩家看到的游戏);
##   · 探针(SaveState.is_probe)缺省 **off** —— 探针日志与截图零广告痕迹(两台机器的日志必须长一样);
##   · Android 且插件在(res://addons/admob/plugin.cfg)—— view/admob.gd(动态查类, 桌面不解析它);
##   · 其余 = fake:show 下一帧发奖(桌面点着玩)。
## ⚠ 回调一律 call_deferred 回主线程再发信号(Godot 4 非主线程不得 emit_signal)。
## ⚠ 方法不叫 preload / show —— 前者是 GDScript 关键字, 后者与 CanvasItem 撞名。

signal rewarded(kind: String)
signal failed(kind: String, why: String)
signal closed(kind: String)

const KINDS := ["coins", "energy"]
const ADMOB_PLUGIN_CFG := "res://addons/admob/plugin.cfg"
const ENV_MODES := ["off", "fake", "fail", "dismiss"]

var _mode := "fake"          # off | fake | fail | dismiss | admob
var _backend = null          # view/admob.gd 实例(只在 admob 模式非空)


func _ready() -> void:
	_mode = pick_mode_for(SaveState.is_probe(), OS.get_environment("SYNC5_ADS"),
		OS.has_feature("android") and ResourceLoader.exists(ADMOB_PLUGIN_CFG))
	if _mode == "admob":
		_backend = load("res://view/admob.gd").new()
		add_child(_backend)
		_backend.rewarded.connect(func(k: String) -> void: rewarded.emit(k))
		_backend.failed.connect(func(k: String, why: String) -> void: failed.emit(k, why))
		_backend.closed.connect(func(k: String) -> void: closed.emit(k))


## 模式选择的算术(纯函数):显式环境变量 > 探针 off > android+插件 admob > fake。
static func pick_mode_for(probe: bool, env: String, android_plugin: bool) -> String:
	if ENV_MODES.has(env):
		return env
	if probe:
		return "off"
	if android_plugin:
		return "admob"
	return "fake"


func mode() -> String:
	return _mode


func has_ad(kind: String) -> bool:
	if not KINDS.has(kind):
		return false
	if _mode == "admob":
		return _backend.has_ad(kind)
	return _mode != "off"


func load_ad(kind: String) -> void:
	if _mode == "admob":
		_backend.load_ad(kind)


func show_ad(kind: String) -> void:
	if not has_ad(kind):
		failed.emit(kind, "not_ready")
		return
	if _mode == "admob":
		_backend.show_ad(kind)
		return
	call_deferred("_fake_finish", kind)


## 假后端的一次播放:按模式收尾(与真后端同序:先奖后关)。测试直打, 不等帧。
func _fake_finish(kind: String) -> void:
	match _mode:
		"fail":
			failed.emit(kind, "fake")
		"dismiss":
			pass
		_:
			rewarded.emit(kind)
	closed.emit(kind)
```

- [ ] **Step 4: `--import` 再跑,确认绿**

```bash
godot --headless --path . --import 2>&1 | tail -2
SYNC5_TEST_DOMAINS=t_ads godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
```
Expected:`0 failed`。

- [ ] **Step 5: 提交**

```bash
git add view/ads.gd view/ads.gd.uid tests/t_ads.gd tests/runner.gd
git commit -m "广告 5/12 view/ads.gd 适配层:has_ad/load_ad/show_ad + 三信号;假后端三条路径;模式选择纯函数(显式 SYNC5_ADS > 探针 off > android admob > fake)+ 新域 t_ads"
```

---

### Task 6: 商店 offer(`denied(why, need)` + 小键 + `ad_requested`)+ ui.json / lingo

**Files:**
- Modify: `view/shop.gd:14`(signal)、`:24-25`(vars)、`_layout()` 第 146 行之后、`close()`、`_on_cshelf_pressed`、`_on_pick`、`_on_reroll`
- Modify: `data/ui.json`(`shop` 节)、`data/lingo.json`
- Modify: `view/phrase.gd:286`(denied 的连接要跟着签名变 —— 本任务先改成占位函数, Task 7 填内容)
- Test: `tests/t_shop.gd`

- [ ] **Step 1: 写失败的测试**(`tests/t_shop.gd` 的 `run()` 末尾)

```gdscript
	# ---- 广告 offer(2026-09-08 二审:长在「想要但拿不到」那一刻)----
	var sh := Shop.new()
	t.get_root().add_child(sh)
	var reroll_pos: Vector2 = sh._reroll_btn.position
	var skip_pos: Vector2 = sh._skip_btn.position
	var skip_text: String = sh._skip_btn.text
	t.check(not sh.ad_offer_visible(), "缺省没有 offer(离线 = 今天的商店)")
	var got := 0
	sh.ad_requested.connect(func() -> void: got += 1)
	sh.show_ad_offer(2, 3)
	t.check(sh.ad_offer_visible(), "show 之后可见")
	t.check(sh._ad_btn.text.find("2") != -1 and sh._ad_btn.text.find("3") != -1, "文案带缺口与数额")
	t.eq(sh._reroll_btn.position, reroll_pos, "刷新键位置不动")
	t.eq(sh._skip_btn.position, skip_pos, "继续键位置不动")
	t.eq(sh._skip_btn.text, skip_text, "继续键文案不动")
	sh._layer.visible = true
	sh._on_ad_offer()
	t.eq(got, 1, "点 offer 发 ad_requested")
	sh.hide_ad_offer()
	t.check(not sh.ad_offer_visible(), "hide 之后不可见")
	sh._on_ad_offer()
	t.eq(got, 1, "藏着时点不发")
	sh.show_ad_offer(0, 3)
	t.check(sh._ad_btn.text.find("差") == -1, "缺口 0 用无缺口文案")
	sh.close()
	t.check(not sh.ad_offer_visible(), "close 收掉 offer")
	var before := sh._layer.get_child_count()
	sh._layout(3)
	sh._layout(4)
	t.eq(sh._layer.get_child_count(), before, "反复 _layout 不再建节点(offer 只建一次)")
	# denied 带缺口(need = 差几◆)
	var needs: Array = []
	sh.denied.connect(func(why: String, need: int) -> void: needs.append([why, need]))
	sh._coins = 1
	sh._reroll_count = 0
	sh._layer.visible = true
	sh._on_reroll()
	t.eq(needs.size(), 1, "刷不起发 denied")
	t.eq(needs[0][0], "reroll", "……why = reroll")
	t.eq(needs[0][1], Economy.reroll_cost(0) - 1, "……need = 刷新价 − 金币")
	sh.queue_free()
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_shop godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head -5
```
Expected:SCRIPT ERROR `ad_offer_visible` not found。

- [ ] **Step 3: 数据**

`data/ui.json` 的 `shop` 节,在 `"skip_text"` 那行之后加:
```json
  "_comment_ad_offer": "广告 offer(2026-09-08 二审):只在编排器判定「买不起 + 有货 + 上限没到 + 不是教学关」时出现, 锚在底栏两键之下、消耗牌碟之上(btn_y 806 + 58 高, cons_y 912 ⇒ 868..908 这 40px 正好), 缺省藏着 —— 两键布局一像素不动。缺口算不出(0)用 plain 文案。",
  "ad_offer_text": "差 %d◆ · 看广告 +%d◆",
  "ad_offer_text_plain": "看广告 +%d◆",
  "ad_offer_pos": [186, 868],
```

`data/lingo.json` 的 `table` 里加(位置随意, 建议放在「◆ 不足」附近):
```json
  "差 %d◆ · 看广告 +%d◆": "Short %d◆ · Watch an ad +%d◆",
  "看广告 +%d◆": "Watch an ad +%d◆",
```

- [ ] **Step 4: 实现 `view/shop.gd`**

信号(第 14 行)改成:
```gdscript
signal denied(why: String, need: int)  # 想买/想刷但钱不够 — 编排器打点;need = 差几◆(算不出 0), 广告 offer 的触发点(2026-09-08)
signal ad_requested()                 # 玩家点了广告 offer — 编排器放广告、发奖、计数(视图不碰钱不数次数)
```

vars(`_skip_btn` 那行之后):
```gdscript
var _ad_btn: Button = null      # 广告 offer 小键(2026-09-08):只在编排器 show_ad_offer 时可见
```

`_layout()` 里 `_skip_btn.pressed.connect(_on_skip)` / `_layer.add_child(_skip_btn)` 那个 `if _reroll_btn == null:` 块之后、`if _cshelf.is_empty():` 之前插入:
```gdscript
	# 广告 offer(2026-09-08 二审:长在「想要但拿不到」那一刻)。**只建一次**(同上那条泄漏教训),
	# 缺省藏着 —— 没 offer 时商店与今天一像素不差(离线玩家的红线)。
	if _ad_btn == null:
		_ad_btn = _button("")
		_ad_btn.custom_minimum_size = Vector2(348, 40)
		_ad_btn.add_theme_font_size_override("font_size", 17)
		_ad_btn.add_theme_color_override("font_color", StageTheme.GOLD)
		_ad_btn.visible = false
		_ad_btn.pressed.connect(_on_ad_offer)
		_layer.add_child(_ad_btn)
	_ad_btn.position = _v2(_cfg["ad_offer_pos"])
	_ad_btn.size = Vector2(348, 40)
```

`close()` 里 `_layer.visible = false` 之前加:
```gdscript
	hide_ad_offer()          # offer 跟着这家店走
```

`_on_cshelf_pressed` 两处 `denied.emit("consumable")` 分别改成:
```gdscript
		denied.emit("consumable", maxi(0, int(_c.price) - _coins))
```
```gdscript
		denied.emit("consumable", 0)     # 压暗不是钱的事, 缺口 0
```

`_on_pick` 里 `denied.emit("price")` 改成:
```gdscript
		denied.emit("price", maxi(0, _price(j) - _coins))
```

`_on_reroll` 里 `denied.emit("reroll")` 改成:
```gdscript
		denied.emit("reroll", maxi(0, cost - _coins))
```

在 `_on_skip()` 之前新增:
```gdscript
## 广告 offer(2026-09-08 二审)。何时能弹由编排器判(有货 / 两级上限 / 不是教学关),
## 视图只管画与转发点击;need = 差几◆(0 = 算不出, 用无缺口文案)。
func show_ad_offer(need: int, amount: int) -> void:
	if _ad_btn == null:
		return
	_ad_btn.text = (String(_cfg["ad_offer_text"]) % [need, amount]) if need > 0 \
		else (String(_cfg["ad_offer_text_plain"]) % amount)
	_ad_btn.visible = true
	_pop(_ad_btn)


func hide_ad_offer() -> void:
	if _ad_btn != null:
		_ad_btn.visible = false


func ad_offer_visible() -> bool:
	return _ad_btn != null and _ad_btn.visible


func _on_ad_offer() -> void:
	if not _layer.visible or not ad_offer_visible():
		return
	ad_requested.emit()
```

- [ ] **Step 5: 编排器的连接跟着签名变**(`view/phrase.gd:286`,只改这一行;函数体 Task 7 填)

把
```gdscript
	shop.denied.connect(func(why: String) -> void: Tape.on("deny", {"why": why}))
```
改成
```gdscript
	shop.denied.connect(_on_shop_denied)
	shop.ad_requested.connect(_on_shop_ad_requested)
```
并在 `_on_shop_skipped()` 之前先放两个占位(Task 7 会整个换掉):
```gdscript
func _on_shop_denied(why: String, _need: int) -> void:
	Tape.on("deny", {"why": why})


func _on_shop_ad_requested() -> void:
	pass
```

- [ ] **Step 6: 跑 t_shop + t_lingo + t_db,确认绿**

```bash
SYNC5_TEST_DOMAINS=t_shop,t_lingo,t_db godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
python3 tools/luagen.py && python3 tools/luagen.py --check
```
Expected:`0 failed`(t_lingo 会检查 ui.json 新叶子都在表里;t_db 会检查 `%` 占位符两边同数)。

- [ ] **Step 7: 提交**

```bash
git add view/shop.gd view/phrase.gd data/ui.json data/lingo.json lua/data/ tests/t_shop.gd
git commit -m "广告 6/12 商店 offer:denied 带缺口 need;show/hide_ad_offer 小键(只建一次、缺省藏、两键布局零改动)+ ad_requested;ui.json/lingo 文案"
```

---

### Task 7: 编排器 —— 商店广告流(发奖三情形 + Tape)

**Files:**
- Modify: `view/phrase.gd`:vars(第 94 行附近)、`_ready`(第 271 行 beacon 之后)、`_open_draft`(第 1701 行 `_shop_buys = 0` 处)、Task 6 的两个占位函数、新增 `_on_ad_rewarded / _on_ad_failed / _on_ad_closed`
- Test: `tests/t_ads.gd`(追加 ③ 纯判定)

- [ ] **Step 1: 写失败的测试**(`tests/t_ads.gd` 的 `run()` 末尾,`a.queue_free()` 之后)

```gdscript
	# ---- ③ 编排器的纯判定(view/phrase.gd 静态函数, 流本身由 tools/adsprobe.gd 驱动)----
	var PV = load("res://view/phrase.gd")
	t.check(PV.ad_offer_ok(true, false, 0, 0), "有货 + 非教学 + 零次 ⇒ 弹")
	t.check(not PV.ad_offer_ok(false, false, 0, 0), "没货不弹")
	t.check(not PV.ad_offer_ok(true, true, 0, 0), "教学关不弹")
	t.check(not PV.ad_offer_ok(true, false, 0, GameConfig.AD_COINS_PER_SHOP), "本店到顶不弹")
	t.check(not PV.ad_offer_ok(true, false, GameConfig.AD_COINS_PER_RUN, 0), "本局到顶不弹")
	# 发奖三情形:store / late / drop(规格 §4.3 表)
	t.eq(PV.ad_reward_case(true, 4), "store", "run 活着且在 DRAFT(4)⇒ 店内入账")
	t.eq(PV.ad_reward_case(true, 2), "late", "run 活着但在拍中(DECISION)⇒ 晚到照发")
	t.eq(PV.ad_reward_case(false, 4), "drop", "run 没了 ⇒ 丢")
	t.eq(PV.ad_reward_case(true, 0), "drop", "FRONT ⇒ 丢(首页没有局)")
	t.eq(PV.ad_reward_case(true, 5), "drop", "END ⇒ 丢(结算屏之后没有店可花)")
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_ads godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head -5
```
Expected:SCRIPT ERROR `ad_offer_ok` not found。

- [ ] **Step 3: 实现**

`view/phrase.gd` vars —— `var beacon: Beacon` 那行之后:
```gdscript
var ads: Ads                 # 激励视频适配层(view/ads.gd, 2026-09-08;探针缺省无货)
```

`_shop_buys` 声明(第 1761 行)之后:
```gdscript
## 本店已发的换金币广告次数(每店上限的账;每局的账在 run.ad_used)。进店归零, 与 _shop_buys 同款。
var _shop_ads := 0
```

`_ready` 里 `add_child(beacon)` 之后:
```gdscript
	ads = Ads.new()
	add_child(ads)
	ads.rewarded.connect(_on_ad_rewarded)
	ads.failed.connect(_on_ad_failed)
	ads.closed.connect(_on_ad_closed)
```

`_open_draft` 里 `_shop_buys = 0` 那行之后:
```gdscript
	_shop_ads = 0          # 广告的每店账(2026-09-08)
	ads.load_ad("coins")   # 进店就预载, 点 offer 时才有货
```

把 Task 6 的两个占位函数换成(放在 `_on_shop_skipped()` 之前):
```gdscript
## 能不能弹广告 offer(纯判定, t_ads 直打):有货 · 不是教学关 · 两级上限没到。
static func ad_offer_ok(has_ad: bool, tutorial: bool, run_used: int, shop_used: int) -> bool:
	return has_ad and not tutorial and Economy.ad_coins_allowed(run_used, shop_used)


## 发奖落在哪种情形(纯判定, t_ads 直打;规格 §4.3 表):
## store = run 活着且在店里 · late = run 活着但店关了(回调晚到, 照发)· drop = 没有局可发。
static func ad_reward_case(run_alive: bool, st: int) -> String:
	if not run_alive or st == St.FRONT or st == St.END:
		return "drop"
	return "store" if st == St.DRAFT else "late"


## 商店里「想要但拿不到」那一刻(2026-09-08 二审:广告 offer 只长在这里)。
## 打点照旧;视图只画, 判断只在这里(经济动作只发生在编排器)。
func _on_shop_denied(why: String, need: int) -> void:
	Tape.on("deny", {"why": why})
	if state != St.DRAFT:
		return
	if ad_offer_ok(ads.has_ad("coins"), run.tutorial, run.ad_used, _shop_ads):
		shop.show_ad_offer(need, Economy.ad_coins())


func _on_shop_ad_requested() -> void:
	if state != St.DRAFT or replace.pick != null:
		return
	if not ad_offer_ok(ads.has_ad("coins"), run.tutorial, run.ad_used, _shop_ads):
		shop.hide_ad_offer()
		return
	Tape.on("ad", {"k": "coins", "ev": "show"})
	ads.show_ad("coins")


## 发奖 —— 一份判定三种情形(ad_reward_case)。⚠ 入账走 Economy.grant(金币上限那条铁律:所有入账都走它)。
## energy 那一支在体力墙那节(_on_energy_ad_rewarded)。
func _on_ad_rewarded(kind: String) -> void:
	if kind == "energy":
		_on_energy_ad_rewarded()
		return
	if kind != "coins":
		return
	var c := ad_reward_case(phrase != null, state)
	if c == "drop":
		Tape.on("ad", {"k": "coins", "ev": "drop"})
		return
	phrase.coins = Economy.grant(phrase.coins, Economy.ad_coins(), run.joker_slots)
	run.ad_used += 1
	var ev := {"k": "coins", "ev": "reward", "coins": phrase.coins}
	if c == "store":
		_shop_ads += 1
		shop.hide_ad_offer()
		shop.set_buys_left(shop._buys_left, phrase.coins)   # 副标题的余额跟上;货架不重掷
		shop._render(false)                                 # 价签的可购性按新余额重算
	else:
		ev["late"] = true
	Tape.on("ad", ev)
	_refresh()


func _on_ad_failed(kind: String, why: String) -> void:
	Tape.on("ad", {"k": kind, "ev": "fail", "why": why})
	if kind == "energy":
		_on_energy_ad_failed()
		return
	shop.hide_ad_offer()
	if state == St.DRAFT:
		var at: Array = DB.ui()["shop"]["ad_offer_pos"]
		fx.float_text(Lingo.t("广告暂时没有,稍后再试"), Vector2(float(at[0]), float(at[1])), Color("ff5f7e"), 90)


## 关掉(含没看完):金币那支什么都不做 —— offer 还在(若仍有货), 上限只数真发出去的钱。
func _on_ad_closed(kind: String) -> void:
	if kind == "energy":
		_on_energy_ad_closed()
```

同时先放三个体力墙的占位(Task 8 填):
```gdscript
func _on_energy_ad_rewarded() -> void:
	pass


func _on_energy_ad_failed() -> void:
	pass


func _on_energy_ad_closed() -> void:
	pass
```

`data/lingo.json` 加:
```json
  "广告暂时没有,稍后再试": "No ad available right now — try again later",
```

- [ ] **Step 4: 跑 t_ads + t_lingo,确认绿**

```bash
SYNC5_TEST_DOMAINS=t_ads,t_lingo godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
python3 tools/luagen.py && python3 tools/luagen.py --check
```
Expected:`0 failed`。

- [ ] **Step 5: 提交**

```bash
git add view/phrase.gd data/lingo.json lua/data/ tests/t_ads.gd
git commit -m "广告 7/12 编排器商店流:denied ⇒ offer(纯判定 ad_offer_ok)· 发奖三情形 store/late/drop(纯判定 ad_reward_case, 入账走 Economy.grant)· Tape ad 事实 show/reward/fail/drop"
```

---

### Task 8: 编排器 —— 体力墙(看完直接开局)+ home.gd 注释 + 删旧文案

**Files:**
- Modify: `view/phrase.gd:1549-1553`(`_deny_no_energy`)+ Task 7 的三个占位 + 新增 `_open_energy_wall / _close_energy_wall / _replay_pending_start`
- Modify: `view/home.gd:333-335`(注释)
- Modify: `data/lingo.json`(删「看广告补体力 · 敬请期待」,加两条)
- Test: `tests/t_ads.gd`(追加纯判定)、`tests/t_lingo.gd` 自动覆盖

- [ ] **Step 1: 写失败的测试**(`tests/t_ads.gd` 末尾)

```gdscript
	# 体力墙的纯判定:有货 且 存档允许(未满 + 今日未到顶)才把墙变成入口;探针强制开关只给 adsprobe 用
	t.check(PV.energy_wall_ok(true, true), "有货 + 存档允许 ⇒ 墙变入口")
	t.check(not PV.energy_wall_ok(false, true), "没货 ⇒ 今天的行为(明天回满)")
	t.check(not PV.energy_wall_ok(true, false), "存档不许(满值/到顶)⇒ 今天的行为")
	t.check(not DB.lingo()["table"].has("看广告补体力 · 敬请期待"), "「敬请期待」那句已删(表里没有)")
```

- [ ] **Step 2: 跑,确认红**

```bash
SYNC5_TEST_DOMAINS=t_ads godot --headless --path . --script res://tests/runner.gd 2>&1 | grep -E "FAIL|RESULT|SCRIPT ERROR" | head -5
```
Expected:SCRIPT ERROR `energy_wall_ok` not found。

- [ ] **Step 3: 实现体力墙**(`view/phrase.gd`,整个替换 `_deny_no_energy` 及其注释,并换掉 Task 7 的三个占位)

```gdscript
## 体力墙(2026-08-26 真闸门 → 2026-09-08 二审:墙本身变成入口)。
## 两条路(首页开始 / 结算屏再来一次)都先 `_open_home()`(首页是底), 有货且存档允许时在其上弹一层:
## 「体力不足」+「看广告 +1⚡ · 马上开局」;看完 ⇒ 入账 ⇒ 关层 ⇒ **下一帧重放 start_run()** ——
## 开局仍只有那一份入口(「第二条入口漏掉主路径的步骤」是这个项目最贵的形状), 多等一帧是让
## Android 的 RESUMED 与 EGL surface 先回来。关掉没看完 / 失败 ⇒ 关层, 回到今天的浮字。
## 首页体力胶囊**只显示, 不是入口**(二审删)。探针恒满 ⇒ 永远走今天的分支;
## `SYNC5_PROBE_ENERGY_WALL=1` 让 adsprobe 在探针里也能把层打开(仅探针生效)。
var _energy_layer: Control = null
var _energy_ad_showing := false
var _pending_start: Callable = Callable()   # 只活到这一层关闭;不落盘、不跨场景


## 纯判定(t_ads 直打):有货 且 存档允许。
static func energy_wall_ok(has_ad: bool, save_ok: bool) -> bool:
	return has_ad and save_ok


func _deny_no_energy() -> void:
	Tape.on("deny", {"why": "energy"})
	_open_home()
	var save_ok := SaveState.can_add_energy_from_ad() \
		or (SaveState.is_probe() and OS.get_environment("SYNC5_PROBE_ENERGY_WALL") == "1")
	if energy_wall_ok(ads.has_ad("energy"), save_ok):
		_pending_start = Callable(self, "start_run")
		ads.load_ad("energy")
		_open_energy_wall()
		return
	fx.float_text(Lingo.t("体力不足,明天回满"), Vector2(243.0, 986.0), Color("ff5f7e"), 90)


func _open_energy_wall() -> void:
	_close_energy_wall(false)
	var layer := Control.new()
	layer.position = Vector2.ZERO
	layer.size = Vector2(720, 1280)
	layer.z_index = 130
	layer.mouse_filter = Control.MOUSE_FILTER_STOP
	# 点空白处 = 关掉(不看):回到今天的浮字
	layer.gui_input.connect(func(e: InputEvent) -> void:
		if e is InputEventMouseButton and e.pressed and not _energy_ad_showing:
			_close_energy_wall(false)
			fx.float_text(Lingo.t("体力不足,明天回满"), Vector2(243.0, 986.0), Color("ff5f7e"), 90))
	var dim := ColorRect.new()
	dim.color = Color(0, 0, 0.02, 0.74)
	dim.size = Vector2(720, 1280)
	layer.add_child(dim)
	var panel := Panel.new()
	panel.add_theme_stylebox_override("panel",
		StageTheme.box(Color(0.04, 0.05, 0.12, 0.96), StageTheme.GOLD, 1, 16))
	panel.position = Vector2(150, 500)
	panel.size = Vector2(420, 190)
	layer.add_child(panel)
	var title := StageTheme.label(Lingo.t("体力不足"), StageTheme.zh(), 23,
		StageTheme.GOLD, HORIZONTAL_ALIGNMENT_CENTER)
	title.position = Vector2(150, 524)
	title.size = Vector2(420, 32)
	layer.add_child(title)
	var go := Button.new()
	go.text = Lingo.t("看广告 +%d⚡ · 马上开局") % SaveState.ad_energy_amount()
	go.add_theme_font_override("font", StageTheme.zh())
	go.add_theme_font_size_override("font_size", 19)
	go.focus_mode = Control.FOCUS_NONE
	for st in ["normal", "hover", "pressed"]:
		go.add_theme_stylebox_override(st,
			StageTheme.box(Color(0.20, 0.14, 0.04, 0.95), StageTheme.GOLD, 1, 12))
	go.add_theme_color_override("font_color", Color("ffe6b3"))
	go.position = Vector2(186, 600)
	go.size = Vector2(348, 46)
	go.pressed.connect(func() -> void:
		if _energy_ad_showing:
			return
		_energy_ad_showing = true
		Tape.on("ad", {"k": "energy", "ev": "show"})
		ads.show_ad("energy"))
	layer.add_child(go)
	add_child(layer)
	_energy_layer = layer


## 关层。`start` = 看完了要开局(下一帧重放 _pending_start);否则清掉意图。
func _close_energy_wall(start: bool) -> void:
	_energy_ad_showing = false
	if _energy_layer != null and is_instance_valid(_energy_layer):
		_energy_layer.queue_free()
	_energy_layer = null
	if start:
		call_deferred("_replay_pending_start")
	else:
		_pending_start = Callable()


func _replay_pending_start() -> void:
	var c := _pending_start
	_pending_start = Callable()
	if c.is_valid():
		c.call()


func _on_energy_ad_rewarded() -> void:
	if not _energy_ad_showing:
		return
	# 入账失败(跨日已回满等)也照样开局 —— 玩家真看了, 而且此时体力本来就够
	SaveState.add_energy_from_ad()
	Tape.on("ad", {"k": "energy", "ev": "reward"})
	fx.float_text("+%d⚡" % SaveState.ad_energy_amount(), Vector2(243.0, 986.0), StageTheme.GOLD, 90)
	_close_energy_wall(true)


func _on_energy_ad_failed() -> void:
	if not _energy_ad_showing:
		return
	_close_energy_wall(false)
	fx.float_text(Lingo.t("广告暂时没有,稍后再试"), Vector2(243.0, 986.0), Color("ff5f7e"), 90)


## 关掉没看完:发奖那条已经把层关了就是 no-op;层还在 = 没看完 ⇒ 回到今天的浮字。
func _on_energy_ad_closed() -> void:
	if not _energy_ad_showing:
		return
	_close_energy_wall(false)
	fx.float_text(Lingo.t("体力不足,明天回满"), Vector2(243.0, 986.0), Color("ff5f7e"), 90)
```

`data/lingo.json`:删除 `"看广告补体力 · 敬请期待": "Watch an ad to refuel — coming soon",`,加:
```json
  "体力不足": "Out of energy",
  "看广告 +%d⚡ · 马上开局": "Watch an ad +%d⚡ · Play now",
```

`view/home.gd:334-335` 两行注释换成:
```gdscript
	# 入口不在这颗胶囊上(2026-09-08 二审:「没需求时囤体力只有计划型玩家会做」)—— 它只显示;
	# 看广告换体力的入口是体力墙本身(view/phrase.gd::_deny_no_energy)。
```

- [ ] **Step 4: 跑 t_ads + t_lingo,确认绿**

```bash
SYNC5_TEST_DOMAINS=t_ads,t_lingo godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
python3 tools/luagen.py && python3 tools/luagen.py --check && python3 tools/mirror.py --check
```
Expected:`0 failed`;mirror 绿(`_deny_no_energy` 不在 APP_MANIFEST 里, 新函数不需要孪生)。

- [ ] **Step 5: 提交**

```bash
git add view/phrase.gd view/home.gd data/lingo.json lua/data/ tests/t_ads.gd
git commit -m "广告 8/12 体力墙变入口:首页为底 + 「看广告 +1⚡ · 马上开局」层, 看完下一帧重放 start_run(唯一开局入口);关掉/失败回今天的浮字;删「敬请期待」;胶囊只显示"
```

---

### Task 9: 仪器 —— bot `ads` 臂 + parity + sim cohort

**Files:**
- Modify: `tools/bot.gd:443`(`coins = _consumables_in_shop(...)` 之前)
- Modify: `tools/parity.py:23-31`(ENTRIES)
- Modify: `data/sim.json`(`cohorts` 追加一条)

- [ ] **Step 1: 先看 parity 现在是绿的(基线)**

```bash
python3 tools/parity.py --check; echo "rc=$?"
```
Expected:rc=0。

- [ ] **Step 2: bot 臂**(`tools/bot.gd`,`coins = _consumables_in_shop(run, coins, slots)` 那行之前插入)

```gdscript
	# ⚑ 广告换金币的**上界臂**(2026-09-08, 规格 docs/superpowers/specs/2026-09-08-ads-design.md §6)。
	# cfg.ads 缺省关 = 零广告基线(所有既有读数逐位不动);开 = 每店只要还许就先领 ——
	# 真人是「买不起才看」, 这里量的是上界。验收带:通关率 +≤5pt、每局多买 ≤2 张(用户喊了才跑)。
	# ⚠ 与游戏侧 view/phrase.gd::_on_ad_rewarded **成对**(parity ENTRIES 守 `ad_coins`);
	#   入账同样走 Economy.grant(金币上限那条铁律)。每店计数在这里恒 0:一店只进一次。
	if bool(cfg.get("ads", false)) and run != null \
			and Economy.ad_coins_allowed(run.ad_used, 0):
		coins = Economy.grant(coins, Economy.ad_coins(), slots)
		run.ad_used += 1
```

- [ ] **Step 3: parity ENTRIES 加键**(`tools/parity.py`,`"take_consumable", "add_wilds", "trim_low_ranks",` 之后)

```python
    # 广告换金币(2026-09-08):游戏侧 phrase.gd 发奖、bot 侧 _draft 上界臂, 两边都要调 Economy.ad_coins
    "ad_coins",
```

- [ ] **Step 4: sim cohort**(`data/sim.json` 的 `cohorts` 数组末尾追加)

```json
  {"name": "adaptive:twin+ads", "bot": "adaptive", "target": "twin", "ads": true}
```
(与 `adaptive:twin` 共用随机种子:`_rng.seed = 90000 + r` 按局号定, cohort 顺序不影响。)

- [ ] **Step 5: 预检 + t_db(sim.json 校验)**

```bash
python3 tools/parity.py --check && python3 tools/evsync.py --check && python3 tools/counts.py --check \
  && python3 tools/luagen.py && python3 tools/luagen.py --check && python3 tools/mirror.py --check && lua lua/check.lua
SYNC5_TEST_DOMAINS=t_db godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -3
```
Expected:六道门全绿(parity 应打印 `ad_coins` 两侧计数都 ≥1);t_db `0 failed`。
⚠ **不跑 sim**(门纪律:用户喊了才跑)。跑法记在 Task 12 的 STATUS 行里。

- [ ] **Step 6: 提交**

```bash
git add tools/bot.gd tools/parity.py data/sim.json lua/data/
git commit -m "广告 9/12 仪器:bot ads 上界臂(缺省关 = 零广告基线)· parity ENTRIES 加 ad_coins · sim 加 adaptive:twin+ads cohort(不跑, 等用户喊)"
```

---

### Task 10: 无头探针 `tools/adsprobe.gd`(假后端驱动两条流)

**Files:**
- Create: `tools/adsprobe.gd`

- [ ] **Step 1: 写探针**

```gdscript
extends SceneTree
## 激励视频两条流的无头回归(2026-09-08)。假后端驱动, 退出码非 0 = 有违规:
##   SYNC5_ADS=fake SYNC5_PROBE_ENERGY_WALL=1 godot --headless --path . --script res://tools/adsprobe.gd
## 流 A(商店):进店 → 把钱清零 → 点刷新(买不起 ⇒ denied ⇒ offer)→ 点 offer → 下一帧发奖
##   ⇒ 金币 +ad_coins、run.ad_used +1、offer 收起;同店再点刷新 ⇒ **不再弹**(每店上限);
##   下一店再来一次 ⇒ ad_used 2;第三店 ⇒ 不弹(每局上限)。
## 流 B(体力墙):首页上直接调 _deny_no_energy()(探针恒满, 靠 SYNC5_PROBE_ENERGY_WALL 开层)
##   ⇒ 层在;点键 ⇒ 下一帧发奖 ⇒ 层没了 ⇒ 再下一帧 **run 开了**(state 离开 FRONT)。
## ⚠ 靠 SYNC5_ADS=fake 显式盖过探针的 off(view/ads.gd::pick_mode_for)—— 不设就全是 no-op, 探针会红。
var _scene: Node
var _frames := 0
var _bugs := 0
var _stage := 0
var _shops := 0
var _coins_before := 0
var _wait := 0


func _initialize() -> void:
	get_root().set_content_scale_size(Vector2i(720, 1280))
	if OS.get_environment("SYNC5_ADS") != "fake":
		print("!!! 需要 SYNC5_ADS=fake(探针缺省 off)")
		quit(2)
		return
	_scene = load("res://view/phrase.tscn").instantiate()
	get_root().add_child(_scene)


func _bug(msg: String) -> void:
	print("!!! BUG: " + msg)
	_bugs += 1


func _process(_delta: float) -> bool:
	_frames += 1
	if _frames > 20000:
		_bug("超时:探针没跑完(stage=%d)" % _stage)
		return _finish()
	if _wait > 0:
		_wait -= 1
		return false
	match _stage:
		0:
			if _frames == 4:
				_scene._on_home_start()
			if _frames == 8:
				_scene.start_run()
				_stage = 1
		1:
			# 等一家店开
			if _scene.state == _scene.St.DRAFT:
				_shops += 1
				_scene.phrase.coins = 0
				_scene.shop._render(false)
				_coins_before = _scene.phrase.coins
				_scene.shop._on_reroll()      # 买不起 ⇒ denied ⇒ offer
				_stage = 2
			else:
				_drive_beat()
		2:
			var expect_offer: bool = _shops <= GameConfig.AD_COINS_PER_RUN
			if _scene.shop.ad_offer_visible() != expect_offer:
				_bug("第 %d 店 offer 可见=%s, 应为 %s" % [_shops, _scene.shop.ad_offer_visible(), expect_offer])
			if expect_offer:
				_scene.shop._on_ad_offer()
				_wait = 2
				_stage = 3
			else:
				_stage = 4
		3:
			if _scene.phrase.coins != _coins_before + Economy.ad_coins():
				_bug("发奖后金币 %d, 应为 %d" % [_scene.phrase.coins, _coins_before + Economy.ad_coins()])
			if _scene.run.ad_used != _shops:
				_bug("run.ad_used=%d, 应为 %d" % [_scene.run.ad_used, _shops])
			if _scene.shop.ad_offer_visible():
				_bug("发奖后 offer 没收起")
			_scene.phrase.coins = 0
			_scene.shop._on_reroll()       # 同店第二次:每店上限 ⇒ 不弹
			if _scene.shop.ad_offer_visible():
				_bug("同一店第二次弹了(每店上限失效)")
			_stage = 4
		4:
			_scene.shop._on_skip()         # 离店
			if _shops >= GameConfig.AD_COINS_PER_RUN + 1:
				_stage = 5
			else:
				_stage = 1
		5:
			# 流 B:回首页, 直接撞墙
			if _scene.state != _scene.St.FRONT:
				_scene._on_quit_run() if _scene.has_method("_on_quit_run") else _scene._open_home()
				_wait = 2
				return false
			_scene._deny_no_energy()
			if _scene._energy_layer == null:
				_bug("体力墙没开层(SYNC5_PROBE_ENERGY_WALL 没生效?)")
				return _finish()
			for c in _scene._energy_layer.get_children():
				if c is Button:
					c.pressed.emit()
			_wait = 3
			_stage = 6
		6:
			if _scene._energy_layer != null:
				_bug("发奖后层没关")
			if _scene.state == _scene.St.FRONT:
				_bug("发奖后没开局(_pending_start 没兑现)")
			return _finish()
	return false


## 拍内:把手牌打出去让局走起来(与 tapeprobe 同款:什么都不做等结算也行, 8s 一拍太慢, 直接推进)。
func _drive_beat() -> void:
	if _scene.state == _scene.St.DECISION and _scene.has_method("_settle"):
		_scene._settle()
	elif _scene.state == _scene.St.INTRO and _scene.has_method("_on_intro_done"):
		_scene._on_intro_done()
	elif _scene.state == _scene.St.END:
		_scene._on_end_retry()


func _finish() -> bool:
	print("adsprobe: shops=%d bugs=%d frames=%d" % [_shops, _bugs, _frames])
	quit(1 if _bugs > 0 else 0)
	return true
```

- [ ] **Step 2: 跑,看退出码**

```bash
SYNC5_ADS=fake SYNC5_PROBE_ENERGY_WALL=1 godot --headless --path . --script res://tools/adsprobe.gd 2>&1 | grep -E "adsprobe|BUG|SCRIPT ERROR"; echo "rc=${PIPESTATUS[0]}"
```
Expected:`adsprobe: shops=3 bugs=0 ...`,rc=0。
若 `_drive_beat` 推不动局(`_settle` 需要 RESOLVE 态等),照 `tools/tapeprobe.gd` 的推进方式改这一个函数,不改被测代码。

- [ ] **Step 3: 反向验证探针自己(改被测代码让它红)**

临时把 `view/phrase.gd::_on_ad_rewarded` 里 `_shop_ads += 1` 注释掉,再跑一次:
Expected:`BUG: 同一店第二次弹了`,rc=1。**改回来**,再跑一次绿。

- [ ] **Step 4: 提交**

```bash
git add tools/adsprobe.gd
git commit -m "广告 10/12 tools/adsprobe.gd:假后端无头驱动商店三店(每店/每局上限)与体力墙(发奖后真开局), A/B 反向验证过"
```

---

### Task 11: AdMob 后端 `view/admob.gd`(动态查类)+ Gradle 开关 + 插件安装清单

**Files:**
- Create: `view/admob.gd`
- Modify: `export_presets.cfg:27`
- 用户侧:装插件、填 App ID、装 Android Build Template

- [ ] **Step 1: 写后端**(全部动态查类 —— 插件不在时本文件照样解析, 桌面/Web 导出不受影响)

```gdscript
extends Node
## AdMob 后端(Poing Studios godot-admob-plugin v5;2026-09-08 调研, 规格 §8.2)。
## ⚠ **只有这个文件碰插件 API**, 且全部**动态查类**(ProjectSettings.get_global_class_list)——
## 插件不在时本文件照样解析, 桌面 / Web 导出与 `--import` 不受影响。
## 契约与 view/ads.gd 相同:has_ad / load_ad / show_ad + rewarded / failed / closed。
## 插件没有 is_loaded():has_ad = 持有一个已加载、未展示的 RewardedAd;dismiss 后置空并立刻重载。
## 所有回调 call_deferred 回主线程(插件是否已转发到 Godot 主线程未核实, 一律兜底)。
## ⚠ 类名 / 回调字段名以插件文档站为准(reference/classes/RewardedAd 等), 真机冒烟时逐个校对:
##   RewardedAdLoader.load(unit_id, AdRequest, RewardedAdLoadCallback{on_ad_loaded, on_ad_failed_to_load})
##   RewardedAd.show(OnUserEarnedRewardListener(func(reward)))
##   RewardedAd.full_screen_content_callback = FullScreenContentCallback{on_ad_dismissed_full_screen_content,
##     on_ad_failed_to_show_full_screen_content(err)}

signal rewarded(kind: String)
signal failed(kind: String, why: String)
signal closed(kind: String)

var _ads := {}          # kind -> RewardedAd(已加载未展示)
var _loading := {}      # kind -> bool
var _cfg: Dictionary = DB.ads()
var _classes := {}      # 类名 -> Script(查过的缓存, null = 不存在)


func _ready() -> void:
	var mobile_ads = _cls("MobileAds")
	if mobile_ads != null:
		mobile_ads.initialize()
	for k in Ads.KINDS:
		load_ad(k)


func _cls(name: String):
	if _classes.has(name):
		return _classes[name]
	for c in ProjectSettings.get_global_class_list():
		if String(c["class"]) == name:
			_classes[name] = load(String(c["path"]))
			return _classes[name]
	push_warning("[Ads] AdMob 插件类 %s 不存在 —— 这一种广告永远无货" % name)
	_classes[name] = null
	return null


func _unit(kind: String) -> String:
	if bool(_cfg["test_mode"]):
		return String(_cfg["test_unit"])
	return String(_cfg["android"].get("rewarded_" + kind, ""))


func has_ad(kind: String) -> bool:
	return _ads.get(kind) != null


func load_ad(kind: String) -> void:
	if has_ad(kind) or bool(_loading.get(kind, false)):
		return
	var loader_cls = _cls("RewardedAdLoader")
	var req_cls = _cls("AdRequest")
	var cb_cls = _cls("RewardedAdLoadCallback")
	if loader_cls == null or req_cls == null or cb_cls == null or _unit(kind) == "":
		return
	_loading[kind] = true
	var cb = cb_cls.new()
	cb.on_ad_failed_to_load = func(err) -> void:
		call_deferred("_on_load_failed", kind, str(err.message) if err != null else "load")
	cb.on_ad_loaded = func(ad) -> void:
		call_deferred("_on_loaded", kind, ad)
	loader_cls.new().load(_unit(kind), req_cls.new(), cb)


func _on_loaded(kind: String, ad) -> void:
	_loading[kind] = false
	var fsc_cls = _cls("FullScreenContentCallback")
	if fsc_cls != null:
		var fsc = fsc_cls.new()
		fsc.on_ad_dismissed_full_screen_content = func() -> void:
			call_deferred("_on_dismissed", kind)
		fsc.on_ad_failed_to_show_full_screen_content = func(err) -> void:
			call_deferred("_on_show_failed", kind, str(err.message) if err != null else "show")
		ad.full_screen_content_callback = fsc
	_ads[kind] = ad


func _on_load_failed(kind: String, why: String) -> void:
	_loading[kind] = false
	_ads[kind] = null
	push_warning("[Ads] %s 加载失败:%s" % [kind, why])


func show_ad(kind: String) -> void:
	var ad = _ads.get(kind)
	if ad == null:
		failed.emit(kind, "not_ready")
		return
	var listener_cls = _cls("OnUserEarnedRewardListener")
	if listener_cls == null:
		failed.emit(kind, "no_listener")
		return
	ad.show(listener_cls.new(func(_reward) -> void:
		call_deferred("_on_rewarded", kind)))


func _on_rewarded(kind: String) -> void:
	rewarded.emit(kind)


func _on_dismissed(kind: String) -> void:
	_ads[kind] = null
	closed.emit(kind)
	load_ad(kind)


func _on_show_failed(kind: String, why: String) -> void:
	_ads[kind] = null
	failed.emit(kind, why)
	load_ad(kind)
```

- [ ] **Step 2: 桌面照常解析(不带插件)**

```bash
godot --headless --path . --import 2>&1 | grep -iE "error|admob" ; echo "rc=${PIPESTATUS[0]}"
SYNC5_TEST_DOMAINS=t_ads,t_db godot --headless --path . --script res://tests/runner.gd 2>&1 | tail -2
```
Expected:没有 admob.gd 的解析错误;`0 failed`。

- [ ] **Step 3: Gradle 开关**(`export_presets.cfg:26-27`)

把
```
; 裸包用预编译模板即可;接 TDS 防沉迷插件时再切 gradle 构建
gradle_build/use_gradle_build=false
```
改成
```
; 2026-09-08 起必须 gradle 构建:AdMob 插件是 Godot 4.2+ 的 v2 Android 插件(addons/ + EditorExportPlugin),
; 官方文档写明 requires the Gradle build process。编辑器先 Project → Install Android Build Template。
gradle_build/use_gradle_build=true
```

- [ ] **Step 4: 用户侧清单(我做不了的, 写进 STATUS 出包段, Task 12 落字)**

1. Godot 编辑器 AssetLib 搜「AdMob by Poing Studios」装到 `addons/admob/`,启用插件,`Project → Tools → AdMob Manager → Android → Download & Install` 拉原生依赖;`Project → Install Android Build Template`。
2. AdMob 后台建 Android 应用 + **两个** rewarded 单元(coins / energy);App ID 按插件文档填在插件设置里;两个 unit ID 填 `data/ads.json` 的 `android.*`;`test_mode` **出正式包那一步**才翻 false。
3. 真机冒烟(测试 ID):进商店把钱花光 → 点买不起的卡 → offer 出现 → 看完 +3◆;体力打到 0 → 「看广告 · 马上开局」→ 看完直接进局;`~/Library/Application Support/Godot/app_userdata/Sync5/tape/` 里那一局的日志要看到 `ad` 的 `show` 与 `reward` 两条。冒烟时在 `_on_rewarded` 里临时打印 `OS.get_thread_caller_id()` 核对回调线程。
4. Play 后台:数据安全表单补 Ads SDK 采集项;发行国家排除 EEA/UK/瑞士(本批不做 UMP);隐私页补广告 SDK 一段。

- [ ] **Step 5: 提交**

```bash
git add view/admob.gd view/admob.gd.uid export_presets.cfg
git commit -m "广告 11/12 AdMob 后端 view/admob.gd(全动态查类, 插件不在时照常解析;has_ad = 持有已加载未展示对象;回调一律 call_deferred)+ Android 导出切 Gradle 构建"
```

---

### Task 12: 文档(telemetry / tech / STATUS / TODO / CHANGELOG / spec 状态)

**Files:**
- Modify: `docs/design/telemetry.md:53`(事件表加 `ad` 行,`deny` 那行之后)
- Modify: `docs/design/tech.md`(§ economy.json 加三键;profile 加两键;新增 ads.json 一行)
- Modify: `STATUS.md`(Android 出包段加 Gradle + 插件 + 用户侧清单;工具链表加 `adsprobe`;回归读数)
- Modify: `TODO.md`(路线 ⑦ 改「已实装,等用户侧四件」)
- Modify: `CHANGELOG.md`(2026-09-08 一节)
- Modify: `docs/superpowers/specs/2026-09-08-ads-design.md`(顶部加「已实装 2026-09-08,计划 = plans/2026-09-08-ads.md」)

- [ ] **Step 1: telemetry.md 事件表**(`deny` 那行之后加)

```markdown
| `ad` | 激励视频 | `k`(coins/energy), `ev`(show=开始放 / reward=真发奖 / fail=失败+`why` / drop=run 没了丢弃), `coins?`(发奖后余额), `late?`(店关了才到的奖) |
```
并在 `deny` 那行的 `why` 枚举里加 `energy`(它早就在打, 表里漏了)。

- [ ] **Step 2: tech.md schema**

在 `### economy.json` 节末尾加一行:
```markdown
- `ad_coins` / `ad_coins_per_shop` / `ad_coins_per_run`(2026-09-08 激励视频换金币:数额 = 一张卡的钱;两级上限只数真发出去的钱;目标分按零广告标定)。
```
在 profile.json 出现处(第 394 行附近)加:
```markdown
- `profile.json` 另有 `ad_energy` / `ad_energy_per_day`(看广告换体力:未满才许、每日上限、入口 = 体力墙);`ads.json` = SDK 配置(`test_mode` 恒 true 直到出正式包;两个 rewarded 单元 ID 归用户填)。
```

- [ ] **Step 3: STATUS.md**

Android 段(第 476 行附近「传 TapTap 前必须换 release keystore」那句之后)加:
```markdown
  **2026-09-08 起 Android 导出走 Gradle 构建**(`gradle_build/use_gradle_build=true`, AdMob 插件是 v2 Android 插件):编辑器先装 Android Build Template;
  插件 `addons/admob/`(Poing Studios v5.0.0, AssetLib「AdMob by Poing Studios」)**归用户装**, App ID 填插件设置、两个 rewarded 单元 ID 填 `data/ads.json`,
  `test_mode` 出正式包那一步才翻 false。插件不在时 `view/admob.gd` 照常解析(全动态查类), 桌面/Web 导出不受影响。
  真机冒烟清单与 Play 后台三件(数据安全表单 / 发行国家排除 EEA·UK·瑞士 / 隐私页补广告段)见 plans/2026-09-08-ads.md Task 11。
```
工具链表加一行:
```markdown
| `SYNC5_ADS=fake SYNC5_PROBE_ENERGY_WALL=1 godot --headless --path . --script res://tools/adsprobe.gd` | 激励视频两条流的无头回归(商店三店上限 + 体力墙发奖后真开局), 秒级 |
```
以及 sim 的 `adaptive:twin+ads` cohort 一句:「验收带 = 通关率 +≤5pt、每局多买 ≤2 张;**未跑**, 用户喊了才跑」。

- [ ] **Step 4: TODO.md 路线 ⑦** 改成:

```markdown
   ⑦ ✅ **商业化已实装(2026-09-08, 规格 specs/2026-09-08-ads-design.md, 计划 plans/2026-09-08-ads.md)**:
     两个需求触发的激励视频入口(商店 denied 那一刻的行内 offer · 体力墙「看广告 · 马上开局」看完直接进局),
     只看海外 Godot 版, 不做内购道具, 不做续命。**欠用户侧四件**(装插件 + App ID / 两个 unit ID / Play 后台三件 / 真机冒烟),
     sim 的 ads cohort 未跑(验收带 +5pt / 2 张)。
```

- [ ] **Step 5: CHANGELOG.md** 顶部加一节(照既有格式,标题写日期与一句话):

```markdown
## 2026-09-08 · 商业化:激励视频两入口(海外 Godot 版)

用户拍板三句:「B(海外)」·「不做内购给我赶紧删了 … 我不做内购道具, 但我要局外看广告买体力, 局内看广告买金币」·「maker 国内你别管了」。
二审(「思考商业化嵌入的合理性」)把入口从「常驻键 / 胶囊」改成**需求触发**:商店里点了买不起的东西那一刻(`denied` 已有)弹行内 offer;
体力墙本身变「看广告 · 马上开局」, 看完下一帧重放同一份 `start_run()`。失败屏续命明确不做(生死线软了, 倍率奖励波动性那套逻辑失去根基)。
两个判断:目标分按零广告标定(广告金币是纯盈余, 3◆/次、每店 1、每局 2)· 入口只在无钟的地方。
落地:data 三份 + db 校验 · `Economy.ad_coins/ad_coins_allowed` · `SaveState` 体力广告纯函数层 · `Run.ad_used` 进快照 ·
`view/ads.gd` 适配层(假后端;`SYNC5_ADS=off|fake|fail|dismiss`;探针缺省 off)· `view/admob.gd`(全动态查类)·
Tape `ad` 事实四种 · bot `ads` 上界臂 + parity `ad_coins` · `tools/adsprobe.gd` · Android 切 Gradle。
Lua 镜像补孪生只为门绿(用户:TapMaker 不管)。
```

- [ ] **Step 6: 全量单测 + 六道门 + 探针**(这是本批唯一一次全量;门 / sim / price 不跑)

```bash
./tools/run.sh unittest /tmp/sync5_ads_unittest.log; echo "rc=$?"
python3 tools/parity.py --check && python3 tools/evsync.py --check && python3 tools/counts.py --check \
  && python3 tools/luagen.py --check && python3 tools/mirror.py --check && lua lua/check.lua
SYNC5_ADS=fake SYNC5_PROBE_ENERGY_WALL=1 godot --headless --path . --script res://tools/adsprobe.gd 2>&1 | tail -1
```
Expected:unittest rc=0(四判据)、六门绿、`adsprobe: … bugs=0`。把 `=== RESULT` 那行的数字写进 STATUS 回归段。

- [ ] **Step 7: 桌面截图自己看**(改了视觉:商店 offer 与体力墙)

```bash
SYNC5_ADS=fake godot --path . -- --fresh
```
手点:进商店把钱花光点一张卡 → offer 出现在两键之下、碟之上,不压任何东西;看完 +3◆ 副标题余额跟着变。
体力墙只能靠 adsprobe(桌面存档体力满);要肉眼看就把 `data/profile.json` 的 `energy_max` 临时改 1 跑两局, **看完改回来**。

- [ ] **Step 8: 提交**

```bash
git add docs/design/telemetry.md docs/design/tech.md STATUS.md TODO.md CHANGELOG.md docs/superpowers/specs/2026-09-08-ads-design.md
git commit -m "广告 12/12 文档:telemetry ad 事件 · tech schema · STATUS 出包(Gradle/插件/用户侧四件)+ adsprobe · TODO ⑦ 已实装 · CHANGELOG 09-08"
```

---

## 自审记录(写完计划后对着 spec 过的)

- **规格覆盖**:§2 数据 → Task 1/6;§3.1 → Task 2;§3.2 → Task 4;§3.3 → Task 3;§3.4 镜像 → Task 2/3/4 各自的 Lua 步;§4.1 → Task 5(+ Task 11 后端);§4.2 → Task 6;§4.3 → Task 7;§4.4 → Task 8;§4.5 生命周期 → Task 11 的 call_deferred + Task 8 的「下一帧重放」;§5 Tape → Task 7/8/12;§6 仪器 → Task 9;§7 测试 → 各 Task 的 Step 1 + Task 10 探针;§8 SDK → Task 11;§9/§10 → Task 12 文档。
- **方法名**:spec §4.1 原写 `ready / show / preload`,`preload` 是 GDScript 关键字、`show` 与 CanvasItem 撞名 ⇒ 全计划统一 `has_ad / load_ad / show_ad`,spec 已同步改。
- **探针与 SYNC5_ADS 的优先级**:spec §4.1 原写「探针强制 off」,adsprobe 要用假后端驱动真流 ⇒ 改成「显式环境变量最优先, 探针缺省 off」;spec 已同步改。
- **体力墙的底**:spec §4.4 写「不回首页」—— 实现上两条路都先 `_open_home()`(首页是底, 层在其上), 玩家看到的仍是「不用再按一次」;spec 已同步改措辞。
- **ads.json 的 app_id**:App ID 进 AndroidManifest, 由插件设置管, 放 JSON 里是死数据 ⇒ 去掉, spec §2 已同步。
