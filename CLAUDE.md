# Sync5 · Project Rhythm

5 秒乐句 × 扑克 Roguelite,Godot **4.6.2** / GDScript,竖屏 **720×1280**。
Lumines 的节奏推进 + Balatro 的构筑。**始终用中文回复。**

> **本文件只放「原则」** —— 规则、架构铁律、美术方向、素材分工。
> 其余四类各有单独文档,**别往这里塞**:
>
> | 想知道什么 | 看哪 |
> |---|---|
> | **现在是什么样**(回归状态/完成度/数据/工具链) | [STATUS.md](STATUS.md) |
> | **接下来做什么**(优先级 + 明确不做的) | [TODO.md](TODO.md) |
> | **经历过什么**(转折和它们的理由) | [CHANGELOG.md](CHANGELOG.md) |
> | **踩过什么坑**(测量纪律/模型覆盖/建模/重构判据) | [LESSONS.md](LESSONS.md) |
>
> **⚑ 判据(往这个文件里加东西之前,先问一遍)**:
> **「去掉所有数字和日期,这句话还站得住吗?」**
> **站得住 = 原则,留在这里;站不住 = 它其实是证据 / 某次改动的经过,按上表移走。**
>
> 例:「数值与内容全部在 `data/*.json`」去掉数字仍成立 = **原则,留**;
> 「实测 `_mean` 抄了 11 份」全是数字 = **证据**,归 [LESSONS.md](LESSONS.md);
> 「起因是我在一局那层抄了 14 份」= **经过**,归 [CHANGELOG.md](CHANGELOG.md);
> 标题写着「踩过坑,别退回去」= 按定义就是 [LESSONS.md](LESSONS.md)。
>
> **美术与渲染的具体做法**(剖面数值、分层顺序、实现细节)归 [`docs/design/ui_meta.md`](docs/design/ui_meta.md);
> **牌型/经济/关卡的具体数值与推导**归 `docs/design/` 对应那篇(`cards` / `levels` / `jokers` / `blinds`);
> **难度曲线与节奏**(死亡率形状、脸的轮次集、Director、新手引导)归 [`docs/design/difficulty.md`](docs/design/difficulty.md),
> 它的**外部依据**在 [`docs/design/research_pacing_retention.md`](docs/design/research_pacing_retention.md)。
> ⚠ **要「调研业内做法」之前先在 `docs/design/` 里 grep** —— 那篇 27k 的调研已经覆盖
> DDA / L4D Director / 心流 / 近失效应 / 短局留存 / 起承転結(踩过,见 [LESSONS.md](LESSONS.md) 八)。
>
> 设计规格在 `docs/design/`(导览 = [`docs/design/README.md`](docs/design/README.md),文档已全部去号按主题命名),
> 建模这条线按 `prior → solving → generating → capability` 读。

---

## 命令

```bash
godot --path /Users/kuma/Projects/Sync5                                    # 跑游戏(主场景 = 首页)
godot --path /Users/kuma/Projects/Sync5 -- --fresh                         # 同上, 但**清掉存档当新玩家跑**(会进教学关)
godot --headless --path . --script res://tests/runner.gd                   # 单元测试(裸跑, 自己看四判据)
./tools/unittest.sh [日志路径]                                              # 单元测试 + 四判据(唯一一份判据;gate.sh 与 CI 都调它)
./tools/gate.sh                                                            # 加了内容就要过的门(耗时看 STATUS.md 门耗时表)
SYNC5_KIT_ID=<joker_id> godot --headless --path . --script res://tools/kit.gd   # 只验一张新小丑牌
./tools/gate.sh <face_id>                                                  # 只验一张新脸(⚠ 见下,不再是十几秒)
godot --headless --path . --script res://tools/pair.gd                     # 守「求解器 = 游戏代码」
godot --headless --path . --import                                         # 新增 class_name **或改了 assets/ 下的文件**后必须先跑
```

**其余探针(生成器/定价/可加性/建模验证/截图)的清单见 [STATUS.md](STATUS.md) 的工具链表。**

⚠⚠ **上面这些耗时标注会过期,而且已经过期过一次** —— S10(扩弃牌枚举,2026-08-14)把单测
从 <300s 拖到 ~716s,而 `gate.sh` 与单脸门**第一步就跑全量单测**,于是「十几秒的快路径」
**地板变成 12 分钟以上**。⇒ **别信文档里的秒数,跑之前先按当前单测时长估。**

⚠⚠ **长探针一律走 `tools/run.sh`**(2026-08-30 立):
```bash
./tools/run.sh sim <日志>          # 带锁跑 sim
./tools/run.sh unittest <日志>     # 带锁跑单测
./tools/run.sh --status            # 谁在跑、锁了多久
```
它记下启动时的工作区指纹, 结束时比对 —— **跑着的时候文件被改过就大字警告并退 90**,
那份读数作废。⚑ 立这条是因为「跑着别改」这条纪律我**一天之内靠自觉守了五次都没守住**
(四次让单测/sim 读到半新半旧, 一次改 `core/` 让 sim 直接 `Stack underflow! (Engine Bug)`)。
**靠记性守不住的纪律要落成机械。**

⚠⚠ **跑任何长探针之前, 先跑这两条秒级检查**(2026-08-30 立, 一天踩四次):
```bash
python3 tools/parity.py --check && python3 tools/evsync.py --check && python3 tools/counts.py --check
```
`parity` 查游戏侧/bot 侧的规则与 action 键是否对齐(不齐 = 模型看不见那条规则,
sim 读数会系统性偏);`evsync` 查 bot 的估值地板与 evbook 是否同步。
**它们秒级, 而门要 30 分钟** —— 先确认代码面收口了, 再启动长跑。

⚠ 三条跑探针的纪律(踩过,详见 [LESSONS.md](LESSONS.md)):
**跑完必须确认退出码**(GDScript 运行时错误不会终止 `SceneTree` 脚本,进程会空转) ·
**读退出码别隔着管道** · **计时必须在无竞争时做**。

⚠ **改了视觉就渲染出来自己看**,不要靠想象。多 agent 并行时各自建隔离探针,
别同时跑同一个截图工具。截图产物是根目录的 `_shot_*.png`,可随时重建。
⚠⚠ **改了 `assets/` 就先跑 `--import` 再截图** —— 否则渲染读的是 `.godot/imported/` 里的
旧产物,**源文件和量出来的数都是新的、屏幕是旧的**(踩过,见 [LESSONS.md](LESSONS.md) 假绿第六种)。

---

## 架构铁律

- `core/` = 纯逻辑,**引擎无关、不含时钟、不 import view**。所有规则在这里。
- `view/` = 渲染 + 计时,**组件化**(2026-08-05 拆分,规格=`docs/design/tech.md`):`phrase.gd` 只做对局编排,
  首页=`home.gd`、信息区=`hud.gd`、商店=`shop.gd`、手牌/缓存/两键=`hand.gd`、小部件=`widgets.gd`;推进状态机在
  `core/run.gd`(引擎无关,有直测)。组件对外只发意图信号,**金币/装槽等经济动作只发生在编排器**。
  sim 同构三拆:`tools/sim.gd`(入口)+`bot.gd`(决策+信念)+`report.gd`(账本+报表),RNG 同实例。
- **⚑⚑ 两层共用,别搞混(2026-08-08)**:`core/beat.gd` 共用**一拍的转移**(游戏 + 探针都用);
  **`tools/runloop.gd`(`RunLoop`)共用一局的循环 —— 只给探针用**,因为游戏是实时异步的、
  共用不了 `for`(docs/design/tech.md)。`view/phrase.gd` 只碰 `Beat`,不碰 `RunLoop`。
  ⚠ **判生死只有这一份**(走 `Run.section_target_for`)——`raisedbar` 事故正是这个形状。
  (14 份 → 1 份的起因与收敛口径见 [CHANGELOG.md](CHANGELOG.md);
  迁移时四个静默坑的完整版见 [LESSONS.md](LESSONS.md)。)
- 新文件命名**一词化无下划线**(用户拍板)。
- **数值与内容全部在 `data/*.json`**(2026-08-05 配置化,schema 见 `docs/design/tech.md`):
  小丑牌 = 效果 DSL(`core/fx.gd` 解释),Boss 脸 = 参数表,关卡/经济/机器人信念表 = 纯数字,
  打点开关 = `tape.json`。
  改卡改平衡 = 改 JSON;`core/db.gd` 校验,未知键/坏引用**在测试里直接红**
  (`tests/t_db.gd` 断言 `DB.load_error() == ""`)。⚠ **是测试期门禁,运行时不拒绝启动** ——
  理由与「要改成严格该先做什么」写在 `core/db.gd` 文件头。
  加新 DSL 操作码要过 docs/design/jokers.md 的门槛。
  机器人的 `_target_mult` 和卡面数额从 jokers.json **推导**,不许再手抄第二份。
- **打点只在编排器打**(`view/phrase.gd` 调 `Tape.on()`),和「金币/装槽等经济动作只发生在编排器」
  同一条线——组件各打各的必然打重、打漏。组件要上报就发信号(shop 的 `denied` 就是为此加的)。
  `core/tape.gd` 守 core/ 铁律:不含时钟(毫秒由 `Time` 静态取且可注入,测试才断言得了)、
  不 import view、除 Card/Joker 外不认识任何游戏对象。
  **口径铁律 = 只记事实,不记特征**(2026-08-06 用户拍板:「记录事实,G/D 那边自己去读。
  特征是会迭代的,你记录了反而不好做」)。判据:**发生过的判定 = 事实(记);没发生的假设 =
  特征(不记)**。**事实完整性的判据 = 能不能重放出任意时刻的局面**。
  (完整推导、可推导清单、2026-08-06 补齐的四类、四个踩过的坑,全在
  [`docs/design/telemetry.md`](docs/design/telemetry.md)。)
- 时长一律走 `GameConfig.phrase_duration()` 等钩子,不要在调用方写死
  (`GameConfig` 是 data/ 之上的静态门面,调用方语法不变;`early_settle()` 提前锁定从钩子插入)。

---

## 已拍板的规则(哪篇是现行、哪篇是前瞻,以 [`docs/design/README.md`](docs/design/README.md) 主结构表为准)

- **候选牌机制作废**。弃牌重抽制:选中手牌/缓存的牌 → 弃牌 → **立刻原位补牌**。
  **⚑⚑ 弃牌收费 1◆/张(2026-08-26 经济 v2,用户拍板 —— 推翻 08-06「弃牌免费」,
  那条又曾推翻更早的「1 金币/张」:转了一整圈,这次带着完整经济闭环回来)**:
  开局 10◆ · 每拍按牌型发钱(`kind_coins`,A 案)· 8 秒与金币双闸门。
  规定与健康带在 [`docs/design/levels.md`](docs/design/levels.md) 经济 v2。
  金币的出口:**弃牌 + 买牌(平价 3◆)+ 洗牌**
  (洗牌 2026-08-26 随超级百搭加:付费整手重掷 + 弃牌堆洗回,只在牌堆有万能时开放。
  ~~升级~~ 2026-08-16~26 曾是主出口,**2026-08-26 用户拍板整体删除**:
  「升级太复杂等于要做好几套经济……删掉是为了减负」)
  (弃牌免费连带的五处变化见 [`docs/design/cards.md`](docs/design/cards.md))。
- ~~升级的三条原则~~ —— **升级系统 2026-08-26 整体删除**(见上条;原三原则与教训归档在
  CHANGELOG/LESSONS,**别再提议加回**:每个升级档都是一套要独立平衡的经济)。
- 手牌恒 5 张;**缓存区恒满 3 格**(2 格不好玩)。缓存不存在"存入空位",**拖拽 = 两张对调**;
  拖到弃牌键 = 单张直弃。理牌 = 按点数排序。
- **点击和拖拽是两种手势, 不许混**(2026-08-06 用户:「点到手牌区的卡和缓存区的卡就直接交换了,
  但我有时候只是想同时对他们弃牌」):**点击 = 纯选择**(手牌与缓存**跨区多选**, 选来一起弃),
  **拖拽 = 对调**。测试里有这条契约(含 A/B 验证)。
  (曾经有一条捷径把第二次点击吞掉、已删,经过见 [CHANGELOG.md](CHANGELOG.md)。)
- **万能牌唯一来源 = 超级百搭**(2026-08-26 用户拍板**取代**百搭,wildcard 已删,
  大小王通道 enable_wilds 一并退役):四张 JOKER 注入(`Deck.add_wilds`,按来源记账不翻倍)
  + 付费**洗牌**动作钓卡(整手重掷 + 弃牌堆洗回);特例价 4◆(「超级卡就4金币」)。
  牌型判定:≤2 张万能暴力代入(每张试 52),**≥3 张走候选构造解析捷径**
  (`Pattern._score_many_wilds`,判型仍走同一份 `_classify`,规则牌自动生效;
  完备性由单测对拍锁着)。
- **盲注一律不可跳过**(2026-08-05 用户拍板):Balatro 的 skip-blind/Tag 奖励系统**明确不做,勿再提议**。
- **关卡结构(2026-08-06 节奏定案,docs/design/levels.md + docs/design/levels.md 为准)**:Run = 巡演 =
  **4 场演出 × 1 盲注 = 4 Section,每盲注 6 个 Phrase**,单拍统一 **8s**
  (走 `phrase_duration()` 钩子)。**一局 ≈ 4.9 分钟**(216s 出牌 + 7 次商店)。
  **4 段全是 BOSS 墙**(用户拍板:「就是异常下来有 4 个阶段性的任务和规则要做,也不做跳过,
  就是必须做」)——小盲/大盲的概念**作废**,递进感由**档位色 + 序号**承担(见美术节)。
  **失败仍然全屏中断**——run 结束了值得停一下,run 还要继续就不该停。任一盲注失败 = run 死。
  ⚑⚑ **「提前结束」整块退役(2026-08-31 用户拍板:「不要提前结束的机制了。我玩起来也不用」)**:
  主动收工键、唱片点击、落袋经济、bot 的「达标即收工」全部删除 ⇒ **每一段都打满全部拍数**。
  ⚠ 早收**没有消失** —— 它变成**纯被动判据**:`结算时刻 − 最后一次动作 ≥ early_finish_left`,
  四张时机卡(速弹/快进/惯性/定格)照常活着, 卡面从「提前收工」改写为「提前打完」。
  ⚑ 判据用**相对剩余**而不是绝对时刻, 因为拍长是会动的旋钮 —— 写死秒数会在改拍长时静默分叉。
- **商店与盲注已解耦(2026-08-06,这次重构的核心)**:商店**每 3 拍开一次**
  (`phrases_per_shop`),所以 4 个盲注有 **7 次商店**——段中 4 次 + 段末 3 次。
  **段中商店开在盲注进行当中**:不结算、不清分、不判生死,`Run.advance()` 返回 `shop_break`,
  编排器只 `_open_draft()` 不推进 section
  (为什么值得这么做、以及 7 次商店的实测,见 [`docs/design/levels.md`](docs/design/levels.md))。
  **商店盲注板因此有两态**:段末讲「下一场」,段中讲**这一段的进度**
  (大字 = 还差多少分、副行 = 已得 X/目标 Y、页脚 = 还剩 N 拍)。
  ⚠ **改段数要顺手核对所有按段索引的表**(表长 ≠ 段数会被**静默截断**成放水盘)——
  清单与锁住它的测试见 [`docs/design/levels.md`](docs/design/levels.md)。
  **盲注公示 = 舞台卡**(2026-08-05 用户:「关卡就是盲注」):
  首页的大玻璃卡与局内盲注板是**同一个对象**(`Widgets.StageCard` 定义外观 + `Widgets.BlindBoard` 是
  局内尺寸),改一处两边同步。商店顶部常驻盲注板(段末讲下一场、段中讲本段进度,见上条);
  无商店的入口(开局/重开)用独立公示卡 `view/intro.gd`(点按跳过,钟在卡消失后才开)。
- 结算公式(2026-08-05 三次修订,用户拍板 chips×mult):
  **`(chips + Σ改基牌) × 牌型倍率 × target倍率(符合条件) × (1 + Σ%) + Σ奖励分`**。
  chips = 牌型基础 chips + 五张牌点数和;每个牌型自带 (chips, mult) 双值(表在 docs/design/cards.md),
  **牌型倍率是乘法链第一环**——稀有牌型与小丑牌相乘而不是被替代。
  (倍率表的三版演进、实测频率、与原作的两处偏离,全在 [`docs/design/cards.md`](docs/design/cards.md)。)
  「改基牌」(VIP、黑胶)加在 chips 上吃全部倍率——必须早抽,后期才值钱;「奖励分」在乘法**后**
  落地——**固定数额的**(和弦 +140、排练 +200)前期是神、后期自然过气,这是杀死万能填充牌的
  Balatro 机制。
  ⚠⚠ **但这条只对固定数额成立,对 `bonus_target_pct` 通道恰好相反**(2026-08-28 实测推翻,
  `tools/cf.gd` 真实构筑口径按段拆):灯牌/回响/尾声/周转写的是**目标分的百分比**,
  而目标分逐段递增(S1 420 → S4 5600)⇒ 它们**跟着膨胀,晚段是早段的 2.50 倍**;
  而改基族的晚/早只有 **0.91**。「前期是神」也没兑现 —— 奖励分族早段均 24.8 分/拍,
  是改基族(103.3)的**四分之一**,是全族最弱。
  ⇒ **写「奖励分会自然过气」当作不用管它的理由,在这个通道上是错的。**
  槽序仍无意义。
  结算链:牌型 → target 小丑 → support 小丑 → 分数/金币(主角被动层 2026-08-24 随主角系统删除)。
- **⚑ Target 倍率两层原则(2026-08-07 用户批 A 案, 已实装)**:
  **① 族内统一** —— 同一张 Target 对它覆盖的所有牌型给**同一个倍率**。
  **② 族间按覆盖面标定** —— 统一之后仍不平, 因为**牌型层管「达到某牌型多难」,
  管不了「这个流派的射程覆盖多少拍」**。所以按各自「超出中性基准的部分」反推倍率
  (覆盖面与终值见 [`docs/design/jokers.md`](docs/design/jokers.md))。
  ⚠ **新 Target 定价从此只需回答一个数**:「它覆盖的牌型占多少拍, 该给几倍」——
  不用再画一张族内阶梯表。测试里锁了**族内一致**这条结构契约。
  **⚑ ③ 那个数按「牌堆给不给」算,不按「实测打出多少」算(2026-08-14 用户拍板)**:
  难度 = **命中该 Target 条件的组合概率**(零策略、8 选 5 取最优),来源 =
  [`docs/design/prior.md`](docs/design/prior.md) 的先验层,**不是** sim 或 Tape 的实测频率。
  三条理由,每条都独立成立:
  **(a) 技术溢价必须归玩家** —— 按实测定价等于「你越会玩, 这个牌型给你的倍率越低」,
  把技术奖励又收回去, 与「会玩的卡 EV 必须严格高于保底卡」直接冲突;
  **(b) 实测口径会漂** —— 它取决于 bot 多强、玩家多熟, 而倍率表不该跟着一个会变的东西走;
  **(c) 组合口径零数据、可精确算**, 新牌型当场出数, 不用等任何人玩。
  **⚑ ④ 倍率不止奖励概率,还奖励波动性(2026-08-27 用户拍板,核心机制)**:
  「倍率和概率确实是正比,但倍率不止奖励概率,还奖励波动性——否则期望一样的情况下
  谁不玩好凑的?」追稀有型 = 高方差策略,凑不成时手是烂的,而段目标是硬生死线 ——
  风险中性的 1/p 定价会让追型「期望持平但更容易死」,理性玩家将永远选对子。
  公式与两个新先验口径(金币约束概率 P(≥k|b) · 波动 CV(k))在
  [`docs/design/cards.md`](docs/design/cards.md) 倍率 v3;λ(风险厌恶系数)归用户手感标定。
  ⚠⚠ **推论(踩过一次)**:当实测频率**远低于**组合概率时,那**不是难度**,
  是**玩家没认出来 / 来不及做** —— **倍率治不了它, 该修的是可读性、时间或规则牌**。
  拿倍率去补识别问题, 补出来的是一个会被规则牌叠加放大的永久超模。
- **小丑牌的三条契约**(roster 现状见 [STATUS.md](STATUS.md),配额表与规则牌见
  [`docs/design/jokers.md`](docs/design/jokers.md)):
  设计原则 16 条 + 配额表在 `docs/design/jokers.md`,Balatro 调研在 `docs/design/research_balatro_jokers.md`。
  卡面文字**英文 ≤7 词**(1.5 秒读懂,测试里有断言)。钩子契约:`apply/on_acquire/on_discard/on_swap/
  on_phrase_end/on_section_end`,成长牌状态存 `Joker.state`。**成长/重置只挂显式动作(弃牌/时机),
  绝不挂结算牌型内容,零挂机成长**——这三条是铁律,新牌必须过。
  ⚑ **构筑反面铁律(2026-08-25 用户)**:「选牌型→凑牌型件→塞最强通用件」这个套路**不许成为
  唯一玩法**——「应该有很大差异化,玩家应该变幻无穷」。判据:获胜构筑的骨架必须不止一种,
  非牌型骨架(行为/经济/缓存/商店/赌)各自要有通往大数的路。
  **对抗设计的完整原则**(盲注=条件+惩罚 / 小丑=条件+激励 · 解法四种 · 三档矩阵 · 归属边界 ·
  两侧纪律)在 [`docs/design/versus.md`](docs/design/versus.md) —— **加新脸新卡先过那篇**。
- **draft 商店已拍板并实装**(2026-08):每个 Section 结算后进商店;首张 Target 免费三选一,Support
  随机三选一**标价不保底**(⚠ **经济 v2 起一律平价 3◆**,镜面的稀缺税也摘了 —— 原文「普通4/罕见6/稀有9,镜面单独 11◆」是升级系统时代的旧价, 2026-08-29 我据此改代码, 门当场红 10 条:`tests/t_economy` 锁着平价这条契约。**特例要回来必须先在 levels.md 说明**),付费刷新(3◆ 起递增);
  **没有「跳过」奖励**(2026-08-06 用户:「有跳过按钮, 不要, 只需要刷新」)——`draft_skip_reward`
  连同配置键一起删了。但那个按钮本身**必须留**, 改叫「继续 ▸」且不给钱:
  它是商店的**唯一免费出口**, 买不起又刷不起时没有它会**卡死在商店里**;
  满槽后变「买新替旧」(旧卡折半回收,交互见下条)。
  **⚑ Target 换旗已推翻重做并实装(2026-08-06 用户拍板)**:「**不应该有任何卡有固定概率,
  大家都是一样的。除了第一轮有 target 之外,其他都是随机的**」——
  **删掉 `target_swap` 的 `chance` 与 `from_section` 两个键**及 bot/shop 里的两段特判分支,
  Target 回到**同一个货架池**按稀有度权重出现,已有 Target 时就是一次普通的「买新替旧」。
  **首张 Target 免费三选一保留**(那是开局引导,唯一的特例)。
  ⚠ **bot 侧保留了一条门**:`cfg.target` 强制的队列不许换旗(除非 `cfg.pivot`)——
  那是**实验者的随机分配**, 是整条 pipeline 唯一干净的因果通道(docs/design/solver_roadmap.md),
  让 bot 自己换掉就没了。
  收入主干 = **Section 通关工资 +3◆** + 牌型金币(4 段结构下工资点只有 4 个,单次商店购买力下降)。
  所有数字在 `data/economy.json`。
- **⚑⚑ 消耗牌(2026-08-30 用户开轴,规格 = [`docs/design/consumables.md`](docs/design/consumables.md))**:
  「消耗牌我认为我们也可以加,**只是不用特别多,我要让决策复杂化**」——
  推翻 archetypes.md 桶一的「无消耗品系统」;**星球牌(= 牌型升级)仍然不做**。
  **分界只有一条:用完之后这张卡还有没有意义** —— 一次性 ⇒ 消耗牌,持续 ⇒ 小丑牌。
  ⚠ 判据**不是**「这张卡死没死」(用户纠正过:「不是为了解决大部分卡没进过赢局而设计消耗牌,
  是因为他确实就适合一次性而非长期」);⚠ **「每段一次」也判一次性**(「一局4次毫无意义」)。
  **栏位 2 格 · 实时可点**(「只在商店用其实有点怪」)· 与小丑槽**互不占用**(那是**装备位**)。
  ⚑⚑ **但货架位是共用的(2026-08-31 用户拍板「其实是 5 选 1」)**:每店 **3 小丑 + 2 消耗 = 5 选 1**,
  消耗牌**没有专属成交名额**, 它和小丑牌抢同一次购买。~~商店每店上架 1 张且不进三选一~~ 已推翻。
  ⚑ 栏位摆在**唱片原来的位置**(音浪层右侧, 62×104 卡形), 恒用**卡牌的红** `SUIT_RED` ——
  颜色本身就是分类:红 = 一次性, 档位色 = 这一关。
  ⚑ **一次性卡的定价量纲与永久卡不同**:永久卡受 4 槽硬限,消耗牌用完即空、
  总量只受钱限制 ⇒ **等值定价必然超模**(实测通关率全线 +15~29pt),见 numbers.md §2.52。
- **满槽替换 = 拖拽对调**(与缓存区同一手势:「拖拽 = 两张对调」),Target 槽**拒绝**接放
  (它是取消位);新旧同屏对照的完整交互见 [`docs/design/ui_meta.md`](docs/design/ui_meta.md)
  与 [`docs/design/levels.md`](docs/design/levels.md) §0.2。

- **1.1 起新增的六条(2026-08-19~21,全部去掉数字仍成立)**:
  · **玩家可见文案一律走 `Lingo.t()` / `data/lingo.json`**,view/ 里裸中文字面量 `t_lingo` 直接红;
    实体名用数据里的英文 `name` 字段;**探针恒 cn、Tape 不走语言层**(日志记事实,两台机器的日志必须长一样)。
  · **`data/ranking.json` 是仪器输出**(`tools/price.gd` → `tools/rankgen.py`),手改无效;
    池里的每张脸都必须在表里(Director 取交,漏一张 = 永不登场且不报错,校验锁着)。
  · **千人千面只换「哪张脸上场」**:context 四开关(novelty/streak_shift/returning/explore_shelf)
    不碰目标分、规则、数值、UI —— 不可察觉是红线,判据 = 攻略作者写不出「必」字。
  · ~~META 资产循环~~ · ~~券~~ —— **⚑ 局外 build 已整体删除(2026-08-24 用户拍板:「取消全部局外 build,
    直接删除代码和资源,包括主角、券,仅保留局内玩法,我们应该专注」)**:主角/券/META 资产(宝石·唱片·
    彩带机·应援团·赛季)/成就·荣誉·档案栏全部退役,代码与素材一起删。**保留**:图鉴、Director 千人千面
    (跨局读战绩圈/faces_seen)、教学关、局内金币 ◆。存档升 v2 清退役键。别再提议加回局外成长系统。
  · **拍长一律取偶数秒**(120 BPM 下任何拍都在小节边界收束,音乐永远对齐)。
- **开局/重开/转正三条入口必须走同一份「开局三步」**(`_begin_run()`:定局数 → 喂 Director → 掷脸 → 记局数)——
  「第二条入口漏掉主路径的步骤」是这个项目最贵的形状之一(2026-08-21 评审:重开曾绕过 min_run 与 Director)。

### 数值的归属(改哪里)

- **小丑牌的平衡数值在一张表里**:`data/jokers.json`。改平衡只动那里,别散到代码各处。
- **⚑ 定价先过宪法**(2026-08-12 用户立规):任何数额/倍率改动走
  [`docs/design/numbers.md`](docs/design/numbers.md) 的三轴框架与六步 SOP;
  概率的三列记录(设计/仪器/真人)在 [`docs/design/probbook.md`](docs/design/probbook.md),
  由 `python3 tools/probbook.py` 重刷 —— **账本是仪器读数,手改无效**。
- ⚠ **发挥系数(完美玩家尺度 → 真人)不许拍初值** —— 等用户自己的 Tape
  (2026-08-07 用户:「我后面会完整玩很久,你到时候用我的系数」)。
  **不要用机器人数据代算,也不要糊一个初值上去。**
  目标分现在是完美玩家尺度、对真人偏难,**就让它偏着**。

---

## 美术方向(已锁定,勿再发散)

**霓虹舞台**。曾经试过写实酒馆 / 疯狂动物城 / 双人成行 / AI 底图,**用户已全部否掉并要求回滚**,
相关素材和试验工具已全部删除。**不要再提写实化或换场景。**

- 调色:青 `#1effec` / 粉 `#ff328d` / 紫 `#7642ff` / 金 `#ff9b2b` / 红 `#ff3632`
  **改色必须量色相和饱和度,不能只看色值像不像**;
  **主色的权威 = `assets/reference/`,改色先采样,别凭记忆值。**
  (二次校色的实测数字见 [`docs/design/ui_meta.md`](docs/design/ui_meta.md)。)
- **背景 = 黑**(2026-08-06 两轮定稿:先「压暗」→ 用户「卡牌底下那个背景做成黑色吧」):
  局内 `BG0/1/2 = 000000/030308/07070f`,首页底色渐变全归零。
  **画面里的光全部由光效层承担**(激光/探照灯/光斑/霓虹边/扫描线),底色不再贡献任何亮度。
  ⚠ **改底色只做一半会看不出来** —— 大面积柔光会把黑重新染回来
  (细节见 [`docs/design/ui_meta.md`](docs/design/ui_meta.md))。
- **盲注档位色 = 四档递进 蓝 `#23cdff`(`StageTheme.BLUE`)→ 橙 `#ff9b2b`(`AMBER`)→ 红 `#ff3632`(`RED`)→ 粉 `#ff328d`(`PINK`)**(逐档升温;⚠ 2026-08-21 前这行写的是二次校色**之前**的三个旧 hex,以 `view/theme.gd` 为准)。
  只在 `Widgets.StageCard.accent_for()` 一处定义,首页舞台卡与局内盲注卡共用。
- **底部页签区不要加板**:那里本来就是黑的,顶栏靠自己用**半透黑**(`draw_card` 的 `body` 参数)
  与它取齐即可(2026-08-05 用户否掉过我给底部补的那块板:「别加东西了」)。
- **全局 chrome 不跟档位变色**:顶部玩家信息栏固定中性近黑(`StageTheme.SLATE` + 半透黑底)——
  它是玩家身份/货币,不属于任何一关;跟着变色等于宣称自己是关卡的一部分,而且左右滑赛程时
  整屏跟着换色会很吵。固定中性之后,**档位色成了整屏唯一会变的东西**,焦点自然落在卡片上
  (2026-08-05 用户提问后拍板)。语义自带颜色的小件不受影响:金币金。
- 花色**只有两色**:♥♦ = `#ff6aa9`,♠♣ = `#9fe9ff`。缓存区的紫**只染边框**,不染花色和数字。
- 字体:**Rajdhani**(数字/拉丁,Medium/SemiBold/Bold)+ 系统中文
- 卡面 = 左上数字 + 中央大花色 + 右下倒置数字。**没有点阵,没有 J/Q/K 立绘**。
- 全屏统一语言:**暗玻璃底 + 主色描边 + 外发光**。不要浅色不透明板。

### 排版(固定,是目标不是草稿)

信息区 → 「♪ 小丑牌 ♪」标签 → 小丑牌 ×4 → **盲注卡 + 音浪 + 消耗品栏 ×2**(同一层,左中右)
→ 均衡器竖条带 → 手牌区 5 张(走圈轨道 + 节拍菱形;主角小人已删,轨道保留)→ 缓存区 3 张 + 理牌/弃牌

**局内盲注 = 一张牌**(`Widgets.BlindCard`):摆在音浪层左边、和右边**消耗品栏两格**对称的一张竖牌。
⚠ 唱片(`VinylDeck`)2026-08-31 退役 —— 它的三个职责(提前收工键 / 牌堆剩余计数 / 转盘装饰)
被用户逐条取消, 位置让给消耗品栏。**牌堆剩余张数现在不显示**(用户拍板)。
**分工:HUD 管「当前数值」(分数/目标、金币、第几拍、进度),
盲注卡管「你在打什么」(档位 + 序号 + 第 N 场 + BOSS 脸预告)。**
尺寸、返工史与首页布局四条见 [`docs/design/ui_meta.md`](docs/design/ui_meta.md)。

**判据:同一个部件要不要共用,看的是语境而不是概念** —— 首页大卡和商店公示板是公示板语境,
可以共用 `StageCard`;手牌旁边那张必须说卡牌的语言。

**对齐类反馈要查「视觉顶端」而不是几何顶端** —— 角标与倒影的光都算进去
(实例见 [`docs/design/ui_meta.md`](docs/design/ui_meta.md))。

关键坐标与全部界面文案在 **`data/ui.json`**(stage/hud/shop/hand/banner 五节,2026-08-05 起):
**改布局改文案 = 改 JSON。** 具体坐标见 [`docs/design/ui_meta.md`](docs/design/ui_meta.md) 与 `data/ui.json` 本身。

### 渲染手法 —— 具体做法全在 [`docs/design/ui_meta.md`](docs/design/ui_meta.md)

剖面数值、分层顺序、玻璃卡三件套、倒影、辉光、光源方向那一整套(近 200 行)都在那篇。
这里只留五条方法论:

- **改了视觉就渲染出来自己看,不要靠想象。**
- **临摹前先把素材的像素剖面打出来**(采样 alpha/灰度找峰),比看图猜快得多也准得多。
- **判断一块视觉结构前,先把它整段裁出来看图,别只点采样** —— 素材残缺处的采样值会把人带沟里。
- ⚠ **数字对完必须看图**:「指标达标但形态错了」是这条线上最贵的坑。
- ⚠ **指令外的加法比做少了贵得多** —— 用户说「加强边缘光」时,先问清「边缘光」指什么再动手。

---

## 素材分工

- **用户出**:小丑牌插画(丢进 `assets/jokers/joker_<id>.png` 即插即用)、卡背。
- **`docs/mockups/` 下的 `*.html` 是用户的 Claude Design 设计稿,是对应界面的权威**(当前:`success.html` +
  `fail.html` = 结算两屏,由 `view/run_end.gd` 实现;`home.html` = 首页,由 `view/home.gd` 实现)。
  **`docs/mockups/godot-handoff/` 的实现手册是舞台卡的权威**(分层顺序、9-slice、主题色梯度都在里面);
  **`docs/mockups/assets/` 是设计稿 html 引用的原始素材**(frame-glass4 / 各色边框 / 四张 A 卡面,
  2026-08-05 用户补进仓库)。**首页大卡的玻璃壳贴素材**(`assets/frames/glass.png`,
  2026-08-12 用户拍板换素材;当年「抠图坏、不能贴」是放大镜下的误判,反转经过见
  [LESSONS.md](LESSONS.md) 判据 5 与 `assets/frames/README.md`);
  **其余玻璃板(顶栏/商店/图鉴页)仍程序化画,`glassface.png` 这个文件名永远别放**——
  放了会让**所有** `draw_card` 调用点被贴图接管,而素材九宫格在小板尺寸下必坏。
  另注:**设计稿 html 引用的图片不会跟着 html 过来**,缺素材要主动问用户要。
- **我出**:其余全部程序化渲染。
- `assets/reference/` 是用户自己用 Claude Design 做的规格 + 参考图,**是美术的权威来源**,
  有疑问先去翻它,别猜。目录里有 README 说明每个文件管什么。

---
